---
id: WRK-028
type: working
status: active
owner: @weilin
created: 2026-06-18
reviewed: 2026-06-18
review-due: 2026-09-16
audience: [human, ai]
landed-into:
---

# Iteration Loop —— Finding 索引（一行一条，永不写成 essay）

> **规范（强制）**：一个 finding = **一行**，每格一个短语。证据→轨迹 dump；修法详情→commit；本表只做索引。
> 状态：`open` 待修 · `confirmed` 已复现待修 · `fixed` 已修+验+回归 · `watch` 观察 · `not-bug` 判断后非 bug（成本/性能/可恢复且行为正确——不算）· `dup` 被他条覆盖。
> 新发现追加在表末。**别删行**（同 D1 Log 语义）。

| ID | 状态 | 问题（一句话） | 范围 | 修法（定位） | 验证（前→后） | commit |
|---|---|---|---|---|---|---|
| F1 | fixed | lazy 工具概览不点名 id 参数 → 模型瞎猜参数名（`query`/`function_name`…） | **系统性 49/50** | 地基：`toolset.Overview` 浮出必填参数名 + `prompt` 渲 `name(args)` + preamble id→search 解析 | function+handler 修前 4/4 错 → 修后 4/4 一次对、零 error；79/91 工具现渲参数 | dfe2a361 |
| F2 | not-bug | "resident vs searchable" 措辞被半误读——但 agent 行为本就正确，非 bug | — | — | — | — |
| F3 | not-bug | 简单任务 ~75K input token（冗长 schema 重发）——成本/性能，**非 bug**（作者明示不算） | — | — | — | — |
| F4 | watch | `run_function` 首调 args 平铺非 `{"args":{…}}`（修 F1 后未复现，疑被 F1 一并修掉） | 待 CONFIRM | — | — | — |
| F5 | fixed | 模型用无效字段类型 `"integer"`（schema 只认 number）→ 一次失败调用 + 恢复 | 系统性（`pkg/schema` 所有 build 实体共享） | `ValidateFields` 归一别名（integer/int/float→number, str→string, bool, dict→object, list→array）原地写 + `FromJSONSchema` 同 | 零 token 单测 `TestValidateFields_NormalizesAliases` 绿；make verify 绿 | _pending_ |
| F6 | fixed | edit 带 set_meta 不更新实体行 name/desc/tags（只移版本指针）→ agent 以为改了名、后端没改 | function+handler（workflow 本就对；agent/control/approval 无 set_meta op） | `Edit` 把 draft meta 带回行 + `SaveVersionAndActivate(v, f)` 同事务 Save 整行（6 文件） | `:edit set_meta` 重命名后 GET 真变；零 token 回归 `Test{Function,Handler}_EditPersistsMeta` 绿；make verify 绿 | e356cf2f |
| F7 | fixed | tool 错误对 LLM 不透明：`Error()` 只给 Message、丢 `Details`，而 workflow 校验把违例节点+真实 CEL 错放在 `Details.reason` → agent 见 "workflow graph is invalid" 盲猜 CEL ~8 次卡死 | **系统性**（tool-error→LLM 边界丢所有工具的 Details） | `loop/tools.go` 加 `llmErrText`，在 executeTool 把 Details 渲进 LLM 可见错（一处修全部工具，原则 #8） | 零 token 单测 `TestLLMErrText` 绿；make verify 绿；agent 重跑见详错、自纠建成 workflow、turn completed（前 ERROR） | 0a6c6986 |
| F8 | fixed | workflow CEL 错说 "undeclared reference to 'X'" 但不列**可用**标识符 → agent 试 payload/trigger/celsius/input/receive 5 次才中 | workflow-only（control/approval/trigger 用固定 env payload/ctx/input，无此问题） | `crud.go` compileGraphCEL 首层错附 "this node may read: [祖先节点 id]" | 零 token 回归 `TestWorkflow_InvalidCELListsAvailableNodes` 绿；make verify 绿 | 10c2e343 |
| F10 | fixed | `invoke_agent` 的 `input` 非 required → 概览只显 `invoke_agent(agentId)`，agent 猜 `prompt`（静默丢）→ 空 input 跑出通用问候**却 ok:true**（误导成功）；search_tools 后用对得正解（30C=86F） | invoke_agent | `input` 设 required：概览现显 `invoke_agent(agentId, input)` + ValidateInput 缺失即 `AGENT_INPUT_REQUIRED`（`{}` 允许 self-contained）+ 描述点名「无 prompt 字段」 | 单测 `TestInvokeAgent_RequiresAgentID` 更新（缺 input 报错）；make verify 绿 | 9f0fc39a |
| F9 | not-bug | `get_flowrun` "not found"——查实：模型把 `trigger_workflow` 返回的 id `fr_…b4a` **截成 `fr_…b4`**（漏末位）后端正确报无；用全 id 重试即中。后端正确、模型复制错+恢复 | — | — | — | — |
| F11 | fixed | 嵌入 provider 对 >512-token chunk 报 "input (590 tokens) too large, physical batch 512" → 语义搜索**静默退化 lexical**（仅 INFO 日志，质量降级隐形） | 嵌入 server llama-server 启动 flags（`search/engine/engine.go`） | `engine.go` 加 `--ubatch-size 2048`（=ctx-size，整块一批嵌） | 重启后 ~600-token function 描述索引：embed server 起、"input too large" 计数 **0**（前反复）、不再退 lexical；make verify 绿 | _pending_ |
| F12 | fixed | chat 回合卡 `streaming` 0-block >5min（`updatedAt`==`createdAt`=无事件到达）。idle-timer(`provider.go:62`,150s)对无事件流应 150s 后 cancel，但 message 未 finalize 到 failed（仍 streaming）→ **疑** idle-cancel 未 finalize message（真 bug）**或** deepseek 对超大 system prompt 退化 hang | — | **= F33** —— round-1 toolpick lane 干净复现（2/2）+ 定根 + 修 | 见 F33 | 假设证实：idle-cancel 确未 finalize（keep-alive 流困死 scanSSELines） |
| F13 | fixed | `create_control` 描述说 when/emit CEL 读 `payload`/`ctx`——但运行时只绑 `input`（dispatch.go:159），field schema + 域模型 + docs 三方全说 `input.*`。描述自相矛盾、**主动诱导** agent 写 `payload.temperature`（运行时未声明 → 控制节点必崩）→ 模型为此耗 ~120 行推理纠结 payload/input、终写错 → `TOOL_ERROR_STORM` 中断整轮 | control 工具描述（与自身 field schema + runtime + docs 矛盾） | `tool/control/lifecycle.go` create 描述重写为「读 `input.*` ONLY、payload/ctx 不在域内」+ 点明两层喂入（workflow 节点 input 映射→喂 `input`，branch 再读 `input.*`） | 重建重跑**同任务**：前=TOOL_ERROR_STORM/45 块/写 `payload.*`/未建成；后=completed、写 `input.temperature`、durable 路由 35→`__port:hot`→"hot warning"·10→`__port:normal`→"normal"（flowrun result ground-truth 验） | _pending_ |
| F14 | fixed | create/edit 时 `celpkg.Compile` 用共享**宽容 env**（声明 payload+ctx+input 三根）→ control/approval 写 `payload.x`/`ctx.x`、sensor 写 `input.x` 编译通过、运行时崩（活化各只绑 input/input/payload）；author-time 不做命名空间校验（**用户选 option b：收紧地基**） | 系统性（control/approval/sensor 三类受限 CEL） | `pkg/cel` 加 `CompileFor(roots,expr)`（恰给定根、**无自动 ctx**）+ `CompileTemplateFor`；control(when/emit)→`[input]`、approval(模板)→`[input]`、sensor(condition/output)→`[payload]` 的 create/edit 校验切到它，镜像真实运行时活化；doc 同步 cel 包注 + platform-pkgs.md | 零 token 单测 `TestCompileFor`（in-ns 编过/out-ns 拒）+ `TestCreate_WrongNamespaceCEL`（control payload.x/ctx.x 拒、input.* 留）绿；既有 control/approval/trigger 测无回归；make verify 绿 | _pending_ |
| F15 | fixed | approval 节点的下游 result 只是 `{decision, reason}`（**非透传 input**）——但 `create_approval` 描述只讲 yes/no 出口、从不说 result 形状 → agent 把下游 action 接 `human_gate.amount`（approval result 无此键）→ park/decide 续跑后 action 运行时崩、run **failed** | **系统性**（任何 approval→下游需原数据的工作流） | 中央修 `opsDoc`（见 F16）列 approval result=`{decision,reason}` only + `create_approval` 描述补「不透传、下游从上游节点读原数据」 | 重跑同任务：前=action 读 `human_gate.amount` 崩、run failed；后=0 error、读 `start.amount`、approve 后 run **completed**（`record_approved` result ground-truth 验） | _pending_ |
| F16 | fixed | `opsDoc`（create/edit_workflow 共享）说节点 input CEL 读「payload/ctx for a trigger's signal, input for node-fed data」——实际读**上游节点 result 按 node id**（`start.amount`）、无 payload/ctx/input 根 → 误导 agent 写 `payload.x` 节点 input（F13/F15 两轮都先撞此、靠 F8 错误提示才纠） | **系统性**（所有工作流节点接线的总入口文档） | `tool/workflow/workflow.go` opsDoc 重写：明示 input CEL 读 `<nodeId>.<field>`（无 payload/ctx/input 根）+ 加 **NODE RESULT SHAPES** 块（trigger/action/control/approval/agent 各 result 形状） | 同 F15 重跑：agent 一次写对所有节点 input（`start.amount`/`approval_node.reason`）、0 error、run completed；docs 本就对（domains/workflow.md:20、scheduler-flowrun.md:37）故 code-only | _pending_ |

| F17 | not-bug | 多轮改 fn 后 agent 自称「active 仍 v1」——查实：turn-2 `get_function` 在 edit **之前**调（正确返 v1 快照），agent 回看该**前置快照**误判 active=v1；后端实际 edit→v2、run 返 v2 输出、当前 active=v2 全对（F6 激活正常运作）。同 F9 模型时序误读、**非 bug** | — | — | — | 同轮 `LLM_STREAM_ERROR`：THINK 截于半句 = deepseek 流瞬断，后端**正确 finalize 到 error**（status=error/code=`LLM_STREAM_ERROR`）→ 证流错 finalize 路径可用（**区别于 F12 卡 streaming**）；turn-4 对话优雅恢复、agent 正确报 v2 + "Smith, Jane"（无 middle 边界对） |

| F18 | fixed | `entryFuncName` 先 `TrimSpace` 再判 `def ` → 选中**缩进的** def（类方法/嵌套）作入口、与 `validateFinal` 列-0 规则矛盾 → driver 按名调缩进函数 → 运行时 NameError；validateFinal 放行（列-0 def 存在）故 create 期不挡 | 系统性（类/嵌套 def 在入口前；多 agent fanout 之 skeptic 变体复现，driver 写成 `multiply(**_input)`） | `function/validate.go` `entryFuncName` 改判 `HasPrefix(line,"def ")`（不 trim、要求列 0） | 零 token 单测 `TestEntryFuncName_TopLevelOnly`（类方法在前→返列-0 入口）绿；make verify 绿 | _pending_ |
| F19 | fixed | `create_function`/`edit_function` 描述只示 `def main(...)`/`def(**kwargs)`、从不说**第一个顶层 def 即入口**（按 **kwargs 调、名无意义、helper 须置后）→ helper-first 的常规 Python 风格 → run 调错 def、opaque TypeError | 系统性（任何 helper-first 写法；skeptic 变体复现 `_avg() got unexpected kwarg`） | `tool/function/build.go` CreateFunction 描述加 ENTRY POINT 段（首列-0 def 是入口、**kwargs、helper 置后/内嵌） | make verify 绿；同 F18 簇重建再行为验 | _pending_ |
| F20 | fixed | `revert_*` 只移版本指针，但 name/desc/tags **不在版本快照**（在实体行、F6 确立）→ revert 不还原改名 → agent 误报「已改回原名」（result 也不含 name 无从自查）。**F6 同类、在 revert 路径** | **系统性 5 实体**（function/handler/control/approval/agent 的 meta 都在行；workflow meta 在 graph 版本内、不受影响——已核 6 个 Version 结构） | 5 个 revert 工具描述加 honest-contract 句（name/desc/tags 非版本化、revert 不动、用 set_meta 改）。值班判 Option B：meta 是身份非行为快照、不应被 revert 改名——只澄清契约+不改行为 | skeptic 变体确诊（revert 到 v1 后 code 还原但 name 仍 `temp_converter`）；make verify 绿 | _pending_ |

| F21 | fixed | `move_document` 的 position 是**非移位绝对索引**：`Service.Move` 裸赋 `d.Position=*in.Position` 只更一行、不挪同级 → 插入已占用槽（如 0）→ 位置撞 + `ListByParent` 的 `created_at` tiebreak 静默按创建序排 → reorder 静默失败 + 同级 position 重复（实测两兄弟都 pos=1）；且 position **只写**：list/read 工具从不返回、agent 看不到顺序只能盲猜 | 系统性（任何 reorder N 个子文档；skeptic 变体复现 pos=1/pos=1 碰撞） | `app/document/document.go` Move 改稳定按索引插入（取同级 ListByParent、剔自身、splice 到请求 idx、0..N-1 重排、UpdateBatch 原子写）；`tool/document/list.go` slim 加 `position` + 描述 | 零 token 单测 `TestMove_StableReorder`（移末位→0 得 C,A,B、position 0/1/2 无撞）绿；make verify 绿 | _pending_ |

| F22 | fixed | MCP ref split-contract：`search_blocks`/RefHint 投出 **name 形** `mcp:<name>/<tool>`（agent 挂载也按名解析），但 workflow `refresolver`+`dispatch`+`capability` 只认 **id 形** `mcp:<mcp_id>/<tool>` → name 形在能力检查 `ErrRefNotFound`、运行时 `MCP_SERVER_NOT_FOUND` **静默失败**，agent 只能暴力猜 mcp_ id（两独立 fanout lane + skeptic 变体复现：name 形 capability 失败、改 id 形即过） | **系统性**（任何把 MCP 工具接入 workflow 的人） | 加 `mcp.Service.ResolveServerID`（先 id 后 name → 规范 mcp_ id）；`refresolver` mcp 分支 + `dispatch` RunAction mcp 分支都改用它（镜像 `mount.go` 按名解析）→ 三处消费方统一接受 name 或 id | 零 token 单测：refresolver `mcp:markitdown/...` name 形现 resolve OK；全后端 build + bootstrap 测绿；doc 同步 `domains/mcp.md`（ref-token 统一） | _pending_ |
| F23 | not-bug | chat agent 无工具注册**自定义** MCP server——**用户拍板：产品设计就是只配置官方市场列表**（非缺口） | 产品决策（marketplace-only） | `install_mcp_server` 描述收口为「仅市场、无自定义 self-hosted、用户问就说明只支持市场」（去掉误导的「去 UI 配」承诺） | 用户决策 marketplace-only、**结案** |
| W1 | fixed | cron trigger 无预测性 `nextFireAt` 字段——agent 只能推理下次触发时刻（仅有 `lastFiredAt` 审计字段）（用户选 option a：加该字段） | 可发现性（cron lane） | 域 `Trigger` 加 `NextFireAt`（读时派生非列）；`attachRuntime` 用新 `croninfra.NextAfter(expr,now)` 算 cron 下次触发；`create_trigger` 描述点明 get_trigger 返 nextFireAt；doc 同步 trigger.md | 零 token 单测 `TestNextAfter`（daily=次日 09:00、minutely、invalid 报错）绿；make verify 绿 | _pending_ |
| W2 | watch | `FinalizeMessage` DB 写失败仅 `Warn` 日志、留 message 行 `status=streaming` 到下次启动 `SweepNonTerminal` | 罕见健壮性（F12 理论残隙；skeptic isReal=false/sys=false） | — | — |
| W3 | fixed | `runQueue`(长生 goroutine)无 `defer/recover` 包 `processTask` → 一次 turn panic 崩**整进程**（与库内 trigger 监听/HTTP 中间件等其它长生 goroutine 的 recover 不一致）；panic 还留 message 卡 `streaming` | 健壮性（单进程本地 app 不应一 turn 崩全部） | `chat/runner.go` processTask 顶部加 defer/recover：带栈 Error 日志 + `failTurn` 把 message finalize 到 error（顺带覆 W2 的 panic-卡-streaming 子情形） | 零 token 单测 `TestProcessTask_PanicRecovered`（panicClient → 回合 finalize 到 error、message_stop 出、进程存活）绿；EndToEnd 不受影响；make verify 绿 | _pending_ |

| F24 | fixed | 每种 trigger 的运行时 fire payload 键（fsnotify `{firedAt,path,eventKind}`、cron `{firedAt}`、webhook `{firedAt,method,path,headers,body\|bodyRaw}`）是**隐形契约**——`create_trigger` 描述/trigger.md 都不列 → agent 猜键、`capability_check` 放行、**第一个真事件才崩**（如 `watch.event` no such key） | **系统性**（任何 trigger→下游需 payload 的工作流；round-2 fsnotify/webhook lane + skeptic 变体复现） | `create_trigger` 描述每 kind 加「Fire payload: {…}」；doc 同步 `trigger.md` dedup 表加 Fire Payload 列 | 重建后行为复验（见 F25/F26 簇）；make verify 绿 | _pending_ |
| F25 | fixed | fsnotify 交付的 `eventKind` 用 fsnotify 原始**大写** Op（CREATE/WRITE/REMOVE…），但 config 词汇是**小写** create/modify/delete（且 modify↔write、delete↔remove 别名）→ agent 按文档枚举写 CEL 过滤永不匹配 | 系统性（任何 fsnotify 事件过滤） | `fsnotify.go` fire 用新 `configEventKind(op)` 归一到配置词汇小写 token（组合事件 `\|` 连），交付端与 config 圆桌一致 | 零 token 单测 `TestConfigEventKind`（Create→create、Write→modify、Remove→delete、Create\|Write→`create\|modify`）绿；make verify 绿 | _pending_ |
| F26 | fixed | webhook 真实可调 URL `/api/v1/webhooks/{triggerId}/{path}` **不可发现**：任何工具描述/参数/result/DTO 都不露这前缀 → agent 把 `config.path` 当全路径、给用户一个 404 URL | 系统性（任何 webhook trigger） | `create_trigger` 描述点明「config.path 只是子路径、外部 POST 到 /api/v1/webhooks/{triggerId}/{config.path}、id=本次返回的」+ trigger.md 同记 | 同 F24 簇行为复验；make verify 绿 | _pending_ |

| F27 | fixed | durable `:replay`（清 failed 节点、留记忆化、按 run 原 pin 版本重走）后端实现完整且**正确**（round-2 replay lane 经直接 POST 验通：只清 failed 节点、保 trigger 记忆化、replayCount 0→1），但 **agent 无工具**调它——失败 run 无法从 agent 侧恢复 | 系统性（任何 agent 想重跑失败 run） | `tool/workflow/runs.go` 加 `replay_flowrun`(sched.Replay 同步→GetRunWithNodes 返更新后 run+nodes)；WorkflowTools 注册（14→15）；描述点明仅 failed 可重放 + 按**原 pin 版本**（失败后改实体不生效、要新 run）；doc 同步 workflow.md/scheduler-flowrun.md | ValidateInput 单测入表（replay no id→err、ok）绿；make verify 绿 | _pending_ |

| F28 | fixed | durable 循环引擎本身**正确**（per-iteration 记忆化、终止、MaxIterations(1000) 干净失败——round-2 loop lane 验通），但**自累加循环不可编排**：循环内前驱在首轮未跑、其 node-id 根在 CEL activation 缺省，cel-go 对表达式引用到的未绑已声明根硬报「no such attribute」**即便在 has() 内**→ `has(check.count)?check.count:start.count`（自然 loop init）也崩 → agent 被迫退化到 side-effecting stateful handler（破坏跨 run 隔离/并发） | **系统性 + 引擎**（任何跨轮累加/重试带态的循环；round-2 loop lane + skeptic 双复现 + cel-go 独立复现 + 验证修法） | `scheduler/walk.go scopeFor` 把每个无 completed result 的已声明图节点绑**空 map**（非缺省）→ `has(pred.f)` 干净为 false、loop init 可写；既有接线不变（只引用 completed 祖先）；opsDoc 加 **LOOP STATE** 段（has()-guard + control emit 携态）；doc 同步 scheduler-flowrun.md | 零 token 单测 `TestScopeFor_BindsAbsentNodesToEmptyMap`（缺省→{}、completed 保 result）+ `TestWalk_LoopWithBackEdge` 不回归；make verify 绿 | _pending_ |

| F29 | fixed | `buffer_one`/`buffer_all` 被接受 + opsDoc 当平等策略广告，但 `overlapDecision` 只实现 serial/skip → 二者静默 = allow_all（round-2 concurrency lane + skeptic 复现 4 run 全并发）。先做诚实 doc-mark；后**用户决策 B + 温柔取消**：对齐干净标准 5 策略 | **系统性 + 引擎特性**（标准重叠菜单：业界 = Temporal 6/K8s 3；我们差「打断式」+ serial≈buffer_all 冗余） | 删 `buffer_all`（=serial 冗余）、加 `replace`；实现 `buffer_one`（defer + `SupersedePendingOlderThan` 丢更早待处理、只留最新）+ `replace`（`cancelRunningForReplace` 标 cancelled+打断 advance、复用 KillWorkflow race-safe 序、不动 lifecycle）；CHECK + IsValidConcurrency + opsDoc + workflow.md/database.md/firing.go 同步 | 单测 `TestSupersedePendingOlderThan`(store) + `TestFiring_OverlapBufferOneDefers` + `TestFiring_OverlapReplace`（在途 run 被 cancel + firing 被消费）绿；make verify 绿 | _pending_ |

| F30 | fixed | trigger `outputs` 字段（声明下游可读 payload）是**自由手填、不校验、与真实 fire payload 对不上**——是 F24 根的设计层（用户选 option a：从 per-kind schema 自动推导） | 系统性（cron/webhook/fsnotify 三固定-payload kind） | 域加 `CanonicalOutputs(kind)`（= listener 真实 payload 字段）；Create/Edit 对 cron/webhook/fsnotify **盖上**规范 outputs（覆盖作者所填、永不漂移），sensor 保留作者自定义（config.output）；工具 outputs 参数描述点明「仅 sensor 需填、固定 kind 自动忽略所填」；doc 同步 trigger.md | 零 token 单测 `TestCreate_CanonicalOutputs`（cron 作者填 bogus→被盖成 `firedAt`；sensor 保留作者 `x`）绿；make verify 绿 | _pending_ |

| F31 | fixed | `create_agent` 的 tools 禁 `ag_` 但**不指路** → agent 白烧半轮建注定失败的 HTTP wrapper（再删）才找到 workflow 路径（EXPLORE round-1 agent-nesting lane） | 可发现性（agent 编排面） | `tool/agent/build.go` configProps tools 描述加「串联 agent 改用 workflow agent 节点」+ domains/agent.md 同步 | 零 token 守卫 `TestConfigProps_AgentChainRedirect`；make verify 绿 | _pending_ |
| F32 | fixed | schema-less callable（自由文本 agent、返回非对象的 fn/hd/mcp）节点结果静默键 `.text`，opsDoc NODE RESULT SHAPES 只讲"结构化字段"不提此 → 编排期不可知、`capability_check` 还 ok:true、第一次跑必崩（agent 靠失败 run 反推 key） | **系统性 + 跨轴**（所有节点 kind 的 schema-less 输出；F16 NODE RESULT SHAPES 的缺口；`toResultMap` dispatch.go:178 真相） | `tool/workflow/workflow.go` opsDoc：agent 行分 declared/free-form、加通用 **SCHEMA-LESS `.text`** 行（覆盖 fn/hd/mcp/agent）+ 点明 capability_check 看不见此键 | 零 token 守卫 `TestOpsDoc_SchemaLessTextConvention`；make verify 绿 | _pending_ |

| F33 | fixed | **= F12 确认+定根**：重多段首轮 → deepseek 流持 200 后只滴 SSE keep-alive 注释行（不发 token）→ `scanSSELines` 跳过注释行不调 fn（ctx 仅在 fn 内查）→ idle-timer(150s) 的 cancel 永不被观测、`ParseStream` 死循环、不报 EventError → message 永卡 `streaming`（11–20min、违反 runner no-streaming-orphan 不变式）。EXPLORE round-1 toolpick lane 2/2 复现 | **系统性 + 跨轴**（共享 `scanSSELines` 喂 ~11 provider；anthropic 自己的 parser 早已在扫描循环查 ctx——它是对的、scanSSELines 是漏的那个） | `infra/llm/transport.go` `scanSSELines` 加 ctx 参数 + 扫描循环顶 `ctx.Err()` 检查（即便流只滴非-data 行也能被 cancel 打断）；~11 provider caller + 2 test 传 ctx | 零 token 回归 `TestScanSSELines_CtxCancelBreaksKeepAliveDribble`（无尽 keep-alive reader + 已取消 ctx → 必 2s 内返 ctx.Err()，旧码死循环超时）；make verify 绿 | _pending_ |

| F34 | fixed | LLM 流以 stopReason=error 收尾但 `streamErr` 空 → message finalize 成 `error` 却 `error=null`（70 block 真活后无因、无恢复提示，用户只见"切断"）。round-2 wasted lane | chat loop（所有 provider 流错路径） | `app/loop/loop.go` stopReason=error 且 errMsg 空时补非空可操作因（"stream ended unexpectedly… 已保存、retry 续"） | make verify 绿；行为复验待（与 F33 同族、补 finalize 诚实性） | _pending_ |
| F35 | fixed | `capability_check_workflow` 对"节点 input CEL 引用上游某分支不发的 key"的图照样报 ok:true/structurallyValid → 运行时 fail-fast 崩整 run，agent 只能靠跑才发现（绿检查超额承诺）。round-2 wasted lane 复现（validate 仅 valid 分支发 order、invalid 分支崩 `no such key: order`） | **系统性**（静态校验器查结构/ref/port、**不查 dataflow**；= F32 的运行时面） | `tool/workflow/capability.go` 描述 + opsDoc 显式声明「不校 node-input dataflow、引用字段须在每条可达分支都存在、缺 key fail-fast 整 run、绿检查仍要 trigger 一次确认」 | make verify 绿 | _pending_ |
| F36 | fixed | `POST /<entity>:iterate` 对不存在/已删 id 返 **202 + 真 conversationId**（非 404）→ 起一个前提缺失的 AI-edit 对话（mention 降级 `(unavailable)`）。`aispawn.Iterate` 不校实体存在（`Triage` 经 renderer 校——不对称）。round-2 iterate lane | ai-ops（:iterate 全实体共享路径） | aispawn 加 `MentionValidator` 端口（chatapp `ValidateMention` 满足、bootstrap setter 接）、Iterate spawn 前校解析→传实体 NOT_FOUND(404)，镜像 Triage 的 renderer eager 校验 | 零 token 回归 `TestIterate_ValidatesTargetExists`（坏 id→not-found 且不 spawn、好 id→spawn）；make verify 绿 | _pending_ |
| F37 | fixed | agent 无法按 id 发现/读取已上传 attachment——只有发消息时 `attachmentIds` 喂 LLM 的路径。round-2 attach lane | 能力缺口 | 加 `list_attachments`+`read_attachment` toolset：新 `Repository.List`（plain SELECT、无新表/列）+ `Service.List`；read 按 Kind 分流——text/document→`ToContentParts(Capabilities{Vision:false,NativeDocs:false})` 抽文本、binary→描述符串（不 dump 字节）、未知 id→软失败串（同 read_document）。注册进 Lazy + 第 11 个 catalog source。新码 `ATTACHMENT_ID_REQUIRED`。**子代理实现+我复核**（修了 agent 漏 bump 的 error-codes 计数 272→273 二处） | 零 token 7 测（含 store List soft-delete-aware）+ make verify 绿 + 同提交 attachment.md/support-services.md/error-codes.md 同步 | _pending_ |
| F38 | fixed | agent 无会话管理工具（archive/compact）→ 用户问"归档/压缩本会话"时，agent **编造不存在的 UI 按钮**（"Compact/Summarize button"）或甩给 UI；compaction 实为自动（无手动路由），agent 不知 | promise≠reality/能力缺口（conversation 管理面） | 加 `manage_conversation` 工具（`action: archive\|unarchive\|pin\|unpin`，窄 `Manager` DIP 端口复用 `conversationapp.Service.Update` 的 PATCH 面、无新端点/表/码；从 ctx 取 convID、对话外降级 tool-result 串，镜像 F46 trace）+ 工具描述 & chat system prompt 新 `conversation_management` 段双声明「压缩自动·无手动 compact 按钮」杜绝臆造 | 零 token 6 测（schema/enum/anti-fabrication 描述锁、no-ctx 降级、4 action 各映射对、ValidateInput 拒枚举外、组含 manage）；make verify 绿 + 同提交 conversation.md/chat.md 同步 | _pending_ |
| F39 | fixed | todo 全完成后 reminder 被抑制（`render.go` open==0→return false）且无 todo_read → 用户"列出我的 todo"时 agent **凭记忆编造**（9 行 vs 真 7 行）。round-2 todo lane（写/进度/整写全绿，仅此读回缺口） | 假成功/能力缺口（todo 读回） | 加常驻 `todo_read`（无参、复用 `Service.ReadRendered`=`Get`+`render`、含已完成项、空软返 cleared 串、无新码）；**不动** reminder 抑制（0-open return false 是对的、`TestSystemReminder_InjectsOnlyWhenOpen` 留绿）。选 todo_read 而非"reminder 注入完成摘要"：后者每轮塞完成内容进 context 且 subagent（非 ReminderProvider）仍受益不到；读工具仅调用时耗 token、chat+subagent 经 ctx 都生效（忠于 F37/F46 read-tool house style） | 零 token：service `TestReadRendered_IncludesCompleted`（completed [x] 可读 + 空 cleared + 全完成仍可读但 reminder 仍抑制）+ tool `TestTodoTools_Names`（组={todo_write,todo_read}）；make verify 绿 + 同提交 todo.md 同步 | _pending_ |

| F40 | fixed(agent 半) | declared outputs 是 advisory：callable 声明 `outputs:[{name:X}]` 但**返回标量**时 `toResultMap` 仍包进 `.text`、忽略声明名 → 下游 `node.X` 崩。round-3 多 lane | **系统性·HIGH**（fn/hd/agent 全受） | **并行调研定 + 用户拍板分两半**：①**agent 半已修**——`InvokeAgent` 终答回解析（`coerceDeclaredOutputs`：JSON 对象直通 / 恰 1 声明+非对象→裹该名 / 2+ 声明+非对象→新 `AGENT_OUTPUT_NOT_STRUCTURED` 大声失败）；②**fn/hd 半=文档**——dispatch `toResultMap` 是测试钉死的核心、声明输出对其保持 advisory（返 dict 才得 `node.<字段>`），workflow.md 讲清不碰核心 | 零 token 3 测（单声明标量→map.decision、多声明 JSON→直通、多声明散文→loud-fail）+ make verify 绿 + 同提交 agent.md/workflow.md/error-codes.md(271→272) 同步 | _pending_ |
| F41 | 评估关闭(非问题·降级) | concurrency=skip 疑对阻塞节点退化成 serial——疑 overlap 信号随同步 Advance 蒸发。round-3 concurrency lane | ~~系统性·HIGH~~→**前提证伪** | **并行调研对抗复核推翻原前提**：overlap 信号**不蒸发**——`SeedRunOnTx` 在 claim 事务内、Advance 前就把 `status=running` 写进 DB（`idx_fr_running`），`CountRunningByWorkflow` 从 DB 读 → skip 逻辑**正确**（run.go:247-252，5 策略全实现）。真实残留=Advance **同步**（单 `drainLoop` goroutine 5s tick 串行扫 workspace）→ 一个长 firing run 会**延后其它待处理 firing 的评估**（手动 HTTP `:trigger` 走各自请求 goroutine、不冻结）——niche 吞吐/及时性，**非 correctness bug**。**从 HIGH 降级**；要异步（per-run goroutine）随时可后做（信号已 DB-durable） | 对抗复核 high confidence·代码逐行核 | 评估关闭(文档) |
| F42 | fixed | `edit_workflow` 的 set_meta **静默吞无效 concurrency 值**（无错、版本升、值不变）——Create 校验、Edit 不校（不对称）→ agent 以为设了策略其实没。round-3 options lane | workflow(Create/Edit 不对称) | `app/workflow/crud.go` Edit 的 concurrency apply 无效即 `ErrInvalidOps`（镜像 Create） | 零 token 回归 `TestEdit_RejectsInvalidConcurrency`；make verify 绿 | _pending_ |
| F43 | not-bug | 「多次 edit 后工作流静默 inactive」——**查实非产品缺陷**：create 起即 inactive（须显式 `:activate`），该 workflow 从未被激活；Edit **确实**保留 lifecycle + 对 active workflow 重绑 trigger（crud.go:67-77「活 workflow 编辑改了入口 trigger ref 必立刻重绑」）。是 **agent 没 :activate 却在摘要暗示 live** 的模型误读、产品正确 | — | — | — | 反自欺：读码纠 lane 过判（同 F9/F17） |
| F44 | not-bug | 构建中 agent turn 以 **0 block + null error** 报错 → 半建实体成**孤儿**留库（= F34 兄弟 + 孤儿面） | 恢复无门（turn 错持久化/回滚） | **并行调研 + 读码：两面皆已覆盖**。FACE 1（0 block + null error）= **F34 已关**（loop.go:189-191 在 stopReason=error 且 streamErr 空时填非空可操作因；messages.error_message NOT NULL DEFAULT ''→null 不可能；0 block 是对的——无内容时非空 error+恢复提示**就是**那个 surface）。FACE 2（孤儿实体）= **per-tool durable commit 的有意设计**（principle #2）：BuildTool 在 runTools 内同步 commit、mid-turn 流错前实体已是合法 Quadrinity 实体（非半建/腐败），可 list/get/delete——回滚会**毁掉用户经一次成功工具调用建的真实工作**（违 durable 模型），不做 | 补零 token 守 `TestRun_StreamError_EmptyErrFillsActionableMsg`（空 Err 流错→error/LLM_STREAM_ERROR + 非空 errMsg + Result 带因 F66）锁 F34 防回归；make verify 绿 | — |
| F45 | 评估关闭(产品设计) | 无**工作区级** dangling-ref/health 审计工具——只有 per-workflow capability_check + per-entity get_relations | 能力缺口 | **用户判定=产品设计、非问题**：坏引用在**运行时 fail-fast** 被当场抓住（dispatch 对无 active 版本的 ref 跳过 pin、跑到坏节点失败、不出错数据），全局体检是 pre-emptive 便利非 correctness；单用户实体量小自会注意。relation 边随实体硬删、无版本列，也无法 cheap 扫。**不做** | — | 评估关闭(产品设计) |
| F46 | fixed | agent 无工具读回本会话的 **subagent 子树**——告用户 trace"不可得"但平台全持久化（E3 嵌套 sub-message 真存、ListMessages 返回但 LoadHistory 排除） | 不可发现 | 加 `get_subagent_trace` 读工具（5 法 Tool）：空 arg→列本对话 subagent runs（subagentRunId/status/finalText/blockCount/spawningToolCallId）、给 id→全 block trace（按 Seq）。窄 DIP `Reader` 端口复用 `LoadThread` 内存按 `SubagentID!=""` 过滤（无新表/列/查询）；convID 取自 ctx。无新错误码（两失败态降级 tool-result 串，同 read_document）。**子代理实现+我复核** | 零 token 5 测（List/Detail-Seq序/NoConversation/UnknownID/ValidateInput）+ make verify 绿 + 同提交 messages.md/agent.md 同步（厘清 get_agent_execution[agent_executions 表] vs get_subagent_trace[sub-message]） | _pending_ |
| F47 | fixed | agent 无工具**决策 parked approval**——能建+触发带 approval 节点工作流，却无法驱动 park 的 run 到完成（REST 有 :decide、无 tool 包）。round-6 双 lane 重confirm | 能力缺口（人在环决策半边从 agent 席不可达） | `tool/workflow/runs.go` 加 `decide_approval` 工具（包 `scheduler.DecideApproval`，ValidateInput 只校 flowrunId、node/decision 由 scheduler 校→免新码）；WorkflowTools 注册(15→16)；doc 同步 workflow.md/scheduler-flowrun.md | 零 token：`TestWorkflowTools_Wiring`(16 工具) + ValidateInput 用例（decide 无 id→err、ok）；make verify 绿 | _pending_ |
| F48 | fixed | 实体 delete 无条件（live 引用也不挡），`purgeRelations` 同 op 删依赖边 → 删后 breakage 经 get_relations 反更难查 | **系统性**（8 个单实体 delete 工具 + relation 地基） | **地基一处强化 + 工具层注入**（避开 Delete 签名级联 ~33 文件的最小忠实修）：`relationdomain/app.Service.CountDependents(kind,id)` 报诚实依赖数=**入向 equip/link**（挂载/外链它的，排除 create/edit 溯源 + 本实体出边，复用 `ListByToAndKinds`、无新 repo 方法）；共享 `toolapp.DependentCounter` 端口 + `AnnotateDependents`/`DependentSuffix` helper；8 个 delete 工具（fn/hd/ag/wf/trg/ctl/apf/sk）持 `deps` 字段、删**前**读计数折进结果（nil 容忍、读失败不阻删）+ 描述点明「结果报依赖数、删前用 get_relations 自查」。delete_document 因递归删 + 字符串结果**显式不纳入**（诚实标注、非静默砍） | 零 token：地基 `TestCountDependents`（入向 equip/link=3、排除 create/出边/无关、空=0、坏 kind 拒）+ helper `TestAnnotateDependents`/`TestDependentSuffix`/`TestDependentCount_NilAndErrorSafe`；make verify 绿 + 同提交 relation.md 同步 | _pending_ |

| F49 | fixed | CEL 自然算术混类型（payload 数=double + 整字面=int）**每次 run 崩裸 `no such overload`**、capability_check 还放行 → agent 烧 4 版本 + 5 失败 run 猜（round-4 malformed lane） | **系统性·HIGH**（DynType 根 + cel-go 无跨类型算术重载；每条节点 input/条件 eval 收口） | `pkg/cel/cel.go` Eval/EvalBool 对 `no such overload` 附可操作提示（cast `int()/double()` 或写 `5.0`） | 零 token 回归 `TestEval_NoSuchOverloadHint`（`start.n+5` 带提示、`int(start.n)+5`→7）；make verify 绿 | _pending_ |
| F50 | fixed | 空 function name 报 `FUNCTION_INVALID_CODE`（"code invalid"）→ 误导用户去查（合法的）代码——`apply.go:62` 把所有 validateFinal 失败统包 ErrInvalidCode | promise≠reality/选错工具·low | `validate.go` validateFinal 各错带对的 sentinel（name→`ErrInvalidName`、code→`ErrInvalidCode`）+ `apply.go` 传它的 sentinel 不再统包 | 零 token 回归 `TestCreate_EmptyName_IsNameError`（空名→ErrInvalidName）；既有 `TestCreate_InvalidCode` 不回归；make verify 绿 | _pending_ |
| F51 | fixed | workflow capability_check 校 mcp **SERVER** 存在但不校 **TOOL** 存在 → 坏 tool 名滑到运行时（实为 `MCP_RPC_ERROR`，复核纠正）；与 handler-method 校验不对称 | 不可发现/promise≠reality·medium | mcp 服务加 `ServerToolNames(serverID)`（读 st.Tools、未连返空）；`MCPExistence` 接口 + refresolver mcp case 把工具名灌进新 `RefInfo.MCPToolNames`；`CapabilityCheck` 加 mcp `/tool` 校验（`mcpTool` helper、镜像 handler `.method`、未连 server 空名则跳过） | 零 token `TestCapabilityCheck_MCPToolName`（坏 tool→problem、未连 server→跳过不误报）+ fakeMCP.tools + make verify 绿 + 同提交 workflow.md 同步 | _pending_ |
| F52 | fixed | MCP server 工具从 **chat 席完全不可调**——`DynamicTools` 是死代码、从未接进 chat toolset。round-14 坐实（mcp.md 宣称 chat 消费、实未接） | 不可发现/能力缺口·**HIGH** | 加 `chat.Deps.DynamicTools func(ctx)[]Tool` provider（build_services 接 `mcptool.DynamicTools(ctx, mcp)`、best-effort）→ `NewSearchTools(lazy, dynamic)` per-request 把 MCP 工具并入 search_tools 排序池；`chatHost.Tools` offer 已 discovered 的、`TryActivateForTool` 按名解析动态工具（不进静态 Toolset/Overview，因 MCP server 是 workspace 域+可变） | 零 token 单测 `TestSearchTools_RanksDynamicMCPTools` + make verify 绿（chat host 无回归）+ mcp.md 同步 + **真 MCP server 端到端验证**：`:8743 v6` 注册 `uvx mcp-server-time`（connected·tools get_current_time/convert_time）→ chat 一回合让 agent 用它→`mcp_calls` 真录 `{tool:get_current_time, status:ok, **triggeredBy:"chat"**, input:{timezone:UTC}, output:真 UTC 时间}`——`triggeredBy:chat` 证明从 chat 席调通（F52 前不可能） | _pending_ |
| F74 | fixed | 嵌套 MCP 结果不进下游 CEL——CallTool 把 content join 成字符串，故 workflow mcp 节点 result = `{text:"<JSON 串>"}`、下游 CEL 读不到结构字段、被迫手搓 parse。round-14 mcpschema lane | promise≠reality/组合摩擦·medium | `dispatch.go` mcp 分支 → `mcpResultMap(out)`：结果串是合法 JSON **对象**就 unmarshal 成 map 直通（CEL 读 `mcpNode.<field>`）、非对象（纯文本/数组/标量）仍走 `toResultMap` 的 `.text`（不破 F32 约定） | 零 token 单测 `TestMCPResultMap`（对象→穿字段·文本→.text·数组→.text）+ make verify 绿 + **真 MCP 端到端**：`:8743 v7` 建 trigger→mcp(`get_current_time`,timezone='UTC') workflow、:trigger→flowrun mcp 节点 result = `{datetime,day_of_week,is_dst,timezone:UTC}`**穿成字段**（非 {text:json}），下游 CEL 可读 `now.timezone`/`now.datetime` | _pending_ |
| F75 | fixed | MCP 校验错在 LLM/workflow 席可操作（点名坏字段），但 HTTP `:invoke` envelope 上塌成不透明 `MCP_RPC_ERROR`（details:null、字段名被剥）——`client.go` 用 `%w: %v` 包 `ErrToolCallFailed`，err.Error() 带真因（LLM 读到）、但 envelope 读 sentinel Message+Details（nil）。round-14 mcpschema lane | promise≠reality·low（仅 HTTP 直调面、LLM 面 OK） | `infra/mcp/client.go` CallTool 两处（err + res.IsError）把真因装进 `ErrToolCallFailed.WithDetails{reason}`（同 F8/F69 pattern；保 errors.Is + err.Error() 仍带因）→ HTTP envelope 现透出 details.reason | make verify 绿（build+mcp 单测）；用 F69 已锁的 WithDetails pattern、未加专测（LOW + infra client 测需 fake session） | _pending_ |
| F53 | not-bug | 上游 MCP tool 错误以普通 content 返回（isError:false）记 status=ok——**忠于 MCP spec**，不强制按内容启发判错；可选：mcp_calls 行 surface isError 标志 | — | — | — | 忠于协议、非 bug；可选增强 |

| F54 | fixed | `noBackendMessage`（无搜索后端时的引导）广告一个 keyless `duckduckgo-search` MCP 作逃生口，但 live registry（97 server）无它、只有需 key 的 Tavily；install 仅市场 → agent 追这死路烧 ~2 轮。round-5 websearch lane | promise≠reality/能力缺口（产品自带引导对不上市场） | `tool/web/search.go` noBackendMessage 改为指真可装的（Tavily+需 key）+ 点明纯 URL 用 keyless WebFetch | 零 token 守卫 `TestNoBackendMessage_NoPhantomKeylessMCP`（不名 duckduckgo、不claim keyless、提 WebFetch）；make verify 绿 | _pending_ |
| F55 | 评估关闭(非问题·自愈) | compaction trigger 用 billed InputTokens、gate 用 message bytes → tool-schema 撑大的 spike 触发后静默 no-op。round-5 compaction lane | 设计议题·medium（非干净 bug） | **对抗复核确认行为属实但是对的**：两把尺非对称是**刻意**——gate 只量 compaction **能压的**（messages+summary，漏 system+schema）、trigger 量总 billed；schema 主导时跳 summarize 正确（压消息救不了 schema）。且工具已**懒加载**（94 lazy 工具平时只占一行概览、非完整 schema、`IsToolDiscovered` 门控），schema 开销本被压着。最糟=每轮一次**免费空估算**、下轮真实用量重触发 → **自愈**（pipeline.go:62-67 注释已述）。要根治（gate 也算 system+schema）改 1 文件 `contextmgr.go`、收益微 | 对抗复核 high·路径全核（build_services.go 在 bootstrap/ 非 app/） | 评估关闭(文档) |
| F56 | not-bug | 「create_skill 对 search_tools 不可见、edit_skill 可见」——**查实无注册不对称**：`SkillTools` 同一 set 返 Create+Edit 都进 Lazy（可搜池）、两者都标 `Build`（build_spec.go:9-10）。create_skill 与 edit_skill **注册/可发现完全对称**、模型也确实成功调到它 | — | — | — | 反自欺：读码纠 lane 过判（搜索排序伪影非注册缺口；同 F43）。search_tools 召回质量是另说、低 |
| F57 | fixed(措辞) | skill 的 allowed-tools preauth 挂 agent 时不生效——挂 agent 只注 Guide、不授预授权。round-5 skillauth lane | promise≠reality/组合摩擦·medium | **对抗复核反转**：「不生效」恰是**正确安全行为**——agent 常无人值守跑、没人确认危险工具；唯一 preauth 本可生效的场景（chat 内 agent-as-tool 嵌套）那里确认闸门本就活、最该确认。故**只改误导措辞**（非补全 preauth=那会破坏安全）：`tool/agent/build.go:40` 工具 schema「pre-activate」→「mount: Guide 注入、allowed-tools preauth 不随挂载到 agent」+ `domain/agent/agent.go:79` 注释同改（agent.md §3/§5 本就正确记「skill 作指南非激活、防预授权泄漏父对话」） | 复核 high·行号纠正(build.go:40 非 :88) + make verify 绿 | _pending_ |
| F58 | confirmed | compaction 严格 turn 边界（runner tail）——单个长 ReAct turn 可 intra-loop 涨到离硬 context 窗 ~2%，loop 内只有 maxSteps 无 token/window 守卫。round-5 compaction lane | 脆弱/白烧·low | 待修：loop 内软守卫（last step input>0.9*窗 → demote hot tool_results / 缩 offered tool 集 / 停） | — | 排期(low) |
| F59 | not-bug | agent 永不能被授予 skill allowed-tools 引用的 built-in orchestrator 工具（call_handler/get_relations）——agent 只挂 fn_/hd_/mcp、无 orchestrator 内建 | — | — | — | 设计边界（agent 显式挂载、非 chat orchestrator 内建），likely intended；产品可加「skill 引用的内建工具对 agent 无效」信号 |

| F60 | fixed | approval `timeout="0s"`（或任意零时长）+ behavior 校验通过、但 run.go:355 把 `d==0` 当永不超时跳过 → run **永 park**、配了 reject 也不触发、无信号、只能 kill 逃。round-6 apptimeout lane | promise≠reality/静默降级·medium（核心 durable timer 本身**绿**：1ms–5s reject 全正确解析） | `domain/approval/approval.go` ValidateForm 拒显式零时长 timeout（`""` 才表永不）；doc 同步 approval.md | 零 token 回归 `TestValidateForm`（`0s`/`0ms`+behavior→ErrInvalidTimeout）；make verify 绿 | _pending_ |
| F61 | fixed | 并发同父 `create_document` position 竞态（check-then-act、无 tx）→ 实测 20 并发 createsdistinct positions 仅 14。round-6 doctree + round-11 重确认（仅外部并行 HTTP 客户端触发） | 组合摩擦/脆弱·low | 新建 store `InsertAtNextPosition`——**单 `db.Transaction` 内 max(兄弟)+1 读 + Insert**（SetMaxOpenConns(1) 使读+插在单连接上原子序列化、关掉竞态、零新 schema）；Create + Duplicate 根都改用它（后裔保 verbatim position）。**拒**唯一 position 索引（Move 重排/Duplicate 复制会瞬时违反）。并行调研定 approach | 零 token 并发回归 `TestCreate_ConcurrentPositionsDistinct`（20 goroutine→position 全 distinct，`-race` 绿）+ make verify 绿 + 同提交 document.md/database.md 同步 | _pending_ |
| F62 | 评估关闭(误读·非问题) | 疑 `search_conversations` 召回把聊天内容跨会话泄露。round-6 multiconv lane | promise≠reality·low | **对抗复核证为误读**：工具**只返回有界片段、非全文**——返回结构无 `Body` 字段（search.go:89-95），片段是 16-token（FTS）或 -60/+180-rune（LIKE）窗口；全文虽索引但唯一取全文的 `Retrieve` **零生产调用方**（search.md 已注"黑盒不可达"）。跨**工作区**硬隔离（`d.workspace_id=?` + `TestSearch_WorkspaceIsolation` 钉死）。单用户本地——你工作区里所有会话都是你自己的、跨会话召回正是这工具**故意**的价值；唯一真隐私边界=跨工作区、守得死。改它(C)会为不存在的威胁毁工具核心价值、违原则 #6/#8 | 对抗复核 high·行号全核 | 评估关闭(无动作) |

| F63 | fixed | handler 多行 body 必须 flush-left（col 0）——`assemble.go writeBody` 给每行加 8 空格、不 dedent → agent 按"在方法里"自然缩进写 → 双重缩进成 Python IndentationError → 因 import 在 try/except 外 → 只表现为不透明 `subprocess crashed`。round-7 | promise≠reality/脆弱·medium | `app/handler/assemble.go` writeBody 先 `dedent`（归一公共缩进到 0、保留相对、flush-left 不动）再加 8 空格 | 零 token 回归 `TestWriteBody_Dedent`（flush-left 不变/均匀缩进归一/嵌套保留/空→pass）；make verify 绿 | _pending_ |
| F64 | fixed | handler **import-time 错**（语法/坏 import/IndentationError，发生在 `from user_handler import HandlerImpl` 模块顶、try/except 外）→ 进程在协议启动前崩、客户端只能报不透明 `subprocess crashed` 无因。round-7 确诊 + round-21 buildhard **具体复现**（mixed-indent 畸形体）。F63 dedent 只解 uniform 缩进 | **HIGH**（恢复无门/不可发现/静默降级；handler 子进程错误透明度） | **比原计划（client stderr 环缓冲·跨层 surgery）更干净**：DriverScript 把 `from user_handler import HandlerImpl` **从模块顶移进 main() 的 init try/except**——语法/import/缩进错现走既有 `init_error`（带 `traceback.format_exc()`）→ client `ErrInitFailed` 携 trace（line 108 已有）→ agent 见真 `IndentationError: unexpected indent (user_handler.py, line N)` 非不透明 crash | Python sim 验证（畸形体→init_error 带 file:line+kind=IndentationError）+ Go build + handler 单测（assembler 完好）+ make verify 绿；走既有已验证 init_error 路径（同 HandlerImpl() 构造错），故未另跑 :8743 端到端 | _pending_ |
| F65 | fixed(措辞) | sensor **level-triggered**：持续坏态每 poll 一条新 firing、无内建 edge-trigger 选项。round-7 sensordeep lane | 脆弱/白烧·medium（非 correctness bug、产品 affordance） | **对抗复核：flowrun 风暴已被并发策略中和**（默认 serial 排队、非真并起一堆）——唯残留=白烧 firing 行 + 无 edge-trigger 选项。故**写清节奏**：`create_trigger` 工具描述补「level-triggered、持续坏态每 poll fire、受并发策略约束、无内建 edge-trigger」+ 顺手修 targetKind 漏 `mcp`（域 config.go:81 三选一）+「method handler only」→「handler/mcp」；trigger.md §3 加电平触发显式注。edge-trigger(存跨 poll 状态)**搁置**，待真有「翻转触发一次」需求 | 复核 high·域校验核 + make verify 绿 | _pending_ |

| F66 | fixed | agent invoke 失败时执行记录记**不透明 `agent loop error`**、丢真 provider 错——loop 捕获了 errCode/errMsg（写进 message 流）但**返回的 `loopapp.Result` 不带**，invoke.go:114 用泛常量。坏 modelOverride invoke 时复现。**= F34 的执行记录面**。round-8 modelovr lane | **HIGH**（恢复无门/静默降级；执行记录真因丢失） | `loop.go` Result 加 `ErrCode`/`ErrMsg` + 终态 return 带上；`agent/invoke.go` 用 `result.ErrMsg`（空才回落常量） | 零 token 回归：`TestRun_MaxStepsReached` 断言 `res.ErrCode/ErrMsg` 非空；make verify 绿 | _pending_ |
| F67 | not-bug | 坏 modelOverride（不存在 apiKeyId / bogus modelId）**set-time 接受**——`validateModelOverride` 只拒空 id。**round-18 确认 = intended**：runtime-graceful 设计（set 时接受、use 时拒、错干净点名有效 model「supported names are deepseek-v4-pro/flash」；与 `SetDefaultSearch` 注释「rejects at call time, UI only offers valid options」一致）。经 F66（invoke 透真因）后该 use-time 错已不透明问题。**撤出待修** | 假成功/脆弱→intended·非bug | — | — | — |
| F68 | fixed | 无 agent 工具发现 model/api-key 配置 → agent 设 modelOverride 全靠猜。round-8。**round-18 升级 HIGH**：被问「列出配置的 key/model」agent 无工具 → **grep 主机 FS + 读 `backend/.env` → 明文泄露真 key + 臆造假审计**（谎称 OpenAI/gpt-5.5）。 | **不可发现/能力缺口/假成功·HIGH（安全：密钥泄露 + 假审计）** | 新建只读工具 `get_model_config`（`app/tool/model/config.go`，无参，注册进 toolset Lazy）——投影三场景默认 ModelRef + `apikey.Service.List`（**KeyMasked 脱敏、绝不出原文**）+ `CapabilityService.List` 可用模型 | 契约单测 `TestGetModelConfig_Contract` + **:8743 v5 行为验证**：agent 问配置→**调 get_model_config**（结果含 defaultModels/keyMasked/deepseek-v4-flash）、**零 FS grep**、key 仅脱敏形（`sk-66c8…` 非明文）；make verify 绿 + 同提交 `support-services.md` 同步 | _pending_ |

| F69 | fixed | author-time control/approval/sensor CEL 编译错**丢弃真 cel-go 因**、只返裸 sentinel（`ErrInvalidCEL`/`ErrInvalidTemplate`）→ agent 必须**猜**怎么修。F8 给 workflow CEL 做了、control/approval/sensor 没（**= 透明度轴 author-time 面**）。round-9 ctlerr lane（透明度 sweep） | promise≠reality/不可发现·medium | control/approval/trigger `crud.go` 三处 CompileFor/CompileTemplateFor 失败处把 `err.Error()` 装进 sentinel 的 `WithDetails{reason,...}`（保 Code/Kind/errors.Is；reason 经 F7 llmErrText 透给 LLM） | 零 token 回归 `TestCreate_InvalidWhenCEL` 断言 Details.reason 非空；F14 命名空间测不回归；make verify 绿 | _pending_ |

| F70 | fixed | `add_node` op 把放错位的**顶层 `input`**（当 `node` 的兄弟而非放其内）**静默丢弃**（ops.go OpAddNode 只读 `node`）→ 节点无接线跑、只在运行时崩不透明 `main() missing N required arguments`。agent 烧 3 次 trigger。round-10 durabledeep lane | promise≠reality/静默降级·medium | `domain/workflow/ops.go` add_node 加 `strayNodeKeys` 检测顶层误放的 input/ref/kind/retry/fromPort → 清晰报错「must be inside node」（经 ErrInvalidOps.WithDetails 透 LLM） | 零 token 回归 `TestApplyOps_StrayTopLevelInputRejected`（误放 input→ErrInvalidOps+Details.reason 含 input；放对→过）；make verify 绿 | _pending_ |
| F71 | fixed | `capability_check` 不校 action/agent 节点的**必填 input 是否接线** → 缺接线的图 ok:true、运行时崩 `missing required argument`。round-10 durabledeep lane | promise≠reality/不可发现·medium | **关键发现推翻「需加 Required 标志」前提**：`pkg/schema` **刻意无 required 标志**（包注释明写「无 required」）、且 `ToJSONSchema` **已视全声明字段必填**——模型本就「**声明即必填**」。故选**用户拍板的 Option A**（顺设计、不动 schema 24 文件）：resolver 把 fn/hd(.method)/agent 的声明 input 名灌进新 `RefInfo.DeclaredInputs`（`FunctionVersionReader` 加 `GetVersion`、新 `fieldNames` helper），`CapabilityCheck` 报每个未接线的声明 input（mcp/control/approval 留空自然跳过）。可选参数约定「别声明成 input、靠代码默认值」 | 零 token 2 测：`TestCapabilityCheck_RequiredInputWiring`（漏接→problem、全接→clean）+ resolver `ResolvesEachKind` 加 DeclaredInputs 断言（fn/hd/agent 各灌对）+ make verify 绿 + 同提交 workflow.md 同步。**（注：首次实现时与后台 F37 agent 同目录 make verify 撞、WIP 被回退；已隔离重做）** | _pending_ |
| F72 | fixed | 跨 workspace `GET /conversations/{id}/messages` + `/usage` 返 **200-空** 而非 404（chat.go:507/529 直查 messages repo、缺前置 Conversations.Get）——**无泄露**（ORM workspace 过滤兜底、零外部内容/token 越界）、仅与其他端点 404 不一致。round-10 isolation lane（D2 边界全程守住） | promise≠reality·low（一致性、非泄露） | `chat.go` ListMessages/Usage 各加前置 `Conversations.Get`→外部/不存在 id 即 `CONVERSATION_NOT_FOUND`（镜像 SystemPromptPreview）；无新码 | 零 token 回归 `TestListMessagesUsage_ForeignConversation404`（直测外部 id→404 路径）；make verify 绿（:8743 binary 预于本 fix、未单独 live curl——单测已覆盖该路径） | _pending_ |

| F73 | fixed | 并发 `:edit` 版本碰撞**泄露泛 `ORM_CONFLICT`**（6 个版本化实体）——版本行 save 的 orm.ErrConflict 未翻译（不像 name 行翻 ErrDuplicateName）。数据完整、仅 wire 码泛化。round-11 concur2 lane | 静默降级·low | **并行调研定 per-entity（非共享码）**——orm.ErrConflict 是刻意泛型、domain 翻具体码是约定。6 域各加 `Err­VersionConflict`（`<ENTITY>_VERSION_CONFLICT`·KindConflict·409），6 store 的 SaveVersionAndActivate（workflow=SaveVersion）版本 save 把 ErrConflict 翻成它（镜像既有 name-conflict 译法） | 零 token 回归 `TestFunction_VersionConflict`（同号版本 save→ErrVersionConflict；其余 5 域机械同形、build+grep 验全 6）+ make verify 绿 + 同提交 error-codes.md（6 码·265→271）+ 6 domain.md 码计数同步 | _pending_ |

| F76 | fixed | workflow 工具描述（opsDoc）**漏 simple-merge/剪枝 join 规则** → agent 臆造「多入边=严格 AND-join→死锁」、删掉 approve(no)→log 边、**首建漏记被拒工单**（静默不完整）。规则在 scheduler-flowrun.md 有、但 create/edit_workflow 工具描述里没。round-16 e2esystem lane | 不可发现/组合摩擦·low（= F8/F32 工具描述教模型类） | `tool/workflow/workflow.go` opsDoc 加 MERGE 行：节点按「实际走到的分支」入边就绪、剪枝入边忽略、汇聚互斥分支到同节点安全无死锁 | make verify 绿（工具描述串、无逻辑改） | _pending_ |
| F77 | fixed | **文本附件无截断帽**（`attachment.go inlineText` 直 `fmt.Sprintf` 无长度检查）、而文档抽取路径有 `truncateForLLM`（cap 400K + 标记）→ 大 text/CSV（实测 3.35MB/~837K token）整体灌进 LLM context、**无 app 侧护栏/信号**、静默溢出。round-16 attachdeep lane | 静默降级/脆弱·low | `inlineText` 也走 `truncateForLLM`、超 cap head-cap + 标 `(truncated)`（对齐文档抽取路径） | 零 token 回归 `TestInlineText_CapsOversized`；make verify 绿 | _pending_ |
| F78 | fixed | 删除/失效的 attachment id **静默从回合丢弃**、无用户可见信号（继续 continue）。round-16 attachdeep lane | 不可发现/静默降级·low | not-found / blob 不可读 id 各发 textNote 占位（`a referenced attachment (id) is no longer available` / `attachment %q is no longer available`）、镜像既有降级模式 | 零 token 回归 `TestToContentParts_NotesMissingPreservingOrder`（占位 + 保序）；make verify 绿 | _pending_ |
| F79 | fixed | webhook HMAC：**正确验证 header（`X-Hub-Signature-256`）从任何工具面都不可发现** → agent 教用户用错 header（`X-Signature-SHA256`）→ 每个正确签名的 POST 全 401。round-16 webhookauth lane（auth 强制/body cap/method gate/dedup 本身全绿） | promise≠reality/不可发现/假成功/脆弱·medium | `create_trigger` 工具描述 webhook 行加**调用方验证契约**：明文→`X-Webhook-Secret`/`?token=`；HMAC→`X-Hub-Signature-256: sha256=hex(hmac_sha256(rawBody,secret))`（头名可经 `signatureHeader` 改）。契约在所有 agent 常读的 create_trigger 描述里、故 get_trigger 无需再surface | 同提交 `domains/trigger.md` 同步精确 header 契约；make verify 绿（工具描述串） | _pending_ |

| F80 | fixed（校准经 F110 修正） | 混合语义搜索**无相关性下限**——`semantic.go fuseSemantic` 只 `cosine > 0` 就纳入 → **无匹配/乱码 query 把整个 workspace 灌出**。round-17 searchdeep lane。⚠️ 本行所述 0.7 校准（"乱码≤0.63/真匹配≥0.81 空隙"）经 round-7 实测证误（真匹配实为 0.549-0.746），下修 0.55 见 **F110** | 静默降级/假成功/promise≠reality·medium | `semantic.go` 加 `cosineFloor = 0.7` 常量、纳入门改 `c > cosineFloor`——**:8743 实测校准**（建 5 主题 fn 语料、临时 log 量原始 cosine：3 乱码 query top ≤0.63 + 4 真匹配 top ≥0.81、**0.63-0.81 空隙**故 0.7 噪声 0.07 margin·真匹配 0.11 margin）；+ 7 个 per-kind `search_*` 工具描述「case-insensitive substring」改「keyword + semantic relevance」（document/search 本就「keyword」） | 零 token 回归 `TestHybrid_CosineFloorRejectsNoise`（cos 0.5 远向量不浮、cos 1.0 近向量浮）+ make verify 绿 + **:8743 v9 端到端**（乱码 omni→**0 hits**·真匹配→对的 fn；前为全实体灌）+ 同提交 search.md 同步 | _pending_ |

| F81 | fixed | handler generator 用 Python `return X` 产终值 → **被静默吞成 null**（status=ok）——driver 的 `for item in result` 看不到 `return` 值（变成 StopIteration.value）；自然惯用法失效、无警告、agent 烧一版才发现要改 `yield 终值`。round-19 hdstream lane | promise≠reality/假成功/静默降级·low | `assemble.go` DriverScript 把 `for` 改 `iter`+`next`+`except StopIteration as si: final=si.value`——**捕获 return 值当终值**（`yield 终值` 仍生效、progress-only 仍 None）；+ `tool/handler/build.go` 工具描述 & `handler.md` 说明两种终值写法都生效 | 三 case Python 逻辑验证（return-X→终值/yield-fin→终值/prog-only→None）+ Go build + handler 单测 + make verify 绿；DriverScript 改 Python 运行时、make verify 不覆盖故另跑逻辑验证 | _pending_ |

| F82 | fixed | handler 注入的**已解密 secret 经 call-log 投影泄露**——用户代码把 secret 嵌进异常 URL / print 时，落 `handler_calls.error_message/logs/output` 明文 at rest + on wire。round-20 hdsecrets lane | 安全·medium（defense-in-depth、非破坏承诺） | **并行调研定可实现路径**（recordCall 原无解密值访问）：`Instance` 加 `SecretValues []string`，spawnInstance 在 LoadConfig+schema 过滤后从 `config[sensitive arg]` 收集解密值；recordCall 落库前 `scrubSecrets` 把这些值从 error/logs/output(if string) 掩成 `********`——只抹平台自注入密钥、保留其余 trace 可调试。config-at-rest 加密 + read-mask 另算 | 零 token 双测：单测 `TestScrubSecrets`（掩 secret 留 trace）+ **端到端 wiring `TestCall_ScrubsLeakedSecretInAudit`**（建 sensitive api_key handler→set config→泄进 call err→断言审计行已掩）+ make verify 绿 + 同提交 handler.md 同步 | _pending_ |

| F83 | fixed | function 运行**无服务端 wall-clock timeout**——与 handler 非对称。inf-loop function 钉死 worker 直到 client 断连（**workflow 节点无「用户导航走」会钉死 scheduler worker**——真 gap）。round-9 + round-21 两次观察 | 脆弱/白烧·low | **并行调研抓到 obvious-fix 陷阱**：`SpawnOpts.Timeout` 会让超时冒成 `ErrSpawnTimeout`（Go err）、ctx.Err()≠DeadlineExceeded → 误记 failed、`ExecutionStatusTimeout` 仍不可达。**正确修**：`RunFunction` 给运行套**外层 ctx** `WithTimeout(FunctionRunSec)`（对标 mcp/calltool.go + handler/call.go）→ deadline 下沉 sandbox exec ctx（pgroup-SIGKILL）+ recordExecution 收 `cctx.Err()` → 既有 ExecutionStatusTimeout 变可达。新 `limits.Timeout.FunctionRunSec`（默认 300s、settings.validate 校 >0） | 零 token 单测 `TestRunFunction_WallClockTimeout`（blocking runner + 1s 限 → status=timeout）+ make verify 绿 + 同提交 function.md 同步 | _pending_ |
| F84 | fixed | **MCP 市场 works-now remote 端点陈旧/失活 → 标"可用"却连不上**。真 agent 装 `cognitionai/deepwiki` → `failed to connect: Gone`（snapshot 给的是**已弃用的 SSE** `/sse`，server 已迁 streamable-http）。GENERALIZE 扫所有 works-now remote：`antfu/nuxt-mcp` 的 host `mcp.nuxt.com` **DNS 直接不解析**（且它本是 per-project Vite 插件、无 hosted remote） | works-now remote（snapshot URL 陈旧/SSE 弃用/host 死） | deepwiki overlay 改 URL→`mcp.deepwiki.com/mcp` + streamable-http（实网 200 验证）；nuxt-mcp **移除**（dead host + 非 hosted server）。这是 registry 陈旧的**事实型**纠正（合 0620 忠实度设计） | 真后端复跑：deepwiki 装机 **status=ready、3 tools**（前 Gone）；nuxt 从市场消失、97 条；make verify 绿 | _pending_ |
| F85 | fixed | **works-now remote 端点 transport_type 标签陈旧（SSE 弃用后多数实为 streamable-http）→ 单一固定 transport 静默连不上**。真后端装 `microsoftdocs/mcp` failed（snapshot 标 sse、实服 streamable-http）；扫所有 remote 又发现 servicebricks 同症但更恶——它服 streamable 却返 text/event-stream 把 go-sdk **挂死 3:55**（per-attempt ctx 不被 sdk 尊重） | works-now remote（snapshot transport_type 陈旧/sdk hang） | 地基 `infra/mcp/client.go`：`connectRemote` **先试声明 transport、失败退另一个**（`remoteTransportOrder`）+ 每次 `remoteConnectAttempt`(25s) 限界（session 比连接 ctx 活得久、取消安全）。microsoft 修；servicebricks 移除（go-sdk 对它挂死、无法可靠交付，同 nuxt） | 真后端复装 microsoft **ready/3 tools**（前 failed）+ deepwiki ready；零 token 单测 `TestRemoteTransportOrder`；make verify 绿 | _pending_ |
| F86 | fixed | **works-now stdio 全量真装扫描（27 零配置候选）**：15 真连上（chrome-devtools 29 / azure-devops 90 / fabric-rti 39 tools…）；3 正确 `MCP_ENV_MISSING`（logfire/launchdarkly/clarity——token 是 **arg 占位**、不在 package env，sweep classifier 漏判为零配置）；**5 个 `initialize: EOF`——进程起后即退**，因需**外部运行时前置**：dbhub→DB DSN、unity-mcp→Unity Editor、mcp-foundry→azure 登录、imagesorcery→opencv、serena→项目/打包 | works-now stdio（需用户自有资源的 server，"works-now" 过度承诺零配置） | **非代码 bug，设计级**：这些是真工具（有前置的用户能用），但对无前置用户 EOF 失败、且市场不传达前置。选项 = ① convey 前置（描述/必填 env，如 dbhub 要 DSN）② 接受其对无前置用户失败 ③ 移除最依赖外部 app 的（unity/imagesorcery）。（用户选）**serena 修好 + 其余标注前置**：serena 加 `Package.From`（`uvx --from git+repo serena start-mcp-server`，registry 调用漏了 start-mcp-server）→ 真装 ready/22 tools；dbhub/mcp-foundry/unity/imagesorcery 加 `RegistryEntry.Prerequisite` 注解（与 registry 描述分开、不覆盖），市场列出「需 DB/azure 登录/Unity/opencv」 | serena 经 catalog 真装 ready/22 tools（前 EOF）；4 个 prereq 在市场列表可见；make verify 绿 | _pending_ server 全 ready（验证主体真可用）；5 失败均 EOF（确诊需前置非配置 bug） | _pending_ |

| F87 | fixed(措辞) | 手动 `trigger_workflow`（StartRun「run now」）**绕过 workflow 并发/重叠策略**（policy 仅 `consumeFiring` 真 fire 路径施加），但工具描述/workflow.md 不说 → agent 测 replace 时见两手动 run 并发、烧 ~120 词推理 + 先错后纠的假设。round-补薄格 concurrency lane | promise≠reality/不可发现·low | `tool/workflow/exec.go` trigger_workflow 描述点明「手动 run 立即跑、不应用并发策略（serial/skip/buffer_one/replace），策略仅治真 trigger fire」+ workflow.md §并发 同步「overlapDecision 仅作用真 fire、手动 :trigger/StartRun 绕过」 | make verify 绿（描述串、无逻辑改，同 F76/F65）+ 同提交 workflow.md 同步；**真 fire 行为已实测**（见元注：webhook replace 取消 2 在途 run、buffer_one defer 2 fire 留最新） | _pending_ |

| F88 | not-bug | capability_check 校声明 input(F71)/method·tool·port(F51) 但**不校引用上游不存在的声明 output 字段** → `x←apply_discount.orderId`（apply_discount 只声明 {discountedTotal,tier}）过绿、运行时 fail-fast 崩 `no such key: orderId`。round-explore dataflow lane（= F35 深层「静态 dataflow 校验」frontier） | promise≠reality/不可发现·low | **GENERALIZE/soundness 反射否决「显然该加」**：声明 output 对 fn/hd **是 advisory**（F40——`toResultMap`(dispatch.go:178) 把 callable **真实返回的 dict 直通**、键随实际返回非声明），故静态按声明 output 校下游引用**不 sound**：会**假阳**（fn 返回声明外键→合法 `node.bonus` 被误标坏）+ **假阴**（fn 少返键→非法 `node.declared` 放行）——比诚实「不校 dataflow」(F35)**更糟**。input 校得动（声明即必填、引擎喂声明 input）≠ output（advisory），不对称是**有意**。唯一 sound 子集=agent 上游（F40 强制声明 output）但 agent-output 接线罕见、不值机械。运行时 fail-fast（精确 `no such key`）+ needsAttention banner（lane 实测均对）是正解 | 读 toResultMap/F40 坐实 advisory；不动代码 | — |

| F89 | fixed | LLM 可见 tool 错误**泄露内部 Go 包.方法路径**（`functionapp.RunFunction: function not found`）——`llmErrText`(tools.go) 渲 `err.Error()` 整条 `fmt.Errorf("pkg.Method: %w", …)` 链而非 sentinel 的 `Message`，违 S20「LLM 读 Message」。round-1(0621) tooload lane | promise≠reality/契约卫生·low（**系统性**：全库 app 层皆 `pkg.Method: %w` 包裹、全经此一处渲 LLM） | `loop/tools.go` llmErrText：`errors.As` 命中 `*errorspkg.Error` 时基于 `de.Message`+Details 构造（非 err.Error() 链），非结构化错误才回退 `err.Error()`——一处净化所有工具错误面 | 零 token 回归 `TestLLMErrText` 加 wrapped-sentinel case（`fmt.Errorf("functionapp.RunFunction: %w", FUNCTION_NOT_FOUND)`→须返干净 "function not found"，旧码返带前缀）；F7 三断言不回归；make verify 绿 | _pending_ |

| F90 | fixed(措辞) | `trigger_workflow` 的 payload 参数说"match the shape the entry trigger emits"但**不给 per-kind 形状** → agent 试错 synthetic payload（猜平铺 `{amount}` 而非 webhook 的 `{body:{amount}}`），**ergo+rename 双 lane 各烧 2 失败 run**。round-1(0621) | 不可发现/白烧·low（双 lane CONFIRM；手动 run 路径，info 在 create_trigger 但 trigger 时不在手边） | `tool/workflow/exec.go` payload 参数描述列出 per-kind fire-payload 形状（webhook 嵌 `body`、cron `{firedAt}`、fsnotify `{path,eventKind,firedAt}`、sensor 配置 output，并指向 create_trigger） | 零 token `TestTriggerWorkflow_PayloadDescribesEntryShape`（params 须含 body/fsnotify/create_trigger）；make verify 绿 | _pending_ |

| F91 | fixed | `list_mcp_marketplace` 无参/无过滤 → 一次**倾倒全部 ~96 个 server inline**（~30KB），agent 找一个通知器也要拉全量、撑爆 context（round-1 ask lane MAX_STEPS 共因之一） | 不可发现/白烧·medium | `tool/mcp/system.go` 加可选 `query` 子串过滤（name+description 大小写不敏感），过滤逻辑抽 `filterMarketViews` 纯函数；描述点明优先用 query；无 query 仍返全部（向后兼容） | 零 token `TestFilterMarketViews`（query 过滤/大小写/不可装隐藏/无 query 返全/暴露 query 参数）；make verify 绿 | _pending_ |

| F92 | fixed | handler 方法调用**无服务端墙钟超时**（`call.go` opt-in `spec.Timeout>0` 才有、默认 0=无界），与 fn(F83)/agent/mcp 强制全局墙钟**不对称** → 失控/`sleep(120)` method 跑满无超时（`elapsedMs:120003`）、钉死常驻实例单 mutex stdio 管道；且 per-method `timeout` 旋钮对 LLM 不可见（add_method op 不列）。round-2(0621) hasync lane | 脆弱/promise≠reality·**HIGH**（系统正确性：无界资源钉管道，同 R 维度） | ①`limits` 加 `HandlerCallSec`（默认 300、镜像 FunctionRunSec；+Schema/WithDefaults/validate 四处）②`call.go` 抽 `methodCallTimeout(spec, global)`：spec.Timeout>0 用之、否则全局兜底——**每次调用必有界** ③`add_method` op 暴露 per-method `timeout`(ms) | 零 token `TestMethodCallTimeout`（spec=0→全局 300s、spec=5000ms→5s、spec=600000→600s、default>0）+ `TestSchema_MatchesStruct` 反射守不破；make verify 绿 + 同提交 handler.md（墙钟默认）同步（function.md:49「与 handler 同款墙钟」此前是 doc-ahead 漂移、本修使其成真） | _pending_ |

| F93 | fixed | LLM 流**无总墙钟**——`provider.go` idle-timer **每个事件 `timer.Reset`**（注释自承"非总墙钟"）→ 模型持续滴事件却不收敛（病态 reasoning/空 delta 循环）时 idle 永不触发、流**永困**：message 卡 `streaming` 15+min、CPU 钉死 118%、**SIGTERM 阻塞 graceful shutdown**（连带泄漏 handler 子进程 + embedder 孤儿）。round-2(0621) vision lane（2nd-tool-turn+attachment 触发 deepseek 病态生成，2/2 复现 + 我亲验 CPU 钉 25min+孤儿） | 脆弱/假成功·**HIGH**（系统正确性：无界资源钉 CPU + 阻塞关机，同 R 维度发烫族） | `limits` 加 `LLMStreamMaxSec`（默认 600、**不随活动重置**；+Schema/WithDefaults/validate）+ `provider.go` Stream 加第二计时器 `AfterFunc(maxStream, cancel)`（与 idle-timer 并行、不 Reset）→ 触发即 cancel streamCtx → ParseStream 断 → yield 区分的 `total budget` ErrProviderError；transport.go/provider.go 注释同步（删"无总墙钟"） | 零 token `TestStream_TotalCapTerminatesNonConvergingStream`（永不结束 SSE 流 + cap=1s/idle=150s → 须 <5s 报 total-budget ErrProviderError；旧码永挂）+ F33 idle 测不回归；make verify 绿 | _pending_；深层（为何 deepseek 在 attachment+tool-history 投影卡=BlocksToAssistantLLM 嫌疑）记 watch、model-specific 不阻塞 |

| F94 | fixed(措辞) | `fire_trigger` 描述"mainly for testing"诱导 agent 传 `{body:...}` 测 webhook workflow，但工具只 `FireManual`（合成 `{manual:true}`）、**body 静默丢** → 监听 workflow 读 `start.body.*` 拿空、agent 须从 activation `{manual:true}` 自诊后退回 trigger_workflow。round-2(0621) longhaul lane | 不可发现/promise≠reality·low | `tool/trigger/manage.go` fire_trigger 描述点明「不带自定义 payload（仅 `{manual:true}`）、要喂数据用 trigger_workflow」+ trigger.md `:fire` 同步 | 零 token `TestFireTrigger_DescriptionRedirectsForPayload`（描述须含 manual/custom payload/trigger_workflow + Parameters 仍合法 JSON）；make verify 绿 | _pending_ |

| F95 | open | capability_check 不校 `start.<field>` vs 入口触发器 **canonical outputs** → agent 把 webhook 触发器当 manual 入口、节点读 `start.topic`（webhook 不发 topic）过绿，真 fire fail-fast。round-2(0621) agnest lane | 假成功/不可发现·low | **候选 sound 修**（异于 F88：固定 kind 触发器 outputs 是 canonical 非 advisory，F30 CanonicalOutputs）：capability_check 对读 `start.<topField>` 校 topField ∈ 触发器 outputs（`body` 在→`start.body.*` 放行；`topic` 不在→警告）；sensor 用作者 config.output。**待排期** | — | 排期(low) |

| F96 | fixed | `create_agent`/`edit_agent` **静默接受不存在的 skill 名**（只校 tools/modelOverride、不校 skill）→ 持 dangling ref、建出 **dead-on-arrival** agent，**只在首次 invoke 才报** `skill not found`（与 F71 eager 校验、与 ValidateTools 不对称）。round-3(0621) skillagent lane（primary 绿：装备 skill 指南真注入嵌套 run；edge 逮此） | 假成功/promise≠reality·medium | `app/agent/crud.go` Create/Edit 加 `validateSkillMounted`（经 invoke **同一** `SkillGuide.Guide` 解析、nil 容忍）→ 不存在返新码 `AGENT_SKILL_NOT_FOUND`(422、details.skill)；`create_agent` skill 描述补「must already exist」 | 零 token `TestCreateEdit_RejectsDanglingSkill`（不存在 skill create/edit 拒、存在过、无 skill 过）；make verify 绿 + 同提交 error-codes.md（新码+计数 283→284/278→279）/overview.md/agent.md 同步 | _pending_ |

| F97 | fixed | F83 函数墙钟**正确封顶但误分类状态**：runaway 被 deadline-SIGKILL 后 `cmd.Wait()` 返 `*exec.ExitError`，`spawn.go` 的 `errors.As(ExitError)` **先**捕获、返 `(Ok=false, nil err)` → DeadlineExceeded 分支不可达 → run.go 走 `case res!=nil` 记 `status=failed`/"python exit -1"（非承诺的 `ExecutionStatusTimeout`），:triage 无法辨 timeout 与代码崩；且 `TestRunFunction_WallClockTimeout` 用 fakeRunner 直返 ctx.Err()、**mock 掉真 SIGKILL 路径**（假信心测）。round-3(0621) resource lane（同轮 live 确认 F92 handler timeout 绿） | promise≠reality/leaked-internals·medium（F83 契约静默降级；handler 路径对、function 路径错=不对称） | `infra/sandbox/spawn.go` SpawnOnce 在 `errors.As(ExitError)` **前**先查 `ctx.Err()`：deadline → `ErrSpawnTimeout`、cancel → 非 nil(含 cause) → adapter 透传 spawnErr → run.go `sandboxErr!=nil` 按 runCtxErr 重分类 timeout/cancelled；删原不可达的 deadline 死分支 | 零 token **真路径**测 `TestSpawnOnce_DeadlineReturnsTimeout`（`sleep 30`+100ms deadline → 须返 ErrSpawnTimeout；NonZeroExit 不回归）；make verify 绿。无 reference 改（function.md:49 早已声称 timeout 分类、本修使其成真——doc-ahead 闭合） | _pending_ |

| F99 | fixed(措辞) | `create_document` 描述称 ">1MB content split into child docs"（读作**自动拆分**），实际硬拒 413 `DOCUMENT_CONTENT_TOO_LARGE` + 让手动拆 → agent 把假契约当事实转告用户、白烧 25 次工具调用试建超大 doc。round-4(0621) bigio lane（三类 cap 本身全绿、显式信号截断、DB-is-truth） | promise≠reality·high（主动误导，非 cap bug） | `tool/document/create.go` 描述改「max 1MB、超出 REJECTED 非 auto-split、须自己拆子文档」+ document.md:16 同步（"超出拆子文档"→硬拒非自动拆） | 零 token `TestCreateDocument_DescriptionNoFalseAutoSplit`（描述无"split…if"误导、含 1MB/REJECTED/yourself）；make verify 绿 | _pending_ |

| F98 | fixed | F96(skill eager 校验)**未泛化**到 agent 其它挂载 ref：① **knowledge** docID dangling → create 接受 + invoke **静默丢弃**（GetBatch WhereIn 少行无错）、run 报 ok（**比 F96 loud DOA 更糟的静默降级**，HIGH）+ mount-health 对 knowledge 失明 ② **tool** fn_/hd_ dangling → create 接受（ValidateTools 只校格式非存在）、invoke dead-on-arrival（=F96 类；**纠正 F96 commit 文案"tools eager 校验"不准——只 eager 格式**）。round-4(0621) mountval lane | 假成功/promise≠reality/静默降级·HIGH | ① `bootstrap/renderers.go` BuildKnowledgePrefix 检 `len(docs)≠ids`→新码 `AGENT_KNOWLEDGE_NOT_FOUND`(details.missing)、invoke 缺 doc **大声失败** ② `app/agent/crud.go` Create/Edit 加 `validateKnowledgeMounted`（复用 BuildKnowledgePrefix）+ `validateToolsMounted`（复用 invoke 同一 `Mounts.CheckHealth`→不可解析返 `AGENT_MOUNT_INVALID`）；build.go knowledge 描述补「must exist」 | 零 token `TestCreateEdit_RejectsDanglingMounts`（dangling knowledge→KNOWLEDGE_NOT_FOUND·dangling tool→MOUNT_INVALID·valid 过·edit 同）；make verify 绿 + 同提交 error-codes.md（新码+计数 284→285/279→280+修 stale 282）/overview.md/agent.md 同步 | _pending_；mount-health 覆盖 knowledge 留 frontier（precheck UI 次要、核心静默降级已由 invoke fail-loud+create 校验闭合） |

| F100 | fixed | chat 回合**无 per-turn 墙钟**（`runner.go` turn ctx 是 `WithCancel(Detached)`、只受 MaxSteps 步数封顶）→ 流/工具卡过每步守卫的回合在 **detached ctx 上永跑**：message 卡 isGenerating、runQueue 协程不返回、`wg.Wait()` **阻塞 graceful shutdown**（detached ctx 不被 shutdown 取消，SIGTERM 需 SIGKILL）。**round-2 vision + round-4 memagent 两度实测**（CPU 钉 180% + 连带泄漏 handler/embedder 孤儿，皆在 **F93 已含** binary 上——F93 是 per-stream、缺 per-turn 兜底） | 脆弱/假成功·HIGH（systems-correctness：无界回合钉资源+阻塞关机，同 0619 发烫 R 维度） | `limits` 加 `ChatTurnSec`（默认 1800、5 处）+ `runner.go` turn ctx 改 `WithTimeout(ChatTurnSec)`（同 F83/F92/F93/AgentInvokeSec 族）→ 超时取消 ctx → loop 在飞 op（respects ctx 的 stream/tool）终止 → finalize 清 isGenerating | 零 token `TestProcessTask_TurnWallClockFinalizes`（ctx-respecting stall client + ChatTurnSec=1s → turn 须 finalize 非卡；旧码 waitClose 2s 超时）+ `TestSchema_MatchesStruct` 反射守；make verify 绿 + chat.md 同步 | _pending_ |

| F101 | open(watch) | **busy-loop 钉 CPU + 阻塞 graceful shutdown 的深层因未定位**：vision/memagent 两度见后端 SIGTERM 阻塞需 SIGKILL + CPU 180% 钉死；F93(stream 总墙钟)+F100(turn 墙钟)兜住 **ctx-respecting** 卡死，但若某 goroutine **不查 ctx** 的紧循环（疑 deepseek 高并发下流式投影/SSE 解析病态、BlocksToAssistantLLM 嫌疑）则两墙钟的 cancel 都打不断。round-4(0621) memagent | 脆弱·HIGH(systems) | **需 pprof 实测定位**：dev-gated `/debug/pprof/goroutine?debug=2` 抓钉死时 goroutine 栈（R 维度已加 pprof）；找到不查 ctx 的紧循环再点修。F100 已兜 ctx-respecting 大部分 | — | 排期(需 reproduce+pprof) |

| F105 | fixed | F97 修对了 status=timeout 但**持久错误消息**仍是泄露的 sandbox 内部 `spawn process timeout`（暗示进程**启动**失败）→ :triage 两度被误导去追幻象 env/冷启动问题（才靠 while-True 代码自纠）。round-5(0621) triagedeep lane（:triage 区分 crash vs timeout 本身全绿、F97 status 实测 live 确认 `function_executions.status=timeout`） | promise≠reality/leaked-internals·medium | `function/run.go` recordExecution timeout 分支 errMsg → 「function run exceeded the Ns wall-clock limit」（镜像 handler RPC timeout / mcp），cancel 分支同改清晰串——不再用 sandboxErr.Error() 的 spawn 措辞 | 零 token 扩 `TestRunFunction_WallClockTimeout` 断言 errorMessage 含 "wall-clock"、不含 "spawn"；make verify 绿 | _pending_ |

| F102 | fixed | create/edit_trigger **不 eager 校验 sensor 目标存在**（fn_/hd_/mcp dangling 接受、只首次 probe 才 loud-fail；F96/F98/F112 校验家族**未泛化到 trigger**）。round-5(0621) trigcfgval lane（fire-time loud-fail=比 F98 静默丢轻，故 medium） | 假成功/promise≠reality·medium | **不需加宽 run-only 端口**（更干净）：新 nil 容忍端口 `SensorTargetValidator`（trigger app，setter 注入、同 relations/entities 姿态），bootstrap 装的 adapter 直接走**既有**读侧（function/handler 的 `Get` + mcp 的 `ResolveServerID`，非 run 端口）；`validate(ctx,…)` 加 ctx + 在 sensor CEL 校验后调它，dangling→新码 `TRIGGER_SENSOR_TARGET_NOT_FOUND`(422、details targetKind/targetId)。create_trigger 描述 + trigger.md + error-codes.md(285→286/280→281)+overview 同步 | 零 token `TestCreate_RejectsDanglingSensorTarget`（fake validator：fn_ghost 拒、fn_real 过）+ `TestSensorTargetValidator_Routing`（fn/hd→Get、mcp→ResolveServerID、unknown→err）；既有 sensor 测因 validator nil 不回归；make verify 绿 | _pending_ |

| F108 | fixed | F82 secret 擦除**只盖 recordCall 审计副本**（事后），**漏 live progress/SSE 路径**——handler print()/stderr 携注入 secret 经 stderr 扇出 `MultiWriter(prog,runTerm,logs)` + onProgress **明文先流上 messages/entities SSE + 持久 progress block**（recordCall 才擦缓冲、SSE 已发出）。round-6(0621) handlersecret lane | 安全/静默降级·HIGH（F82 兄弟、live-stream 面） | `app/handler/call.go` 加 `scrubbingWriter`（逐 Write `scrubSecrets`）包 stderr 扇出 sink + onProgress 的 yieldBytes 擦——secret 在到 SSE/block **前**掩码（recordCall 全缓冲擦仍兜跨写切分的 secret） | 零 token `TestScrubbingWriter`（secret 写入前掩 ******** + 报原长）+ F82 `TestScrubSecrets` 不回归；make verify 绿 + handler.md 同步 | _pending_ |

| F103 | fixed | cron 校验器**静默接受 `@every`** 但 error message + trigger.md §4 都断言「@every 不支持」（契约谎言/doc-drift）；且 @every 在非分钟对齐时刻 fire 而 dedupKey 截到分钟=潜在去重对齐隐患。round-5(0621) trigcfgval lane | promise≠reality·low（+对齐隐患） | `infra/trigger/cron/cron.go Validate` 在 ParseStandard 前显式拒 `@` 前缀串（与 doc + 分钟桶 dedup 设计一致）→ crud.go 翻成 `TRIGGER_INVALID_CRON`（消息「@every 不支持」即成真）；trigger.md 本已说不支持、无需改 | 零 token `TestValidate_RejectsAtDescriptors`（@every/@daily/@hourly/garbage 拒、5 字段过）；make verify 绿 | _pending_ |

| F104 | fixed | runtime **flowrun 节点错误列泄露 Go 包路径** `functionapp.RunFunction`（持久化+用户面，get_flowrun / :triage 读）——=F89(llmErrText 修了 LLM 工具面)的**兄弟**、在 flowrun-record 面。round-5(0621) wfnodeval lane | promise≠reality/leaked-internals·medium（系统性） | `scheduler/dispatch.go` 加 `nodeErrText`（F89 模式：errorspkg.Error→Message+Details、裸错误透传），5 个 failNode 站点（input/action/agent/control/approval）的 `err.Error()`/`%v err` 全换之——dangling fn ref 现身干净 "function not found"、function 崩溃的 Python traceback 仍原样透 | 零 token `TestNodeErrText`（wrapped sentinel→干净 Message、Details 附加、raw traceback 透传）；make verify 绿；无 reference 改（节点错误列格式未文档化） | _pending_ |

| F106 | fixed | `chatapp.Send` **每条消息 auto-unarchive**（chat.go:292，本是 feature）→ 在**当前对话**用 manage_conversation(archive) 后下条消息**静默解档**，且工具描述/system prompt 不告诉 agent → archive 当前 thread 静默无效、agent 无信号警告。round-5(0621) convmgmt lane（HIGH） | promise≠reality/hidden-contract·high | `tool/conversation/manage.go` 描述加「归档当前 thread 形同无效——给归档 thread 发任何消息会自动解档；想保归档须停止发消息」（option a，使 agent 能警告而非静默被撤销）+ conversation.md 同步 | 零 token 扩 `Test_manageConversation_Schema` 断言描述含 "unarchive" 披露；make verify 绿 | _pending_ |

| F107 | fixed | **manage_conversation 无 rename** → agent 被要求改名时**臆造 UI 手势**「click the title in the sidebar」（=F38 杀掉的 UI 臆造类、在 rename 面复现）。`UpdateInput.Title` + HTTP PATCH 本支持改名、工具不对称。round-5(0621) convmgmt lane | promise≠reality/能力缺口·medium | `tool/conversation/manage.go` 加 `rename` action（enum +1 + switch 臂 `in.Title=&a.Title`，复用 `Service.Update` 的 Title；rename 需非空 title）+ 描述加「rename 也别臆造 UI 手势、用本工具」+ conversation.md 同步 | 零 token `Test_manageConversation_Rename`（rename 设 Title、缺 title 拒、有 title 过）+ enum 断言更新含 rename；make verify 绿 | _pending_ |
| F121 | fixed | **空分页列表返 `data:null`（非 `[]`）** → 共享 `envelope.Data` 带 `omitempty`（为错误响应省略 data 而设），nil 类型化 slice 装 interface 序列化成 JSON `null` → client 遍历 `.data` NPE，违 N1/N4。round-9 entitydelete lane（live curl 复现：空 functions 列表 `data:null`）。**系统性**：所有 `Paged` 列表端点共此一处 | promise≠reality·low（但跨切面契约 + 前端 DTO 期望数组） | `response/envelope.go`：另立 `pagedEnvelope`（Data **不** omitempty、HasMore 恒在）+ `emptySliceIfNil`（反射把 nil slice 换同类型空 slice → 序列化成 `[]`）；`Paged` 改用之；`writeJSON` 入参 `envelope`→`any`（容两类型）——一处修全部列表 | 零 token `TestPagedEnvelope_EmptyIsArray`（nil slice·空 slice·nil string-slice 三种都须出 `data:[]` 含键）；`TestPagedEnvelope`（非空）/`TestSuccess`/`TestError` 不回归；make verify 绿 | _pending_ |
| F120 | fixed | `PATCH /limits` 把**显式 0 静默重置成默认**（返 200、非 400）→ `functionRunSec:0` 这种越界值被 `WithDefaults`（在 validate 前跑、把零值字段从 Default() 回填）掩盖、静默弹回 300，client 以为设成功了。round-9 limitslive lane（**与 F115 同族**：present-zero vs absent） | promise≠reality/脆弱·medium（校验静默绕过） | `app/settings/settings.go PatchLimits`：把 `WithDefaults` 移到 unmarshal **之前**（补全 BASE、防 cur 残零）、unmarshal patch 后**直接 validate**（不再 WithDefaults）→ 每个 limit 正下限故显式 0/负被 validate 的 `<=0` 拒成 `SETTINGS_LIMITS_INVALID` | 零 token 扩 `TestPatch_RejectsOutOfRange`（+`functionRunSec:0`·`maxSteps:0`·`triggerRatio:0` 须拒）；`TestPatch_PersistsAndHotSwaps` 不回归；make verify 绿（api.md 已称越界 400、无 drift） | _pending_ |
| F119 | fixed | **无 agent 可达的「不重启改名」工具** → handler 的 no-restart `UpdateMeta` 服务方法只接 HTTP PATCH（编辑器按钮）、不在 LLM 工具表 → agent 搜 rename 工具搜不到、错误断定"无非重启路径"，只能用 edit_handler（F117 前=重启抹态）。round-9 handlermeta lane（HIGH，主推此项；F117 是其 defense-in-depth 副项） | promise≠reality/能力不可发现·HIGH（F117 补可发现性） | 加 `update_handler_meta` + `update_function_meta` 工具（S18 5 法、包既有 `UpdateMeta` 服务方法、零新业务逻辑、params `{id,name?,desc?,tags?}` 全可选）：只改 row、不铸版本、不重启/不重建 env；注册进 registry；revert_*/edit_handler 描述改指向它 | 零 token `Test{Function,Handler}Tools_Wiring` 计数 9→10·11→12 + 新工具名断言；make verify 绿 + handler.md(12 工具)·function.md(10 工具) 同步 | _pending_ |
| F172 | fixed | **`/api/v1/*` 的 404（无路由）/405（方法不允许）不走 N1 envelope——标准库 ServeMux 漏纯 `text/plain`** → `GET /api/v1/nonexistent`→404 `text/plain` "404 page not found"；`DELETE /api/v1/functions`→405 "Method Not Allowed"。总解析 `{error:{code,message}}` 的客户端在每个未知路由/错误方法上 JSON 解析失败。round-21 httpenvelope lane（HIGH 确认，REST 实证） | 契约/N1·HIGH（错误 envelope 逃逸口） | `router/chain.go` 加 `envelopeMuxErrors` 中间件（最内层包 mux）：拦 `/api/v1/*` 的 404/405 `WriteHeader`、改写成 `responsehttpapi.Error` 的 N1 envelope（`ROUTE_NOT_FOUND`/`METHOD_NOT_ALLOWED`）、吞掉 stdlib 纯文本 body；仅拦 404/405 状态、其余响应（含大 body）原样透传不缓冲；405 的 `Allow` header 保留 | 零 token `TestChain_MuxErrorsEnveloped`（404→ROUTE_NOT_FOUND envelope、405→METHOD_NOT_ALLOWED envelope、非 /api/v1 404 仍纯文本）；make verify 绿（router 非 handlers/middleware 故不触 S6 裸-response.Error 守卫）+ error-codes.md（2 个新 transport 合成码）同步 | _pending_ |
| F171 | fixed | **`edit_agent` 静默吞掉 name/description/tags（返成功、改动丢失）** → name/desc/tags 在 agent **行**、非 edit_agent 编辑的版本化 config（经 update_agent_meta 改），edit_agent 收到它们既不应用也不报错、返 success、LLM 还报"已应用 tags"——DB 实证 tags 仍空。round-21 revertinterleave lane（MED·high conf） | promise≠reality·medium（与 fn/wf set_meta 不对称） | `tool/agent/build.go` EditAgent.ValidateInput 加 present-key 检测：args 含 name/description/tags 即拒 `AGENT_META_NOT_IN_EDIT`（新码、指向 update_agent_meta）；真 config 编辑（prompt 等）照过 | 零 token `TestEditAgent_RejectsMetaFields`（name/desc/tags 各拒、prompt 编辑过）；make verify 绿 + error-codes.md（AGENT_META_NOT_IN_EDIT）+ agent.md（拒 meta 字段、码 5→6）同步 | _pending_ |
| F170 | fixed | **空列表端点 documents/skills/memories 返 `{"data":null}` 而非 `{"data":[]}`——与有值时 `[]` 不一致、崩客户端 for-of** → 这些非分页 List handler 用 `response.Success`（不过 `emptySliceIfNil`，只 `Paged` 过），故空 nil slice 序列化成 null（F121 只盖了分页列表）。round-21 httpenvelope lane（MED·high conf，REST 实证空→null/有值→[]） | 契约/N4·medium（F121 缺口） | `response.Success` 统一过 `emptySliceIfNil`（非 slice body no-op、单对象不变）——一处修全部用 Success 的列表端点恒 `[]` | 零 token `TestSuccessEnvelope_NilSliceIsArray`（nil slice→[]、单对象透传）；make verify 绿 | _pending_ |
| F169 | fixed | **MCP 市场 env var 信息损：(H2) `list_mcp_marketplace` 把**可选** env 全标 `requiredEnv`（registry 的 required/optional 区分被抹）→ agent 错告用户可选键是必填；(M5) `MCP_ENV_MISSING` 安装错丢掉缺失变量**名**（`%w` 后 `%s` 尾被 Surface 剥掉）** → 装 context7 无 env 实测 status=ready，agent 却言之凿凿"CONTEXT7_API_KEY 必填、不填会崩"。round-21 mcpenvgate lane（HIGH H2 + MED M5·high conf） | promise≠reality/leaked-info·HIGH（projection 丢真信息） | (H2) `tool/mcp/system.go` `envView` 加 `Required bool`、从 `ev.Required` 取；`marketView.RequiredEnv`→`Env`（携 required 标）；工具描述改"each env 带 required 标、required:false 是可选别误报必填"。(M5) `mcp/install.go` 缺 env 错改 `ErrEnvMissing.WithDetails({missing})`（名进 Details、穿 Surface+N1 envelope） | make verify 绿（构造带 required-env 的 registry entry 单测成本高、改为 trivial 字段透传 + WithDetails、既有 filterMarketViews 测不回归、lane 复核）+ mcp.md（line27 已称 is_required 取自 registry、本修使代码符之）同步 | _pending_ |
| F168 | open | **round-20 9 个 medium 一并记（探后 fix 清单，逐条短语）** | round-20 lanes | 以下未修候选：(M2) `search_flowruns`/`GET /flowruns` 静默接受非法 status（如 `status=parked`）返空、误导 agent→应拒非法 status；(M3) marketplace 把**可选** env var 标 `requiredEnv`、与 install gate 不符→诱导 agent 误报必填；(M4) `MCP_ENV_MISSING` 丢掉缺失变量名（HTTP envelope + LLM 错误都没名）→应把名进 Details；(M5) conversation PATCH `attachedDocuments` 接受任意/已删 doc id（HTTP 200 零校验）——F167 已使渲染时可见警告兜底、但 attach 时无校验；(M6) capability_check 对无 input 接线但分支 CEL 引用 `input.X` 的 control 节点过绿、运行时 fail-fast（F71/F156 邻域、F88 空间）；(M7) `GET /flowruns/{id}` 返全部 flowrun_nodes 不分页（1001 迭代 loop=678KB/2003 行）、`?limit/?cursor` 不支持→大 loop run 重负载；(M8) `:reindex` 非原子 purge-then-rebuild→重建窗口内并发搜索返不全/空、无信号；(M9) "not valid JSON" 错误不可恢复、诱导 agent 误归因（F165 已修撞名核心、此为残留措辞） | — | — | — |
| F167 | fixed | **对话挂载的文档被删后静默丢弃（模型/用户零信号）——违团队自己的 F98「悬挂挂载须大声失败」原则** → `RenderAttached`→`ResolveAttached` 用 `GetBatch`（WhereIn 静默丢缺失 id）、不比对 requested vs returned，故已删附件渲成空 `<documents>` 段被 prompt 丢掉；而 agent 知识路 `BuildKnowledgePrefix` 正为此硬化过（diff 缺失→`AGENT_KNOWLEDGE_NOT_FOUND` 大声失败）。round-20 documentversioning lane（HIGH 确认，实测删 doc 后对话仍跑、grounding 静默丢含 SECRET_MARKER） | 静默降级·HIGH（F98 反模式在对话轴重现） | `RenderAttachedAsXML(docs, missing)` 加 missing 参→缺失 id 渲 `<document id=… missing="true">` 可见警告行；chat renderer `RenderAttached` 比对 atts vs docs 算 missing 传入（对话附件降级为 prompt 内警告、非像知识那样让整回合失败=太激进）；知识路传 nil（它已大声失败在前） | 零 token `TestRenderAttachedAsXML_MissingWarning`（present 渲内容、missing 渲 `missing="true"` 警告、空输入渲空、全 missing 仍警告）；make verify 绿 + document.md（缺失附件可见警告）同步 | _pending_ |
| F166 | fixed | **移除 MCP server 孤儿化每条 agent/workflow→它的 equip 关系边（purge 按 mcp_ id、边按 NAME 键）** → `computeMountEdges` 把 `mcp:<名>/tool` 削成**名**存 equip 边 toId（如 "markitdown"），而 `RemoveServer.purgeRelations(srv.ID=mcp_…)` 按 id purge、永不匹配名-键边 → server 删后悬挂 agent→mcp 边存活、污染依赖图（关系图正声称 agent 依赖一个已删 server）。round-20 mcpmarketplace lane（HIGH 确认，get_relations 实证孤儿边存活） | 数据完整性/可观测性·HIGH（关系图与实体配置不一致） | `app/mcp/relations.go` `purgeRelations` 改变参、按**每个**键 purge；`install.go RemoveServer` 传 `srv.ID, srv.Name` 两键；`NamesByIDs` 先按 id 再按 name hydrate（名-键节点也显示名）。targeted 修（停孤儿）；更深的"边统一存 mcp_ id"（option a，需 resolver 入 edge-sync）为已知设计项 | 零 token `TestRemove_PurgesRelationsByIdAndName`（fake RelSyncer 记 purge 键、RemoveServer 须按 mcp_ id + 名两键 purge）；make verify 绿 + mcp.md（equip 边 name-or-id 键、两键 purge）同步 | _pending_ |
| F165 | fixed | **`rawArgsKey="raw"` 哨兵撞名：任何工具若其参数缩成单个名为 `raw` 的串字段就永久不可调** → F125 的"参数无法解析"哨兵字面就是 `{"raw":<原文>}`，故合法的单字段调用 `{"raw":"..."}`（工具确有 `raw` 串参，valid JSON）被 `isUnparseableArgs` 误判成哨兵 → executeTool 报 "not valid JSON"、不跑工具。round-20 agentskillchain lane（HIGH 确认） | 撞名/工具不可调·HIGH（F125 同族 sentinel 撞名） | `app/loop/stream.go` `rawArgsKey` 由 `"raw"` 改 `"__anselm_unparsed_args__"`（保留内部 token、任何工具参数都不可能取此名）；`isUnparseableArgs` 用常量自动检新键、注释同步 | 零 token `TestParseToolArgs_RepairsDirtyJSON` 扩断言（`{"raw":"..."}` 不被误判哨兵）+ `TestExecuteTool_UnparseableArgsClearError` 改用常量构哨兵；make verify 绿（内部哨兵、无契约/doc 改） | _pending_ |
| F164 | fixed | **🔒 平台注入的 sensitive init-arg 在 handler 调用错误里**明文泄露**（H3 spawn 失败路径耐久记录 + H4 返给 LLM/用户的实时错误两面）** → F82 的 secret 掩码只施于 recordCall 的**持久副本 errMsg**，故 (H4) 返调用方的实时错误（`call_handler`→agent→用户）仍含明文密钥；且 (H3) spawn 失败路径以 `inst==nil` 记账、连那道掩码都被绕过 → DB `handler_calls.error_message` 存全文密钥（REST/get_handler_call/:triage 皆可读）。method 路记录已掩、实时错误未掩=不对称定位 bug。round-20 handlerconfig lane（HIGH×2·security，SQLite 实证 `sk-SECOND-key-bbbb` 明文落库） | **安全/密钥泄露·HIGH**（F82/F108 同族——错误面缺口） | `app/handler/call.go` 加 `scrubErr(err, secrets)`（掩结构化错误 Details 里 Wrap 抬上来的 traceback + 把 cause 重指到掩码串，保留外层 Code 供 errors.Is）；**构造处施加**：`spawn.go` init 失败 `Wrap` 后即 `scrubErr(..., secretVals)`（覆盖 inst==nil 路、secretVals 当场可得，无需 plumb 出去）+ `call.go` method 路 `scrubErr(mapCallErr(...), inst.SecretValues)` → 审计记录与实时返回两面皆净 | 零 token `TestScrubErr`（带密钥 Details 的结构化错误→Surface+`.Error()` 均不含密钥、含 `********`、errors.Is 外层码仍匹配、nil 无操作）+ 既有 `TestCall_ScrubsLeakedSecretInAudit` 不回归；make verify 绿 + handler.md（错误面也掩 §3）同步 | _pending_ |
| F163 | open | **无 `list_approval_inbox` 工具：agent 找不到 park 在审批节点的 run** → agent 编造不存在的 `query` 参数、`search_flowruns` 静默忽略后只能逐个肉眼看。round-19 approvalexpiry lane（MED） | 能力不可发现（同 F146 枚举缺口） | **候选**（未修，clean tool-add）：加 `list_approval_inbox`/`list_parked_approvals` 工具，调 scheduler 的 inbox/parked-nodes 查询（REST 端点已有、只是没浮成工具），同 F146 list_conversations 套路 | — | — |
| F162 | open | **`PATCH`/`POST /limits` 改的是**全局**状态、却要 per-workspace auth header**——一个 workspace 静默改全部 workspace 的运行上限。round-19 workspacelimits lane（MED） | 隔离/clarity·medium（**单用户本地 app 全局 limits 本属设计**，误导在于 header 必填却作用全局） | **候选**（未修）：文档/响应提示 limits 是单一全局设置（或 /limits 不要求/忽略 workspace header）；单用户场景非正确性 bug | — | — |
| F161 | open | **删除被依赖实体 purge 掉两端 relation 边 → 依赖在关系图里隐形、悬挂 ref 仍留在依赖方 config，图静默少报损坏** + 依赖方无 proactive needsAttention（agent 无 capability_check、workflow 的 needsAttention 仅由失败 run 反应式置）。round-19 relationgraph lane（HIGH#1 + M2/M3 cluster） | 假成功/可观测性·HIGH（但**actionable 核心已由 F160 修**：delete 现返引用方 ref、agent 能逐个修；残留是更深的 relation-model 设计） | **候选**（未修，需设计）：(a) delete 不 purge 入向 equip/link 边、转 tombstone 使图仍显示已坏依赖；或 (b) delete 时 proactive 给每个依赖方置 needsAttention/dangling flag（agent 也加 capability_check_agent 浮 MountResolver.CheckHealth）。多实体 relation-model 改、延后正式设计、不仓促 | — | — |
| F160 | fixed | **delete 工具的修复提示「run get_relations 修引用方」无从执行——只返计数、引用方 id 删后无处可查** → delete 前算了 dependents 计数但只折计数进结果，purge 抹边后 agent 无从知道是**哪些**实体引用了被删者。round-19 relationgraph lane（MED 确认，delete 自带的 note 不可跟随） | 能力不可发现/promise≠reality·medium（delete-impact 不可操作） | relation Service 加 `ListDependents`（返入向 equip/link 边的 `[]*Relation`）；`tool/dependents.go` 端口加之 + `DependentRefs` helper（返 `{kind,id}` ref）；`AnnotateDependents`/`DependentSuffix` 改吃 ref（结果带 `dependents`(ref 列表)+`dependentCount`+note）；8 个 delete 工具 call site 改 `DependentRefs` | 零 token 重写 `dependents_test`（DependentRefs nil/err 安全、AnnotateDependents 折 ref+count、DependentSuffix 点名 id）；make verify 绿 + relation.md（ListDependents 返 ref）同步 | _pending_ |
| F159 | fixed | **两处错误面泄露内部面包屑**（错误清洗家族 F89/104/122 的新缺口）：(M4) handler `recordCall` 持久化 `callErr.Error()`、把 Go 层面包屑（"handler.Client:"、"context deadline exceeded"）漏进 `handler_calls.errorMessage`——get_handler_call/REST/LLM 读回的耐久记录带面包屑，而实时 LLM 面已用 Surface 剥掉；(M7) author 期 CEL 编译错把 Go 调用路径 "cel.Compile"/"cel.CompileFor"/"cel.CompileTemplate" 漏进 agent 可见错（裸 fmt.Errorf、Surface 对裸错透传故不剥）。round-19 handlercrash + errorsurface lane（两 MED·high conf） | leaked-internals·medium（错误清洗家族缺口） | (M4) `app/handler/call.go recordCall` 改 `errMsg = errorspkg.Surface(callErr)`（记录与实时面一致、含 Details/traceback 不漏 Go 路径）；(M7) `pkg/cel/{cel,template}.go` 把 "cel.Compile*" Go 路径前缀改用户语义（"invalid CEL expression/template %q"、"CEL scope build"）——bare 诊断直出 | 零 token `TestCompile_ErrorNoGoPathLeak`（坏 expr→错误不含 "cel.Compile"、含 "CEL"）；handler M4 经既有 `TestSurface` + 一行委托覆盖；make verify 绿 + handler.md（recordCall Surface 清洗）同步 | _pending_ |
| F158 | fixed | **function 运行超时给调用方返误导错误（`SANDBOX_SPAWN_TIMEOUT`/"spawn process timeout"）——F105 只修了耐久记录、漏了返回路径** → 命中墙钟时 `recordExecution` 把记录消息清洗成 "exceeded the Ns wall-clock limit"，但 `RunFunction` 返**裸** sandboxErr 给 HTTP `:run` + `run_function` 工具 → agent 见 "spawn process timeout"（暗示**启动**失败、正是 F105 想消的幻象、误导 :triage 追冷启动）。handler 路对称正确（mapCallErr→ErrInstanceRPCTimeout 记录+返回都清）。round-19 workspacelimits lane（HIGH 确认，DB record="wall-clock" vs HTTP 504 SANDBOX_SPAWN_TIMEOUT 分叉） | error 不透明/agent-misleading·HIGH（F105 同族、返回路径缺口） | `domain/function` 加 `ErrRunTimeout`（504/`FUNCTION_RUN_TIMEOUT`/"exceeded its wall-clock time limit"）；`app/function/run.go` RunFunction 返回前 `errors.Is(cctx.Err(),DeadlineExceeded)` 即 `errorspkg.Wrap(ErrRunTimeout, sandboxErr)`（镜像 handler call.go）、记录与返回同义 | 零 token 扩 `TestRunFunction_WallClockTimeout`（返回 err 须 `Is(ErrRunTimeout)` + `Surface` 含 "wall-clock" 不含 "spawn"）；make verify 绿 + error-codes.md（`FUNCTION_RUN_TIMEOUT`）+ function.md（超时返回也清洗）同步 | _pending_ |
| F157 | fixed | **workflow `:edit` 带 set_meta op 孤儿化新版本 + 留陈旧 active 指针（返回它从不激活的版本）** → 确定性、单请求即复现：`crud.go Edit` SetActiveVersion 把 DB 指针移到新版本，但 set_meta 分支的 `SaveWorkflow(w)`（整行 upsert）用**陈旧** w（`ActiveVersionID` 读 w 时还是旧版本、line 217 才更新）→ clobber 指针回旧版本 → 新版本孤儿、meta 编辑后静默仍跑编辑前的图。唯 workflow 有此 bug（fn/hd/ag 用 SaveVersionAndActivate / 列范围 meta 更新）。round-19 concurrentedit lane（HIGH 确认，DB edit 返 v13 而 active 仍 v11、trigger 跑的是 v11） | 数据完整性/promise≠reality·HIGH（违 edit_workflow "立即生效"、诱导 agent 编造假模型） | `app/workflow/crud.go` Edit 在 set_meta 的 SaveWorkflow **之前**置 `w.ActiveVersionID = versionID`（移上原 line 217 的赋值）——整行 upsert 遂带正确新指针；纯图编辑无 SaveWorkflow 故本就对 | 零 token `TestEdit_SetMetaMovesActivePointer`（set_meta 编辑后 active 指针须=新版本、description 生效）+ 既有 `TestEdit_NewVersionPointerMoves` 不回归；make verify 绿 + workflow.md（set_meta SaveWorkflow 前先更指针）同步 | _pending_ |
| F156 | open | **capability_check 数据流校验不对称：查 INPUT 侧（F71 声明 input 接线）但不查 OUTPUT/READ 侧** → 节点读 `X.field`、X 有声明 outputs 但不含 `field`（且非 `.text`）→ 运行时 no-such-key、capability_check 不警。round-18 capabilitydeep lane（MEDIUM） | 假成功/F71 对称缺口 | **候选**（未修）：resolver 浮出 DeclaredOutputs，capability_check 解析各节点 input CEL 的裸 `<nodeId>.<field>` 读、当 producer 有声明 outputs 且 field 不在其中（且非 .text/schema-less）即报 problem。须谨慎 .text/schema-less/agent-outputs/diamond 边界（F88 空间），评估假阳风险后再做 | — | — |
| F155 | open | **执行记录只存 modelId（串）、从不存服务该次运行的 apiKeyId** → 两个 key 暴露同名模型时审计行无法区分哪个 key 跑的。round-18 modelswitch lane（MEDIUM·observability） | 可观测性·medium | **候选**（未修）：把解析出的 apiKeyId（+provider）连同 modelId 持久化到 Execution 行 + 暴露给 get_*_execution；fn/hd/ag 三执行记录同 | — | — |
| F154 | open | **API-key 解析失败时执行记录丢掉目标 modelId** → 失败 run 的审计行说不出它本要打哪个模型。round-18 modelswitch lane（MEDIUM·observability） | 可观测性·medium | **候选**（未修）：在 ResolveAgent 失败前从声明的 override 捕获 modelId、早返错误路径也把它带进 recordExecution | — | — |
| F153 | open | **模型/key 引用在三条写路径（agent override · conversation override · workspace scenario default）全部未校验即持久化** → 拼错的 modelId（`deepseek-v9-ultra`）/不存在的 apiKeyId（`aki_deadbeef…`）写入成功、只在 invoke 时才以 provider 400 / "api key not found" 失败。round-18 modelswitch lane（HIGH·确认但**运行时 fail-loud 干净**、非静默腐败=延迟发现摩擦） | eager 校验缺口·HIGH（但实务低于静默腐败类） | **候选**（未修，需设计）：写时用 CapabilityService 解析 ref（apiKeyId 存在 + modelId 被该 key 服务）、拒以 dedicated 422；三路径各注入校验器。延后做正式多路径设计、不仓促半修；运行时已干净失败故非数据腐败 | — | — |
| F152 | open | **spawn subagent 的 chat 回合跑了 35m21s——同时超 600s 单流上限 + 1800s ChatTurnSec 兜底（F100）** → subagent 单条 LLM 流跑 ~34min vs 600s 上限、整回合超 30min 截止 5min 才经 ctx cancel 结束。round-18 subagentspawn lane（HIGH·确认） | 资源/liveness·HIGH（墙钟家族 F83/92/93/100） | **未修，需活体复现**：`provider.go Stream` 代码审查**看似正确**（httpReq 建在 streamCtx 上 line 78、两上限都 cancel streamCtx）——finding 的"请求没建在 streamCtx"根因假设**不成立**，真因须复现 34min 挂起才能定位（疑 DeepSeek 网关流的 resp.Body 读不随 ctx cancel 及时返回）。不对引擎资源安全路径做盲改 | — | — |
| F151 | fixed | **`read_attachment` 对图片的描述符过度承诺、诱导无视觉模型 thrash** → 描述符说"Images reach the model only when attached to a chat turn"，对**无视觉**模型是空头支票（图片附上也看不到）、agent 反复试图"附加"。round-18 attachments lane（MEDIUM） | promise≠reality/agent-misleading·medium | `tool/attachment/read.go` 图片描述符改诚实（caps 无关）："图片仅当模型有视觉支持**且**附在 chat turn 时才被看到——若当前模型纯文本则根本看不到、别反复试，请用户描述或换视觉模型" | 零 token 更 `attachment_test`（描述符含 "cannot turn its content into text"+"vision"）；make verify 绿 | _pending_ |
| F150 | fixed | **被取消的 subagent 向父报"completed"、只带 preamble 文本——无取消/失败信号** → `Spawn` 无论终态都返 `result.LastMessage, nil`，父的 Subagent tool_result 标 completed/error=None，隐藏工作被截断、父把部分答案当权威。round-18 subagentspawn lane（MEDIUM 确认，DB 实证 cancelled run→completed tool_result） | 假成功/promise≠reality·medium | `app/subagent/subagent.go` 抽 `annotateTerminal(result)`：非 `StatusCompleted` 时前缀终态原因（"[subagent did not finish cleanly (...) — its answer below is partial...]")、干净完成原样透传 | 零 token `TestAnnotateTerminal`（completed 透传、cancelled/error 前缀终态+保留文本）；make verify 绿 | _pending_ |
| F149 | fixed | **`get_subagent_trace` 没从 subagent 自身工具集剔除——隔离泄漏** → `filterTools` 只 strip `Subagent`、漏 `get_subagent_trace`，故 general-purpose subagent（空白名单=父全集减 Subagent）仍带它、能读**父**对话的其它 subagent trace；违 build_services.go 装配注释「a subagent's tool set never includes it」。round-18 subagentspawn lane（MEDIUM 确认） | 隔离泄漏/promise≠reality·medium | `app/subagent/registry.go` 加 `traceToolName` 常量 + `filterTools` 一并 strip（与 Subagent 同）——subagent 读的是父 trace、自己没兄弟可读 | 零 token 扩 `TestFilterTools`（general-purpose 须同时剔除 Subagent + get_subagent_trace）；make verify 绿 + messages.md（从 subagent 工具集剔除）同步 | _pending_ |
| F148 | fixed | **envfix 靠「丢掉装不上的依赖」来"修复"→ 静默丢用户声明的包 + 标 env ready（假就绪）** → LLM 改依赖循环允许把 `[badpkg]` 改成 `[]`，空 venv 装成功→`envStatus=ready`、`dependencies` 被改写成 `[]`；代码若 import 该包则运行时 ModuleNotFoundError，且声明被丢、不可追溯。连带 H4：失败 env 的 :run 重跑丢包 fixer 后照跑、`FUNCTION_ENV_NOT_READY` 不可达。round-18 funcdeps lane（HIGH×2+MED 确认，DB 实证 deps:[]+ready 而代码 import 缺失包） | 假就绪/静默丢失·HIGH（系统正确性：缺包绿 env） | `app/envfix/envfix.go` Provision 加守卫：LLM 建议**缩短到原始声明数以下**即拒（renate/松版本保持包数=修复，丢包=非修复）→ break、env 保 failed + 真实装错、声明 deps 不丢；buildFixPrompt 加「NEVER drop a declared package」规则。env 保 failed 使 run.go `ensureEnv` 返 not-ready→`FUNCTION_ENV_NOT_READY` 重新可达（修 H4） | 零 token `TestProvision_RejectsDepDrop`（首装失败+LLM 返空→拒、OK=false、FinalDeps 保原包、丢包重装永不执行）；make verify 绿 + function.md（修复不丢包、ENV_NOT_READY 可达）同步 | _pending_ |
| F147 | fixed | **`write_memory` 更新既有记忆时静默取消置顶 + 改作者归属（pinned→false、source→ai）** → `Upsert` 全对象覆盖 `Pinned/Source`，而 write_memory 工具永远传 source=ai、不设 pinned → 用户置顶的逐字注入安全规则（"NEVER deploy on Fridays"）一次 LLM 编辑就被降级成懒加载目录行（pinned 全文注入→仅 name+desc 一行）。round-18 memoryfacts lane（HIGH 确认，DB+磁盘实证 pinned:false/source:ai） | 静默降级/promise≠reality·HIGH（违工具自身承诺 + memory.md「pinned 是用户策展动作」） | `app/memory/memory.go` Upsert 改 read-modify-write：既有记忆更新只改 content/description、**保留**现有 `Pinned`（仅经专用 pin/unpin 端点改）+ `Source`（不可变作者归属）；create 仍用输入 | 零 token `TestUpsert_UpdatePreservesUserCuration`（建 pinned/user 规则→LLM source=ai 编辑→仍 pinned+source=user+内容更新）；make verify 绿 + memory.md（更新保留策展）同步 | _pending_ |
| F146 | fixed | **无 `list_conversations` 工具：「列出我所有对话」无忠实路径** → agent 只能猜 `search_conversations` 的搜索词（内容回忆、无匹配文本即漏），把部分搜索结果当**完整列表**呈现。round-17 chatbranch lane（MEDIUM×2 确认 M7+M8） | promise≠reality/能力不可发现·medium（枚举缺口） | 加 `list_conversations` 工具（S18 5 法、`mgr.List`/ListFilter、游标分页、默认仅 active/`includeArchived` 含归档、`nextCursor` 防一页误当全集）；Manager 接口加 `List`；注册进组 + `search_conversations` 描述硬化「内容回忆非枚举、勿当完整列表」 | 零 token `Test_listConversations_EnumeratesAndPages`（枚举+nextCursor+默认仅 active+includeArchived 含归档）+ `_LimitClamp`（50 上限/20 默认）；make verify 绿 + conversation.md（list_conversations + search 非枚举）同步 | _pending_ |
| F145 | fixed | **agent 工具挂载的 `name` 字段被接受+持久化+`get_agent` 返回、运行时却静默忽略**（resolver 永远用实体 live 名）→ schema 宣传 `{ref,name}` 可设别名、实则无效，且 rename 后 `get_agent` 显示陈旧存名。round-17 renamecascade lane（MEDIUM 确认，DB 实证设 alias 后执行 transcript 仍用 live 名） | promise≠reality·medium（settable-but-inert=两头不讨好） | 不改运行时（live 名自动跟随 rename=好行为）、改**诚实 schema**：`tool/agent/build.go` configProps 的 `tools` 描述改"每项 {ref}…总以实体当前名呈现、无法 alias"+ `name` 属性标"可选、运行时忽略、始终用 live 名" | make verify 绿（描述串、无逻辑/契约改；运行时本就用 live 名、无 doc 同步） | _pending_ |
| F144 | fixed | **`create_trigger` 的 webhook 项描述漏掉内建 body-hash 去重 → agent 对用户断言错误的幂等契约** → agent 言之凿凿"无 webhook 去重、同 body POST 两次=两 run"，实则 sha256(body)+分钟桶去重（同 body 同分钟内塌成 1 firing，D3 `idx_trf_dedup`）；姊妹 sensor 项**显式**写了"无去重"、webhook 项却omit（不对称误导）。round-17 webhookpayload lane（MEDIUM 确认，实测 3 次同 body POST→1 firing/1 run） | promise≠reality/agent-misleading·medium | `tool/trigger/build.go` webhook 项加一句幂等契约（同 body 同分钟塌成 1 firing/workflow、不同 body 必触发、下一分钟同 body 再触发），镜像 sensor 项的去重说明 + trigger.md:42 已有语义 | make verify 绿（trigger.md:42 已记去重、tool 描述与之对齐、无 doc 同步） | _pending_ |
| F143 | fixed | **max_steps 终态错误文案 "continue to resume" 对一次性 agent invoke 宣传了不存在的续跑能力** → loop.go 被 chat + 一次性 invoke 共用，chat 可后续消息续、invoke 无同 run 续跑，文案一概承诺 resume 误导。round-17 agentbudget lane（MEDIUM 确认） | promise≠reality·medium | `app/loop/loop.go` max_steps 文案改对两宿主都诚实："结果部分——后续消息续（chat）、或调高步数预算/简化任务"，不一概承诺 resume | make verify 绿（loop_test 仍断言 errCode=MAX_STEPS_REACHED+errMsg 非空、不锁文案；无 doc） | _pending_ |
| F142 | fixed | **失败（max_steps/错误）的 agent 执行把裸中间叙述文本当 `output` 返回、未按声明 outputSchema 整形** → 声明对象输出的 agent 非 OK 收尾时 `output` 是裸串、读执行记录的消费者会信一个不合形的"答案"。round-17 agentbudget lane（MEDIUM 确认，DB 实证 max_steps 执行 output 为裸 narration） | 类型不一致/假答案·medium（F40 邻域——声明输出非 advisory） | `app/agent/invoke.go`：有声明 Outputs 时 `output` 必是 coerce 后对象**或 nil**——非 OK 终态（含 coerce 失败）置 `nil`、绝不留裸串（裸叙述仍存 transcript blocks 供调试） | 零 token `TestService_InvokeFailedTerminalNullsDeclaredOutput`（EventError 终态+2 声明字段→OK=false 且 Output=nil）；make verify 绿 + agent.md（非 OK 终态置空输出）同步 | _pending_ |
| F141 | fixed | **离线/失败 MCP server 在 agent-mount + mount-health 路被报成 "mcp tool not found on server"——误导：怪工具实则 server 宕** → `mount.go mcpTool` 找到 serverID 但 `ListTools` 只列 callable server、工具缺席→落到 `ErrToolNotFound`，即便工具本在该（现离线）server 上；正确的 `MCP_SERVER_DOWN` 哨兵存在且直调路径在用、mount 路却没用。round-17 mcplifecycle lane（MEDIUM 确认） | error 不透明/agent-misleading·medium（排错被引向改挂载 vs 重连 server） | `tool/mount/mount.go mcpTool`：找到 serverID 后查 `IsCallable(s.Status)`，不可调即返 `ErrServerNotConnected`（MCP_SERVER_DOWN）而非落到 ListTools→ErrToolNotFound；与直调路径对称。**M2（capability_check 放过离线 MCP server）判 by-design 不修**：MCP 状态是瞬态（进程内、重启即重连），capability 故障它会经 F135 门控过度挡住重启后激活；运行时已 fail-loud `MCP_SERVER_DOWN` | 零 token `TestResolve_MCPOfflineServer`（failed server 的挂载→ErrServerNotConnected 非 ErrToolNotFound）+ 既有 `TestResolve_MCPMount` 加 StatusReady 不回归；make verify 绿 + agent.md（mount-health 离线报 server-down）同步 | _pending_ |
| F140 | fixed | **空 ops 的 `edit_handler` 静默重启常驻实例、抹内存态、却读着像 no-op** → 结果 `opsApplied:0` + 版本不变，但 env 重建 + restart 已抹掉有状态 handler 的内存态；有状态计数器一次"空编辑"就丢全部 state、agent/用户不知。round-16 handlersession lane（MEDIUM 确认，DB 实证 count 重置） | 静默降级·medium（F117 邻域——meta-edit 不重启的反面：空 ops 偏重启却不显） | `tool/handler/build.go` `buildOutput` 加 `restarted bool` 参；Execute 在 `len(ops)==0` 时传 true → 结果带 `restarted:true` + `restartNote`（态被抹、想重置用 restart_handler）；create 传 false（不重启）；edit_handler 描述补"空 ops 重启抹态、非 no-op" | 零 token `TestBuildOutput_EmptyOpsRestartIsVisible`（空 ops→restarted:true+note；正常 op→无 restarted、版本号自证变更）；make verify 绿 + handler.md §5（空 ops 重启抹态、带 restarted）同步 | _pending_ |
| F139 | fixed | **`:triage`/`:iterate` 的 aispawn system prompt 谎称"produce a pending version the user reviews"——平台无 pending/accept、编辑立即 LIVE** → triage 对 active workflow 的 edit_workflow 会**静默改写在跑的生产图**而 AI 以为只是暂存提案；DB 实证 triage 的 edit_function 即成 active 版本（"a lie-by-system-prompt"）。round-16 triagefail lane（HIGH 确认） | promise≠reality/agent-misleading·HIGH（prompt 断言一个 docs 明确否定的工作流——overview/function/architecture 皆 "无 pending/accept"） | `app/aispawn/aispawn.go` 重写 `iterateSteer`+`triageSteer`：删"pending version the user reviews"、改"edit 立即生效为新 active 版本（无 review 闸；错了用 revert_*）"；triage 额外点明"editing an ACTIVE workflow 立即改在跑的图"。docs 本就对（无需改）、只是 prompt 与之对齐 | make verify 绿（aispawn 测不回归；prompt 串改、无契约/码改、docs 已正确无需同步） | _pending_ |
| F138 | fixed | **replace/skip/buffer_one 并发策略对普通（非 park）workflow 的真实触发从不生效** → `DrainFirings` 把每条 firing 内联 advance 到**完成**才决策下一条，故第二条的 `overlapDecision` 见 `running=0`、策略不触发；两条背靠背真实 fire 都跑完、0 cancelled（连 allow_all 都退化为串行）。round-16 concurrencypolicy lane（HIGH 确认，DB 实证 run B 在 run A 完成后 1ms 启动=严格串行；既有 scheduler 测仅靠**预 seed** 一个 running run 才过、从不走两条同到的真实路径） | 假成功/承诺失效·HIGH（文档化的并发策略对最常见工作流默默失效） | `app/scheduler/run.go` 把 `consumeFiring`→`claimFiring`（去掉内联 Advance、返 seed 的 runID+fctx）+ `DrainFirings` 改**两阶段**：阶段1 按序 claim/seed 批内每条（每存活者落 running 后再决策下一条），阶段2 顺序 advance——使后到兄弟的 overlapDecision 能见先到兄弟在途。advance 仍**顺序**（无并发 run 执行→无 handler 管道/单连接竞争）；replace 抢占先 seed 的兄弟（cancelRunningForReplace 标 cancelled）使其阶段2 Advance no-op；piled-up firing 在下一批同样被正确 collapse | 零 token `TestFiring_OverlapReplace_SameBatch`（一批两 firing→恰 1 completed+1 cancelled、action 仅survivor 跑一次；无修则 2 completed/0 cancelled 失败）+ 全 5 既有 overlap 测不回归；make verify 绿 + workflow.md（两阶段 drain §并发）同步 | _pending_ |
| F137 | fixed | **删 workflow 留下已入队的 pending firing 成永久孤儿，scheduler 每 5s 错误循环它、永不终结** → `overlapDecision` 的 `GetWorkflow` 对已删 workflow 返 `WORKFLOW_NOT_FOUND`、被当可重试错误记日志跳过、firing 永留 pending→每 tick 重试+重记（实测 53+ 次且增长）。round-16 triggercron lane（HIGH 确认） | 资源/正确性·HIGH（无界错误循环 + 卡死 pending 行；firing 是 Log 表故孤儿永久） | `app/scheduler/run.go` `claimFiring`：`overlapDecision` 返错 `errors.Is(ErrNotFound)` 时终态 `MarkFiringOutcome(FiringShed)`（既有"不会跑"终态）而非返可重试错误——孤儿 firing 一次性 shed、循环止 | 零 token `TestFiring_DeletedWorkflowSheds`（GetWorkflow 返 not-found→drain 两次后 0 pending、0 run）；make verify 绿 + trigger.md（shed 含 workflow 已删）同步 | _pending_ |
| F136 | fixed | **handler `add_method` op 把 method 字段放 op 顶层（而非嵌套 `method` 下）时静默丢弃** → 顶层 `body` 被丢、method 落空 body（什么都不做=假成功）；或全平时触发误导的 "method.name required"。round-15 opvalidate lane（MEDIUM 确认） | 静默降级/agent-misleading·medium（F111/F125 同族——op-shape 校验面） | `app/handler/apply.go` add_method 加 `unexpectedKeys(raw,"op","method")` 守卫：op 顶层除 op/method 外有键即大声拒、连同正确形状 `{op:add_method,method:{name,body,inputs,outputs}}` 指回（错误经 `ErrOpInvalid.WithDetails` 的 reason 达 LLM，F111 路径）；`method` 内部多余字段不管（非 op 顶层） | 零 token `TestAddMethod_MisplacedFieldsFailLoud`（顶层 body 错位→拒含 "body"+"nested under"；全平→点 nesting；正确嵌套仍落 body）；make verify 绿（op 形状未变、无新码/契约改、无 doc 同步） | _pending_ |
| F135 | fixed | **`:activate`/`:stage` 给停泊 workflow 上线/待命前不跑能力检查** → agent 能激活引用了被删/拼错实体（悬挂 ref）、缺 hd 方法/mcp 工具、漏接必填 input 的 workflow，上线后**每次真实触发都 fail-fast 且不可见**（停泊图的损坏只在触发真正发生时浮现）。round-15 opvalidate lane（HIGH 确认） | 假成功/eager 校验缺口·HIGH（agent 挂载 F98/F112 同族——本应在承诺点校验运行将解析的同一批 ref，workflow 独缺这道） | `app/workflow/execution.go` 加 `ensureRunnable`（跑 `CapabilityCheckByID`、`!OK()` 即拒 `WORKFLOW_NOT_RUNNABLE`、违例进 `details.problems`）+ Activate/Stage 在 Attach 前调它。门控**只落上线/待命承诺点**：create/edit 不门控（增量构建保持自由）、手动 `:trigger` 不门控（单次显式 run 失败立即可见）、nil resolver（仅结构模式）放行。加 `ErrNotRunnable` 哨兵 + 两工具描述（refuses non-runnable、先 capability_check） | 零 token `TestArm_RejectsUnresolvableRef`（悬挂 ref→Activate+Stage 均拒 `ErrNotRunnable` 且**不 Attach**；sound 图正常 arm、门控不过度拒）；make verify 绿 + error-codes.md（`WORKFLOW_NOT_RUNNABLE`）+ workflow.md（待命门控 §4、码 16→17）同步 | _pending_ |
| F134 | fixed | **`edit_agent` 部分编辑静默抹掉挂载的 tools/knowledge**——工具层 full-replace，只改 prompt 的编辑把 config 其余字段清空（实测 ~40% 丢配率）、"整配替换先读"的描述**拦不住**（LLM 仍只发改的字段）。round-15 editagentmerge lane（HIGH 确认、"2/5 silent wipes"） | 静默降级/假成功·HIGH（F126 update_node 同类但其描述已明示 REPLACE=GREEN；本处描述未拦住故须真修） | `tool/agent/build.go` edit_agent Execute 改**合并**——`svc.Get` 取 active 版本 config 作基底、`mergeConfig` 只覆盖请求中**实际出现**的字段（present-key 检测 via `map[string]RawMessage`；缺省保留、显式空 `[]`/`""` 清空）；prompt 不再 required（部分编辑无须重发）+ 删 `AGENT_ID_PROMPT_REQUIRED` 哨兵/码 + 描述改合并语义。HTTP `PUT` 路径仍整配替换 | 零 token `TestMergeConfig_PreservesOmittedClearsExplicit`（只改 prompt→skill/knowledge/tools 保留、显式空 tools 清空、非 tools 字段不动）+ `TestEditAgent_ValidateInput_NoPromptRequired`；make verify 绿 + agent.md（edit 合并语义、工具校验码 6→5）+ error-codes.md（删 `AGENT_ID_PROMPT_REQUIRED`）同步 | _pending_ |
| F133 | fixed(guidance) | **control 分支无出边 → run 报 completed（false-success）、capability_check 不警**——`reconcileControlPorts` 只查边→port、不查 port→边。round-14 controlport lane（HIGH 确认） | 假成功/组合摩擦·high（但**无出边分支是合法终端**——硬查会假阳破坏有意终端，F88 不 sound，故走 guidance） | `tool/workflow/workflow.go` opsDoc fromPort 节加「WIRE EVERY BRANCH YOU EXPECT TO ACT：无出边分支=合法终端、路由到它 run 以 completed/无错收尾无下游动作、capability_check 分不清有意终端与漏接边——该 act 的分支务必接边」+ workflow.md §CapabilityCheck 同步「不查反向、由作者负责」 | make verify 绿（描述/doc，无逻辑改——backend 行为本就正确、gap 是作者可能漏接，同 F88/F128 走指引非不 sound 硬查） | _pending_ |
| F132 | fixed | **结构化输出 agent 拒绝散文后的 ```json 围栏**——`coerceDeclaredOutputs` 只剥**开头**的围栏，LLM 自然形态（"Here is the result:\n```json\n{…}\n```"）散文在前→围栏没剥→整串 unmarshal 失败→`AGENT_OUTPUT_NOT_STRUCTURED`（模型约半数时间带围栏，故概率性终态失败）。round-14 agentstructured lane（HIGH） | 脆弱/假成功·HIGH | `app/agent/invoke.go` 抽 `extractFencedBlock`：从答案**任意位置**抽第一个 ``` … ``` 块（跳过 ```json 语言标签行）、无围栏整答原样试；2+ 声明 + 纯散文仍大声失败（F40 不回归） | 零 token `TestCoerceDeclaredOutputs_FencedJSONWithProse`（散文+围栏 JSON 解析对、裸 JSON 仍解、纯散文 2+ 字段仍败）；make verify 绿（围栏处理是实现细节、契约"JSON 对象→解析/散文→败"不变、无 doc 同步） | _pending_ |
| F131 | fixed | **handler 方法异常+`__init__` 异常的真因+traceback 被错误清洗剥掉** → `client.go` 把 Python 错误+traceback 放进 `fmt.Errorf("%w: %s\n%s")` 包裹，而 `errorspkg.Surface`（F89/F104/F122 共用、剥 fmt 包裹链防 Go 路径泄露）只渲 sentinel Message → 每条 agent/flowrun 路径只剩不透明 "handler.Client: call failed"，违 §4「traceback 给 LLM 自纠」（doc-ahead claim）。round-13 handlerwferr+bigsystem lane | error 不透明/leaked-internals·HIGH（错误清洗家族反噬——清洗 Go 路径连带剥掉有用 traceback） | `infra/handler/client.go` 抽 `pyErr(sentinel,errStr,trace)`（call+init 两路）：error+traceback 进 sentinel **Details**（Surface 渲）+ WithCause 保审计。**但 init 路 round-15 实测仍 RED**——`spawn.go:113` 用 `fmt.Errorf("%w: %v", ErrInstanceSpawnFailed, initErr)` 把结构化 initErr **拍平出链**（%v）、外层无 Details 的 SpawnFailed 遮蔽 traceback。**真修**：地基加 `errorspkg.Wrap(sentinel,cause)`（抬 cause 的 Details 到 sentinel + WithCause，替所有 `%w: %v` 包结构化错误的点）；spawn.go:113 + mapCallErr 两处改用之。LLM provider 错误经非-Surface（`.Error()`）路径达用户（F66 验、不动） | 零 token `TestCallFailedErr_SurfacesTraceback`（infra）+ `TestWrap_LiftsCauseDetails`（pkg/errors）+ **`TestSpawn_BrokenInitSurfacesTraceback`（app 全 spawn 路集成测——单测过而集成坏正是 init 漏发之因）**；make verify 绿 + handler.md（spawn/init 也 Wrap）同步 | _pending_ |
| F130 | fixed | **skill body 自带 frontmatter → 双 frontmatter、allowedTools 静默丢**——平台从 args 组装 frontmatter + 原样存 body，body 若 `--- … ---` 开头则文件双 frontmatter、body 里的 allowedTools 沦为正文不被解析/不生效。round-12 skilluse lane | 静默降级/脆弱·medium | `app/skill/mutate.go validate` 加 `bodyHasLeadingFrontmatter` 检测（`---` 围栏开 + 之后闭合围栏；孤立 `---` 分隔线无闭合放行）→ 拒 `SKILL_INVALID_FRONTMATTER`（details 指明 frontmatter 取自参数、body 仅正文）；create_skill body 描述同步禁 frontmatter | 零 token `TestCreate_RejectsBodyFrontmatter`（带 frontmatter body 拒、普通 body 过、孤立 `---` 分隔线过）；make verify 绿 + skill.md（body 不得自带 frontmatter）同步 | _pending_ |
| F129 | fixed | `list_mcp_marketplace` 多词查询的 **AND（F113）太严** → 自然能力短语（"fetch web page"）总有某词不在任何 server 的 name+desc 里 → AND **静默返 0**。round-12 mcpworkflow + round-11 mcpreal2 双 lane（F113 收尾） | 组合摩擦·medium | `tool/mcp/system.go` filterMarketViews 改 **OR 匹配 + 按命中词数排序**（命中更多词的 server 排前、命中 ≥1 词的都纳入、`tokenMatchCount` 替 `matchesAllTokens`）；空查询仍全部；query 参数描述改"按命中词数排序、部分命中也出"。**修正 F113 的 AND 过严** | 零 token 改 `TestFilterMarketViews`（"database notifications" 跨 server 须返 2 非 0、"send notifications database" 命中 2 词的排第一）；make verify 绿 | _pending_ |
| F128 | fixed(guidance) | **菱形/条件 join 的 false-green**——节点读 `X.field`、X 在 control/approval 分支一侧，另一分支跑时 X 没运行→结果空→`no such key` 崩；capability_check 只查 X 是结构 ancestor、不查是否必跑，故**过绿却运行时炸**。round-11 workflowbig lane | 假成功/组合摩擦·high（但 F88 立：静态 dataflow 校验不 sound、硬查会假阳，故**走 guidance 非加静态分析**——同 F16/F35） | `tool/workflow/workflow.go` opsDoc input-CEL 节加「CONDITIONAL/DIAMOND READS」：明示条件分支 ancestor 读须 `has(X.field)?X.field:fallback` 守（同 LOOP STATE 的 has() 模式）+ 直说 capability_check 过不绿是「YOUR responsibility, not a safety-net failure」。runtime 已 fail-loud（F104 清洗）、agent 可 has() 恢复——本修使其**主动**守而非崩后补 | make verify 绿（描述串、无逻辑改；workflow.md 不到 opsDoc 粒度故无同步） | _pending_ |
| F127 | fixed | **mount-health 预检不覆盖知识文档** → `MountHealth` 只 `CheckHealth` fn/hd/mcp 工具挂载，知识 doc 不查 → create 后被**删的知识文档在红点预检里隐形**、仅下次 invoke 才大声失败（与 invoke 实际校验不对称）。round-11 agentknow2 lane（F98 家族） | promise≠reality/可观测性·medium | `app/agent/crud.go` MountHealth 加 `knowledgeHealth`（复用 invoke 的 `BuildKnowledgePrefix`——它在 `AGENT_KNOWLEDGE_NOT_FOUND` 的 details.missing 报缺失集）：每个挂的知识 doc 一条 `ref=doc_…` 行、被删标 unhealthy，使预检覆盖 invoke 校验的两轴（tool+knowledge） | 零 token `TestMountHealth_CoversKnowledge`（建带知识 agent→预检健康→删该 doc→预检该行 unhealthy+allHealthy=false）；make verify 绿 + agent.md（知识也覆盖）同步 | _pending_ |
| F126 | fixed | `update_node` 的 patch 描述说"merged"但**嵌套对象（如 `input`）整体替换、非深合并** → 改一个 input 字段会**静默丢掉同节点其它 input 键**（声明 input 失接线、capability_check/运行时才炸）。round-11 workflowbig lane（edit_agent 同类但其描述已明示 REPLACE=GREEN，故只此处需修） | 假成功/静默降级·medium | `tool/workflow/workflow.go` opsDoc update_node 描述改明"patch 仅**顶层**合并；含入的对象字段（尤 input）**整体替换**非深合并——改一个 input 字段须重发全部 input 键" | make verify 绿（描述串，无逻辑改）+ 同提交 workflow.md（顶层 merge patch、嵌套整体替换）同步 | _pending_ |
| F125 | fixed | **畸形 JSON 工具参数 → 误导的 "ops is required"**——`parseToolArgs` 解析失败就把原文塞 `{"raw":…}` 哨兵直送工具，工具找不到字段报语义错（如 "ops is required"），把 agent 从真问题（JSON 没解析）引开。round-10 functiondeep lane | error 不透明/agent-misleading·HIGH（系统性：所有工具调用的参数解析咽喉） | `loop/stream.go parseToolArgs`：unmarshal 失败先 `jsonrepairpkg.Repair`（恢复控制符/不平衡括号等常见 LLM 脏 JSON→工具拿真字段、无误导错），仍不行才留 `rawArgsKey` 哨兵；`loop/tools.go executeTool` 在 ValidateInput 前检测哨兵→明确报 "your tool arguments were not valid JSON"（不跑工具）。无工具用 "raw" 参数故哨兵无歧义 | 零 token `TestParseToolArgs_RepairsDirtyJSON`（控制符脏 JSON→真字段、纯垃圾→哨兵）+ `TestExecuteTool_UnparseableArgsClearError`（哨兵→清晰 JSON 错、不跑工具）；make verify 绿 | _pending_ |
| F124 | fixed | `manage_conversation` 的 rename **非空守卫只查字面 `""`、纯空白标题（`"   "`）绕过** → 空改名混进去。round-10 conversationflow lane（F107 收尾） | promise≠reality·low | `tool/conversation/manage.go`：ValidateInput 守卫 + Execute 写入点都改 `strings.TrimSpace(title)==""` 拒；写入存 trim 后的标题（带空格标题存得干净） | 零 token 扩 `Test_manageConversation_Rename`（`"   "` 在 ValidateInput+Execute 都拒、`"  Padded  "` 存成 `"Padded"`）；make verify 绿 | _pending_ |
| F123 | fixed | **agent 无改名工具 + revert_agent 描述指向不存在的 `edit_agent set_meta`** → agent 的 name/desc/tags 不在版本化 config 内（edit_agent 改不了），UpdateMeta 服务方法只接 HTTP PATCH，无 LLM 工具 → agent 无法被改名、revert_agent 还误导去用不存在的机制。round-10 agentiterate lane（F119 的 agent 缺口） | promise≠reality/能力不可发现·medium（补全 meta-工具家族至 agent） | 加 `update_agent_meta` 工具（S18 5 法、包既有 `UpdateMeta`、params 全可选）：只改 row 不铸版本；注册进 registry；revert_agent 描述改指向它（删假的 set_meta 引用） | 零 token `TestAgentTools_NamesAndCount` 计数 9→10 + 新工具名断言；make verify 绿 + agent.md（10 工具）同步 | _pending_ |
| F122 | fixed | **agent 执行错误面泄露 Go 包路径**（`invoke.go:123` `res.ErrorMsg = runErr.Error()`）→ 挂载解析失败把 `functionapp.Get` 等内部标识泄进 `:triage`/`get_agent_execution` 读的执行记录。F89(llmErrText)/F104(nodeErrText) 同款 sanitizer 的**第三处缺口** | promise≠reality/leaked-internals·medium（错误清洗家族第 3 面） | **强化地基**（原则 #8，第三次出现不再抄）：抽 `errorspkg.Surface(err)`（errors.As→Message+Details、裸错误透传）进 `pkg/errors`；`llmErrText`/`nodeErrText` 改为薄委托；`invoke.go` 的 runErr/perr 两处错误面改用之 | 零 token `TestSurface`（wrapped sentinel 去 Go 路径留干净 Message、Details 排序、裸错误透传、nil→""）+ 既有 `TestLLMErrText`/`TestNodeErrText` 不回归；make verify 绿 | _pending_ |
| F118 | fixed | handler **坏 `__init__` 编辑静默 brick 实例**——`edit_handler` 后 env 照样 `envStatus:ready`、但 `restart` 的 spawn 失败被吞（Info 日志），工具结果**不含 runtimeState** → agent 读到"成功"编辑、不知 handler 已 stopped/不可用。round-8 handlerlifecycle lane | promise≠reality/脆弱·medium（"edit 立即生效 + revert 兜底"是有意设计，故只补**可见性**：surface spawn 失败让 agent 能 revert，非改激活语义） | `tool/handler/build.go`：edit_handler Execute 后 `svc.Get` 读 RuntimeState 进 `buildOutput`，非 running 时附 `runtimeWarning`（坏 init/缺 config→get_handler 查详情/改码/revert）；create 不报（新建本就未 running、噪声）；描述同步（F117 meta-only 不重启 + runtimeState 信号） | 零 token `TestBuildOutput_SurfacesRuntimeState`（running→有 state 无 warn、stopped→有 warn、create ""→静默）；make verify 绿 | _pending_ |
| F117 | fixed | handler **纯 meta 编辑（set_meta 改名/改述、零代码变更）铸冗余同码版本 + 重启常驻实例、静默抹掉内存态** → 有状态计数/统计 handler 一次改名就丢全部 state，且 `edit_handler` 是 agent 唯一改名路径（无 update_handler_meta 工具）。round-7 handlerstate + round-8 handlerlifecycle 双 lane | promise≠reality/脆弱·HIGH（有状态 handler 的耐久悬崖） | `app/handler/{apply.go,crud.go}`：Edit 加 `allMetaOps(ops)` 短路——全 set_meta 时只 `SaveHandler`（更 name/desc/tags 行）+ 返 active 版本，**不铸版本、不 ensureEnv、不 restart**（name/desc/tags 在 Handler 行非版本化快照、常驻实例跑的没变）；真代码/schema op 仍走完整版本+重启。ApplyOps 已校验改名 | 零 token `TestEdit_MetaOnlyNoVersionNoRestart`（建有状态 handler→call(spawns=1)→meta-only rename→spawns 仍 1·version 仍 1·name 已改·无 v2）；`TestEdit_BumpsVersionAndRestarts`（代码 edit 仍 v2+restart）不回归；make verify 绿 + handler.md（§1/§5 纯 meta 不重启）同步 | _pending_ |
| F116 | fixed | handler **spawn/`__init__` 失败留不下审计行** → `call_handler` 经 `manager.Get`（懒 spawn）失败（坏 init 体/缺 config/env 未就绪/sandbox 挂）时直接返回、**不 recordCall** → 失败调用在历史里**隐形**、`failedCount` 仍 0、operator 调试 workflow 节点失败无迹可读。round-7 handlerstate + round-8 handlerlifecycle 双 lane | promise≠reality/脆弱·HIGH（可观测性黑洞） | `app/handler/call.go`：`manager.Get` 失败路径加 `recordCall(inst=nil, callErr=err)`（记 `status=failed` 行，ctx 超时/取消则 timeout/cancelled）；`recordCall` 改 nil-instance 容忍（`inst.ID`→守卫、instanceId 留空）。传原始 err（干净 domain 消息）记账、包裹 err 返调用方 | 零 token 扩 `TestConfig_GatesSpawn`（config 缺→call 失败后 `SearchCalls` 须见 1 failed 行 method=ping、`failedCount=1`）；make verify 绿 + handler.md（§4 记账：spawn 失败也记账）同步 | _pending_ |
| F115 | fixed | `POST /sandbox:gc?olderThanDays=0` 被**静默强制成默认 30**（`d>0` 守卫把 0 当未设）→ operator 无法强制回收**刚孤儿化**的 venv（如 F109 trim 的版本 env、首日就想清）→ F109 文档化的手动补救对近期泄漏失效。round-8(0621) revertchurn lane | 脆弱·medium（F109 的手动逃生口） | `handlers/sandbox.go` 守卫 `d>0`→`d>=0`（0 = 立即回收所有空闲；负/非数仍回落 30）+ 解析抽纯函数 `gcOlderThanDays`（可单测）+ handler doc 注释说明 0 语义。GC(ctx,0)→cutoff=now→回收所有空闲（app 层已测）；被回收 env 懒重建 | 零 token `TestGCOlderThanDays`（"0"→0、"7"→7、""/"-5"/"abc"→30）；make verify 绿（api.md 不记 sandbox 端点 query 参数粒度、无 doc 同步） | _pending_ |
| F114 | fixed | CEL `overloadHint`（F49 加）对**任何** "no such overload" 都无条件推 `int()/5.0` → 对 **string-vs-number**（`input.name > 5`、name 是串）与 num+string **误导**：强转 int 无用、真问题是字段不是表达式假设的类型。round-8 celedge lane 的 agent 亲自**拒绝**了该错误建议（"writing 5.0 doesn't help because the comparison is still between a string and a number"） | promise≠reality/agent-misleading·HIGH（系统性：每条节点 CEL 求值唯一收口） | cel-go 只给裸 "no such overload"、无操作数类型故无从分支——`pkg/cel/cel.go overloadHint` 改为**同时列两种常见因**：(1) int-vs-double 字面量（cast/5.0）(2) string-vs-number 不可比（确认字段真实类型、显式 int()/double()/string()、或 size() 取串长），不再把一种当唯一解 | 零 token `TestEvalBool_StringNumberMismatchHint`（`input.name > 5` with name="Alice" → hint 须含 "string"+"actual type"）+ `TestEval_NoSuchOverloadHint`（"cast one" 仍在）不回归；make verify 绿（hint 文本无文档引用、无 doc 同步） | _pending_ |
| F113 | fixed | `list_mcp_marketplace` 的 `query`（F91 加）是**单次整串子串匹配** → 自然多词能力查询（"database query"/"send notification"，LLM 表达意图的常态）要求词**连续出现**、**静默返 0**。round-8(0621) mcptools2 lane | 组合摩擦·low | `tool/mcp/system.go` filterMarketViews 改 `strings.Fields(q)` 切词 + 抽 `matchesAllTokens`（逐词 AND、跨 name+description、任意位置）；query 参数描述点明多词 AND、不需连续子串 | 零 token 扩 `TestFilterMarketViews`（"send notifications" 跨词 / "notifications slack" 乱序 / "query database" 乱序 各匹配对的 server；"database notifications" 无单一匹配→0）；make verify 绿 | _pending_ |
| F112 | fixed | **撞名挂载在 `create_agent` 被接受、却每次 invoke 0 步即败**（F98 eager 校验缺口）——`CheckHealth`（F98 用作 create/edit 预检）逐挂载**独立**解析、**漏掉跨挂载合成同名检测**，而 `Resolve`（invoke 路径）正按撞名拒整组 → 单独可解析、合起来撞名的两挂载过校验、铸出 DOA agent。round-7(0621) agentmaxtools lane | 组合摩擦/白烧 at scale·medium（F98 同族、不对称） | `app/tool/mount/mount.go` CheckHealth 加 `seen` map（合成名→首 ref）：第二个撞名挂载标 unhealthy + 撞名 error——与 Resolve 对称，使 `validateToolsMounted`（拒任一 unhealthy）在 create/edit 即拒 | 零 token 回归 `TestCheckHealth_FlagsNameCollision`（fn_1 挂两次→首健康、次撞名 unhealthy）；`TestCheckHealth_MixedMounts` 不回归；make verify 绿 + 同提交 agent.md（撞名检测对称）同步 | _pending_ |
| F111 | fixed | `create_function`/`edit_function`（+handler）op 校验失败只给 LLM 不透明 `invalid build op`、**藏真因** → camelCase 名（`refundProcessor`）被拒但 agent **不知为何、thrash 3 次**才靠用户点破。根因：具体原因（snake_case 规则等）经 `fmt.Errorf("%w: %v")` 串尾附加，而 `llmErrText`（F89）为防泄 Go 调用路径**剥掉整条包裹链**、连带吞掉可操作原因。round-7(0621) searchcalib lane（建语料时撞上） | error 不透明/agent-misleading·medium（**系统性**：F1/F7/F89 同族、build-op 校验面） | function：`validate.go` 的 `validateIncremental`/`validateFinal` 改返 `ErrOpInvalid`/`ErrInvalidCode`/`ErrInvalidName` 的 `.WithDetails({reason})`（原因进 Details——`llmErrText` 渲染、HTTP N1 error.details 也得）`.WithCause`（日志保链）；`apply.go` 三 wrap 点同改。handler：`apply.go` 三 wrap 点把 `err.Error()` 作 reason 进 Details（validate* 返纯错、apply 统一包） | 零 token 回归 `TestCreate_OpaqueReasonSurfacedInDetails`（camelCase 名→Details.reason 含 "lowercase"）+ `TestCreate_InvalidCode` 扩断言 Details.reason 含 "top-level def"；`errors.Is` 不回归（WithDetails 克隆按 Code 仍 Is）；make verify 绿 | _pending_ |
| F110 | fixed | **F80 的 0.7 cosineFloor 经 round-7 精测证明误标、静默砍 ~38% 语义召回**——round-17 加 0.7 时的"乱码 ≤0.63 / 真匹配 ≥0.81 空隙"是**未实测的臆断**；round-7 searchcalib 在真 8 实体语料读 `search_embeddings` 的 float32 BLOB 精算余弦：真 paraphrase 命中实测 **0.549-0.746**（中位 0.714、**从不达 0.81**）、乱码 top **0.537**——0.7 落在真命中分布**内部**，砍掉 3/8（37.5%）真召回（email 0.617 / shipping 0.677 / inventory 0.549 全被静默丢）。round-7(0621) searchcalib lane（直接 API 探+BLOB 精算，非 agent chat） | 静默降级·**HIGH**（语义召回部分归零且 API 不暴露 cosine 无从自诊） | `app/search/semantic.go` cosineFloor `0.7→0.55`（过乱码 0.537、捞回明确真命中 0.617/0.677；唯一落选 inventory 0.549 距噪声仅 0.012=本模型噪声级不可分；下游 RRF+top-100 已对弱命中降权，前置过滤只需噪声闸非精度门）+ 注释用实测数据重写（删假的 ≥0.81/空隙说法） | 零 token 回归 `TestHybrid_CosineFloorAdmitsGenuineMatch`（cos 0.62 真命中须浮=旧 0.7 会误丢、cos 0.53 噪声仍拒）+ `TestHybrid_CosineFloorRejectsNoise` 注释同步；make verify 绿 + search.md 同步（含实测校准表） | _pending_ |
| F109 | fixed | `TrimOldestVersions`（edit 越 cap 50 时）硬删旧版本行**但不销毁其 per-version venv** → 频繁 edit 的 fn/handler **每越 cap 漏一份 venv 到盘**（owner=`id_envID`、含 torch 等可达百 MB），仅手动 `POST /sandbox:gc`（默认 30 天 LastUsedAt）回收、**无自动调度器** → 无界磁盘泄漏。round-7(0621) versionchurn（round-6 finding 落地） | 系统正确性/资源泄漏·medium（单用户本地 app、随 edit 累积） | store `TrimOldestVersions` 改返 `([]string,error)`（删前 pluck 被删版本 env_id）+ domain Repository 接口同步 + app `SandboxRunner` 加 `DestroyEnv(owner)`（adapter 委托 `svc.Destroy`、env 不存在 no-op）+ crud.go Edit `reclaimTrimmedEnvs` 逐个销毁（best-effort warn）；function+handler 双实体（control/approval/workflow 无 per-version env 不涉） | 零 token store `TestVersion_TrimProtectsActive` 扩断言返被删版本 env_id（fn+hd）+ app `TestEdit_ReclaimsTrimmedVersionEnv`（create+50 edit→恰销毁 v1 env owner）；make verify 绿 + function.md/handler.md 同步 | _pending_ |

## 元注（一次性，非 finding）
- **DOC-ALIGN 里程碑（0620，10 read-only agent 扇出、每面 diff 文档 vs 权威代码、计数真去数代码）—— 8 面零 drift、逮 2 跨面 drift（均已修）**：per-commit 同步守得很牢（api.md 169+7 路由/全 :action 动词、database.md 38 表/39 ID 前缀/全 CHECK 值集、error-codes.md 279 码、events.md 3 流/4 帧、conversation+chat/todo/relation/mcp[96 catalog/22 oauth] 全逐字吻合）。**2 跨面 drift**（正是 DOC-ALIGN 要兜的「单条提交视角看不到的跨面项」）：① **overview.md「265 码」陈旧**（error-codes.md 早 bump 到 279、overview 交叉引用没跟）→ 改 279;② **workflow.md 引旧符号 `SupersedePendingOlderThan`**（F29 对抗审查时已改名 `SupersedeAllButNewestPending`、doc 没跟）→ 改名;顺手修 ③ scheduler_test.go:749 注释同一旧测试名 `TestSupersedePendingOlderThan`→`TestSupersedeAllButNewestPending`（同根 rename drift、bonus）。make verify 绿。**结论：per-commit 纪律 + 里程碑 DOC-ALIGN 双层有效——前者挡住 95%+、后者逮跨面残漏。**
- **round-explore（0620，3 lane 并发、各演 user 真 deepseek 多轮、判后端 ground-truth）—— 0 可修 bug、全绿/by-design（强收敛续）**：① **advopts GREEN**——agent modelOverride/挂载 fn/knowledge doc/handler config-update→restart 四个广告选项**use-time 全真生效**（negative control 坐实：同 workspace 带/不带 override 两 agent 的 `agent_executions.model_id` 分别为 deepseek-chat/v4-flash；knowledge 的 ZEPHYR-7/Bartholomew 只存 doc body 却出现在输出；handler config 改后**新 instance id** 证真重启非 stale）。② **recovery GREEN**——code-bug 失败 run：agent 经 get_flowrun 诊断→edit_function 修 v2→**正确选 fresh trigger**（知 replay 用旧 pin、实测 replay 的 fr replayCount=1 仍 pin 旧 buggy 版、fresh run pin 新版 completed）；`replay_flowrun` 描述把「replay 按原 pin、改 code 要 fresh trigger」gotcha 写在 agent 读得到处 → 无 `:rerun-with-latest` 是 D1 record-once 有意（历史 run 不可变）。③ **dataflow core GREEN**——order-processing 工作流 agent 接线 `<nodeId>.field` 全对、声明 object output 穿进下游 CEL（discountedTotal:900 非 .text 吞）、两分支两 total 值终态全对；逮 F88（capability_check 不校声明 output 引用——读码判 not-bug，static 校 unsound）+ 一个**模型行为非后端**料（agent 谎称删了坏节点实未删、忽略 get_workflow 返的 needsAttention:true——后端正确置 banner+精确 reason，model 没读，非 backend bug）。**3 lane 高价值未覆盖格全绿/by-design——产品续高度硬化。**
- **round-补薄格（0620，对 fresh :8749 当前源 + deepseek）—— 两薄格补绿 + 逮 F87**：① **kill-9 真崩溃恢复 from agent 席 GREEN**——agent 一回合建 trigger→approval→fn durable workflow + trigger(amount=500)→parked（start completed 记忆化 + approval parked）；`kill -9`（硬杀、非优雅）后同 data dir 重启 → run 存活（status running/parked、`start.amount=500` 记忆化保留、pinnedRefs apfv/fnv 版本 pin 保留）→ agent 崩后 decide approval → resume 沿 yes 分支 → log_it 用**记忆化的** start.amount=500 → `{logged:true,amount:500}` → completed。**record-once 记忆化 + 版本 pin 硬扛 kill -9**（durable 引擎 T3 唯一未验缺口闭合）。② **concurrency replace/buffer_one from agent 席 VERIFIED（真 fire 路径）**——逮 F87（手动 trigger_workflow 绕过策略，已修描述）后，经**真 webhook POST** 实测：`replace`→fire 时 overlapDecision 取消 2 个在途 parked run（A/B→cancelled）；`buffer_one`→run C parked 时连 fire 2 次、**无新 run 起**（C 仍唯一 running）、2 firing defer（newest `pending` + older `superseded`，证 SupersedePendingOlderThan 留最新）。两策略真 fire 端到端验通（F29 单测的 consumeFiring 路径实跑坐实）。
- **round-21（0619，对 fresh v3 :8743）**：**chaos 鲁棒性全绿**（深 JSON→400·病态 CEL→干净拒无 panic·unicode/RTL/控制符→422或保留·mem-bomb→OS overcommit·inf-loop→pgroup-SIGKILL 容器化·环图→422·并发 dup→1+5×409·SQLi 头→401；零 500/panic/腐败/逃逸）+ F83（function 无 timeout 设计观察）· buildhard **第二个新域复杂系统（内容审核管线）编排全绿**（8 实体三分支+cron 全对、agent 自恢复）、唯一料 = **F64 具体复现**（handler mixed-indent 畸形体→不透明 "subprocess crashed"·无 create-time 语法检查——F63 dedent 只解 uniform 缩进、mixed 缩进=agent 写的畸形 Python 仍撞 F64 不透明面）。**round 20-21 连续零新 clean fix——综合收敛确认**。
- **round-20（0619，对 fresh v3 :8743）**：**relation 图全绿**（深度/diamond 去重/删边更新/cycle/14+ 实体规模全对；真 5+ 跳链结构上不可能=fn/hd 是叶、agent 不挂 agent、wf 不嵌 wf，故 max uses 深度=2 跳、诚实文档化）· **replay/durable 恢复全绿**（record-once 记忆化早节点·仅失败节点重跑、pinned-version replay、approval 存活、replay_count 幂等）· hdsecrets 逮 F82（secret 经 call-log 泄露·defense-in-depth、debatable）。**产品综合硬化**。
- **round-19（0619，对 fresh v2 :8743）**：**chat 体验深用全绿**（cancel→cancelled+partial 存无孤儿、confirm/deny=副作用前中断、并发 2nd→409 STREAM_IN_PROGRESS 干净终态、6 轮上下文连贯、空→400 EMPTY_CONTENT）· **i18n 全绿**（CJK desc/tags/content 零 mojibake round-trip、locale 是软指令=有意、name 拒 CJK=有意 slug 标识约束）· hdstream 逮 F81（generator return 终值被吞，已修）。
- **round-18（0619）**：**conversation 管理全绿**（rename 持久不被 auto-title 覆盖、archive/unarchive 正确、soft-delete→404、分页 cursor 0 重 0 漏、usage 与逐条 token 和精确零漂）· modelmgmt **逮 F68 升级 HIGH**（agent 无配置工具→grep FS 读 .env **泄露明文 key** + 臆造假审计）。**F67 经 round-18 确认为 intended-非bug**：坏 modelOverride/default-model set-time 接受是 runtime-graceful 有意设计、use-time 错干净点名有效 model（与 SetDefaultSearch 注释一致）——撤出待修。**⚠️ :8743 binary 又 stale**（建于 F72 前、缺 F72/F76-F79）——convmgmt2 重测 F72 区逮到（200 而非 404）；**已重 build v2（HEAD 全 23 fix）**、本提交后重启 :8743。**复犯 manual 铁律**：每次 commit fix 后必重 build backend——loop 长跑中要周期性重启 fresh，否则新 fix 不 live。
- **round-17（0619，对 fresh :8743）**：**memory 深用全绿**（12 写全召回、按名 upsert 替换不重、forget 真删无幻影召回、workspace 隔离 401）· searchdeep 逮 F80（语义搜索无相关性下限→无匹配 query 灌全 workspace，medium·需阈值校准；语义召回/排序/类型 scoping 本身绿）· convmgmt **lane 丢于 Anthropic 限流**（非 deepseek/产品，待重跑）。
- **round-16（0619，对 fresh :8743）**：**e2e 系统搭建绿**（agent 把 support-ticket triage 全系统正确编排——3 fn + control + approval + webhook trigger + workflow，三路径全经**真 webhook POST** 验通；选型/接线/capability_check/真触发全对）· attachdeep 逮 F77（文本无截断帽）+ F78（stale-id 静默丢）· webhookauth 逮 F79（HMAC header 不可发现，medium；auth/dedup/body-cap 本身全绿）。**少探面（e2e 编排细节 / attachment / webhook auth）仍有真料**——多为「工具描述漏载关键规则 / 静默降级」族。
- **round-15（0619，对 fresh :8743）全绿**：document 块编辑（markdown-tree、字节精确 round-trip、单块编辑 siblings 不动、reorder 连续、1MB guard 精确、200 项不腐）· :triage AI 诊断（正确诊断+可操作修、无幻影会话、pin-replay 须 fresh trigger=有意 pin 语义）· notification/needsAttention 生命周期（点亮/熄灭/不误报/正确 scoped）**三条全绿、零 defect**。**收敛广化**：core + document + triage + notification 全硬化；近期 defect 仅出 MCP 深面（F52/F74/F75）。
- **round-14（0619，对 fresh :8743）**：**tool-pick 准确性全绿**（5 个相似功能 fn + 4 个相邻 agent 里，agent 每次选对、无则建新、有完美匹配则复用不重建——选错工具/不可发现/白烧/假成功 四镜全过；建议 de-prio 单发 toolpick，再探须升难度 >10 实体/双实体平局/同形错名）· **skill 深用全绿**（danger gate 恰为 dangerous 调用触发、body 32KB cap 干净；2 候选均 model-misread——模型 union 误猜 + danger 低估，非后端 bug）· **mcpschema 逮 2 真 defect**：F52（坐实 HIGH，DynamicTools 死码、doc 过度承诺 chat-MCP）+ F74（嵌套 MCP 结果不进 CEL，medium）+ F75（HTTP invoke MCP 错不透明，low）。**收敛中仍有 MCP 面深料**——少探面尚有真 bug。
- **round-13（0619）行为复验全绿（对 fresh :8743 当前源）**：补完 round-12 被 stale binary 堵死的复验——**8 项全 GREEN、零回归**：F49（cast hint→agent 1 步恢复 vs 旧 4 版本）· F32（schema-less `.text` 首试接对、文档化规则可发现）· F69（create_control 时返真 cel-go 因含 line:col+caret）· F47（`decide_approval` 真把 parked run 推到 completed、补全人在环）· F60（`0s` timeout 被拒 APPROVAL_INVALID_TIMEOUT）· F66（invoke 真透 provider 400 错「supported API model names are …」非泛 "agent loop error"）· F31（agent→agent 被工具 schema 导向「build a workflow with an agent node」）· F36（`:iterate` 不存在→404 AGENT_NOT_FOUND）。**本会话 18 fix 行为侧坐实**。
- **【★关键运维发现 0619 — 探针曾打在 stale backend 上】**：round-12 行为复验时发现 :8742 的 backend（PID 8934）启于 **2026-06-18 16:25**，**早于本会话 16:25 后提交的所有 fix**（F31+/F49/F66/F69/F70…）——即 **round 6-12 的行为探针测的是 mid-session 旧码、非当前源**（loop 复用既存 backend、从未重启拉新码；早期 F1/F18-F22 因 16:25 前提交故 live）。**fix 本身无损**：每条都 commit + `make verify` 单测锁 + **代码阅读确诊**（反自欺纪律救场——findings 锚当前源、非仅 binary 行为），只是"已修格的行为复验"对 stale binary 失真。**已自主规避**（不碰共享 :8742——harness 亦拦截 kill）：`go build` 当前源 → 另起 :8743（fresh binary、独立 /tmp/anselm-reverify），直 curl 确认 **F69/F14/F70/F47/F60 五项 live + 正确**（:8742 上 details:null·payload.x 被接受·无 cast hint；:8743 上全对）。**铁律入 README manual**：探针**必须打在『当前源 build 的 fresh backend』**（专用端口 + 独立 data dir）、**绝不复用既存/stale 进程**——否则 find/verify 全失真。
- **round-11（0619）≈收敛完成**：delcascade（删被引用实体→消费 run 干净报 ref-not-found、链可恢复、pin 闭包不受影响）+ largescale（大图/多 input·output/长内容/版本 cap-50 全无截断·腐败）**两条全绿**；concur2 仅**重确认 F61**（并发 create_document position 碰撞，20→14 distinct，**仅外部并行 HTTP 客户端触发**——agent 工具调用串行故 agent 不触发；damage 经 created_at tiebreak 软化）+ F73(low)。**本轮零新 clean fix——产品已高度硬化**。create_function 名竞态/serial-trigger firing 路径/D2 隔离 全绿/by-design。
- **round-10（0619）**：durabledeep——**深 durable 循环引擎全绿**（25 迭代累加器 + 双体节点循环各 per-iteration 正确记忆化、按真条件退出、远低于 MaxIterations；scopeFor「≤iter 最大 completed」对多体循环验对）；逮 F70/F71（authoring 层 input 丢弃 + capability dataflow）。isolation——**D2 workspace 隔离边界全程守住**（跨 ws 读/写/run 全 404/401、无泄露；ORM workspace 过滤是兜底），仅 F72 一致性 nit。concurrency lane 丢于 Anthropic API 瞬错（非 deepseek/产品）。
- **round-9（0619）透明度轴 sweep**：故意在 function/control/trigger 各处制造失败、查真因是否可操作到达 agent。**fnerr 全绿**（自定义异常/ImportError/非JSON返回 真 Python traceback 逐字穿 run_function+flowrun节点+执行记录；唯一设计观察：function 运行无强制 timeout，绿/设计完备非 opacity）· **trgerr 全绿**（trigger 失败路径可追）· **ctlerr 运行时绿**但逮到 F69（author-time CEL 错丢因）。透明度轴本身已大体硬化（F7/F8/F33/F34/F35/F49/F63/F66 + F69），剩 F64（handler import）+ F67（modelOverride set-time）同族待修。
- **round-8（0619）**：approval timeout approve/fail 行为（补全 reject 后的三件套、均诚实兑现承诺语义）· handler config 工具（update 重启带新 init-args、merge-patch 保键、clear 停实例、重启清内存态=有意 durable 设计）**两条全绿**；仅 modelOverride 面有料（F66 HIGH 修 + F67/F68 队）。**收敛持续**：8 轮里 clean fix 多为 message/契约/校验改良 + 偶发 HIGH（流错透明度 F33/F34/F66 同族、handler 透明度 F63/F64 同族）。
- **round-7（0619）收敛深化**：mcpruntime（mcp 运行时错路径——分层可操作错、双落 flowrun_nodes+mcp_calls 交叉链、agent 轻松诊断）· maxcompose（一 agent 挂 fn+hd+mcp/doc+skill 全生效、handler 跨 invoke 保态、doc 实时注入）· ctlmulti（多分支 control first-true-wins 全对）**三条全绿**；仅 handler-build 面（F63 修/F64 队）+ sensor level-trigger（F65）有料。产品**高度硬化**。
- **F47 经 round-6 双 lane（apptimeout + largegraph）独立重confirm**：approval 的**人工决策半边从 agent 席不可达**——无 `decide_approval` / `list_approval_inbox` 工具（HTTP `:decide` + `/flowrun-inbox` 都在、没工具包）。agent 能建+触发 approval workflow 却永远无法批/拒，park 的 run 也只能 kill。**确诊 feature 缺口**（待做：包 `scheduler.DecideApproval` 成工具）。
- **收敛信号（round-5/6）**：真 clean bug 产出率降、not-bug/设计议题占比升（F43/F55/F56 读码纠为非 bug）——产品在硬化。剩余高价值多为**设计判断**（F40/F41/F52/F55/F57/F62）+ feature（F37/F45/F46/F47/F39）。
- **round-2 子系统全验证通过 + 关键修法行为复验**（2026-06-18，真 deepseek + ground-truth）：① **fsnotify/webhook/cron 触发、durable replay、结构化循环、concurrency(serial/skip)、skill allowed-tools preauth、agent knowledge** 八条 lane 全端到端验证可转（durable 引擎内部全对，bug 全在隐形契约/未实现广告，已系统性修——F24~F29）。② **F28 累加循环行为复验**：agent 读新 opsDoc LOOP STATE 指南、自写 `has(inc.count)?inc.count:init.count`、flowrun count 0→1→2→3 逐迭代记忆化 + done 终止（前必崩 cel-go "no such attribute"）。③ **F22 真 MCP server 运行时复验**：经 HTTP PutServer 连真 `@modelcontextprotocol/server-everything`（status=ready），agent 用 **name 形** `mcp:everything/echo` 接 workflow、capability + dispatch 全通、flowrun completed、echo 节点产 `{text:"Echo: hello F22"}`（前 name 形必 ErrRefNotFound/MCP_SERVER_NOT_FOUND）——闭合 F22 唯一运行时验证缺口。
- **为什么这 loop 值得**：F1 那条轨迹 `golden J5` 只断言"版本>1"是绿的；轨迹判官却抓到模型把 `get_function` 调错绕一圈——终态测试瞎、判官看见。
- **workflow + durable 子系统验证通过**（2026-06-18）：F7+F8 修后，agent 建成 workflow（trigger→convert→classify）、`trigger_workflow` 跑通；durable 引擎逐节点记忆化、结果正确（celsius=100 → convert `{fahrenheit:212}` → classify `{label:"hot"}`，三节点 completed）。"整套工程"在此方向确认能转。
- **handler 子系统验证通过**（2026-06-18）：agent 建 counter handler、add(5)→5、add(3)→**8**（resident 进程跨调用保态）、get()→8。有状态常驻"灵魂"正常。
- **control 路由子系统验证通过**（2026-06-18，F13 修后）：agent 建 trigger→control→{hot_label,normal_label} 工作流，durable 引擎 first-true-wins 路由：35→`__port:hot`→"hot warning"、10→`__port:normal`→"normal"（两条 flowrun 各节点 result ground-truth 验）。control 分支结构化路由 + emit 透传在此确认。
- **trigger sensor 自主触发子系统验证通过**（2026-06-18）：agent 一次 0-error 建 handler(level)+sensor(每 5s 探 `read_level`、condition `payload.value>100`、output `{level:payload.value}`)+alert workflow+activate。set level=150 后 sensor 每 5s **自主 fire→spawn flowrun→alert** 节点产 `{alert:true,level:150,message:"Level 150 exceeded threshold of 100"}`（flowrun 节点 result ground-truth 验）。**带着思考纠偏**：firing 记录全 `status:started`、`matched/firedAt` 为 null，初看似「卡住未 finalize」——读 `domain/trigger/firing.go` 知 `started`=「claimed+flowrun created」**即终态-ok**（单 status 枚举、本就无 matched/firedAt 列，jq 取空字段才显 null）→ **非 bug**，自主 fire 全链路正常。差点把非 bug 当 bug，读码纠偏（反自欺铁律）。
- **多轮迭代编辑子系统验证通过**（2026-06-18）：4 轮对话 agent 建 fn v1→改加 middleName/改格式 v2（`edit_function` 改同实体非新建、改前 `get_function` 读态、改后 run 测）→ 边界（无 middle 不留尾空格/句点）正确 → 经一次 deepseek 流瞬断后 turn-4 优雅恢复、正确报 v2。多轮上下文承接 + edit 路径（F6 域）在多轮压力下稳。
- **approval park/resume 子系统验证通过**（2026-06-18，F15/F16 修后）：agent 建 trigger→control→approval→action 退款工作流，trigger(250)→control 路由 `over_100`→approval 节点 **park**（run 续 running）→ 经 `POST /flowruns/{id}/approvals/{node}:decide {decision:yes}` 决策→ run **resume**→ 下游 action 读 `start.amount`(250)+`approval_node.reason`→ 产 "Refund of \$250.00: ✅ Approved by manager"→ run **completed**（全节点 result ground-truth 验）。durable 人在环 park/resume/decide 全链路在此确认。
- **harness 修**：`testend/loop/setup.sh` 固定 workspace 名 "loop" → 重跑 `WORKSPACE_NAME_CONFLICT` → `ws=null` 卡住 loop 自身；改唯一名（`loop-时间戳-RANDOM`）。属"harness 工程"维度。
- **F29 并发实现经多 agent 对抗审查 + 修**（2026-06-18）：5 维对抗审查（race / durable / cancel 边界 / crash / 语义）判 race + durable 健全，抓到 **2 major + 1 minor 真问题、全修**：① **buffer_one 在 `running==0` 短路里漏 supersede** → 旧待处理 firing 仍执行（违「只留最新」）→ 改 `SupersedeAllButNewestPending`（收敛到最新、与 running 无关、order-independent）+ 回归 `TestFiring_OverlapBufferOneRunsNewestOnly`（2 待处理无 run → 只跑 1）；② api.md 漏同步（仍写 buffer_all、缺 replace）→ 补；③ replace 取消 parked run 留死审批收件箱项 → `CancelParkedNodes` 在 replace+kill 收掉（+ `TestCancelParkedNodes`）。**审查值**：真抓到一个 correctness bug——终态测试本会绿、对抗审查看见。
- 永久回归 test：`selfiter_confirm_f1_*`、`_f1batch_*`（F1）· `Test{Function,Handler}_EditPersistsMeta`（F6）· `TestLLMErrText`（F7）· `TestWorkflow_InvalidCELListsAvailableNodes`（F8）。
