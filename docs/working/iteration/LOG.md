---
id: WRK-028
type: working
status: active
owner: @weilin
created: 2026-06-18
reviewed: 2026-06-18
review-due: 2026-09-16
audience: [human, ai]
landed-into:
---

# Iteration Loop —— Finding 索引（一行一条，永不写成 essay）

> **规范（强制）**：一个 finding = **一行**，每格一个短语。证据→轨迹 dump；修法详情→commit；本表只做索引。
> 状态：`open` 待修 · `confirmed` 已复现待修 · `fixed` 已修+验+回归 · `watch` 观察 · `not-bug` 判断后非 bug（成本/性能/可恢复且行为正确——不算）· `dup` 被他条覆盖。
> 新发现追加在表末。**别删行**（同 D1 Log 语义）。

| ID | 状态 | 问题（一句话） | 范围 | 修法（定位） | 验证（前→后） | commit |
|---|---|---|---|---|---|---|
| F1 | fixed | lazy 工具概览不点名 id 参数 → 模型瞎猜参数名（`query`/`function_name`…） | **系统性 49/50** | 地基：`toolset.Overview` 浮出必填参数名 + `prompt` 渲 `name(args)` + preamble id→search 解析 | function+handler 修前 4/4 错 → 修后 4/4 一次对、零 error；79/91 工具现渲参数 | dfe2a361 |
| F2 | not-bug | "resident vs searchable" 措辞被半误读——但 agent 行为本就正确，非 bug | — | — | — | — |
| F3 | not-bug | 简单任务 ~75K input token（冗长 schema 重发）——成本/性能，**非 bug**（作者明示不算） | — | — | — | — |
| F4 | watch | `run_function` 首调 args 平铺非 `{"args":{…}}`（修 F1 后未复现，疑被 F1 一并修掉） | 待 CONFIRM | — | — | — |
| F5 | fixed | 模型用无效字段类型 `"integer"`（schema 只认 number）→ 一次失败调用 + 恢复 | 系统性（`pkg/schema` 所有 build 实体共享） | `ValidateFields` 归一别名（integer/int/float→number, str→string, bool, dict→object, list→array）原地写 + `FromJSONSchema` 同 | 零 token 单测 `TestValidateFields_NormalizesAliases` 绿；make verify 绿 | _pending_ |
| F6 | fixed | edit 带 set_meta 不更新实体行 name/desc/tags（只移版本指针）→ agent 以为改了名、后端没改 | function+handler（workflow 本就对；agent/control/approval 无 set_meta op） | `Edit` 把 draft meta 带回行 + `SaveVersionAndActivate(v, f)` 同事务 Save 整行（6 文件） | `:edit set_meta` 重命名后 GET 真变；零 token 回归 `Test{Function,Handler}_EditPersistsMeta` 绿；make verify 绿 | e356cf2f |
| F7 | fixed | tool 错误对 LLM 不透明：`Error()` 只给 Message、丢 `Details`，而 workflow 校验把违例节点+真实 CEL 错放在 `Details.reason` → agent 见 "workflow graph is invalid" 盲猜 CEL ~8 次卡死 | **系统性**（tool-error→LLM 边界丢所有工具的 Details） | `loop/tools.go` 加 `llmErrText`，在 executeTool 把 Details 渲进 LLM 可见错（一处修全部工具，原则 #8） | 零 token 单测 `TestLLMErrText` 绿；make verify 绿；agent 重跑见详错、自纠建成 workflow、turn completed（前 ERROR） | 0a6c6986 |
| F8 | fixed | workflow CEL 错说 "undeclared reference to 'X'" 但不列**可用**标识符 → agent 试 payload/trigger/celsius/input/receive 5 次才中 | workflow-only（control/approval/trigger 用固定 env payload/ctx/input，无此问题） | `crud.go` compileGraphCEL 首层错附 "this node may read: [祖先节点 id]" | 零 token 回归 `TestWorkflow_InvalidCELListsAvailableNodes` 绿；make verify 绿 | 10c2e343 |
| F10 | fixed | `invoke_agent` 的 `input` 非 required → 概览只显 `invoke_agent(agentId)`，agent 猜 `prompt`（静默丢）→ 空 input 跑出通用问候**却 ok:true**（误导成功）；search_tools 后用对得正解（30C=86F） | invoke_agent | `input` 设 required：概览现显 `invoke_agent(agentId, input)` + ValidateInput 缺失即 `AGENT_INPUT_REQUIRED`（`{}` 允许 self-contained）+ 描述点名「无 prompt 字段」 | 单测 `TestInvokeAgent_RequiresAgentID` 更新（缺 input 报错）；make verify 绿 | 9f0fc39a |
| F9 | not-bug | `get_flowrun` "not found"——查实：模型把 `trigger_workflow` 返回的 id `fr_…b4a` **截成 `fr_…b4`**（漏末位）后端正确报无；用全 id 重试即中。后端正确、模型复制错+恢复 | — | — | — | — |
| F11 | fixed | 嵌入 provider 对 >512-token chunk 报 "input (590 tokens) too large, physical batch 512" → 语义搜索**静默退化 lexical**（仅 INFO 日志，质量降级隐形） | 嵌入 server llama-server 启动 flags（`search/engine/engine.go`） | `engine.go` 加 `--ubatch-size 2048`（=ctx-size，整块一批嵌） | 重启后 ~600-token function 描述索引：embed server 起、"input too large" 计数 **0**（前反复）、不再退 lexical；make verify 绿 | _pending_ |
| F12 | fixed | chat 回合卡 `streaming` 0-block >5min（`updatedAt`==`createdAt`=无事件到达）。idle-timer(`provider.go:62`,150s)对无事件流应 150s 后 cancel，但 message 未 finalize 到 failed（仍 streaming）→ **疑** idle-cancel 未 finalize message（真 bug）**或** deepseek 对超大 system prompt 退化 hang | — | **= F33** —— round-1 toolpick lane 干净复现（2/2）+ 定根 + 修 | 见 F33 | 假设证实：idle-cancel 确未 finalize（keep-alive 流困死 scanSSELines） |
| F13 | fixed | `create_control` 描述说 when/emit CEL 读 `payload`/`ctx`——但运行时只绑 `input`（dispatch.go:159），field schema + 域模型 + docs 三方全说 `input.*`。描述自相矛盾、**主动诱导** agent 写 `payload.temperature`（运行时未声明 → 控制节点必崩）→ 模型为此耗 ~120 行推理纠结 payload/input、终写错 → `TOOL_ERROR_STORM` 中断整轮 | control 工具描述（与自身 field schema + runtime + docs 矛盾） | `tool/control/lifecycle.go` create 描述重写为「读 `input.*` ONLY、payload/ctx 不在域内」+ 点明两层喂入（workflow 节点 input 映射→喂 `input`，branch 再读 `input.*`） | 重建重跑**同任务**：前=TOOL_ERROR_STORM/45 块/写 `payload.*`/未建成；后=completed、写 `input.temperature`、durable 路由 35→`__port:hot`→"hot warning"·10→`__port:normal`→"normal"（flowrun result ground-truth 验） | _pending_ |
| F14 | fixed | create/edit 时 `celpkg.Compile` 用共享**宽容 env**（声明 payload+ctx+input 三根）→ control/approval 写 `payload.x`/`ctx.x`、sensor 写 `input.x` 编译通过、运行时崩（活化各只绑 input/input/payload）；author-time 不做命名空间校验（**用户选 option b：收紧地基**） | 系统性（control/approval/sensor 三类受限 CEL） | `pkg/cel` 加 `CompileFor(roots,expr)`（恰给定根、**无自动 ctx**）+ `CompileTemplateFor`；control(when/emit)→`[input]`、approval(模板)→`[input]`、sensor(condition/output)→`[payload]` 的 create/edit 校验切到它，镜像真实运行时活化；doc 同步 cel 包注 + platform-pkgs.md | 零 token 单测 `TestCompileFor`（in-ns 编过/out-ns 拒）+ `TestCreate_WrongNamespaceCEL`（control payload.x/ctx.x 拒、input.* 留）绿；既有 control/approval/trigger 测无回归；make verify 绿 | _pending_ |
| F15 | fixed | approval 节点的下游 result 只是 `{decision, reason}`（**非透传 input**）——但 `create_approval` 描述只讲 yes/no 出口、从不说 result 形状 → agent 把下游 action 接 `human_gate.amount`（approval result 无此键）→ park/decide 续跑后 action 运行时崩、run **failed** | **系统性**（任何 approval→下游需原数据的工作流） | 中央修 `opsDoc`（见 F16）列 approval result=`{decision,reason}` only + `create_approval` 描述补「不透传、下游从上游节点读原数据」 | 重跑同任务：前=action 读 `human_gate.amount` 崩、run failed；后=0 error、读 `start.amount`、approve 后 run **completed**（`record_approved` result ground-truth 验） | _pending_ |
| F16 | fixed | `opsDoc`（create/edit_workflow 共享）说节点 input CEL 读「payload/ctx for a trigger's signal, input for node-fed data」——实际读**上游节点 result 按 node id**（`start.amount`）、无 payload/ctx/input 根 → 误导 agent 写 `payload.x` 节点 input（F13/F15 两轮都先撞此、靠 F8 错误提示才纠） | **系统性**（所有工作流节点接线的总入口文档） | `tool/workflow/workflow.go` opsDoc 重写：明示 input CEL 读 `<nodeId>.<field>`（无 payload/ctx/input 根）+ 加 **NODE RESULT SHAPES** 块（trigger/action/control/approval/agent 各 result 形状） | 同 F15 重跑：agent 一次写对所有节点 input（`start.amount`/`approval_node.reason`）、0 error、run completed；docs 本就对（domains/workflow.md:20、scheduler-flowrun.md:37）故 code-only | _pending_ |

| F17 | not-bug | 多轮改 fn 后 agent 自称「active 仍 v1」——查实：turn-2 `get_function` 在 edit **之前**调（正确返 v1 快照），agent 回看该**前置快照**误判 active=v1；后端实际 edit→v2、run 返 v2 输出、当前 active=v2 全对（F6 激活正常运作）。同 F9 模型时序误读、**非 bug** | — | — | — | 同轮 `LLM_STREAM_ERROR`：THINK 截于半句 = deepseek 流瞬断，后端**正确 finalize 到 error**（status=error/code=`LLM_STREAM_ERROR`）→ 证流错 finalize 路径可用（**区别于 F12 卡 streaming**）；turn-4 对话优雅恢复、agent 正确报 v2 + "Smith, Jane"（无 middle 边界对） |

| F18 | fixed | `entryFuncName` 先 `TrimSpace` 再判 `def ` → 选中**缩进的** def（类方法/嵌套）作入口、与 `validateFinal` 列-0 规则矛盾 → driver 按名调缩进函数 → 运行时 NameError；validateFinal 放行（列-0 def 存在）故 create 期不挡 | 系统性（类/嵌套 def 在入口前；多 agent fanout 之 skeptic 变体复现，driver 写成 `multiply(**_input)`） | `function/validate.go` `entryFuncName` 改判 `HasPrefix(line,"def ")`（不 trim、要求列 0） | 零 token 单测 `TestEntryFuncName_TopLevelOnly`（类方法在前→返列-0 入口）绿；make verify 绿 | _pending_ |
| F19 | fixed | `create_function`/`edit_function` 描述只示 `def main(...)`/`def(**kwargs)`、从不说**第一个顶层 def 即入口**（按 **kwargs 调、名无意义、helper 须置后）→ helper-first 的常规 Python 风格 → run 调错 def、opaque TypeError | 系统性（任何 helper-first 写法；skeptic 变体复现 `_avg() got unexpected kwarg`） | `tool/function/build.go` CreateFunction 描述加 ENTRY POINT 段（首列-0 def 是入口、**kwargs、helper 置后/内嵌） | make verify 绿；同 F18 簇重建再行为验 | _pending_ |
| F20 | fixed | `revert_*` 只移版本指针，但 name/desc/tags **不在版本快照**（在实体行、F6 确立）→ revert 不还原改名 → agent 误报「已改回原名」（result 也不含 name 无从自查）。**F6 同类、在 revert 路径** | **系统性 5 实体**（function/handler/control/approval/agent 的 meta 都在行；workflow meta 在 graph 版本内、不受影响——已核 6 个 Version 结构） | 5 个 revert 工具描述加 honest-contract 句（name/desc/tags 非版本化、revert 不动、用 set_meta 改）。值班判 Option B：meta 是身份非行为快照、不应被 revert 改名——只澄清契约+不改行为 | skeptic 变体确诊（revert 到 v1 后 code 还原但 name 仍 `temp_converter`）；make verify 绿 | _pending_ |

| F21 | fixed | `move_document` 的 position 是**非移位绝对索引**：`Service.Move` 裸赋 `d.Position=*in.Position` 只更一行、不挪同级 → 插入已占用槽（如 0）→ 位置撞 + `ListByParent` 的 `created_at` tiebreak 静默按创建序排 → reorder 静默失败 + 同级 position 重复（实测两兄弟都 pos=1）；且 position **只写**：list/read 工具从不返回、agent 看不到顺序只能盲猜 | 系统性（任何 reorder N 个子文档；skeptic 变体复现 pos=1/pos=1 碰撞） | `app/document/document.go` Move 改稳定按索引插入（取同级 ListByParent、剔自身、splice 到请求 idx、0..N-1 重排、UpdateBatch 原子写）；`tool/document/list.go` slim 加 `position` + 描述 | 零 token 单测 `TestMove_StableReorder`（移末位→0 得 C,A,B、position 0/1/2 无撞）绿；make verify 绿 | _pending_ |

| F22 | fixed | MCP ref split-contract：`search_blocks`/RefHint 投出 **name 形** `mcp:<name>/<tool>`（agent 挂载也按名解析），但 workflow `refresolver`+`dispatch`+`capability` 只认 **id 形** `mcp:<mcp_id>/<tool>` → name 形在能力检查 `ErrRefNotFound`、运行时 `MCP_SERVER_NOT_FOUND` **静默失败**，agent 只能暴力猜 mcp_ id（两独立 fanout lane + skeptic 变体复现：name 形 capability 失败、改 id 形即过） | **系统性**（任何把 MCP 工具接入 workflow 的人） | 加 `mcp.Service.ResolveServerID`（先 id 后 name → 规范 mcp_ id）；`refresolver` mcp 分支 + `dispatch` RunAction mcp 分支都改用它（镜像 `mount.go` 按名解析）→ 三处消费方统一接受 name 或 id | 零 token 单测：refresolver `mcp:markitdown/...` name 形现 resolve OK；全后端 build + bootstrap 测绿；doc 同步 `domains/mcp.md`（ref-token 统一） | _pending_ |
| F23 | not-bug | chat agent 无工具注册**自定义** MCP server——**用户拍板：产品设计就是只配置官方市场列表**（非缺口） | 产品决策（marketplace-only） | `install_mcp_server` 描述收口为「仅市场、无自定义 self-hosted、用户问就说明只支持市场」（去掉误导的「去 UI 配」承诺） | 用户决策 marketplace-only、**结案** |
| W1 | fixed | cron trigger 无预测性 `nextFireAt` 字段——agent 只能推理下次触发时刻（仅有 `lastFiredAt` 审计字段）（用户选 option a：加该字段） | 可发现性（cron lane） | 域 `Trigger` 加 `NextFireAt`（读时派生非列）；`attachRuntime` 用新 `croninfra.NextAfter(expr,now)` 算 cron 下次触发；`create_trigger` 描述点明 get_trigger 返 nextFireAt；doc 同步 trigger.md | 零 token 单测 `TestNextAfter`（daily=次日 09:00、minutely、invalid 报错）绿；make verify 绿 | _pending_ |
| W2 | watch | `FinalizeMessage` DB 写失败仅 `Warn` 日志、留 message 行 `status=streaming` 到下次启动 `SweepNonTerminal` | 罕见健壮性（F12 理论残隙；skeptic isReal=false/sys=false） | — | — |
| W3 | fixed | `runQueue`(长生 goroutine)无 `defer/recover` 包 `processTask` → 一次 turn panic 崩**整进程**（与库内 trigger 监听/HTTP 中间件等其它长生 goroutine 的 recover 不一致）；panic 还留 message 卡 `streaming` | 健壮性（单进程本地 app 不应一 turn 崩全部） | `chat/runner.go` processTask 顶部加 defer/recover：带栈 Error 日志 + `failTurn` 把 message finalize 到 error（顺带覆 W2 的 panic-卡-streaming 子情形） | 零 token 单测 `TestProcessTask_PanicRecovered`（panicClient → 回合 finalize 到 error、message_stop 出、进程存活）绿；EndToEnd 不受影响；make verify 绿 | _pending_ |

| F24 | fixed | 每种 trigger 的运行时 fire payload 键（fsnotify `{firedAt,path,eventKind}`、cron `{firedAt}`、webhook `{firedAt,method,path,headers,body\|bodyRaw}`）是**隐形契约**——`create_trigger` 描述/trigger.md 都不列 → agent 猜键、`capability_check` 放行、**第一个真事件才崩**（如 `watch.event` no such key） | **系统性**（任何 trigger→下游需 payload 的工作流；round-2 fsnotify/webhook lane + skeptic 变体复现） | `create_trigger` 描述每 kind 加「Fire payload: {…}」；doc 同步 `trigger.md` dedup 表加 Fire Payload 列 | 重建后行为复验（见 F25/F26 簇）；make verify 绿 | _pending_ |
| F25 | fixed | fsnotify 交付的 `eventKind` 用 fsnotify 原始**大写** Op（CREATE/WRITE/REMOVE…），但 config 词汇是**小写** create/modify/delete（且 modify↔write、delete↔remove 别名）→ agent 按文档枚举写 CEL 过滤永不匹配 | 系统性（任何 fsnotify 事件过滤） | `fsnotify.go` fire 用新 `configEventKind(op)` 归一到配置词汇小写 token（组合事件 `\|` 连），交付端与 config 圆桌一致 | 零 token 单测 `TestConfigEventKind`（Create→create、Write→modify、Remove→delete、Create\|Write→`create\|modify`）绿；make verify 绿 | _pending_ |
| F26 | fixed | webhook 真实可调 URL `/api/v1/webhooks/{triggerId}/{path}` **不可发现**：任何工具描述/参数/result/DTO 都不露这前缀 → agent 把 `config.path` 当全路径、给用户一个 404 URL | 系统性（任何 webhook trigger） | `create_trigger` 描述点明「config.path 只是子路径、外部 POST 到 /api/v1/webhooks/{triggerId}/{config.path}、id=本次返回的」+ trigger.md 同记 | 同 F24 簇行为复验；make verify 绿 | _pending_ |

| F27 | fixed | durable `:replay`（清 failed 节点、留记忆化、按 run 原 pin 版本重走）后端实现完整且**正确**（round-2 replay lane 经直接 POST 验通：只清 failed 节点、保 trigger 记忆化、replayCount 0→1），但 **agent 无工具**调它——失败 run 无法从 agent 侧恢复 | 系统性（任何 agent 想重跑失败 run） | `tool/workflow/runs.go` 加 `replay_flowrun`(sched.Replay 同步→GetRunWithNodes 返更新后 run+nodes)；WorkflowTools 注册（14→15）；描述点明仅 failed 可重放 + 按**原 pin 版本**（失败后改实体不生效、要新 run）；doc 同步 workflow.md/scheduler-flowrun.md | ValidateInput 单测入表（replay no id→err、ok）绿；make verify 绿 | _pending_ |

| F28 | fixed | durable 循环引擎本身**正确**（per-iteration 记忆化、终止、MaxIterations(1000) 干净失败——round-2 loop lane 验通），但**自累加循环不可编排**：循环内前驱在首轮未跑、其 node-id 根在 CEL activation 缺省，cel-go 对表达式引用到的未绑已声明根硬报「no such attribute」**即便在 has() 内**→ `has(check.count)?check.count:start.count`（自然 loop init）也崩 → agent 被迫退化到 side-effecting stateful handler（破坏跨 run 隔离/并发） | **系统性 + 引擎**（任何跨轮累加/重试带态的循环；round-2 loop lane + skeptic 双复现 + cel-go 独立复现 + 验证修法） | `scheduler/walk.go scopeFor` 把每个无 completed result 的已声明图节点绑**空 map**（非缺省）→ `has(pred.f)` 干净为 false、loop init 可写；既有接线不变（只引用 completed 祖先）；opsDoc 加 **LOOP STATE** 段（has()-guard + control emit 携态）；doc 同步 scheduler-flowrun.md | 零 token 单测 `TestScopeFor_BindsAbsentNodesToEmptyMap`（缺省→{}、completed 保 result）+ `TestWalk_LoopWithBackEdge` 不回归；make verify 绿 | _pending_ |

| F29 | fixed | `buffer_one`/`buffer_all` 被接受 + opsDoc 当平等策略广告，但 `overlapDecision` 只实现 serial/skip → 二者静默 = allow_all（round-2 concurrency lane + skeptic 复现 4 run 全并发）。先做诚实 doc-mark；后**用户决策 B + 温柔取消**：对齐干净标准 5 策略 | **系统性 + 引擎特性**（标准重叠菜单：业界 = Temporal 6/K8s 3；我们差「打断式」+ serial≈buffer_all 冗余） | 删 `buffer_all`（=serial 冗余）、加 `replace`；实现 `buffer_one`（defer + `SupersedePendingOlderThan` 丢更早待处理、只留最新）+ `replace`（`cancelRunningForReplace` 标 cancelled+打断 advance、复用 KillWorkflow race-safe 序、不动 lifecycle）；CHECK + IsValidConcurrency + opsDoc + workflow.md/database.md/firing.go 同步 | 单测 `TestSupersedePendingOlderThan`(store) + `TestFiring_OverlapBufferOneDefers` + `TestFiring_OverlapReplace`（在途 run 被 cancel + firing 被消费）绿；make verify 绿 | _pending_ |

| F30 | fixed | trigger `outputs` 字段（声明下游可读 payload）是**自由手填、不校验、与真实 fire payload 对不上**——是 F24 根的设计层（用户选 option a：从 per-kind schema 自动推导） | 系统性（cron/webhook/fsnotify 三固定-payload kind） | 域加 `CanonicalOutputs(kind)`（= listener 真实 payload 字段）；Create/Edit 对 cron/webhook/fsnotify **盖上**规范 outputs（覆盖作者所填、永不漂移），sensor 保留作者自定义（config.output）；工具 outputs 参数描述点明「仅 sensor 需填、固定 kind 自动忽略所填」；doc 同步 trigger.md | 零 token 单测 `TestCreate_CanonicalOutputs`（cron 作者填 bogus→被盖成 `firedAt`；sensor 保留作者 `x`）绿；make verify 绿 | _pending_ |

| F31 | fixed | `create_agent` 的 tools 禁 `ag_` 但**不指路** → agent 白烧半轮建注定失败的 HTTP wrapper（再删）才找到 workflow 路径（EXPLORE round-1 agent-nesting lane） | 可发现性（agent 编排面） | `tool/agent/build.go` configProps tools 描述加「串联 agent 改用 workflow agent 节点」+ domains/agent.md 同步 | 零 token 守卫 `TestConfigProps_AgentChainRedirect`；make verify 绿 | _pending_ |
| F32 | fixed | schema-less callable（自由文本 agent、返回非对象的 fn/hd/mcp）节点结果静默键 `.text`，opsDoc NODE RESULT SHAPES 只讲"结构化字段"不提此 → 编排期不可知、`capability_check` 还 ok:true、第一次跑必崩（agent 靠失败 run 反推 key） | **系统性 + 跨轴**（所有节点 kind 的 schema-less 输出；F16 NODE RESULT SHAPES 的缺口；`toResultMap` dispatch.go:178 真相） | `tool/workflow/workflow.go` opsDoc：agent 行分 declared/free-form、加通用 **SCHEMA-LESS `.text`** 行（覆盖 fn/hd/mcp/agent）+ 点明 capability_check 看不见此键 | 零 token 守卫 `TestOpsDoc_SchemaLessTextConvention`；make verify 绿 | _pending_ |

| F33 | fixed | **= F12 确认+定根**：重多段首轮 → deepseek 流持 200 后只滴 SSE keep-alive 注释行（不发 token）→ `scanSSELines` 跳过注释行不调 fn（ctx 仅在 fn 内查）→ idle-timer(150s) 的 cancel 永不被观测、`ParseStream` 死循环、不报 EventError → message 永卡 `streaming`（11–20min、违反 runner no-streaming-orphan 不变式）。EXPLORE round-1 toolpick lane 2/2 复现 | **系统性 + 跨轴**（共享 `scanSSELines` 喂 ~11 provider；anthropic 自己的 parser 早已在扫描循环查 ctx——它是对的、scanSSELines 是漏的那个） | `infra/llm/transport.go` `scanSSELines` 加 ctx 参数 + 扫描循环顶 `ctx.Err()` 检查（即便流只滴非-data 行也能被 cancel 打断）；~11 provider caller + 2 test 传 ctx | 零 token 回归 `TestScanSSELines_CtxCancelBreaksKeepAliveDribble`（无尽 keep-alive reader + 已取消 ctx → 必 2s 内返 ctx.Err()，旧码死循环超时）；make verify 绿 | _pending_ |

| F34 | fixed | LLM 流以 stopReason=error 收尾但 `streamErr` 空 → message finalize 成 `error` 却 `error=null`（70 block 真活后无因、无恢复提示，用户只见"切断"）。round-2 wasted lane | chat loop（所有 provider 流错路径） | `app/loop/loop.go` stopReason=error 且 errMsg 空时补非空可操作因（"stream ended unexpectedly… 已保存、retry 续"） | make verify 绿；行为复验待（与 F33 同族、补 finalize 诚实性） | _pending_ |
| F35 | fixed | `capability_check_workflow` 对"节点 input CEL 引用上游某分支不发的 key"的图照样报 ok:true/structurallyValid → 运行时 fail-fast 崩整 run，agent 只能靠跑才发现（绿检查超额承诺）。round-2 wasted lane 复现（validate 仅 valid 分支发 order、invalid 分支崩 `no such key: order`） | **系统性**（静态校验器查结构/ref/port、**不查 dataflow**；= F32 的运行时面） | `tool/workflow/capability.go` 描述 + opsDoc 显式声明「不校 node-input dataflow、引用字段须在每条可达分支都存在、缺 key fail-fast 整 run、绿检查仍要 trigger 一次确认」 | make verify 绿 | _pending_ |
| F36 | fixed | `POST /<entity>:iterate` 对不存在/已删 id 返 **202 + 真 conversationId**（非 404）→ 起一个前提缺失的 AI-edit 对话（mention 降级 `(unavailable)`）。`aispawn.Iterate` 不校实体存在（`Triage` 经 renderer 校——不对称）。round-2 iterate lane | ai-ops（:iterate 全实体共享路径） | aispawn 加 `MentionValidator` 端口（chatapp `ValidateMention` 满足、bootstrap setter 接）、Iterate spawn 前校解析→传实体 NOT_FOUND(404)，镜像 Triage 的 renderer eager 校验 | 零 token 回归 `TestIterate_ValidatesTargetExists`（坏 id→not-found 且不 spawn、好 id→spawn）；make verify 绿 | _pending_ |
| F37 | confirmed | agent 无法按 id/name 发现或读取已上传的 attachment——只有发消息时 `attachmentIds` 喂进 LLM 的路径（那条工作、绿）。'读我传的 att_xxx' 无解。round-2 attach lane | 能力缺口（attachment 无 agent 读路径） | 待修：加 list_attachments + read_attachment toolset（复用 `attachmentapp.ToContentParts`、像 document/function 那样作 catalog source） | — | 排期（feature） |
| F38 | confirmed | agent 无会话管理工具（archive/compact）→ 用户问"归档/压缩本会话"时，agent **编造不存在的 UI 按钮**（"Compact/Summarize button"）或甩给 UI；compaction 实为自动（无手动路由），agent 不知 | promise≠reality/能力缺口（conversation 管理面） | 待修：暴露小会话管理工具（archive/pin 当前会话）+ 让 agent 知 compaction 自动（prompt/工具描述），别编 UI | — | 排期 |
| F39 | confirmed | todo 全完成后 reminder 被抑制（`render.go` open==0→return false）且无 todo_read → 用户"列出我的 todo"时 agent **凭记忆编造**（9 行 vs 真 7 行）。round-2 todo lane（写/进度/整写全绿，仅此读回缺口） | 假成功/能力缺口（todo 读回） | 待修：open==0 仍注入紧凑"已完成 N 项"摘要，或加轻量 todo_read | — | 排期（低） |

| F40 | confirmed | declared outputs 是 advisory：callable 声明 `outputs:[{name:X}]` 但**返回标量**时 `toResultMap`(dispatch.go:178) 仍包进 `.text`、忽略声明名 → 下游 `node.X` 崩、声明输出契约静默 no-op（4 失败 run + 4 edit 才接好本已工作的实体图）。**= F32 运行时深层面**。round-3 ergonomics/datapass/options 多 lane 复现 | **系统性·HIGH**（fn/hd/agent 全受；dispatch 核心） | 待修(careful)：toResultMap 接 declared outputs，恰 1 声明+标量→用声明名（非 .text），多/零声明+标量→.text（保 schema-less）；agent invoke 对声明结构输出但返标量也 coerce/loud-fail。穿 declared outputs 进 3 dispatch 站点 + 单测 + 行为复验 | — | **wind-down careful 修** |
| F41 | confirmed | concurrency=skip 对**阻塞节点**工作流静默退化成 serial（无 firing 被丢）——overlap 信号需贯穿整 run，但 Advance 同步、信号随之蒸发。round-3 concurrency lane（90s sleep 节点、skip 从不 skip） | **系统性·HIGH·引擎**（同步 Advance 阻塞 overlap 探测） | 待调查：scheduler 是 per-run goroutine 还是阻塞？overlap 需贯穿-run 在飞信号 | — | **wind-down 调查+修** |
| F42 | fixed | `edit_workflow` 的 set_meta **静默吞无效 concurrency 值**（无错、版本升、值不变）——Create 校验、Edit 不校（不对称）→ agent 以为设了策略其实没。round-3 options lane | workflow(Create/Edit 不对称) | `app/workflow/crud.go` Edit 的 concurrency apply 无效即 `ErrInvalidOps`（镜像 Create） | 零 token 回归 `TestEdit_RejectsInvalidConcurrency`；make verify 绿 | _pending_ |
| F43 | not-bug | 「多次 edit 后工作流静默 inactive」——**查实非产品缺陷**：create 起即 inactive（须显式 `:activate`），该 workflow 从未被激活；Edit **确实**保留 lifecycle + 对 active workflow 重绑 trigger（crud.go:67-77「活 workflow 编辑改了入口 trigger ref 必立刻重绑」）。是 **agent 没 :activate 却在摘要暗示 live** 的模型误读、产品正确 | — | — | — | 反自欺：读码纠 lane 过判（同 F9/F17） |
| F44 | confirmed | 构建中 agent turn 以 **0 block + null error** 报错 → 半建实体成**孤儿**留库（= F34 兄弟 + 孤儿面） | 恢复无门（turn 错持久化/回滚） | 待修：错的 turn 持久化部分 block + 非空 error；turn 内建实体被 surface 或回滚 | — | 排期 |
| F45 | confirmed | 无**工作区级** dangling-ref/health 审计工具——只有 per-workflow capability_check + per-entity get_relations，无扫 agent tool refs 的 | 能力缺口（无全局健康检查） | 待修：加 `get_broken_references` 读工具扫所有 workflow 图 + agent mounts 找指向 soft-deleted/无 active 版本的 ref | — | 排期(feature) |
| F46 | confirmed | agent 无工具读回本会话的 **subagent 子树**——告用户 trace"不可得"但平台全持久化（E3 嵌套子消息真存） | 不可发现（subagent trace 读回） | 待修：加 `get_subagent_trace`(子会话) 读工具 | — | 排期(feature) |
| F47 | confirmed | agent 无工具**决策 parked approval**——能建+触发带 approval 节点工作流，却无法驱动 park 的 run 到完成（REST 有 :decide、无 tool 包） | 能力缺口（可能 human-in-loop 故意，但挡住 agent 自测 approval 接线） | 待判/待修：加 decide_approval tool，或确认故意 + 文档说明 | — | 排期(待判) |
| F48 | confirmed | 实体 delete 无条件（live 引用也不挡），`purgeRelations` 同 op 删依赖边 → 删后 breakage 经 get_relations 反更难查 | 静默降级·low（本地优先"信任用户"边界） | 待修：至少 delete 工具结果 surface dependent count | — | 排期(low) |

| F49 | fixed | CEL 自然算术混类型（payload 数=double + 整字面=int）**每次 run 崩裸 `no such overload`**、capability_check 还放行 → agent 烧 4 版本 + 5 失败 run 猜（round-4 malformed lane） | **系统性·HIGH**（DynType 根 + cel-go 无跨类型算术重载；每条节点 input/条件 eval 收口） | `pkg/cel/cel.go` Eval/EvalBool 对 `no such overload` 附可操作提示（cast `int()/double()` 或写 `5.0`） | 零 token 回归 `TestEval_NoSuchOverloadHint`（`start.n+5` 带提示、`int(start.n)+5`→7）；make verify 绿 | _pending_ |
| F50 | fixed | 空 function name 报 `FUNCTION_INVALID_CODE`（"code invalid"）→ 误导用户去查（合法的）代码——`apply.go:62` 把所有 validateFinal 失败统包 ErrInvalidCode | promise≠reality/选错工具·low | `validate.go` validateFinal 各错带对的 sentinel（name→`ErrInvalidName`、code→`ErrInvalidCode`）+ `apply.go` 传它的 sentinel 不再统包 | 零 token 回归 `TestCreate_EmptyName_IsNameError`（空名→ErrInvalidName）；既有 `TestCreate_InvalidCode` 不回归；make verify 绿 | _pending_ |
| F51 | confirmed | workflow capability_check 校 mcp **SERVER** 存在但不校 **TOOL** 存在（`entityIDOf` 砍到 server）→ 坏 tool 名滑到运行时；与 handler-method 校验不对称 | 不可发现/promise≠reality·medium | 待修：CapabilityCheck 对 mcp action ref 也验 `/tool` 在 server ToolDefs（镜像 capability.go:88 handler-method 校验） | — | 排期 |
| F52 | confirmed | MCP server 工具从 **chat 席完全不可调**——`DynamicTools` 是死代码、从未接进 chat toolset（mcp 工具只经 agent/workflow 挂载用，F22 绿） | 不可发现/能力缺口·HIGH（但「chat 是否该直调 mcp」= 设计判断） | 待判/待修：接 per-workspace `DynamicTools` 进 chat host，或确认只走挂载 + 删死代码 | — | 排期(待判) |
| F53 | not-bug | 上游 MCP tool 错误以普通 content 返回（isError:false）记 status=ok——**忠于 MCP spec**，不强制按内容启发判错；可选：mcp_calls 行 surface isError 标志 | — | — | — | 忠于协议、非 bug；可选增强 |

| F54 | fixed | `noBackendMessage`（无搜索后端时的引导）广告一个 keyless `duckduckgo-search` MCP 作逃生口，但 live registry（97 server）无它、只有需 key 的 Tavily；install 仅市场 → agent 追这死路烧 ~2 轮。round-5 websearch lane | promise≠reality/能力缺口（产品自带引导对不上市场） | `tool/web/search.go` noBackendMessage 改为指真可装的（Tavily+需 key）+ 点明纯 URL 用 keyless WebFetch | 零 token 守卫 `TestNoBackendMessage_NoPhantomKeylessMCP`（不名 duckduckgo、不claim keyless、提 WebFetch）；make verify 绿 | _pending_ |
| F55 | confirmed | compaction **trigger 用真 billed InputTokens**(978K>阈值触发)、但 demote 后 **gate 用 persisted block bytes** 量 → tool-schema 撑大的 context spike 触发 compaction 后**静默 no-op**（不真压）。round-5 compaction lane | 静默降级/白烧·medium（contextmgr trigger/gate 量纲不一致） | 待修：gate 量与 trigger 同口径（真 billed InputTokens / 含 offered tool-schema 的估计） | — | 排期 |
| F56 | confirmed | `create_skill` 对 `search_tools` 不可见 → 模型调它前取不到 schema（edit_skill 可见、不对称）。round-5 skillauth lane | 不可发现/组合摩擦·medium | 待修：create_skill 同 edit_skill 一样进 search/lazy-discovery 源 | — | 排期 |
| F57 | confirmed | skill 的 **allowed-tools preauth 挂 agent 时不生效**——只 inline `activate_skill` 授；挂 agent 只注 Guide、不授预授权 → 用户以为 skill 的工具权限随挂载生效、实没。round-5 skillauth lane | promise≠reality/组合摩擦·medium | 待判/待修：compose 两面，或 create_agent 挂带 allowed-tools 的 skill 时 surface「preauth 不随挂载生效」 | — | 排期(待判) |
| F58 | confirmed | compaction 严格 turn 边界（runner tail）——单个长 ReAct turn 可 intra-loop 涨到离硬 context 窗 ~2%，loop 内只有 maxSteps 无 token/window 守卫。round-5 compaction lane | 脆弱/白烧·low | 待修：loop 内软守卫（last step input>0.9*窗 → demote hot tool_results / 缩 offered tool 集 / 停） | — | 排期(low) |
| F59 | not-bug | agent 永不能被授予 skill allowed-tools 引用的 built-in orchestrator 工具（call_handler/get_relations）——agent 只挂 fn_/hd_/mcp、无 orchestrator 内建 | — | — | — | 设计边界（agent 显式挂载、非 chat orchestrator 内建），likely intended；产品可加「skill 引用的内建工具对 agent 无效」信号 |

## 元注（一次性，非 finding）
- **round-2 子系统全验证通过 + 关键修法行为复验**（2026-06-18，真 deepseek + ground-truth）：① **fsnotify/webhook/cron 触发、durable replay、结构化循环、concurrency(serial/skip)、skill allowed-tools preauth、agent knowledge** 八条 lane 全端到端验证可转（durable 引擎内部全对，bug 全在隐形契约/未实现广告，已系统性修——F24~F29）。② **F28 累加循环行为复验**：agent 读新 opsDoc LOOP STATE 指南、自写 `has(inc.count)?inc.count:init.count`、flowrun count 0→1→2→3 逐迭代记忆化 + done 终止（前必崩 cel-go "no such attribute"）。③ **F22 真 MCP server 运行时复验**：经 HTTP PutServer 连真 `@modelcontextprotocol/server-everything`（status=ready），agent 用 **name 形** `mcp:everything/echo` 接 workflow、capability + dispatch 全通、flowrun completed、echo 节点产 `{text:"Echo: hello F22"}`（前 name 形必 ErrRefNotFound/MCP_SERVER_NOT_FOUND）——闭合 F22 唯一运行时验证缺口。
- **为什么这 loop 值得**：F1 那条轨迹 `golden J5` 只断言"版本>1"是绿的；轨迹判官却抓到模型把 `get_function` 调错绕一圈——终态测试瞎、判官看见。
- **workflow + durable 子系统验证通过**（2026-06-18）：F7+F8 修后，agent 建成 workflow（trigger→convert→classify）、`trigger_workflow` 跑通；durable 引擎逐节点记忆化、结果正确（celsius=100 → convert `{fahrenheit:212}` → classify `{label:"hot"}`，三节点 completed）。"整套工程"在此方向确认能转。
- **handler 子系统验证通过**（2026-06-18）：agent 建 counter handler、add(5)→5、add(3)→**8**（resident 进程跨调用保态）、get()→8。有状态常驻"灵魂"正常。
- **control 路由子系统验证通过**（2026-06-18，F13 修后）：agent 建 trigger→control→{hot_label,normal_label} 工作流，durable 引擎 first-true-wins 路由：35→`__port:hot`→"hot warning"、10→`__port:normal`→"normal"（两条 flowrun 各节点 result ground-truth 验）。control 分支结构化路由 + emit 透传在此确认。
- **trigger sensor 自主触发子系统验证通过**（2026-06-18）：agent 一次 0-error 建 handler(level)+sensor(每 5s 探 `read_level`、condition `payload.value>100`、output `{level:payload.value}`)+alert workflow+activate。set level=150 后 sensor 每 5s **自主 fire→spawn flowrun→alert** 节点产 `{alert:true,level:150,message:"Level 150 exceeded threshold of 100"}`（flowrun 节点 result ground-truth 验）。**带着思考纠偏**：firing 记录全 `status:started`、`matched/firedAt` 为 null，初看似「卡住未 finalize」——读 `domain/trigger/firing.go` 知 `started`=「claimed+flowrun created」**即终态-ok**（单 status 枚举、本就无 matched/firedAt 列，jq 取空字段才显 null）→ **非 bug**，自主 fire 全链路正常。差点把非 bug 当 bug，读码纠偏（反自欺铁律）。
- **多轮迭代编辑子系统验证通过**（2026-06-18）：4 轮对话 agent 建 fn v1→改加 middleName/改格式 v2（`edit_function` 改同实体非新建、改前 `get_function` 读态、改后 run 测）→ 边界（无 middle 不留尾空格/句点）正确 → 经一次 deepseek 流瞬断后 turn-4 优雅恢复、正确报 v2。多轮上下文承接 + edit 路径（F6 域）在多轮压力下稳。
- **approval park/resume 子系统验证通过**（2026-06-18，F15/F16 修后）：agent 建 trigger→control→approval→action 退款工作流，trigger(250)→control 路由 `over_100`→approval 节点 **park**（run 续 running）→ 经 `POST /flowruns/{id}/approvals/{node}:decide {decision:yes}` 决策→ run **resume**→ 下游 action 读 `start.amount`(250)+`approval_node.reason`→ 产 "Refund of \$250.00: ✅ Approved by manager"→ run **completed**（全节点 result ground-truth 验）。durable 人在环 park/resume/decide 全链路在此确认。
- **harness 修**：`testend/loop/setup.sh` 固定 workspace 名 "loop" → 重跑 `WORKSPACE_NAME_CONFLICT` → `ws=null` 卡住 loop 自身；改唯一名（`loop-时间戳-RANDOM`）。属"harness 工程"维度。
- **F29 并发实现经多 agent 对抗审查 + 修**（2026-06-18）：5 维对抗审查（race / durable / cancel 边界 / crash / 语义）判 race + durable 健全，抓到 **2 major + 1 minor 真问题、全修**：① **buffer_one 在 `running==0` 短路里漏 supersede** → 旧待处理 firing 仍执行（违「只留最新」）→ 改 `SupersedeAllButNewestPending`（收敛到最新、与 running 无关、order-independent）+ 回归 `TestFiring_OverlapBufferOneRunsNewestOnly`（2 待处理无 run → 只跑 1）；② api.md 漏同步（仍写 buffer_all、缺 replace）→ 补；③ replace 取消 parked run 留死审批收件箱项 → `CancelParkedNodes` 在 replace+kill 收掉（+ `TestCancelParkedNodes`）。**审查值**：真抓到一个 correctness bug——终态测试本会绿、对抗审查看见。
- 永久回归 test：`selfiter_confirm_f1_*`、`_f1batch_*`（F1）· `Test{Function,Handler}_EditPersistsMeta`（F6）· `TestLLMErrText`（F7）· `TestWorkflow_InvalidCELListsAvailableNodes`（F8）。
