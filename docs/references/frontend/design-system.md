---
id: DOC-045
type: reference
status: active
owner: @weilin
created: 2026-06-22
reviewed: 2026-06-22
review-due: 2026-09-22
audience: [human, ai]
---

# 前端设计系统 + UI 套件（An*）

> demo 的视觉语言（`demo/core/tokens.css` + `core/primitives/*`）以 app 级严谨度移植到 Flutter:
> **设计令牌**（值源）+ **An\* 组件套件**（原语），让 features 纯组装、零 bespoke 样式。
> 本篇随套件 **逐组同提交** 填充（见 §4 路线）。架构/分层见 [`architecture.md`](architecture.md)；决策见 [`ADR 0004`](../../decisions/0004-frontend-flutter-architecture.md)。

## 1. 设计令牌（`core/design`，唯一值源，禁内联 px/hex/ms）

- `tokens.dart` —— 主题无关几何/时间:`AnSpace`(4 网格间距)· `AnRadius`(tag/button/chip/card/island/pill)· `AnSize`(行高 32、控件 28、图标 16/12/20、光标 1.5×高 16、就地编辑最小宽 32、就地编辑框内距 editBoxPadX 8/editBoxPadY 4(纵向布局补偿不加行高)、状态图标 40、骨架行 12、步进当前点 18、标签×命中 18、三岛列宽、窗体外廓)· `AnMotion`(fast 120 / mid 240 / slow 340 / breath 1800ms + 打字机 typePerChar 55/deletePerChar 28/typeHold 1400/typeGap 400 + easeOut/spring 缓动)。
- **`AnMotionPref`**(`tokens.dart`,挨着 `AnMotion`)——无障碍动效门控单源:`reduced(c)`/`reducedOrAssistive(c)`(`MediaQuery` aspect 访问器,只在标志翻转 rebuild)。**每个动画 An\* 件 build() 里读它**;装饰循环(shimmer/光标/打字机/breath)门控 `reducedOrAssistive`(屏幕阅读器活跃时持续动效是噪声)。见 §2 动效纪律。
- `colors.dart` —— `AnColors` ThemeExtension(明暗双值 + lerp,糖 `context.colors`)。**中性 chrome + toB 蓝 accent + 功能色**:`accent`=蓝(demo `#0071e3`/暗 `#0a84ff`)——主动作/选中/聚焦/run 状态显蓝;`ok`/`warn`/`danger`(+ soft)语义色 + `skeletonBase`/`skeletonHighlight`(骨架哑底+扫光,单色)。值镜像 demo `tokens.css`。
- `typography.dart` —— `AnText`,模数字阶锚 13px 正文。**UI=随包 MiSans VF**(`assets/fonts/MiSansVF.ttf`,变量字体 wght 150–700,Latin+简中,全平台同字面)+ PingFang SC 兜底;**渲染压细**——正文/标签/次级 Light(w300)、强调 Regular(w400)、标题 Medium(w500),摆脱 MiSans 在 Regular 下的厚重(ExtraLight 200 部分字偏细)。**每个样式同时给 `fontWeight` + 显式 `fontVariations('wght')`**——否则 `TextField`/`EditableText` 不渲染对的 VF 字重,就地编辑框会比展示文字更粗更宽。**重定权一律走 `AnText.<style>.weight(FontWeight)` 扩展**(双轴同改:`fontWeight` + `wght` `FontVariation`)——base 样式已钉 `wght` 轴,直接 `copyWith(fontWeight:)` 会被轴覆盖、渲染原重(VF 上轴优先于 weight;G3.3 立、AnGroupLabel 同修)。**代码=JetBrains Mono**(随包,OFL)。
- `theme.dart` —— 装配 `ThemeData`,注册 `AnColors` 扩展,剥 Material 涟漪 + **hover/highlight/focus/splash 全置透明**(表面自管态,杜绝 Material 默认灰叠加)。

## 2. 命名 + 纪律

- 组件类 `An<Name>`(`core/ui/`);文件 `an_<name>.dart`。纯框架无关模型在 `core/model/`(无 Flutter import)。
- **颜色/度量只走 token**:widget 内禁裸 hex/rgb/`Color(0x…)`/px(只有 `core/design` 可声明色值)——`make verify` 加 grep 门禁机械兜底（套件落地时接）。
- **悬停/选中底色淡入铁律**:`AnimatedContainer` 静止底用**目标色 alpha=0**——统一走 `Color.whenActive(active)` 扩展(`core/design/colors.dart`),绝不用 `Color(0x00000000)`(transparent black,lerp 到浅色经暗中点=暗闪)。
- **共享判定/令牌(去重单源)**:`Set<WidgetState>.isActive`(hover/press/focus 任一,`an_interactive.dart`)各控件统一取「视觉激活」;`AnOpacity.disabled` 统一禁用变暗;占满宽统一名 `block`(AnButton/AnInput/AnDropdown/AnActionGroup)。
- **交互基座 = `FocusableActionDetector`**(`AnInteractive`,原则 #8):hover/focus 由平台高亮模式驱动(焦点环只在键盘聚焦显)、Enter/Space 走标准 `ActivateIntent`、禁用即不可聚焦、`Semantics(button)` 无障碍。下拉菜单据此 + `FocusScope`/`FocusTraversalGroup` + autofocus 选中行得键盘导航(方向键 + Enter)。
- **文案只走 i18n**:严禁硬编码中英文,走 slang `context.t.<key>`(见 §3)。
- **动效 + 生命周期纪律(动画件铁律,源自 G2 调研 [`WRK-037`](../../archive/g2-feedback-states/README.md)):** ① **每个纯动效都附"成品感"静态兜底**(WCAG 2.2.2/2.3.3)——`AnMotionPref` 门控,reduced 时**不启动**循环(`.repeat()` 默认不被 `disableAnimations` 缩短)并渲染**收尾完成的静止帧**(Skeleton 静态哑底/Typewriter 完整主句/Stepper 瞬切/StatusDot 实心点);② **急切初始化**——controller/ticker/timer 绝不放 `late final =` 字段初始化器(惰性首次读可在 teardown 触发→崩),在 `initState` 赋值;③ **永远 dispose + mounted 守卫**;自重排 Timer 持有于字段、dispose 取消;④ **离屏即静默**——循环视觉走 vsync 控制器(TickerMode 离屏自停),Timer 仅用于离散相位切换;⑤ **单 ticker 默认**(`SingleTickerProviderStateMixin`);⑥ **作用域 rebuild**——`AnimatedBuilder(animation,builder,child)` + 循环叶子裹 `RepaintBoundary`;⑦ **异步状态→`Semantics(liveRegion)`(永远 polite),warn/danger 急迫另发 `SemanticsService.sendAnnouncement(assertive)`**;装饰动效 `ExcludeSemantics`;⑧ **状态不靠色单独**(图标+文字+朗读词,WCAG 1.4.1)。matrix 门禁加 **reduced 轴**(每 specimen 在 `disableAnimations` 下须 `pumpAndSettle` 收敛——忘门控的循环卡死=FAIL)。
- **reduced 门控选择(G3 起明确,源 [`WRK-038`](../../archive/g3-rows-cards/README.md) §7):动效分三类、门控不同。** ① **循环/装饰动效**(shimmer / breath / typewriter / 光标闪烁)走 `reducedOrAssistive`(屏读活跃时持续动效是噪声);② **功能性一次性 reveal**(AnRowDetail 高度揭示、AnRow icon↔chevron 互换+旋转)走 `reduced`(真时 `Duration.zero` 即时到位、状态一致);③ **功能性微反馈/微过渡**(focus 边 / hover 提墨 / press / AnDropdown caret 旋转 / 就地编辑 idle↔editing swap)本就一次性 settle、天然过 matrix reduced 轴,**可不显式门控**(若用 `Animated*` 则 reduced 时取 `Duration.zero`)。**铁律:绝不用 `reducedOrAssistive` 门控功能性反馈 / reveal**(会剥夺屏读用户的 hover/focus/展开线索)——`reducedOrAssistive` 只留给装饰循环。
- 注释 S11 双语 Why-not-What。

## 3. G0 地基设施（已落）

套件开建前的共享基座——所有 An\* 组件都依赖它:

| 设施 | 位置 | 职责 |
|---|---|---|
| **AnIcons** | `core/ui/icons.dart` | 语义图标单源:领域键 → Lucide 字形(`lucide_icons_flutter`)。渲染**细字重族 `Lucide300`**(≈demo 1.7 笔画,默认 `Lucide` 偏粗;包内各字重共享码点,改一处 `_family` 即换粗细)。具名字段(`AnIcons.agent`…)+ 数据驱动 `byKey`/`toolIcon`/`node`;未知 → `fallback`。移植 `icons.js` + `entity-kinds.js`。 |
| **AnBrandIcon** | `core/ui/an_brand_icon.dart` | 品牌/项目图标三源:`.anselm`(随包 app 标 SVG,细边描 squircle 轮廓)· `.svg`(内联 logo 串,currentColor→ink)· `.glyph`(字母圆角底兜底);`size` sm/md/lg + `managed`(accent 底)/`elevated`(浮起)。`flutter_svg` 渲染 `assets/brand/anselm-icon.svg`。 |
| **AnStatus / AnTone** | `core/model/status_state.dart` | 状态折叠单源(纯 Dart):后端任意状态字串 → 5 通用态(idle/run/wait/err/done)+ 语义 `tone`(err→danger/wait→warn/done→ok/run→accent/idle→none)。徽章/点不再各写 if 链。移植 `state-model.js`。 |
| **AnInteractive** | `core/ui/an_interactive.dart` | 交互基座:hover/focus/pressed/disabled 统一态(`Set<WidgetState>` 喂 builder),指针 + 键盘(Enter/Space)激活;**禁用时不可聚焦、指针/按键都不激活**(对齐 demo disabled-passthrough 门)。取代手搓 MouseRegion。 |
| **i18n（slang）** | `lib/i18n/` | 类型安全 `context.t.<key>`,`en`(base)+ `zh_CN` 双语;`TranslationProvider` 裹 app 根、`LocaleSettings.useDeviceLocaleSync()` 选语言。生成 `strings.g.dart` 经 `dart run slang`(**入库**,不走 build_runner)。 |

## 4. UI 套件路线（G0–G2 已落，G3–G6 待建，逐组同提交填本表）

| 组 | 组件 | 状态 |
|---|---|---|
| G0 地基设施 | AnIcons · AnBrandIcon · AnStatus/AnTone · AnInteractive · i18n（+ **AnMotionPref** 动效门控） | ✅ 已落 |
| G1 基础控件 | StatusDot · Badge(+Tone 色) · GroupLabel · Button · Input · ActionGroup · EditAffordance · **InlineEdit** · Dropdown(+ **AnPopover** 浮层基座) | ✅ 已落 |
| G2 反馈态 | Callout · State · Stepper · Skeleton · Typewriter · Tags（+ **DryIntrinsicWidth** 共享原语） | ✅ 已落 |
| G3 行与卡 | Row · RowDetail · Card · InfoCard · Section · Field · Kv · ThinTable · RefPill（+ AnTwoZone 右锚两区 · AnLeadValue 动态键值行 · AnAutoGrid 响应式块网格 · AnEditableValue 双锚编辑核 · AnSeamlessField 编辑字段叶子 共享原语） | ✅ 已落（9 件 + 5 原语,G3.1–G3.10 + 对齐修订） |
| G5 代码与数据 | CodeEditor · JsonTree · VersionDiff（+ **SyntaxColors** 语法调色板 · **`highlightCode`** 唯一同步 tokenizer · **`lineDiff`** LCS · **AnCodeSurface** 共享代码框 地基；建造规范 [`WRK-040`](../../archive/g5-code-data/README.md)；用户拍板 5 决策=移植 demo 正则/LCS·CEL 上色·单字段 diff·JSON-pointer） | ✅ 已落（G5.0 地基 + G5.1–G5.3 三件 + AnCodeSurface,逐件对抗复审硬化） |
| G4 导航与壳 | Toolbar · Inspector(右岛壳) · Page · Menu · Tabs · SidebarList · OceanHeader · Shell 扩展（+ **AnScrollBehavior** 局部隐滚动条 · **AnMenu** on AnPopover 共享原语；建造规范 [`WRK-039`](../../archive/g4-nav-shell/README.md)；AnWireList 推迟到 graph-editor 批） | ✅ 已落（7 件 + 地基 + 2 原语 + 复审硬化,G4.0–G4.11；左岛 collapse / 浮动海洋头 / 键盘树 roving 推迟·真机验） |
| G6 浮层 | Dialog · Toast（+ **AnOverlayController/Host** 命令式派发[NotifierProvider] · **`shadowWin`**/**`toastMaxWidth`** token 地基；建造规范 [`WRK-041`](../../archive/g6-overlays/README.md)；用户拍板 2[toast 盖 dialog / 软上限 5] + 自决 3[NotifierProvider / action 走 AnInteractive / 留痕 architecture.md]；**Menu 已在 G4**、**AnFloating≡AnPopover 不重做**） | ✅ 已落（AnDialog + AnToast + Overlay host + 命令式 API,对抗复审 + 3.41.9 SDK 实证纠正） |

**G1 要点**:Button/Dropdown/EditAffordance 等可交互件都搭在 `AnInteractive` 上(态/激活/禁用一致);`AnInput` 用 `TextField`,**需 Material 祖先**(app 壳与 gallery 都提供)。`AnDropdown` 未做"桩"——已用 **AnPopover**(Flutter `OverlayPortal` + **`CustomSingleChildLayout` / `SingleChildLayoutDelegate`**,点外/Esc/祖先滚动 关)落地真菜单;**定位用 delegate 而非 `CompositedTransformFollower`**——delegate 先量子件再据子件+屏幕尺寸定位,故能**翻转**(优先 `alignEnd` 尾对齐、越界翻到首对齐)+ **夹取**(按 `pad`=s8 夹入屏内、下方放不下翻上方),纯仿射 follower 做不到(G4 用户验证修订;同 Flutter 自身 Dropdown/Tooltip delegate,#8)。此基座原计划在 G6,因 G3 的 Field/Kv 也需提早到 G1(G6 的 Menu/Dialog/Toast 复用它)。`AnTone→色`映射在 `core/ui/tone.dart`。`block`/`full`(占满宽)需有界父——无界父下优雅退化(不崩),见各件 LayoutBuilder。**AnDropdown 两区**(**`AnTwoZone`**,触发器与菜单行共用;G3.1 已从 private `_TwoZone` 升格为 `core/ui/an_two_zone.dart` 公共共享原语——G3 的 Section·InfoCard head·Row 尾槽都复用此骨架、不再各搓 Row+Spacer,原则 #8;**AnKv/AnField/AnEditableValue 不用它、改用 `AnLeadValue`**:键值行要 value 能缩 + 吃尽余量贴右(动态),而 AnTwoZone 的 trailing 是内容宽、不缩,几何不同):label 占满左 + meta 上限右(≤45%)+ trailing(任意 Widget,如箭头/勾/动作)钉右,两者各自省略。**菜单宽 = 触发器宽夹 `[menuMin 200, menuMax 360]`**(紧凑 ghost 触发器也容富行、不溢出);开/关有 **fade + 自顶微缩放**动效(`AnPopover`,`AnMotion.fast`)。**`AnPopover` 的 `AnimationController` 在 initState 急切创建**(非懒 `late final =`,否则没开过的浮层在 dispose 才首次访问→崩)。

**`AnInlineEdit` 就地重命名**(组合 `AnEditAffordance` + seamless `AnInput`,定高行、切换不跳):idle 文字 + 铅笔跟字尾(超长省略 + 铅笔钉右);editing 换自适应框,**随打字增长、撑满行宽后按钮(取消/保存)钉右、框横滚**(光标可见)。Enter 存、Esc 弃、进编辑全选。增长封顶用**框架原生 `IntrinsicWidth`**(经共享 `DryIntrinsicWidth`(`core/ui/dry_intrinsic_width.dart`)垫片绕开 TextField 在嵌套/滚动上下文的 dry-layout 断言)按**输入框自身渲染树**定宽——所见即所得,**非**逐键 `TextPainter` 量宽(后者须逐字节重镜像 `AnText.body`,即 typography 记过的变量字体宽度漂移坑);本地化按钮宽经 Row 的 `Flexible` flex pass **自动让位**(无须手算 `−按钮宽`)。光标高 `caretHeight=16`(< 行高 18.2、贴合文字;kit 内**单行** `AnInput` 共用,多行用 Flutter 默认随行缩放);空框 `inlineEditMin=32` 兜底可点;尾部留 `caretEndPad=3`(光标宽+一丝)防行尾末字符被光标压住(flutter#24612);rename 不给 placeholder 避 hint 宽污染(flutter#93337);`startEditing` 进编辑即全选(Finder/F2 习惯)。`DryIntrinsicWidth` 同被 `AnTags` 内联添加框复用。

**G2 反馈态要点**(全 HAND-ROLL、**零新增包**;动画件遵 §2 动效纪律 + `AnMotionPref` 门控):**AnCallout** 通栏语气条(severity→AnTone:info→accent;两机制 a11y:polite liveRegion + warn/danger assertive announce;Stateful 仅为 announce)· **AnState** 空/载/错整块(error **单色**、红留 Callout;loading 仅短等待用 `CircularProgressIndicator.adaptive`、内容加载委托 AnSkeleton;一个 Semantics 读「标题. 提示」)· **AnStepper** 步进点(done/current/upcoming 三态各异、不靠色;1-based;**带 `onStepTap`**——已完成节点 AnInteractive + 「跳到第N步」按钮标签)· **AnSkeleton** 骨架(一个 ShaderMask `srcATop` 扫光,线性平移、`RepaintBoundary`;reduced=静态哑底;row/card/text/lines)· **AnTypewriter** 打字机(一个 controller 相位机 type→hold→delete→循环,**字素安全** `String.characters`;光标移动实/停顿呼吸;两层 a11y:动画文字 ExcludeSemantics + 外层暴露完整短语、liveRegion=false)· **AnTags** 可编辑标签集(Wrap 药丸 + 健康点 + 内联添加;重复拒+闪;空框 Backspace 删末;每×自有按钮语义)。

**G3 行与卡要点**(建造规范见 [`WRK-038`](../../archive/g3-rows-cards/README.md),逐件随提交填本段):**地基增强批**(G3.2)——`AnColors.accentLine`(hairline 描边色,供 emphatic 行 / accent 卡)· `AnInteractive.expanded`(透传 `Semantics(expanded:)`,供 collapsible 行 / RowDetail,null=非披露不误报)· `AnGroupLabel.padding` 参数化(供 Section caption 复用同源字色/字重)· §2 reduced 三桶门控政策。**AnRefPill**(G3.3,A3 行内提及药丸):类型图标(`AnIcons.byKey` 单源开放 + "?" 兜底,**不另抄 kind→icon 表**)+ 文案;`id` 非空才可点(`AnInteractive` button、派 `{kind,id}` 经装配层转 select,**原语不碰导航**)、空=纯标注(不可聚焦、键盘穿透);`ConstrainedBox(maxWidth: block)` + 省略(长名挤不破行、无界父下也安全);a11y 单节点「{类型}: {名称}」(WCAG 1.4.1)、图标装饰整 visual `ExcludeSemantics`;hover 提墨=功能性微反馈(`reduced` 门控、`Duration.zero` 兜底)。**`kind` = 后端 EntityKind 线缆值**(`relation/entitykind.go`,事实源:`document` 非 `doc`、含 `control`/`approval`,11 类);`AnIcons.byKey` 与 `ref.*` i18n 同 speak 该词表(装配层直传 wire kind、不归一),开放集未知 kind 兜底「?」+ 原样词;`an_ref_pill_test` 有抗漂移闸门(11 类 byKey 必非 fallback)。**AnSection**(G3.4,D1 小节):`caption`(大写灰 meta,**复用 `AnGroupLabel`** padding=zero 单源)/ `plain`(`AnText.strong` 文档级标题、h3 之下有意最小档)双 variant;`actions` 经 **`AnTwoZone`**(label 左 Expanded + 右锚)+ **`AnActionGroup`(不带 `end`——AnTwoZone 已右锚,`end` 会撑无限宽崩 Row)**;有 label 或 actions 即渲 head。children 统一块间距(归容器);a11y `Semantics(container, explicitChildNodes)` 子件各自可达 + label 为 **`header` 节点读原始大小写**(视觉可能大写、`ExcludeSemantics` 视觉、`isHeader` flag,**真机 VoiceOver 验证待补**——decision②)。响应式 **grid 变体经 AnAutoGrid**(G3.5 回填:`grid` / `gridMinColWidth` 参,body 委托 AnAutoGrid)。**AnAutoGrid**(G3.5,响应式块网格 = CSS `repeat(auto-fit, minmax(W,1fr))`):**先证伪原生 delegate**(`SliverGridDelegateWithMaxCrossAxisExtent` 由单一 `childAspectRatio` 钉死全表统一行高=`minExtent==maxExtent`,违 demo 各行按内容;`flutter_layout_grid` 的 minmax 未实现 #25)→ **HAND-ROLL** 编排 `LayoutBuilder`(测宽)+`Wrap`(流式、每行按内容高)+`SizedBox`(等宽列)+ 一条列数公式(`n=floor((w+gap)/(minW+gap)).clamp(1, childCount)`、`colW=floorToDouble((w-(n-1)gap)/n)` 向下取整等宽且避 FP 误折行——代价:余 <n px 作不可见右边距、非严格 1fr 均摊,换确定性);无界宽→单列降级;`minColWidth` 默认 `AnSize.block`、gap 默认 `AnSpace.s16`,**无新增 token**;纯静态、reduced N/A、不加 Semantics 包裹(子件各自可达)。**AnLeadValue**(G3 对齐修订引入,**AnKv/AnField/AnEditableValue 共用的动态键值行几何核**,**自研槽式渲染** `SlottedMultiChildRenderObjectWidget`):leading 贴内容靠左 + value 吃尽「行宽 − leading」**真实剩余**、贴右,长到撞回 leading 才省略 = **动态分配**(键优先、值填到键)。**不是 Row**——`RenderFlex` 做不到,三条死路皆经对抗复审实测证实:① 中间 `Expanded`「grow」按 flex 因子**预分配**、loose `Flexible` 余量困自身 slot 不回收(三 flex 子被切等三份、value 停 ~⅔——根因 [RenderFlex](https://api.flutter.dev/flutter/rendering/RenderFlex-class.html) + Scott Stoll Deep Dive 4);② `LayoutBuilder` 封顶在**任何 intrinsic pass**(IntrinsicHeight/Width、baseline)下崩;③ 非 flex 封顶 leading **不让位** → 窄编辑行 ✓✕ 溢出。故自研 `RenderBox` 直接测算:leading 取 `min(内容, 0.6·avail)`(私有常量 `_leadingMaxFraction` 防饿死闸,value 留 ≥0.4·avail + ✓✕ 永远在),value 拿真实剩余贴右(`wrap` 填满左对齐换行);**实现 intrinsic**(IntrinsicHeight/Width 安全)、**无界宽降级**为内容宽(不崩)。`afterLeading`(铅笔)贴 leading 右、`afterValue`(✓✕)钉最右。`an_lead_value_test` + `an_field_test` 几何 + 回归断言:值右缘=行右缘、短 leading 值跨行中点(证非 50/50)、超长 leading 省略不溢出、IntrinsicHeight 行不崩、无界宽降级、窄行编辑 Save 不溢出。**AnEditableValue**(G3.6,**AnField + AnKv 共用的双锚就地编辑核**,= demo field.js):leading(key/label)+ 值,平时只读、hover 时 key 右冒铅笔(常驻树键盘可达、opacity 揭示)→ 点铅笔值换 **`AnSeamlessField`**(或内联 `AnDropdown`)、value 右出 取消/保存(**两锚**,异于 AnEditAffordance 同处三连)。**Enter/✓/失焦提交**(decision① blur-commit:`AnInput.onTapOutside` 地基增强透传),Esc/✕ 取消;**abort 经一次性 `_finished` 守卫优先 + ✓✕ 套 `TextFieldTapRegion`**(点 ✓✕ 不算字段外、不误触失焦提交=取消优先);焦点回落**按完成来源判定**:字段 **Enter/Esc(键盘)回落**铅笔续键盘导航;**点 ✓✕ 与失焦提交(指针)不回落**(否则铅笔被聚焦 → `revealPencil` 读 hasFocus 卡可见 + 画焦点框、而非隐回默认)。**不能用 `FocusManager.highlightMode` 判**——桌面上鼠标指针也是 `traditional`(只有触摸是 `touch`),分不开鼠标/键盘,故按调用点判定。**指针完成还需重建前 `primaryFocus?.unfocus()`**:聚焦的字段/✓✕ 被移除时 Flutter 会把焦点**自动恢复**到最近幸存者(铅笔)→ 重建前(同步)卸掉编辑区焦点杜绝之(`unfocus` 是微任务、先于下一帧重建 drain,故移除时已无焦点可恢复);进编辑 `SemanticsService.sendAnnouncement`(polite,i18n `a11y.editingField`)。`rowHeight` 参数化(Field `islandHead`44 / Kv `row`32)、展示值镜像 `AnInput` seamless 样式(mono 同步)故切换不跳;`Semantics(container, explicitChildNodes)` 子件各自可达。行几何(key | 铅笔 `afterLeading` | value 吃余量贴右 | ✓✕ `afterValue`)经 **`AnLeadValue`** 排布(input/select 两式同核;编辑态单行不 wrap)。**`AnSeamlessField`**(就地编辑字段叶子,AnInlineEdit + AnEditableValue 共用):`DryIntrinsicWidth` + `caretEndPad` + `inlineEditMin` + seamless `AnInput` + Esc/Enter/onTapOutside;选区策略由调用方设(rename 全选 / 改值光标落末)——AnInlineEdit 重构复用之、行为不变(`onTapOutside:null` 不失焦提交,重命名点别处不静默改名)。**AnKv**(G3.7,C3 紧凑定义列表):key 左 · value 经 **`AnLeadValue`** 吃余量贴右(动态、撞 key 才省略)、每行 `AnSize.row`、靠字色(key `inkMuted` / value `inkFaint`)+ 留白分层(无横线);**可编辑行**(`AnKvRow.editable` + 非空 `onChanged`)经 **`AnEditableValue`** 就地编辑(铅笔→框/下拉、失焦提交、取消优先),**只读行**为单一 merge 的「label: value」语义节点(无铅笔、key→value 连读);`mono`=值等宽 + tabular(id/hash)、`wrap`=长值换行;改一行→重建整列经 `onChanged` 派出(**whole-list**,对齐 AnTags 非 demo 位置参)。决策⑤:AnKv + AnField 同 `an_field.dart`(AnEditableValue 已独立公开,故同文件仅为这两件)。`AnEditableValue.startEditing` 供 gallery/matrix 直展编辑态。**AnField**(G3.8,C2 键值大行,行高 `AnSize.islandHead`44 比 Kv 高、阅读型):**三态**——① value+editable → `AnEditableValue` 就地编辑;② value 只读 → 经 **`AnLeadValue`** 贴右的只读值(单 merge「label(, hint): value」语义);③ **value==null → 渲 `child` 控件**(下拉/开关,经 `AnLeadValue` 贴右、无铅笔)——Field 区别于 Kv 的本质(承载控件);label=ink、value=inkMuted(异于 Kv 的 muted/faint);可带 hint 副行(meta+faint 多行)。tabular 同 Kv 无条件。

**值列 tabular 铁律**(G3.7 起):`AnKv`/`AnField`/`AnEditableValue` 的 value 文本与编辑框(`AnInput`/`AnSeamlessField` 的 `tabular` 参)**无条件 tabular figures**——**AnKv** 同 demo `.v tabular-nums` 常驻;**AnField** 为**有意增强**(demo Field `.v` 实无 tabular)以与 AnEditableValue 编辑态对齐(idle↔editing 数字同宽)+ 数值列对齐;mono 仅切字体族。

**AnCard / AnInfoCard**(G3.9a):**AnCard**(有边卡)——hairline 描边画盒内(圆角无灰尖)+ `AnRadius.chip` + surface 底;`variant=accent`→`accentLine` 边、`pad=tight`→紧凑、`selectable[+selected]`→可点卡(`AnInteractive` button,hover→`lineStrong`、选中→`accentLine` 2px,功能性微反馈 `reduced` 门控);**不设 demo 的 `row` 旗标**(CSS flex-direction 不映射单 child,要横向卡传 Row child;卡内多块间距 demo `--sp-2` 随之交 child 自管);收口 settings/MCP/onboarding 重复卡皮肤;非 selectable 也包 `Semantics(container, explicitChildNodes)`(子件各自可达,同 InfoCard)。border 优先级:选中 2px accent > selectable hover lineStrong > accent 变体 > 中性。**AnInfoCard**(无边信息单元)——head(icon + title + meta,仅有内容时渲)经 **`AnTwoZone`**(title 占满省略 + meta 让位右,icon 在外前置)、title=`AnText.meta`+`inkFaint`+w600(`.weight` 双轴)+ `header` 语义、icon `iconSm`+inkFaint;head min-height `AnSize.control`(28,卡头垂直节奏);body=**单 slot** child(多块由 child 自带 Column gap,同 AnCard);actions 走 `AnActionGroup`(上距 `AnSpace.s12`、无则塌);`Semantics(container, explicitChildNodes)`、icon `ExcludeSemantics`。

**AnRow / AnRowDetail**(G3.9b):**AnRow**(C1 核心行)——三列 `[lead(icon16) | label 1fr | trail]`,整行一个 `AnInteractive`(hover 态驱动 lead/trail 揭示;`passive` 不可聚焦;`collapsible` 折叠仅鼠标 → v1 不暴露 `expanded` 语义,详见下文 a11y 待补)。lead = 状态 `dot` 或 icon↔chevron(collapsible **hover 换 chevron** `_HoverSwap` + `open` **`AnimatedRotation` 转 90°**);trail = meta 与 hover 揭示的 `actions` 叠**同一右锚**(`_HoverSwap`:`Stack(centerRight)` 双 `AnimatedOpacity` 互换不重排,隐藏层 `IgnorePointer`+`ExcludeSemantics`)。`hint`→行变高(`islandHead`)顶对齐、hint 换行;`selected`→`surfaceActive`,`emphatic`+selected→`accentSoft` 底 + 左**实心 `accent` 条**(`Stack`+`PositionedDirectional`+`ClipRRect`;demo `inset --line-2 0 0 --accent` 是满饱和 accent、异于 AnCard 选中边的淡 `accentLine`);`depth` 每级 `iconLg`(20)缩进;`mono` 等宽 label。**短行内容垂直居中**(`Stack(alignment: center)`——`minHeight` 撑高时 `RenderStack` 默认 `topStart` 会把单行内容顶对齐=旧 bug;定位 accent 条上下双钉满高、不受 alignment 影响,高 hint 行 content≥行高时居中偏移为 0=自然排版;G3 对齐修订)。**opacity 互换即时**(demo 明示「不入过渡」,`_HoverSwap` 用 `Opacity` 非 Animated;只 chevron 旋转走 `AnMotion.spring`)、bg 提墨走 `AnMotion.fast`。**a11y**:collapsible 行**透 `expanded` 披露语义**(屏读播报折叠/展开,G4.6 提升、删旧 v1 false-promise);**键盘展开 ←/→ 归树消费方**(`AnSidebarList` 的 roving 焦点组),**不塞进 AnRow**(避免双键盘属主);lead 折叠点击仍仅鼠标(`GestureDetector`),非树场景下展开靠消费方提供键盘途径;`_HoverSwap` 隐藏层 `IgnorePointer`+`ExcludeSemantics`+**`ExcludeFocus`**(opacity0 不挡焦点遍历,否则不可见 action 钮成焦点陷阱);带 `dot` 行的状态语义由调用方在 label/meta 复述(dot 装饰 ExcludeSemantics)。**AnRowDetail**(C1d)——row + open 时下方经 **`AnExpandReveal`**(G4 用户验证修订抽出的共用折叠/展开原语)展开 detail 面板(缩进 `icon+s8+s8` 对齐 label 起点 + 底 hairline);**受控**(open 由调用方持有、行 onSelect/onToggle 翻转)。reduced 门控:chevron 互换/旋转 + 展开 + hover 皆功能性 → `reduced`、`Duration.zero` 兜底。**`AnExpandReveal`**(`core/ui`,共用折叠/展开揭示):**控制器驱动 `ClipRect` + `Align(heightFactor)`(ExpansionTile 习语)、仅向下、reduced 即时**;**非 `AnimatedSize`**——AnimatedSize 在 performLayout 内子件 resize 会自脏断言、**嵌套**(组含类型含树枝=sidebar 形态)即崩,ClipRect+Align 可安全嵌套;open 显 child、否则补间到 0 高**且全收后从树移除**(收起的行不可聚焦/不被屏读);`duration=Duration.zero` 强制即时(供过滤强制展开,逐键补间会卡)。AnRowDetail + AnSidebarList(group/type/branch 三级)共用之。

**AnThinTable**(G3.10,C4 对齐多列、**非表格无 chrome**):搭 Flutter **内置 `Table`**(`RenderTable` = CSS subgrid 等价:一组共享列轨、测一次、跨行对齐;**Flutter 无 subgrid**,见 [`WRK-038`](../../archive/g3-rows-cards/README.md) 附录 T1)。列轨:首列 `FlexColumnWidth(1)` 吃富余、其余 **`MinColumnWidth(IntrinsicColumnWidth(), FractionColumnWidth(0.9/(N-1)))`**——贴短内容、长值随表宽缩不溢出(**裸 intrinsic/`FixedColumnWidth` cap 不缩**:RenderTable 只压 flex 列,窄表 + 长非首值会静默溢出且 RenderTable 不报错;Fraction 随表伸缩恒 ≤ 表宽,cap=0.9/(N-1) 留 ≥0.1·W 给首列;G3.10 复审 HIGH 修);表头=灰 meta + w600 + 底对齐 + min-height `controlSm`、**无线**;首列主值 `ink`、其余 `inkMuted` + tabular,**hover/选中全格提墨**(非首列 →`ink`,demo `.td` 提墨)。`selectable`→单选(**`TableRow.decoration` 整行高亮**=subgrid 行等价,**选中底 `surfaceActive`**——demo `.on`=island-4 + 与 AnRow 选区同底,非 T1 附录初稿的 accentSoft;命中=每格透明层 `MouseRegion`+`GestureDetector`,因 `TableRow` 非 widget 不能挂手势 #42609,**非 Material `TableRowInkWell`**;hover 经表级 `onExit` 清 + 格级 `onEnter` 设、不闪);`rows` 身份变 `didUpdateWidget` 清选区;海量/独立滚动退官方 `two_dimensional_scrollables` TableView。a11y:`Table` 自带表格语义 + 表头 `header`;selectable 行**仅首格**为语义节点(`button`+selected+`onTap`+「col: val, …」行摘要,其余格 `ExcludeSemantics`——每行恰一个可操作节点、不 N 重复)。`TableRow.decoration` 静态(hover 即时,Table 限制;reduced 下即时本就正确)。

**G6 浮层要点**(建造规范见 [`WRK-041`](../../archive/g6-overlays/README.md);用户拍板 2 + 主循环自决 3,详见该篇 §1):G6 = UI kit **最后一组**,造 **AnDialog + AnToast + AnOverlayController/Host + 命令式 API**。**Menu 已在 G4 落**;**AnFloating(demo F1)≡ AnPopover**——其两项浮层短板(视口避让 / 首帧锚宽跳变)已在 **G4 用户验证修订**经 `CustomSingleChildLayout`+`SingleChildLayoutDelegate` 重写解决(翻转+夹取不出屏 + 锚矩形经 GlobalKey RenderBox 在 build 读、无 `LayerLink` 首帧 null),**本组不重做**;且 dialog/toast 均**非锚定**,不经此基座(`an_popover.dart` 头注释同步纠正)。
**AnDialog**(F4 模态确认框,`an_dialog.dart`,v1 = `anConfirmRoute`→`Future<bool>`):机制 = 自建框架 **`RawDialogRoute`** 经注册的 root navigator `push`,**非** `showDialog`/`showGeneralDialog`(后者强制 Material 转场 + SafeArea + black54 遮罩 + 需 caller context)——直接构造可换 `scrim` 遮罩 + spring 转场、且拿 `Route` 句柄做**单实例**(新 confirm 前 `removeRoute` 旧的)。**3.41.9 SDK 实证纠正**:`closedLoop` 闭环焦点陷阱是 `showDialog`/`DialogRoute` 的**默认值**(`material/dialog.dart`),但**直接构造 `RawDialogRoute` 不默认**(走 ModalRoute 全局 `parentScope`),故必须**显式传 `traversalEdgeBehavior: closedLoop`** 否则 Tab 逃出 modal;焦点陷阱/归还(所有关闭路径走同一 `Navigator.pop`、route 统一兜)/ Escape 关(`_DismissModalAction.maybePop()`,与 `barrierDismissible` 同闸)/ 点遮罩关 / `scopesRoute` 语义(`buildPage` 硬包)**全框架白送**(逐条核 `widgets/routes.dart`)。卡 = `Center`→`SizedBox(width: min(content 720, vw−s24×2))`→`ConstrainedBox(maxHeight: vh−s48)`→`DecoratedBox(surface + island20 + hairline line + **shadowWin**)`→`ClipRRect`→`Column[head/body/foot]`:head(islandHead 44 + title `AnText.strong` 省略 + close `AnButton.iconOnly(md)` + 下 hairline)· body(`Flexible`+`SingleChildScrollView` + message `inkMuted` 行高 1.6,空则无)· foot(`AnButton` cancel[ghost,**autofocus**=安全默认,Enter 不误删] + confirm[confirmTone→danger/primary,pop(true)] 右对齐 + 上 hairline);转场 `FadeTransition(animation)` 套 `AnimatedBuilder(animation.drive(CurveTween(spring)))` 应用 `translate(s8→0)`+`scale(.98→1)`(**用 `drive(CurveTween)` 非 `CurvedAnimation`**——后者须 dispose、inline 创建泄漏);reduced→`Duration.zero` 进退瞬时。
**AnToast**(F3 屏角瞬时栈,`an_toast.dart`,**纯展示无 Riverpod**):宿主 = `MaterialApp.builder` 静态挂自管 `Overlay`?——实落用 **`AnOverlayHost` 的 `Stack`**(host **不 watch** toast 列 → `child`[Navigator] 不因 toast 重建;仅 `_AnToastLayer`[`ConsumerWidget` watch] 重建)+ `Positioned(right/bottom s24)` 右下小区(栈外天然穿透、免 `IgnorePointer`)+ `Column(verticalDirection: up)` 最新在底向上堆,**不裁不限高**(软上限 5 已兜高度;**绝不 ClipRect**——会把每条 chip 的 `shadowPop` 在 Column 紧边界四向切断[真机复审揪出的截断 bug];host Stack 已裁到屏、是唯一想要的边界)。单条 `AnToast`(`StatefulWidget`+`SingleTickerProviderStateMixin`):**急切建 `AnimationController`**(绝不 late-final 懒初始化,AnPopover dispose 崩教训;reduced 在首个 `didChangeDependencies` 改时长 0)+ 自管自动消隐 `Timer`(缺省 4s,`Duration.zero`=常驻,WCAG 2.2.1 兜底)+ `_dismiss`=`reverse().whenComplete(onDismissed)`(自驱离场→再通知 controller 移除,**不需** demo 的 transitionend 兜底——`whenComplete` 确定);chip = `surface`+`chip` 圆角+hairline+`shadowPop`(= AnMenuSurface 同款白岛)+ **`Stack`** 布局(`Padding(Row[Expanded text + action? + close])` + `Positioned` 满高 **tone 色条**;**非 `IntrinsicHeight`+`Row(Expanded)`**——RenderFlex 含 flex 子件答不了 intrinsic-height 会抛);tone 色条 `width s4`(=demo `--grid`,**`none→inkFaint`** 特判非 `AnToneColors` 的 inkMuted)、close `AnButton.iconOnly(sm)`、**action 走 `AnInteractive` 小钮**(accent 文字 + accentSoft hover;`AnButton` 无 accent 变体,全 demo 仅此一处用、不为单点膨胀按钮——决策 4)。**宽 = `maxWidth: toastMaxWidth=360`,文字 `Expanded` 吸余把 close 钉右 → chip 恒 360**(均匀宽、栈右缘齐);demo 的 280→360 内容弹性收缩 **v1 推迟**(stock widget 做不到「shrink-to-content + 下限 280 + 关钮钉右」三者并存,须自研 RenderObject[同 AnLeadValue],对瞬时件不值)。**a11y**:`liveRegion` 在**文字节点**(polite 播报),**非整 chip**(包整 chip 会把 close/action 按钮 merge 进一个节点、labels+isButton 泄漏、文字重复)——`Semantics(liveRegion,label)`+`ExcludeSemantics(Text)` 一节点;**绝不夺焦**(背景探针 show 后 hasFocus 恒 true);**不用 `SemanticsService.announce`**(桌面坏 + VoiceOver 抢读 + deprecated)。
**AnOverlayController / Host**(`core/overlay/an_overlay.dart`):**经典 `NotifierProvider<AnOverlayController, AnOverlayState>`**——**禁用 legacy `ChangeNotifierProvider`**(riverpod 3.3.2 仅 `legacy.dart` 导出、与 widget-local `AnPopoverController extends ChangeNotifier` 撞用途)。`showToast(text,{tone,action,duration})` 全程**无 context** 直驱(id 单调计数确定性 + **软上限 5、超出立即移最旧**=决策 2)+ `dismissToast(id)`;`confirm(...)` 经 host 在 `initState` 注册的 **root navigator key**(app/gallery 各建 `GlobalKey<NavigatorState>` 给 `MaterialApp.navigatorKey` + 经构造注入 host + ref 接入 controller——ADR 0004 干净替代全局 key,**非**隐藏全局单例)push,文案由调用方[有 context]传、service 不持 i18n。**z 序偏离(决策 1,用户已认)**:host 的 toast 层渲在 `child`[Navigator]之上 → toast 浮在 modal dialog 上(与 demo `--z-dialog > --z-toast` 反)——toast 非阻断、modal 下弹 toast 极罕见、复刻 demo 序须把 toast 拖进 `Overlay.of(rootOverlay)` 的 `LookupBoundary` 雷区,故接受偏离 + 头注释留痕。**token 地基**:`AnColors.shadowWin`(dialog 卡阴影,**单层** light `rgba(0,0,0,.20) blur50 (0,16)`/dark `.50`,light/dark/copyWith/lerp 四处;勿据此类推 island/float/pop 三档暗色 offset 各异)+ `AnSize.toastMaxWidth=360`(语义独立于 menuMaxWidth/stateMaxWidth 的同值 360)+ tone 色条**复用 `AnSpace.s4`**(否决新增 `toastBar`)。**装配脚手架**:`app.dart` `home:`→`builder:` 包 `AnOverlayHost`(StatefulWidget 持 navigatorKey);`gallery_main` 补 `ProviderScope` + `gallery_app`/`gallery_matrix_test`/`capture_gallery` 同步裹 ProviderScope(否则浮层 specimen 的 `ref.read` 当场崩);gallery `_itemBlock` blurb 加 `Flexible`+省略(长 blurb 不溢出,真机截图揪出)。**推迟**:`openDialog(builder)` 富内容口 · `context.showToast()` 糖壳 · `AnModalCard` 抽象(**勿名 AnIslandCard**——避撞现存 chip 档 `AnIsland`;单消费者内联) · go_router root-Navigator 归属(预埋注释) · notification rail(属 feature 非 kit)。验收:`an_overlay_test`(controller 5 单测)+ `an_toast_test`(9,渲染/liveRegion 不夺焦/消隐/sticky/reduced/注入)+ `an_dialog_test`(9,真路径 host+confirm:四关闭路径 + autofocus 安全 Enter + scopesRoute)+ matrix 四轴 + 截图实证(静态 chip 全 tone + 开启态 dialog 居中遮罩 + 角落栈软上限 5)。

**推迟到各自 feature**(耦合 SSE/reducer/图模型,非套件):Chat 的 BlockTree/Composer/ApprovalGate/EntityWorkspace · 图的 GraphCanvas/KindLegend · 调度的 NodeGantt/RunBoard · 文档的 DocEditor/Outline。

**G4 导航与壳要点**(建造规范见 [`WRK-039`](../../archive/g4-nav-shell/README.md),逐件随提交填本段):**G4.0 地基**——`AnSize.tab`=34(文字下划线 tab 高,**有意比 row 高 1u**,demo `--tab-h`)· **`AnScrollBehavior`**(`core/ui`,`ScrollBehavior` 子类 override `buildScrollbar`→child 隐滚动条 + `dragDevices` 开鼠标/触控板拖滚;**仅局部** `ScrollConfiguration` 包住要隐滚动条的可滚区,绝不 app-root——否则碾压 AnPage 的 RawScrollbar)· `GallerySpecimen.height` 字段(滚动宿主件 AnPage/AnTabs/AnInspector/AnSidebarList 根部高贪婪,matrix host 仅约束宽=无界高崩,故给定高;matrix host + gallery 渲染器同步消费)。**AnToolbar**(G4.1,D6 三区工具条,**非卡**):`leading | main | trailing` 对齐骨架(CSS `auto 1fr auto`)——左右组经 **`AnActionGroup`**(**不带 `end`**,end 撑无限宽崩 Row;main 的 `Expanded` 已把 trailing 推右缘)、main 是**中间弹性轨**(`Expanded`)title 左 packed w600 省略 + meta 次级(**非居中**,demo `.main` 左对齐 inline-flex;title/meta 各 `Flexible` 省略=换 no-overflow,舍 demo meta:flex-none);`bordered`→顶栏(底 hairline + `surface` 岛底 + pad s16/s8)、`compact`→`control`(28)否则 `row`(32);`title`/`meta` 缺省走 `center` 自定义槽。无 i18n(title/meta 由调用方传)。**AnInspector**(G4.2,D4 右岛内容壳,**命名非 AnRightIsland**——它只渲 head+body、**不画岛皮**(壳的 `AnIsland` 供,故消费方用 `AnIsland(padding: zero)` 包它免双层 pad)、**不管宽**):`head`(`Icon` inkFaint 装饰 + `Expanded(title` body w600 省略`)`,`islandHead`44 高、仅顶 pad)+ `Expanded(body)`(`ScrollConfiguration(AnScrollBehavior)` 隐条 + `SingleChildScrollView` pad s16/s8 + `Column(spacing s12)` 块流堆叠);`headless`→去头、`child` 占满自管滚动(流式工作台)。**v1 不挂 `complementary` landmark role**(空 label/多实例会断言,留 app 装配全景);仍带语义兜底。**AnPage**(G4.3,D3 海洋记录页脚手架):唯一滚动区 + 居中 `AnSize.content`(720)内容列 + **overlay 滚动条 `RawScrollbar`**(thumb `lineStrong`/`pill`/`s4`/`minThumbLength controlSm`;**不手搓** demo 的 rAF thumb 数学 + 700ms idle-hide,#8)——overlay 不占 gutter、内容列宽不抖;顶 pad `islandHead + s12` 让出浮动头(大标题坐头栏下不被切)、LR `s24`、底 `s48`;`Center` 横向居中(海洋更宽则居中)。`controller` 可传(chat `scrollToBottom` / 折叠联动下游读 offset)否则自管;默认 scroll 显隐(真机验,§9)。**AnMenu**(G4.4,F2 浮层命令/选项菜单,**新共享原语** on `AnPopover`):分组小标题(`AnMenuSection`,留白+灰 meta w600、无分割线、缩进对齐项标签)+ 命令行(`AnMenuItem`:`前导(icon 或 checked→勾)| 标签 | meta`,row 高、`AnInteractive` hover 提墨 + 键盘激活、`r-btn` 圆角 hover 底)+ `danger`(红字 + dangerSoft hover)/ `disabled`(opacity + inert)/ `checked`(前导勾,选择在前导非尾)/ `keepOpen`(多选 toggle 不收)。`anchorBuilder(context, toggle, isOpen)` 建触发器接到按钮 onPressed;pick 跑 onTap 后收起(除非 keepOpen);`alignEnd` 右对齐;`onClose` 转发。复用 AnDropdown 的 popover + 菜单宽夹 `[menuMin,menuMax]` + `FocusTraversalGroup`。消费者:sidebar sliders(Sort/Display)· row-more · 壳 ⋯。**AnTabs**(G4.5,B5 文字下划线视图切换器,**非分段器**):tab 按钮(`AnInteractive`,灰 inkFaint→hover inkMuted→选中 ink)+ **一条弹簧下划线**(GlobalKey 后帧量选中 tab 的 offset/width → `AnimatedPositioned`/`AnMotion.spring`,**在滚动内容坐标系内**随 tab 滚)+ 下接 **`IndexedStack` keep-alive panes**(隐藏不销毁,编辑/滚动/流态保留)。多 tab 横滚(`AnScrollBehavior` 隐条);受控(`value`+`onSelect` 仅用户点选派,**与 AnRow/AnSidebarList 同名**)。**手搓非 Material TabBar**(波纹/M3 indicator/46px/tab-role 断言/桌面遍历自动切 bug 全冲突)。a11y=**button + `selected`**(**不挂 tab/tabBar SemanticsRole**——该 role 结构子断言会因下划线/viewport/body 在子树而崩,WRK §4 铁律);键盘 Tab 遍历 + Enter/Space 激活 + ←→Home/End **roving 移焦不自动选**(手动激活,`Shortcuts`+`FocusTraversalGroup`)。**复审硬化**:`WidgetsBindingObserver`(didChangeMetrics/TextScaleFactor)+ build 时幂等补量 → 下划线在窗口/字号/内容/重排后**自愈**(否则停旧几何到下次点选);`selected` 跟 `_index`(非裸 value)→ value 失效时高亮/下划线/pane 三者一致落 tab 0。`Expanded(IndexedStack)` 须有界高父(同 §4 GallerySpecimen.height);count 徽标(tabular)、lazy-build(IndexedStack 全建)留后。**AnInlineEdit 参数化**(G4.6,决策#3 跨切已发布原语):`AnInlineEdit` + `AnSeamlessField` + `AnInput` 加 `style`(TextStyle)+ `AnInlineEdit.minHeight`(行高),默认保持 body/`control` 行为不变;让 `AnOceanHeader` 的 **H2(24px)标题能就地改名而字号/行高不跳**(idle Text 与编辑框共用 `style`)。`AnInput.style` 覆写值列/正文基样式、色仍按 readOnly/enabled。**AnSidebarList**(G4.8,C5 左岛侧栏复合件):**纯 `SidebarModel`(`core/model`)** = `groups→types→rows` 递归 + `sidebarVisibleIds(model, query)` 纯过滤(行或子孙命中 label/meta 即可见、**祖先回填**;空 query 空集=不过滤;纯文本子串、CJK/正则元字符/尖括号惰性=注入安全)——**单测 9(含 5 电池 + 祖先回填 + CJK + injection)**。widget:New 行 + 域内过滤(`AnInput` seamless,驱动隐藏非命中 + 强制展开命中祖先 + 隐空 type/组)+ sliders(`AnMenu`)+ 树(group 头 collapsible chat 式 / type 头 `AnRow` passive collapsible / 实体行 `AnRow` 递归 depth + dot/icon/meta/hint/actions),New/过滤头与行**共用行首列**(+/🔍 与行图标对齐)。选中**受控**(`selectedId`+`onSelect`);折叠态内部(默认全开,`Set` 折叠键 group:label/g$gi/t$ti/r:id;过滤强制展开)。`AnIcons.plus` 新增。**键盘树 roving(↑↓ 移行 / ←→ 折叠展开,WAI-ARIA tree)推迟**:v1 行 Tab 可达 + Enter 选 + 鼠标折叠 + AnRow 已透 expanded 语义(播报态);箭头键导航留后续。复审修:类型头改**展开按钮**(onSelect=toggle,键盘可达,非 mouse-only)、组折叠键按位置(同名组不串)、SidebarRow.id 全模型唯一不变式 + 折叠/过滤瞬时态文档。**AnOceanHeader**(G4.9,D5 海洋页头,坐海面无卡):`Column`[ crumb 行(`Text.rich` 面包屑 + 灰 `/` 分隔、整行省略;`Expanded` + 右 `AnActionGroup` actions,minHeight `control`)· **H2 标题**(`onTitleChange` 非空→**就地改名** `AnInlineEdit(style: h2.weight(w600), minHeight: h2 行盒)`,否则只读 `Text(h2)`;v1 单行省略、wrap 推迟)· meta `Wrap`(badges/dots,s16 gap)],底 pad `s24`。滚动收起联动(大标题滑出→壳紧凑头)归下游 `AnPage` 滚动宿主(本头无可滚),v1 只渲静态头。无 i18n(crumb/title/actions/meta 调用方传)。**AnShell 右岛揭示**(G4.10,壳骨架扩展):`inspectorOpen`(默认 true)揭示/收起右岛——间距 + 岛宽一起 `AnimatedContainer` 动画 `0↔AnSize.rightIsland`(`AnMotion.mid`/`easeOut`,reduced→`Duration.zero`),内容经 `OverflowBox(min/max=rightIsland, centerStart)` 钉满宽 + `ClipRect` 裁切 = **滑入/滑出而非重排**(收起期间右岛内容不挤压重流);收起时海洋(`Expanded`)正好多得 `rightIsland + shellGap`(`an_shell_test` 量证)。一个 feature 选中实体时开右岛、否则收。**推迟项(均需真机验,#8 + verify-by-real-run)**:① **左岛 collapse / 重开**(纠缠 macOS 红绿灯——折叠后红绿灯所在加高标题栏归属、`panelLeft` 钮位置 + scrim 命中,见 window_setup);② **浮动海洋头紧凑联动**(大标题随滚动收进壳紧凑头——依赖尚未接的 `AnPage` 滚动 offset 联动);③ **键盘树 roving**(同 AnSidebarList G4.8 推迟)。三者落地前壳只渲静态左岛(可拖宽)+ 静态海洋 + 可揭示右岛。**G4 整体复审硬化**(11-agent 5 镜对抗复审 → 16 confirmed,0 HIGH;逐条修):**a11y** — ① **`AnPopover` 焦点归还**:开浮层前记 `FocusManager.primaryFocus`,关时(pick/Esc/点外)反向播完 `requestFocus` 回触发器(裸 `FocusScope` 不像路由自动恢复,否则键盘/屏读落到 root,WCAG 2.4.3);AnMenu/AnDropdown 共享受益;② **`AnMenu` 首项自动聚焦**:首个非 disabled 项 `autofocus`(descendant autofocus 胜过浮层 scope),开菜单即落项 0、方向键即用(对齐 AnDropdown);③ **header 语义补全**:`AnInspector` 头标题 + `AnOceanHeader` 只读标题各包 `Semantics(header:true)`(屏读 rotor/H 跳标题,镜像 AnInfoCard;header 是 role flag、空标题不断言;editable 分支的输入框不挂);④ **`AnShell` 右岛收起惰化**:`!open` 时内容(仍满宽布局供滑动)套 `ExcludeFocus+ExcludeSemantics+IgnorePointer`(ClipRect 只裁绘制/命中、不剥语义/焦点,否则 0 宽裁切后的按钮成隐形焦点陷阱;同 `AnRow._HoverSwap`)。**token/动效纪律** — ⑤ **reduced-motion 门控补齐**:`AnMenu` 项 hover 底 + `AnShell._Grip` hover 细线 + `AnInput` 聚焦边三处 `AnimatedContainer` 改 `reduced ? Duration.zero : AnMotion.fast`(功能性微反馈,对齐 AnRow 先例);⑥ **`AnMenu` 去 `Color` 字面量**:danger 静止底 `const Color(0x00000000)` → `c.dangerSoft.whenActive(active)`(走 alpha-0 单源、避 lerp 暗中点)。**布局** — ⑦ **`AnPage` 双滚动条**:RawScrollbar 内补 `ScrollConfiguration(AnScrollBehavior)` 压制继承的 Material Scrollbar(桌面 `MaterialScrollBehavior` 给每个竖向 scrollable 包条 → 否则双 thumb;唯一漏网的滚动面,`an_page_test` 在 macOS override 下闸)。**复用/一致** — ⑧ **`AnText.metaTabular()` 单源**:meta 尺寸 tabular 数字习语(行/标签/菜单/下拉/步骤尾随计数)抽进 `typography.dart`(此前 5 处各抄一份,镜像 `AnText.value()` 规则、防漂移);⑨ **`AnSidebarList` New 行复用 `AnRow`**(去自搓行脚手架,与实体行同几何/hover;仅 chat 式可折叠**组头**因右锚 chevron 仍自搓)+ sliders semanticLabel 走 `context.t.a11y.displayOptions`(去硬编码英文,新 i18n 键);⑩ **`AnTabs.onPick` → `onSelect`**(与 AnRow/AnSidebarList 受控选择同名)。**推迟(documented)**:`_Placeholder` 骨架占位英文/inline 样式(私有 dev-only、待 feature 内容替换)· `AnMenu._entry` 行骨架不强折进 `AnRow`(菜单要 check 前导/danger 色/keepOpen 风味,AnRow 不载、折入会污染核心行——保两实现,token 同源故 retune 不漂)。**G4 用户验证修订**(真机 gallery 跑出的 4 项,联网调研 demo + Flutter best-practice 后修):① **AnMenu/AnPopover 宽 + 定位** —— 宽:`_menu` 包 **`IntrinsicWidth`**(贴最宽行内容、夹 `[menuMin 200, menuMax 360]`,镜像 demo shrink-to-fit;原 `Column(stretch)` 在 ConstrainedBox 下永远撑满 360);定位:**AnPopover 改 `CustomSingleChildLayout`/`SingleChildLayoutDelegate`** 翻转+夹取不出屏(见 G1 要点;`alignEnd`+`gap` 替 `targetAnchor`/`followerAnchor`;锚矩形经 GlobalKey 读 RenderBox;AnDropdown 仍精确匹配触发器宽、不用 IntrinsicWidth)。② **AnSidebarList 折叠动效** —— group/type/branch 三级经 **`AnExpandReveal`**(见 G3 AnRowDetail);过滤强制展开传 `Duration.zero` 即时(逐键高度补间会卡)+ 组头 chevron 同步即时。③ **就地编辑框(所有编辑)** —— `AnSeamlessField` 加 **`framed`**:编辑态画 demo 的 1px `lineStrong` 内描边 + `surface` 底 + `tag` 圆角框,**纵向零布局高**(框经 `Stack(clipBehavior:none)` + `Positioned`(top/bottom `-editBoxPadY`)溢出固定行余量,demo 负 margin 等价)、**仅右生长**(`Padding(end: editBoxPadX)` 真占位,左溢出故文字不跳);AnInlineEdit/AnEditableValue 传 `framed:true`,AnOceanHeader H2 `minHeight` += `editBoxPadY*2` 留纵向余量。④ **AnShell 右岛阴影** —— 与左岛同一 `AnIsland`(阴影 `shadowFloat` 同源);`_RightReveal` 改用 `AnimationController` + `AnimatedBuilder`,**敞开稳态用空裁切器 `_UnclippedRect`**(ClipRect 仍在树保元素身份、但不裁)故 float 阴影完整、与左岛一致;**仅滑动中**(`!fullyOpen`)真裁切隐溢出(原 always-on ClipRect 把阴影裁成死角尖尖);收起态 `OverflowBox` 满宽不重排 + `ExcludeFocus/Semantics/IgnorePointer` 惰化。**G4 用户验证修订(二)+ 复审硬化二批**:① **浮层列表统一标准 `AnMenuSurface` + `AnMenuRow`**(`core/ui`,**AnMenu 与 AnDropdown 共用单源**)——`AnMenuSurface`=面板壳(surface + 发丝边 + chip 圆角 + 浮影 + **四周 s4 内距**)、`AnMenuRow`=可选行(row 高 + **圆角内缩药丸** `AnRadius.button`,hover/active 经 alpha-0 `whenActive` 淡入、`danger` 用 dangerSoft、reduced 门控、禁用惰化)。修 AnDropdown 选中/hover 底**原为直角贴边满铺**(纵 s4 无横内距 + `color` 无圆角)→ 现与 AnMenu 一致(圆角内缩悬浮);内容(菜单勾在前导、下拉勾在尾)仍各异、仅行壳+面板壳标准化(解 deferred #13)。② **AnPopover 祖先滚动即关**:开着时听最近可滚祖先 `ScrollPosition`,滚动 → `controller.close`(同平台菜单/下拉;delegate 在 build 时取锚矩形,移动锚无法不滞后一帧重定位,故关而非让浮层悬停在错位内容——比原 `CompositedTransformFollower` 实时跟随更合规)。③ **CurvedAnimation 泄漏修**:`AnPopover._scaleCurve` + `AnExpandReveal._factor` 提为字段并在父控制器前 `dispose`(CurvedAnimation 持父监听、不 dispose 即泄漏)。④ **doc-code 同步**:AnRowDetail dartdoc + gallery caption 从「AnimatedSize」更正为「AnExpandReveal」(#9)。

**G5 代码与数据要点**（建造规范见 [`WRK-040`](../../archive/g5-code-data/README.md)，用户拍板 5 决策=移植 demo 正则/LCS·CEL 上色·VersionDiff 仅单字段·复制路径 JSON-pointer；逐件随提交填本段）：**G5.0 地基**（已落）——**`SyntaxColors`**（`core/design/colors.dart`，**独立 `ThemeExtension`**：6 色 comment/keyword/string/number/function/arg，双 brightness 值镜像 demo tokens.css One Light/One Dark；`arg` 按值镜像 `AnColors.accent`、测试锁不变式；独立扩展而非塞进 280 行的 AnColors——语法子板自成一概念，糖 `context.syntax`）· **`AnText.code`**（`typography.dart`，mono 12px/height 1.6 = demo `--t-meta`/`--lh-prose`，**代码面/行号共用同一引用**保对齐，区别于 13/1.5 的内联 `mono`）· **`AnSize.trail`=36**（行号槽下界：demo 20px 在 mono 仅容 ~2 位数，升为容 4 位数；widget 内动态计宽夹到此）· **`highlightCode(code,{lang,colors})→List<TextSpan>`**（`core/ui/syntax_highlighter.dart`，**唯一同步 tokenizer 铁律**：移植 demo 单正则、语言无关，三件全调它绝不另起第二套；**同步纯函数**——流式立即 repaint/逐行 diff 着色全靠它，异步预载包一律否决；**CEL 插值 `{{}}`/`${}` 经 arg 组天然上色**=决策 4 由统一 tokenizer 直接满足、无额外分支，`lang` 仅为 API/标签稳定；`_followedByParen` 只认 ASCII 空白=有意窄于 JS `\s`、避 per-identifier substring 的 O(n²)）· **`lineDiff(before,after)→List<({DiffOp op,String text})>`**（`core/model/code_diff.dart`，移植 demo LCS 逆向 DP+回溯直出顺序 ctx/add/del，**同步纯函数**；**退化闸 = LCS 矩阵 cell 数 `(m+1)(n+1)>4M`**，命名 `lineDiffMaxCells`、忠实移植 demo——**G5.0 复审修订**：原研究拟「m+n 行数闸」被实测证伪[m+n 与 m\*n 不同量纲、平衡型 m=n=2500 漏网撑 ~50MB]，cell 数才是真实成本；空 before 由调用方[AnVersionDiff]渲染为整段 ctx）。地基纯函数脱 widget golden 单测先行（diff 恒等/纯增删/中段改/多重复行/**平衡型 cell 退化** · highlight 无损往返/各 token/CEL · SyntaxColors lerp·copyWith·注册·arg 不变式，29 测）。**G5.1 AnCodeEditor**（E1 唯一代码块/轻编辑原语）——**read-only=`SelectableText.rich(highlightCode)`;editable=`TextField` + 自定义 `_HighlightController.buildTextSpan`(Flutter 原生语法高亮 input、非透明叠层——消解 WRK 原拟「透明 TextField 叠 RichText pixel-perfect」的 #1 HIGH:光标/选区/滚动全原生、零叠层风险,更 #8)**;两路同走唯一 `highlightCode`,`buildTextSpan` 读 `context.syntax` 自动跟主题。`re_editor` 已评估否决(自带 re_highlight、与单 tokenizer 决策冲突、CEL 无覆盖)。**scroll-host 双态**(`LayoutBuilder`:无界=内容高、父 AnPage/AnInspector 滚;有界=`Flexible(SingleChildScrollView)` body 纵滚 + bar 固定——两态都不崩);read-only 非 wrap 横滚、editable soft-wrap(TextField 不能净 nowrap+hscroll,v1 简化)。框=私有 `_codeFrame`(DecoratedBox 实色内描边 + ClipRRect + `c.surface` 白岛底,非 AnCard——其强制 pad/无 clip/无 focus 边不适配;G5.3 第二消费者再抽共享)。bar(copy 成功亮 check/失败翻 `feedback.copyFailed` tooltip 不谎报 · wrap toggle · edit→cancel·save 用 AnButton.iconOnly+Tooltip,钮间 4px;**Tab 拦为插 4 空格**(`CallbackShortcuts`,同 demo))+ 动态行号 gutter(单 Text 右对齐、`AnSize.trail` 下界、`ExcludeSemantics`)+ lang 标签(`AnText.meta` MiSans、`ExcludeSemantics`)。**a11y**:容器 `Semantics(label「Code block, {lang}, N lines」)`(非逐行)、gutter/lang `ExcludeSemantics`、SelectableText 单节点。**编辑态行号/a11y 随每次键入刷新**(控制器 listener→setState,复审 HIGH 修:demo per-keystroke repaint 等价)。**性能**:单 RenderParagraph + 每键全量 highlight(无虚拟化=Flutter 限),面向轻编辑/短片段,>~5000 行虚拟化/截断留 §9。i18n 新增 `action.{copy,wrap}`/`feedback.{copied,copyFailed}`/`a11y.{codeBlock,codeBlockPlain}`(复用 `action.{edit,cancel,save}`)+ 新图标 `AnIcons.copy`。13 单测(状态机 + 编辑行号同步回归 + Tab 缩进 + copy 失败 + a11y)+ matrix 四轴 + 截图验(明亮通透、语法色正确)。G5.1 对抗复审 3 镜 17 项(全探针实证零误报)折入(HIGH 行号脱钩 + 5 MED a11y/copy/Tab/底色)。**G5.2 AnJsonTree**（E2 唯一 JSON/结构化展示原语）——**Flutter 内置 `TreeSliver` 虚拟化**(`treeNodeBuilder` 只建可视行、解 650KB 悬崖)+ `TreeSliverIndentationType.none` 自绘缩进(`depth*iconLg`)+ `treeRowExtentBuilder`→`AnSize.row`(定高=虚拟化前提)。**类型按 Dart runtime type 分派**(Map/List/String/num/bool/Null,**非 demo typeof**;unknown→toString 兜底)+ **环检测**(identity seen 祖先集→`[Circular]`)+ 节点 upfront 建封顶 `_maxNodes=2000`(→`N more`)/`_maxVal=500`。branch=可折叠行(`AnInteractive` 可点 toggle + hover + 透 `expanded` 语义 + chevron `AnimatedRotation`),leaf=`Row[Flexible(key) | ': '/'  ' | Flexible(value 类型着色)]`(双 Flexible 独立省略、长 key 不吞 value;**空集合渲为 leaf 不假装可展开**)。**有界高滚动宿主**(SDK 确认 TreeSliver **不支持 shrinkWrap**——须父给高,同 AnPage;`CustomScrollView`+`ScrollConfiguration(AnScrollBehavior)` 隐条)。reduced→`toggleAnimationStyle: AnimationStyle.noAnimation`。**TreeSliver 回归 SDK+探针核实**:#153889 在 3.41.9 已修(仍用 noAnimation 作干净 reduced 路径)· #167928 经 IndentationType.none 化解(paint offset 0)· 研究期的 #178962 实为仍 open 的 #170757(快速双击 in-flight 动画 paint-null,探针未复现、§9 真机验)。**a11y**:容器 `Semantics(label「JSON tree, N items」)` + branch `expanded` 语义 + 行级 merge。i18n `tree.{invalidJson,circular,moreItems}` + `a11y.jsonTree`。类型色走 `SyntaxColors`(string→str / number→num / bool→kw / null→com+italic)。11 单测(渲染/折叠/类型/环/错误/showRoot/空/数组/注入/dedicated a11y/虚拟化滚动)+ matrix 四轴 + 截图验。G5.2 对抗复审 3 镜 16 项(SDK 源码逐字核实 + 6 探针、剔 1 误报)折入(2 HIGH a11y 容器 label + dedicated 测 · MED 空集合假分支/leaf 双 Flexible · LOW 截断色/null 斜体/注释纠正)。**G5.3 AnVersionDiff**（E3 版本 diff 原语）+ **AnCodeSurface**（共享代码框)——**单框 unified diff**(非双栏/非字符级):`before`/`after` 经 **`lineDiff`**(G5.0 LCS)→ ctx/add/del;add 软绿底(`okSoft`)+ ok 基色 / del 软红底(`dangerSoft`)+ danger 基色 / ctx 无底+muted。**行内着色走唯一 `highlightCode`**——基色经父 `TextSpan.style` 染、token span 覆盖、plain 继承基色(demo `.dl.add .ct{color:ok}`+`.cd-* override` 的 Flutter 等价,唯一高亮源铁律守住)。三列 `[行号 | 符号 | 代码]`:**行号=新文件逻辑行**(del 无号不 ++);**行号列统一固定宽**(`TextPainter` 测最大行号、floor `AnSize.trail`——逐行 floor 会让不同位数行错位,故算一统一宽,复审 LOW)。顶栏 range(mono tabular)+ note + +N/−N;`before` 空=最早版本整段 ctx;`bare` 去框。body 横滚(`IntrinsicWidth`+`minWidth` viewport=demo `.dl min-width:100%`)+ 纵向 scroll-host 双态 + **纵向呼吸内距**(上 s8/下 s12,在 IntrinsicWidth 内、行外→落白底无 tint,复审 MED)。**a11y**:容器 `Semantics(label「Diff, N added, M removed」)` + 行级 merge(`Semantics(excludeSemantics)` + 「Added/Removed: code」前缀,ln/sign 不单独念)。i18n `a11y.diff` + `diff.{added,removed}`。**性能**:无虚拟化 + 逐行 `IntrinsicWidth` 投机布局,面向短字段(`lineDiff` 退化闸接住跑飞、>5000 行上游截断)。**`AnCodeSurface`**(`core/ui`,**G5 第二消费者处抽出的共享代码框**,WRK-040「≥2 才抽」):DecoratedBox 发丝 `line` 边 + ClipRRect + `surface` 白岛;`bare` 去框 / `focused` accentLine 边(编辑态)。**AnCodeEditor 重构复用**(原私有 `_codeFrame` 删,13 测仍绿)。**无灰尖真因**=Flutter 把 uniform 边描成**单条连续 round-rect 路径**(非 CSS 4 斜接边在角叠加不透明度),故半透明 `c.line` 也无灰尖——`Border.all` 在此安全、demo 须 inset box-shadow(复审 MED 纠正原「实色边」误述)。**v1 仅单字段文本 diff**(Function.code/Agent.prompt/Control.when·emit/Approval.template);Handler 多部件 + 结构化多字段 diff 推迟 entities 版本视图 feature。11 单测 + matrix 四轴 + 截图验。G5.3 对抗复审 3 镜 9 项(SDK/demo 逐字核实 + 探针、剔 1 误报)折入(无 HIGH · MED 注释纠正 + body 纵距 · LOW 行号列统一宽/perf 注)。

## 5. 验收（套件每组 + 整体）

- **gallery**(`make gallery`,G1 立起):双栏目录复刻 demo `reference.html`,每组件**全态**做成 specimen。两条截图路(均非 `_test.dart`、不进自动门禁):**① 无头(`make shots`)**——`capture_{shell,gallery}.dart` 经 Skia `toImage` 逐类出 `test/dev/out/{shell,gallery_0..3}.png` 对照 demo 保真(强制 reduced 出确定性静帧;秒级,免 Xcode)。`toImage`/`toByteData` 是引擎线程真异步,在 `testWidgets` 的 fake-async zone 里 Future 永不解析→hang(flutter#49317/#50783),故包进 `tester.runAsync()`。**② 真跑(`make shot-app`,脚本 `test/dev/shot_app.sh`)**——构建+启动 gallery .app、osascript 读窗口 rect、`screencapture -R` 截运行中窗口 → `test/dev/out/app.png`,核对 **Impeller 真渲染 + OS chrome(红绿灯/真阴影)**(无头=Skia,渲染差异见会话 Impeller 调研;`BUILD=0` 复用已构建 app)。
- **matrix widget-test**(进 `fe-verify`):复刻 demo `matrix.mjs` —— 能 build / 受限栅格内不溢出 / 富文本转义安全 / 渲染存在 / disabled 键盘穿透 / **reduced-motion 轴**(每 specimen 在 `disableAnimations` 下 `pumpAndSettle` 须收敛——忘门控的循环卡死=FAIL);**+ 压力床**五电池(空/超长/海量/极值/注入)specimen。
- **工程纪律**:tokens 不硬编码(grep 门禁)· i18n 无硬编码中英文 · 层依赖 · S11 注释 · 本篇 1:1 同步。

## 6. codegen

slang:`dart run slang`(读 `slang.yaml` + `lib/i18n/*.i18n.json` → `strings.g.dart`)。产物入库(deterministic、fresh checkout 直接 analyze)。freezed/json 暂未引入——套件是纯 widget、`AnStatus` 是原生 enum,无需;待契约 DTO 层落地再接。
