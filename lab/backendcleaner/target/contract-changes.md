# 契约变更日志（驱动覆盖后的前端 / testend 兼容）

> 重写时每改一处对外契约（REST envelope / 路由 / SSE 事件 / error code），在此**追加一条**。
> 覆盖回 `backend/` 后，前端 + testend 照此表逐条施工——这是兼容工作的**唯一施工图**，记糙了兼容就会乱。
> 现有契约有 AI 瞎改的部分，把它改对**也要记**（"为什么"注明"修正 AI 瞎改"）。

## 记录格式

每条：模块/波次 · 类型 · 原契约 · 新契约 · 为什么 · 前端&testend 受影响点。

| # | 模块/波次 | 类型 | 原契约 | 新契约 | 为什么 | 前端/testend 受影响点 |
|---|---|---|---|---|---|---|
| 1 | errors（M0.4 R0012） | error code + auth header | code `UNAUTH_NO_USER`；header `X-Forgify-User-ID` | code `UNAUTH_NO_WORKSPACE`；header `X-Forgify-Workspace-ID` | user→workspace 全局正名 | 前端 401 拦截/重选逻辑判的 code；`localStorage.activeUserId`→`activeWorkspaceId`；请求头改名（**✅ M0.7 auth middleware 已落地**：`HeaderWorkspaceID` + `UNAUTH_NO_WORKSPACE` + `?workspaceID=` for SSE；code 在 errors 定型） |
| 2 | SSE 三流（M0.4/M0.5） | **端点改名 + 数据结构全量重构** | 端点 `/api/v1/{eventlog,forge,notifications}`；三流异构事件（eventlog 5 强类型 message/block · forge 4 强类型 forge_* · notifications 通用 `{type,id,data}`） | 端点 `/api/v1/{messages,entities,notifications}`；**统一「流式树」协议**：`Envelope{seq,scope,id,frame}`，frame ∈ open/delta/close/signal，node 为 `{type,...}` 判别联合（见 `stream-protocol.md`） | eventlog→messages、forge→**entities**（全实体流式总线：创建/编辑流式内容 + 运行流式输出 + 双输出）；三流数据结构标准化统一、scope/id 升信封层 | 前端 SSE 消费层全改：EventSource URL、事件解析（按 `frame` + `node.type` 判别联合）、消息树递归渲染（open→delta→close）、entities 面板消费、双输出按共享 node id 关联；testend 三流 SSE 断言全改 |
| 3 | workspace（M1.1 R0018） | **端点改名 + 字段重构 + error code** | 端点 `/api/v1/users{,/{id},:activate}`；字段 `username`(slug,唯一)+`displayName`；error `USER_NOT_FOUND`/`USERNAME_*`/`CANNOT_DELETE_LAST_USER`/`LANGUAGE_INVALID` | 端点 `/api/v1/workspaces{,/{id},:activate}`；字段 `name`(自由展示名,唯一,去 displayName+去 slug)；error `WORKSPACE_NOT_FOUND`/`WORKSPACE_NAME_REQUIRED`/`WORKSPACE_NAME_TOO_LONG`/`WORKSPACE_NAME_CONFLICT`/`CANNOT_DELETE_LAST_WORKSPACE`/`WORKSPACE_LANGUAGE_INVALID` | user→workspace 全局正名；slug（旧作文件路径）退场 → name 自由展示名 | 前端 workspace CRUD/picker/onboarding：URL、字段 username/displayName→name、错误码判定、API 客户端 createUser→createWorkspace；testend workspace 断言全改 |
| 4 | apikey（M1.2 R0019） | **字段重构 + error code + `:test` 返回瘦身** | 字段含 `userId`、`modelsFound`(解析后)、`isDefault`；error `INVALID_PROVIDER`/`BASE_URL_REQUIRED`/`API_FORMAT_REQUIRED`/`KEY_REQUIRED`/`API_KEY_NAME_CONFLICT`/`API_KEY_PROVIDER_NOT_FOUND`；`:test` 返 `{ok,message,latencyMs,modelsFound}` | 字段 `userId`→隐藏(workspace 隔离)、`modelsFound`删→`testResponse`(后端原始返回，不外露)、删 `isDefault`；error 统一 `API_KEY_*` 前缀(`API_KEY_INVALID_PROVIDER`/`API_KEY_VALUE_REQUIRED`/`API_KEY_BASE_URL_REQUIRED`/`API_KEY_API_FORMAT_REQUIRED`/`API_KEY_DISPLAY_NAME_CONFLICT`，删 `API_KEY_PROVIDER_NOT_FOUND`)；`:test` 返 `{ok,message,latencyMs}` | apikey 收窄：选 key / 解析下放；端点本身不变（`/api-keys`+`/providers`）| 前端 key 管理：**「有哪些模型」改从 model 模块 `/model-capabilities` 拿**（不再从 `:test` 的 modelsFound）；error code 改判；删 isDefault UI；testend apikey 断言改 error code + `:test` 返回体 |
| 7 | permissions/hooks/settings（M1.9 R0027 判定解散） | **端点删 + error code 删 + 文件砍** | 端点 `/api/v1/settings{,/limits,:reload}` + `/api/v1/permissions/{tools,test}`（7 个）；error `INVALID_SETTINGS`/`BLOCKED_BY_RULE`；`settings.json` 文件 | 全删——permissions/hooks/settings 整个解散 | 中央配置门控是错误抽象（Forgify 单人本地闲置：交互嫌烦 bypass / 无人值守没人答 ask）；危险控制该工具自管、limits 用默认、hooks 砍、settings.json 砍 | 前端/testend：删 testend Permissions.tsx + 所有 `/settings`·`/permissions` 调用；危险控制 UI（若有）改对接工具层（波次2）；limits 不再可配 |
| 8 | document（M1.10 R0028） | **隔离改名 + attach 砍子树 + 事件名 + 适配器对齐** | `userId` 隔离；`AttachedDocument` 含 `includeSubtree`（子树自动注入）；SSE 事件 `document.updated` `{id, action:"content_updated"}`；catalog source 带 Granularity/InvokeTool/Category | `workspaceId` 隔离（orm 自动）；`AttachedDocument` **去 includeSubtree**（只注入单篇、不展开子树）；事件 `document.created/updated/moved/deleted` `{documentId,path,parentId?}`；catalog source 对齐新 `Item`（去 Category）、wikilink→`link` 边（4 动词） | 去 GORM + workspace 隔离；子树注入炸 context 故砍；三/四适配器对齐前三模块收窄后的新接口 | 前端：文档树 user→workspace；挂载 UI **去 includeSubtree 选项**；事件订阅改 `document.*` 多动作；`:iterate` 留波次 6 |
| 6 | sandbox（M1.8 R0026） | **路由重构 + runtime 新增 + 事件升格** | env destroy `POST /sandbox/envs/{id}:destroy`；runtime destroy `POST /sandbox/runtimes/{id}:destroy`；`POST /sandbox/{action}`（`:gc`/`:retry-bootstrap`/`runtimes:install`，前导冒号 hack）；SSE 事件 `sandbox_env`.`env_rebuilt` `{id,status,error?}` | RESTful + N5：`POST /sandbox/runtimes`（装）、`DELETE /sandbox/runtimes/{id}`、`DELETE /sandbox/envs/{id}`、`POST /sandbox:gc`、`POST /sandbox:retry-bootstrap`、`/conversations/{id}/sandbox-envs/{kind}:reset`；**新增 docker runtime**（三 runtime）；事件升格 notification `sandbox.env_status_changed`/`sandbox.env_deleted` `{envId,status,ownerKind,ownerId,errorMsg?}` | 去 GORM + ResolveExec 抽象 + Python+Node+Docker（registry 调研 92%+缺口）；hacky 路由→规范；通知接 R0024 体系 | 前端 sandbox 管理 UI：runtime install/destroy 改 RESTful、gc/retry 改 `:action` 字面、env 通知改订 `sandbox.env_*`；testend sandbox 路由 + 事件断言全改 |
| 5 | model（M1.3 R0020）| **端点删/增 + 字段 + error code** | `model-configs` 端点(GET/PUT `/api/v1/model-configs/*`)+表存在；`model-capabilities` 返 `modelsFound`；workspace 无默认模型字段 | 删 `model-configs` 端点+表；新增 `PUT /api/v1/workspaces/{id}/default-models/{scenario}`(body ModelRef) + workspace 响应/表 +3 默认字段(`defaultDialogue`/`defaultUtility`/`defaultAgent`)；`model-capabilities` 重写返 `[{apiKeyId,keyName,provider,modelId,...,knobs[]}]`(knobs 各家原生 key/值，去 modelsFound)；新增 error `MODEL_SCENARIO_INVALID`/`MODEL_NOT_CONFIGURED`/`MODEL_REF_INVALID`；apikey anthropic 探针内部改打 `/v1/models`(envelope 不变) | model 收窄为聚合薄层、各家原生旋钮零中立抽象、默认选择搬 workspace 偏好 | 前端：模型选择 UI 改从 `model-capabilities` 拿 `knobs` 通用渲染(各家原生)、默认模型走 workspace `default-models` 端点、删所有 `model-configs` 调用、读 workspace 响应的 3 默认字段；testend：删 model-configs 断言、加 default-models + 新 capabilities 结构 + `MODEL_*` error 断言 |
| 9 | todo（M1.10 后 M1.11 R0029） | **重铸为 TodoWrite + 工具/字段/错误码/前端全翻** | 4 工具 `TodoCreate/List/Get/Update`（逐条 CRUD）；item 7 字段含 `blocked_by/owner/metadata/description`+逐项 `td_` id；SSE `todo` 入 **notifications**（前端 handler 返 `[]`=假看板）；testend 调死链 `GET /conversations/{id}/todos`（后端无此端点）；error `TODO_NOT_FOUND`/`TODO_SUBJECT_REQUIRED`/`TODO_INVALID_STATUS` | **1 工具 `TodoWrite`**（整列替换，波次 2/3 接）；item 3 字段 `content/activeForm/status`(3 态)；新增**只读** `GET /api/v1/conversations/{id}/todos?subagentId=`；live 改走 **messages 流** `signal`（`node.type=todo`、scope=conversation、payload `{conversationId,subagentId?,todos}`）；**删 3 错误码**（校验只回 tool-result 串、不冒泡 HTTP）；删 `td_`（PK=`scope_id`=对话/subagent 多态键） | 旧是 CRUD 数据库错抽象：注入没接（LLM 看不见自己清单）、前端看板假（返 `[]`）、testend 调死链。重铸对标 Claude Code TodoWrite：双层可见（工具回显 + 每轮 reminder 注入）+ messages 流 live + REST 初值；subagent 各自一张清单、不污染父看板 | 前端：删旧（无真消费——`todo` 通知 handler、误名的 TodoTab 实为 Ask tab）、**建真任务看板**（GET 初值 + 订 messages `todo` signal、按 `subagentId` 嵌子树）；testend：死链 `/todos` 改打新只读端点 + 断言 `{conversationId,subagentId,todos}`；写入全走 LLM `TodoWrite`、无前端写路径 |
| 10 | tool（波次 2 · M2.1 R0030） | **工具接口瘦身 + 危险字段重构（LLM-facing schema + 前端气泡）** | Tool **9 方法**（含 `CheckPermissions`/`IsReadOnly`/`NeedsReadFirst`/`RequiresWorkspace` + `PermissionMode` 4 模式 + `PermissionResult`）；注入字段 `summary` + `destructive`(boolean) + `execution_group` | Tool **5 方法**（`Name`/`Description`/`Parameters`/`ValidateInput`/`Execute`，删 4 个权限/死 flag + 整套 PermissionMode/Result）；注入 `summary` + **`danger`(枚举 safe/cautious/dangerous)** + `execution_group`；危险 = LLM 逐次自报 + 逐次确认（无中央配置门控） | M1.9 解散中央权限门控落到工具层：`CheckPermissions`/4 模式/3 死 flag 全仓无消费者（仅已解散的 permissionsgate 用过 IsReadOnly）；`destructive` boolean → `danger` 三级更细；懒加载（Toolset resident/lazy + activate_tools）**保留**——与 catalog 实体菜单正交，解决「未来巨大 tool description 烧 token」 | 前端：工具调用气泡读 **`danger`**（三级显示 + `dangerous` 弹确认等用户同意）替代旧 `destructive` boolean；LLM 注入的工具 schema 多 `danger` 枚举字段（必填）；删一切 permissions/settings UI（已随 M1.9）；testend 工具断言改 `danger` 字段 |
| 16 | toolset 工具发现机制（波次 2 · M2.3#4 R0036） | **activate_tools 按类激活 → search_tools 检索式懒加载** | LLM 用 `activate_tools(category)` 激活整组工具(function/handler/…)、整组 schema 加载并锁定整对话；激活状态按 category 记 | LLM 用 **`search_tools(query)`** 按需检索 → 返命中工具完整定义、按**工具名**记 discovered；lazy 工具默认只在 system prompt 出"一行概览"(name+一句话)、完整 Parameters 按需取；与 catalog(实体名录)正交互补 | 2026 调研(对抗核实):按类激活是 legacy(整组锁定/粗粒度/仍 bloat)，业界收敛检索式；模型自拟 query 比按类选高 20-30 点 | 前端：工具气泡多 `search_tools` 一个常驻工具；lazy 工具默认不在工具列表(LLM search 后才出现)；**无 activate_tools UI**；testend：工具断言去 activate_tools、加 search_tools |
| 15 | web（波次 2 · M2.3#3 R0035） | **WebSearch 去 MCP tier + 去 provider 遍历 + 接 SearchKeyPicker；danger 自报** | WebSearch 三阶（BYOK 遍历 `SearchProviderPriority` → MCP tier 代理 duckduckgo → 引导）；工具 schema 带 `destructive`(boolean)；旧 web.md 把 MCP 当"搜索备选物理引擎" | WebSearch **二阶**（单把 `default_search_key_id` BYOK → 引导）；**删 MCP tier**（MCP 搜索走独立 `tool/mcp` 平级工具）；工具注入 `danger`(枚举,LLM 自报)替 `destructive`；返回 JSON 形状不变 `{query,source,results:[{title,url,snippet}],truncated}` | MCP 搜索代理是错误抽象（冗余 + web→mcp 越界）；provider 自动遍历乱烧钱 → 单把显式 key（R0034）；danger 对齐 #10 | 前端：WebSearch 结果渲染不变；工具气泡 `danger` 替 `destructive`（已随 #10）；**无 MCP 搜索 fallback UI**（装了搜索 MCP 走 tool/mcp 独立工具）；testend：web 工具断言去 MCP tier、`danger` 字段 |
| 14 | workspace 搜索配置（波次 2 · M2.3#3 前置 R0034） | **workspace 加默认搜索 key + 新端点 + 建 domain/websearch** | 无独立搜索配置（旧 WebSearch 靠 apikey `SearchProviderPriority` 自动遍历 brave/serper/tavily/bocha 找 key） | workspace 加 `default_search_key_id` 列；新端点 `PUT/DELETE /api/v1/workspaces/{id}/default-search`（body `{apiKeyId}`）；建 `domain/websearch`（provider 词表 + `SearchKeyPicker` 端口）；workspace 响应多 `defaultSearchKeyId` 字段 | M1.2 收窄 apikey「选 key 显式化、防乱烧钱」——搜索 key 不再系统自动遍历，用户显式选**一把**（provider 由 key 隐含）；这是 M1.2 主动推迟的"搜索配置"现在补上，对齐 model 的 default-models | 前端：Settings 加"默认搜索 key"选择器（从 `category=search` 的 apikey 单选），调 PUT/DELETE default-search；读 workspace 响应的 `defaultSearchKeyId`；testend：workspace 断言加 default-search 端点 + 字段 |
| 13 | search + 文件工具 path 语义（波次 2 · M2.3#2 R0033） | **新增 search 工具集 + 全文件工具 path 改「绝对 + `~`、无 cwd」** | 旧 search 2 工具 `grep_search`/`glob`（文档假名，实际 Grep/Glob），path 省略默认 `os.Getwd()`（进程 cwd）；旧 search.md 讲 `Permissions`/`protectedPaths` + 多线程扫描 + `SearchBlock` SSE + 5 虚构 sentinel | search **3 工具 `LS`/`Glob`/`Grep`**（LS 新增列目录）；**所有文件工具（search 3 + filesystem 3）path 必填、支持 `~`（`fspath.Expand` 展开 os home）、永远绝对、无 cwd 默认**；Grep 双后端（rg/stdlib）；`output_mode`(content/files_with_matches/count) + `-A/-B/-C` + `-n` + `-i` + `multiline` + `head_limit`；danger LLM 自报工具不碰；无 HTTP/DDL/error code | 桌面 agent 无项目根/无 cwd（全电脑范围、靠交互定位）；照搬 Claude Code `os.Getwd()` 默认在桌面 app = 搜 app 安装目录、错；cwd 概念全局废弃 | 前端：tool_call 气泡多 `LS` 工具（列目录渲染），无新 UI；testend：search 断言用 `LS`/`Glob`/`Grep`（旧假名 `grep_search`/`glob` 若有断言改掉）、path 参数断言改「必填、绝对或 `~`」、Grep 各 flag 断言；**无 error code 断言**（软返串如 `No matches`/`denied by safety guard`/`must be absolute`） |
| 12 | filesystem（波次 2 · M2.3#1 R0032） | **工具名 + 文档整篇重写**（无 HTTP 端点、无 error code、无 DB 表） | 工具名 `fs_read`/`fs_write`/`fs_edit` 等假名（文档错写）；文档列 `ErrPathForbidden`/`ErrPathOutside`/`ErrFileSyncError`/`ErrNotFound`/`ErrIsDirectory` 5 个 sentinel + HTTP 状态 + wire code（虚构——工具失败不冒泡 HTTP）；旧文档讲 `settings.json/protectedPaths` 鉴权（M1.9 已砍）、`IsReadOnly` flag（M2.1 已砍）、1MiB 读上限 + 二进制 base64（虚构）；`forgify.db` 禁区描述（实际 `~/.forgify/`） | 工具名 `Read`/`Write`/`Edit`（首字母大写、`Edit` 替代 `fs_edit` 等假名）；**无 error code**（工具失败软返 tool-result 串给 LLM）；**无 DB 表**；**无 HTTP 端点**；危险路径靠 `pathguard.Allow`（读+写共用 deny）/`AllowWrite`（再 ∪ `.git/`/`.env*`/`node_modules/` 等写专属）两级守卫——R0003 已铺好、本轮 filesystem 消费；写前必读靠 `pkg/agentstate.SeenFiles`（new pkg） + ctx 携带 + fail-closed | 旧文档腐烂（M1.9/M2.1 已砍 + 虚构上限/sentinel/工具名）+ R0003 升级两级守卫消费 | testend：若有 `fs_*` 工具名断言改大写首字母；若断言 filesystem 错误码（HTTP）→ 改断 tool-result 返串前缀（如 `File not found:` / `denied by safety guard:` / `must be read first` 等）；前端：tool_call 气泡显示 filesystem 工具按 `Read/Write/Edit` 三种渲染（无新 UI 元素） |
| 11 | loop / messages（波次 2 · M2.2 R0031） | **SSE 消息流数据结构（messages 流 node 词表）+ tool_call danger** | eventlog block 协议：`EmitBlockStart(attrs)`/`DeltaBlock`/`StopBlock`；block type 含 `progress`/`message`；tool_call attrs 带 `destructive`(boolean) | messages 流 `open/delta/close`；node.type ∈ `{text,reasoning,tool_call,tool_result}`（**砍 progress/message**，嵌套走 `Open.ParentID`）；node content 形状（text `{content}` / reasoning `{content,signature}` / tool_call `{name,arguments,summary,danger}` / tool_result `{content}`）；**close 带 result 快照**（delta ephemeral 不入 buffer）；tool_call 节点带 `danger`(三级)替 `destructive` | loop 重写接 stream 统一协议（eventlog→messages，词表下放 producer=loop）；danger 纯标记（对齐 #10、#2）；嵌套层级走 stream `Open.ParentID` 故砍 progress/message 块型 | 前端消息树渲染从 eventlog block 协议改 **messages 流**（按 frame + node.type 判别、open→delta→close）；tool_call 气泡读 node content 的 `danger`/`summary`（对齐 #10）；砍 `progress`/`message` 块型渲染（subagent 子树改按 stream `parentId` 嵌套）；testend messages 流断言全改 |
| 17 | function（波次 3 · M3.1 R0037） | **版本模型去 accept 状态机 + polling 剥离 + 端点/字段/错误码/triggered_by 重构** | 版本 `status`(pending/accepted/rejected) + 用户 accept 转正；端点 `GET /functions/{id}/pending` + `pending:accept` + `pending:reject`；`kind`(normal/polling) + `polling_interval` + `set_kind`/`set_polling_interval` ops；`triggered_by` ∈ chat/workflow/http/test；execution 带 `hints`(OutputEmpty/SignificantlySlower) + aggregates(p95/avg/各状态计数)；错误码 `FUNCTION_PENDING_NOT_FOUND`/`_RUN_FAILED`/`_AST_PARSE_FAILED`/`_ENV_FAILED`/`_DEPENDENCY_RESOLUTION` | **线性版本 + 自由 active 指针，无 accept**（create/edit 立即生效、写 `v(max+1)`；revert 纯移指针不删"更新的"版本）；**删 pending 三端点**；**polling 整个剥离**（删 kind/polling_interval/2 ops——polling 触发源是独立概念）；`triggered_by` ∈ **chat/agent/workflow/manual**（执行体轴）；execution **砍 hints**、aggregates 只 `{okCount,failedCount}`；错误码删 5、加 `FUNCTION_INVALID_CODE`、`FUNCTION_OP_INVALID` 改 422；create/edit 结果带 `envFixAttempts`（env 自愈历史）；新 `fnenv_` 前缀登记 | 版本号简化去 accept 断点（用户拍板）；polling 独立化（不寄生 function）；env-fix 抽共享 `app/envfix`（function/handler/trigger 复用）；execution 去审计剧场（hints/p95 无人真消费） | 前端：**删 pending UI**（accept/reject 按钮 + pending 草稿区）；版本列表显示 active 可能非最新号（诚实标"当前 vN，之后还有…"）；**删 polling kind 选项 + 间隔配置**；`triggered_by` 筛选值改 chat/agent/workflow/manual；execution 详情去 hints；错误码改判（删 5 + 加 INVALID_CODE）；create/edit 结果读 `envFixAttempts` 展示自愈；testend：删 pending:accept/reject + GET pending 断言、kind/polling 断言、改 triggered_by 值、错误码断言、execution 结构去 hints |
| 18 | handler（波次 3 · M3.2 R0038） | **进程生命周期重构（MCP 式单例常驻）+ 版本去 accept + 端点/字段/错误码/triggered_by 重构 + 加 :restart** | per-owner 实例（`map[Owner]map[name]`，两套 call：chat 即起即杀=无状态 / workflow registry 复用）；版本 status pending/accept + 用户转正；端点 `GET pending`+`pending:accept`+`pending:reject`、config `POST`；`triggered_by` ∈ chat/workflow/http/test；错误码 `HANDLER_PENDING_NOT_FOUND`/`_INSTANCE_NOT_FOUND`/`_ENV_FAILED`/`_AST_PARSE_FAILED`/`_CONFIG_INVALID`、`_INSTANCE_CRASHED`(422)/`_INSTANCE_RPC_TIMEOUT` | **MCP 式单例常驻**（每 handler 一个进程：boot 开局起 / restart 重置 / shutdown 退出关；chat/agent/workflow 共享同一实例 + 状态——真有状态）；**方案 A 版本**（无 accept，删 pending 三端点）；config 端点改 `PUT`（merge patch + 重启实例）+ **新增 `POST :restart`** + **`restart_handler` 工具**（对话内"坏了重启"）；`triggered_by` ∈ **chat/agent/workflow/manual**；handler 响应新增 `runtimeState`(running/stopped/crashed) + `configState` + `missingConfig`；call 审计砍 hints；错误码删 5、加 `HANDLER_INVALID_CODE`、`HANDLER_INSTANCE_CRASHED`→`HANDLER_CRASHED`(502)、`_INSTANCE_RPC_TIMEOUT`→`HANDLER_RPC_TIMEOUT`、spawn 失败 422→502、OpInvalid 400→422；新 `hdenv_`/`hdi_` 前缀 | 旧 per-owner + 两套 call 自我矛盾（chat 即杀=有状态类退化无状态）、生命周期机器过重（lazy/crash/race/owner-teardown/文档吹的 idle-GC）；MCP 模型 3 动词清爽 + handler 真有状态；**restart 是有状态服务的「重置按钮」**（crash 重生救不了"活着但状态坏了"，用户拍板加 LLM 工具）；env-fix 复用 `app/envfix` | 前端：**删 pending UI**；handler 卡片显示 `runtimeState`（运行/停止/崩溃状态点）+ `configState`（缺哪些 init-args）；config 改 `PUT`；**加 `:restart` 按钮 + `restart_handler` 工具气泡**；`triggered_by` 筛选值改；错误码改判（`HANDLER_CRASHED`/`HANDLER_RPC_TIMEOUT` 等）；testend：删 pending 断言、config POST→PUT、加 :restart、改 triggered_by 值 + 错误码 + 加 runtimeState/configState 字段断言 |
| 19 | trigger（波次 3 · M3.3 R0039） | **trigger 从 workflow 节点提升为独立实体 + polling→sensor(CEL) + 引用计数生命周期 + 3 表** | trigger 是 workflow 图内节点 `(workflowId, nodeId)`、无独立 id、端点在 `GET /workflows/{id}/triggers`；polling 寄生 function（active version `kind=polling` + `functionRef`，poll→`{events,nextCursor}`）；旧表 `trigger_schedules`+`trigger_firings`+`polling_states`（gorm）；前缀 `ts_`/`tfi_`；触发只能启宿主 workflow | **独立实体**（`trg_`，地位同 function：进 catalog + relation **第 9 节点** + **8 个 LLM 工具**）；**4 source** cron/webhook/fsnotify/**sensor**（删 manual——手动跑是 workflow 自己的能力）；**sensor** = 绑 function 或 handler.method（看 active 版本）+ `intervalSec`(≥5) + CEL `condition`/`output`（替旧 events/nextCursor 协议；要状态绑 handler）；**引用计数生命周期**（Attach/Detach：≥1 个 active workflow 引用才起 listener，N workflow 共享一个 listener，fire 扇出多条 Firing）；**3 表** triggers/trigger_firings/trigger_activations（`trg_`/`trf_`/`tra_`）；**Activation 动作日志**（触没触发都记 return_value/detail，"为什么没触发"可查）；独立端点 `/triggers` + `:fire` + `/activations`；durable firing 保留（persist-before-act + 单事务 claim ADR-021，claim 留波次 4）；CEL 抽共享 `pkg/cel`（workflow 节点控制复用）| trigger 独立可复用（多 workflow 共享一个 cron listener，不再每 workflow 画一个重复 listener）+ 独立观测面 + LLM 一等公民锻造；polling 填坑（M3.1 剥离后无家）→ sensor 用 CEL 通用化、绑 handler 得状态；Activation 让"为什么没触发"可查；manual 不该占 source（不监听任何东西）| 前端：**新 trigger 列表/详情页**（`refCount`/`listening` 状态点）；sensor 配置 UI（targetKind function/handler + targetId + intervalSec + CEL condition/output）；**activation 日志查看**（"为什么没触发"：return_value + detail）；8 工具气泡；workflow→trigger 监听关系 UI 随波次 4 workflow；删旧 workflow 节点内嵌 trigger 编辑；testend：trigger CRUD/`:fire`/activation 断言、sensor CEL（触发/不触发/出错）、引用计数共享 listener |
| 20 | skill（波次 3 · M3.4 R0040） | **重写为文件式指令载体 + 砍 execution/search + 端点/错误码重构** | 端点 `:import`+`:refresh`+`/{name}/body`+`{nameAction}`(:invoke)；旧 skill.md 讲 512KB body / 扁平 `*.md` / 假占位符 `$USER_ID`；`skill_executions` 表(`ske_`)+ execution 查询工具；search_skills(LLM rerank)；常驻 1s 轮询热重载 | 端点删 `:import`/`:refresh`/`/body`、`:invoke`→`:activate`；**5 工具**(activate/get/create/edit/delete，**无 search_skills、无 execution 查询**)；**纯按需扫描**(无轮询/缓存/fingerprint)；**无 skill 表、无 execution 审计**(纯文件式)；目录式 `skills/<name>/SKILL.md`(32KB)；allowed-tools=**预授权**(非限制)；**AI 可创建**(source=ai)；disable-model-invocation 实装；错误码 `ErrSkillNotFound`→`ErrNotFound`(wire 不变)、`SKILL_INVALID_NAME` 422→400、加 `SKILL_FORK_REQUIRES_AGENT`(422)/`SKILL_SUBAGENT_UNAVAILABLE`(503)、删 `SKILL_EXECUTION_NOT_FOUND` | skill 本性是 memory 近亲(文件式注入物)非 function 近亲(执行实体)；execution/search 对 skill 是抽象错配(校验剧场)；对齐 memory 范式最干净 | 前端：skill 管理 UI 去 import/refresh/body、`:invoke`→`:activate`；**无 execution 日志、无 search_skills**；编辑器去版本概念(编辑即覆盖)；fork skill 需波次 5 subagent；testend：skill 断言去 `:import`/`:refresh`/`:invoke`/execution、改错误码、用 `:activate` |
| 21 | mcp（波次 3 · M3.5 R0041） | **重写为 GitHub registry 全量市场 + 加密表 + stdio/remote 双 transport + 容器实体 catalog；砍 mcp.json/审计/health/search** | mcp.json 文件存储；硬编码 24 curated；`mcp_calls`(mcl_)+`mcp_health_history`(mch_) 两表 + 审计/health-history 端点；`call_mcp_tool`/`search_mcp_tools`/`get/search_executions` 工具；端点 `:import`(multipart)+health-history；catalog 合成 server 描述 | **`mcp_servers` 表**(mcp_，`config_enc` 加密 {env,headers})；market = **对接 GitHub registry API 99 全量**(embed snapshot + cache 兜底)；transport **stdio(sandbox IOTransport)+sse+streamable-http**；**catalog 报 server+全工具名**(Item.Members 新字段)；**4 系统工具** list_mcp_marketplace/install_mcp_server/uninstall_mcp_server/**reconnect_mcp**(重置,对齐 restart_handler) + 动态工具 `mcp__server__tool`(进 search_tools 检索池、不进 Overview)；端点 GET/PUT/DELETE `/mcp-servers/{name}`+`/stderr`+`:reconnect`+`tools/{tool}:invoke`+`:import`(JSON)+GET `/mcp-registry`+`:install`；10 错误码 `MCP_*`；删 `mcl_`/`mch_` | mcp 是协议网桥；硬编码市场→对接官方 registry(99 全量、自动更新)；审计/health/search 是校验剧场(承 skill 那轮先例)；进程池单例 + 加密 config 对齐 handler；工具平级暴露(R0035 定调) | 前端：MCP 页改「server 列表(实时 status)+市场(99 条 install,填 env)+stderr 查看+`:reconnect` 按钮+试调用」；删 health-history 图表/审计日志/search_mcp UI；danger 三级(随 #10)；testend：mcp 端点断言全改(server CRUD/registry/install/reconnect/tools:invoke)、删审计/health 断言、错误码 `MCP_*` |
| 22 | sandbox runtime-tool 解析（波次 3 · M3.5 R0042 留口1 补做） | **ResolveExec 认 runtime 自带包运行器（npx/uvx/dnx）+ PATH 注入 runtime bin + 第 4 runtime dotnet + docker 空-Cmd 走 image entrypoint**（无 HTTP 端点 / 无 error code / 无 DB 表——纯 sandbox 内部 spawn 解析） | R0026 三 runtime(python/node/docker)，`ResolveExec` 只把裸名解析进 per-owner env（venv / node_modules/.bin / docker run）；空-Cmd 一律拒；runtimeRef 相对 sandboxRoot；无 dotnet | **node** ResolveExec 认 `npx/npm/node/corepack`→`<runtimeRef>/bin/<cmd>`；**python** 认 `uvx/uv`→`EnsureTool("uv")` 同目录(python 本就 uv-backed、uv 早在)；**新增 dotnet 第 4 runtime**(`DotnetEnvManager`：CreateEnv/InstallDeps no-op、`dnx/dotnet`→`<runtimeRef>/dnx` 顶层)；**docker 空-Cmd 放行**(image entrypoint 无命令)，非-docker 传**绝对 runtimeRef**；非-docker spawn **PATH 前插** `<rt>/bin`+`<rt>`(npx shebang `#!/usr/bin/env node` 需 node 可寻) | R0041 mcp stdio transport 用 `SpawnLongLived` 起 MCP server 进程，但其启动方式 `npx -y <pkg>`/`uvx <pkg>`/`dnx <pkg>` 是 runtime 自带运行器、不在 env 内——R0026 ResolveExec 不认；真机 e2e(context7 via npx) 逼出 PATH 注入硬集成点；dotnet 覆盖 registry 最后 .NET 类(99/99) | **装配影响（非前端契约）**：M7 `cmd/server` 装配要**注册 dotnet RuntimeInstaller(mise `dotnet@`) + DotnetEnvManager**，且 node/python/docker/dotnet **四套 runtime 全注册**(现在 sandbox `envManagers` 无人填)。前端/testend：**无新对外契约**(sandbox runtime 注册纯进程内、不经 REST)；e2e_test(build tag `e2e`)真机才跑、离线 CI 不跑 |
| 23 | agent（波次 3 · M3.6 R0043） | **重写为「配置好的 LLM worker」+ 版本去 accept + invoke 端口注入 + search 子串 + execution 简化 + relation 5 出边 + 端点/字段/错误码/triggered_by 重构** | 版本 `status`(pending/accepted) + 用户 accept 转正 + `needsAttention`/`attentionReason` 列；端点 `GET /agents/{id}/pending`+`pending:accept`+`pending:reject`；`search_agents`(LLM 相关性排序)；execution aggregates 带 p95/avg；service 持 LLM 依赖(picker/keys/factory)；workflow `dispatch_agent` 裸 `config.prompt` 内联 loop 路径(不落表)；错误码 `AGENT_NAME_DUPLICATE`/`AGENT_NO_PENDING`/`AGENT_TOOLS_AGENT_REF_FORBIDDEN`(400)/`AGENT_INVALID_MODEL_OVERRIDE`(400)；`triggered_by` 含 http/test | **线性版本 + 自由 active 指针，无 accept**(create/edit 立即生效写 `v(max+1)`；revert 纯移指针)；**删 pending 三端点 + 去 `needsAttention`/`attentionReason`**；`search_agent` 改 **name/desc/tags 大小写不敏感子串**(无 LLM rerank)；**invoke 三依赖走 `InvokeDeps` 端口注入(DIP)**：`LLMResolver`/`ToolsProvider`(全局池按白名单过滤)/`KnowledgeProvider`(doc 前缀)——service 不再持 LLM 具体依赖；execution aggregates 只 `{okCount,failedCount}`(去 p95/avg)；`triggered_by` ∈ **chat/workflow/manual**(无 agent——员工不调员工)；**invoke 是唯一执行入口**(invoke_agent 工具 / `:invoke` / workflow agent 节点都经它，落 `agent_executions`)；relation **5 出边**(全 KindEquip，OtherKind 区分 fn→Function/hd→Handler/mcp→MCP/knowledge→Document/skill→Skill，无 agent→agent) + forged 入边(create v1/edit v>1)；outputSchema 三态 free_text/enum/json_schema(enum 注入 + coerceEnum 规整)；错误码 `AGENT_NAME_CONFLICT`/`AGENT_TOOLS_AGENT_REF`(422)/新增 `AGENT_TOOL_REF_BLANK`(422)/`AGENT_INVALID_MODEL_OVERRIDE` 改 422；9 工具(search/get/create/edit/revert/delete/invoke_agent/search_agent_executions/get_agent_execution，**无 accept 工具**) | agent 是「按引用挂载六件、跑 ReAct」的配置载体非执行实体；版本去 accept 断点对齐 function R0037/handler R0038；invoke 端口注入隔离 LLM 依赖(agent 自己不拥有 model/tool/knowledge)；search 子串对齐 backend-new 统一范式(LLM rerank 是校验剧场)；execution 去审计剧场(p95/avg 无人消费)；pending/attention 整套删 + CheckPermissions(M1.9 残留)删 + dispatch_agent 内联 prompt 路径删 | 前端：**删 pending UI**(accept/reject 按钮 + pending 草稿区 + needsAttention 徽标)；版本列表显示 active 可能非最新号(诚实标"当前 vN，之后还有…")；`triggered_by` 筛选值改 chat/workflow/manual；execution 详情/聚合去 p95/avg(只 okCount/failedCount)；错误码改判(`AGENT_NAME_CONFLICT`/`AGENT_TOOLS_AGENT_REF`/加 `AGENT_TOOL_REF_BLANK`、删 `AGENT_NO_PENDING`)；agent 锻造 UI 挂载六件(prompt/skill/knowledge/tools/outputSchema/modelOverride)；testend：删 pending:accept/reject + GET pending 断言、改 triggered_by 值、错误码断言、execution aggregates 去 p95/avg、search_agent 子串语义。**装配影响**：M7 `cmd/server` 装配 `agentstore.New + agentapp.NewService + SetRelationSyncer + SetInvokeDeps`(注入真实 LLMResolver/ToolsProvider/KnowledgeProvider) + catalog RegisterSource + relation Namers['agent'] + AgentTools 进 `Toolset.Lazy` + `NewAgentHandler.Register` + `db.Migrate` 收 `agentstore.Schema` |
| 24 | tool/memory·document·shell（波次 3 · M3.7 R0044） | **三工具适配器补齐（无 REST 端点，纯 LLM system tool）+ shell.md DOC-121 整篇重写纠正** | 旧 backend 工具：老 9 方法接口（`IsReadOnly`/`NeedsReadFirst`/`RequiresWorkspace`/`CheckPermissions`）；`write_memory` 带 `type` 四类（user/feedback/project/reference）；旧 shell = cwd 状态机（`cd` 拦截 + `AgentState.Cwd`）+ python/node auto-route + `permissionsgate` allow/deny；shell.md DOC-121 停在 2026-05-03（30s 超时 / `run_bash` / `allow-deny` permissions / 6 个 HTTP 错误码 `PERM_DENIED`·`SHELL_*`（从未进 error-codes.md）/ auto-route 当核心原理） | 三适配器实现 **5 方法接口**（framework 注入 summary/danger/execution_group）：**memory** read/write/forget_memory（write **无 type**、`source=ai` 内定、不暴露 `pinned`）；**document** 7 工具（`edit_document`→Service.`Update`、`delete` **砍 destructive**、errorsdomain 转软失败串）；**shell** Bash/BashOutput/KillShell + ProcessManager（**无 cwd**、危险**硬拦截 6 条**（rm -rf //~、sudo、mkfs、dd of=/dev/、fork bomb，非 allow/deny）、**auto-route 留波次5**、120s/600s 超时、256KB ring、`bsh_` 前缀内存态）；**shell.md 整篇重写**对标 filesystem.md（叶子工具/无端点/无错误码） | 补齐波次 3 工具适配器收官；**不能照抄旧**（memory 砍 type、shell 砍 cwd / auto-route / permissions 皆 backend-new 决策）；shell.md DOC-121 严重 stale（孤岛错误码连 error-codes.md 都没进）必须重写（原则#9） | **工具无 REST 端点 → 前端不直接调、testend 无端点断言**；shell.md 旧讲的 `run_bash` 端点 / 6 个 `SHELL_*`·`PERM_DENIED` 错误码**本就不存在**（旧 backend 也靠 permissionsgate、非 shell 域）→ 无需 testend 改动；**装配影响 M7**：`MemoryTools`/`DocumentTools` 进 `Toolset.Lazy`、`NewShellTools()` 进 `Toolset.Resident`、`Manager.Stop()` 关停回收；`bsh_` 前缀登记 database.md S15 |
| 25 | control（波次 4 前置 · R0045） | **新增 control 逻辑实体**（全新端点组，旧 backend 无对应） | （无——control 是 18-graph-model-redesign 重定型产物：workflow control 节点的逻辑物化成独立实体，旧 backend 把 case 逻辑内联在图里） | 新增 `/api/v1/controls` 端点组（CRUD + `:edit`/`:revert` + versions，**无 `:run`**）；8 个 `CONTROL_*` 错误码；relation 第 10 类 EntityKind `control`（前缀 `ctl_`）；6 个 `*_control` LLM 工具（Lazy）；database `control_logics`/`control_logic_versions` 两表 + `ctl_`/`ctlv_` 前缀 | 新实体的图模型地基（节点引用实体、统一心智）；workflow 波次 4 的 `control` 节点引用它、scheduler 经 `Resolve` 取 pin 版本 branches 求值 | **前端**：workflow 编辑器 `control` 节点 inspector「引用哪个 `ctl_` + 各 port 连线」（波次 4/7 装配后）；**testend**：controls 端点 CRUD + `CONTROL_*` 错误码断言（M7 总装配后可端到端）；**装配影响 M7**：`ControlTools` 进 `Toolset.Lazy`、`ControlHandler` 注册、catalog source + relation namer 注册、`controlstore.Schema` 进 migrate |

| 26 | approval（波次 4 前置 · R0046） | **新增 approval 渲染实体**（apf_/apfv_，全新端点组） | （无——18 重定型产物：旧 approval 节点的 prompt 模板 + 决策规则内联在图里，现物化成独立实体） | 新增 `/api/v1/approvals` 端点组（CRUD + `:edit`/`:revert` + versions，**无 `:run`**）；7 个 `APPROVAL_*` 错误码；relation 第 11 类 EntityKind `approval`（前缀 **`apf_`**，≠ `apv_`=运行时）；6 个 `*_approval` LLM 工具（Lazy）；database `approval_forms`/`approval_form_versions` 两表 + `apf_`/`apfv_` 前缀；**pkg/cel 加 `{{ CEL }}` 模板支持**（`Template`/`CompileTemplate`/`Render`，agent.prompt 波次 4 复用） | 18 图模型 5 节点 × 5 实体的第 2 个落地（control 后）；workflow 波次 4 的 `approval` 节点引用它、scheduler 经 `Resolve` 取 pin 版本 template + 规则、渲染 + park（apv_ 运行时表） | **前端**：workflow 编辑器 `approval` 节点 inspector「引用哪个 `apf_` + yes/no 连线」（波次 4/7）；**testend**：approvals 端点 + `APPROVAL_*` 断言（M7 装配后）；**装配影响 M7**：`ApprovalTools` 进 `Toolset.Lazy`、`ApprovalHandler` 注册、catalog/namer 注册、`approvalstore.Schema` 进 migrate |
| 27 | 全实体 I/O 统一（波次 4 前置） | **统一实体 I/O 到 `pkg/schema.Field`**（字段重命名/新增 + 类型删除 + DB 列 + CEL 根变量；新增 `internal/pkg/schema` 地基） | fn version `parameters []ParameterSpec` + `returnSchema map`（DB 列 `function_versions.parameters`/`.return_schema`）；hd `MethodSpec.args []ArgSpec` + `.returnSchema map`（methods JSON blob 内字段 `args`/`returnSchema`）；ag version `outputSchema`（三态 `OutputSchema{free_text\|enum\|json_schema}`，DB 列 `agent_versions.output_schema`，enum 硬约束 + coercion）；trg 无 outputs；ctl/apf `when`/`emit`/`template` 读 `payload`/`ctx`（无 inputSchema 声明） | **共享 `schema.Field{name,type,description}`**（极简：无 required/default/enum/嵌套，塑形交运行时 CEL；`,json` 存 `TEXT NOT NULL DEFAULT '[]'`）：fn/ag version + hd `MethodSpec` 改 `inputs`/`outputs`（DB `function_versions`/`agent_versions` 列 `parameters`+`return_schema`/`output_schema` → `inputs`+`outputs`；hd methods blob 内 `args`→`inputs`、`returnSchema`→`outputs`，**无列改名**）；trg +`outputs`（`triggers.outputs` 列声明扇出 payload 字段）；ctl/apf +`inputSchema`（`control_logic_versions`/`approval_form_versions` 加 `input_schema` 列），`when`/`emit`/`template` 改读 **`input.*`**；fn forge op `set_parameters`/`set_return_schema` → `set_inputs`/`set_outputs`；hd `add_method` 的 `method.args` → `.inputs`（+可选 `.outputs`，去 required/default）；ag config `outputSchema` → `inputs`/`outputs`（outputs 非空→指示答 JSON object，空→自由文本）；**pkg/cel 加第 3 根变量 `input`**（与 `payload`/`ctx` 并列）。**删** `ParameterSpec`/`ArgSpec`/`OutputSchema`/`OutputSchemaKind` 类型。mcp **不变**（tool `inputSchema` 仍 JSON Schema 供 LLM tool-calling，统一 Field 视图 wiring 时派生） | workflow 节点要统一读实体 Inputs（喂什么）/ Outputs（下游读什么）——5 节点 × 实体图模型的统一 I/O 地基；agent enum 路由退场、改由 control 实体读 agent 输出字段路由；ctl/apf 入参显式声明（inputSchema）+ CEL 统一 `input` 根，消除 payload/ctx 歧义 | **前端**：function/handler/agent 编辑器的参数/返回/输出 schema 表单全改为统一 `Field[]`（去 required/default/enum UI；agent 去三态 outputSchema 选择器）；trigger 编辑器 +outputs 字段；control/approval 节点 inspector +inputSchema 声明 + `input.*` 提示（波次 4/7 装配后）；**testend**：fn/ag create/edit body `parameters`/`returnSchema`/`outputSchema` → `inputs`/`outputs`，hd `add_method` `args`→`inputs`，trg +outputs、ctl/apf +inputSchema 断言；**无新端点 / 无新前缀**（仅既有实体 body + DB 列变更） |
| 28 | ctl/apf I/O 收尾 + mcp 转换器（波次 4 前置 · R0047 前） | 字段改名 + 删冗余 `outputs` + pkg/schema 加转换器 | （#27 落地）ctl/apf version `input_schema` 列 + `inputSchema` 字段；其后试加了对称 `outputs` 列/字段 | ctl/apf `input_schema`→`inputs`（`InputSchema`→`Inputs`，与 fn/hd/ag/trg 命名对齐）；**删 ctl/apf 的 `outputs` 列/字段**——输出派生不存储：control 读各分支 `emit` 的 keys（per-port，比并集准），approval 恒为 `{decision,reason}` 常量；pkg/schema 加 `FromJSONSchema(raw)→[]Field`（mcp tool 的 server JSON Schema → 统一 Field 视图，workflow wiring 时用，**未接入 mcp**——无消费方，留 workflow 轮） | 研究统一模型心智发现：「对称」是「都声明 **Inputs**」（每个实体都要被调用，`:run`/`:invoke` 表单 + 节点接线都需一张入参清单），**不是**「都硬塞 Outputs」；ctl/apf 的输出本就派生（ctl=emit）/固定（apf=常量），单存 `outputs` 是冗余，且并集形态丢失 per-port 精度。故 Inputs 全留、ctl/apf 的 Outputs 删 | **前端**：control/approval 节点 inspector 去掉 outputs 声明 UI（仅留 inputs）；**testend**：ctl/apf create/edit body 去 `outputs`、`inputSchema`→`inputs` 断言；**无新端点 / 无新前缀**（仅 ctl/apf body + DB 列变更） |
| 29 | workflow 静态图实体模块（波次 4 编排者首落地 · R0047） | **新增 workflow 编排图实体**（wf_/wfv_，全新端点组 + 错误码 + 工具 + 2 表 + relation 第 12 类 + pkg/cel ScopedEnv） | （18/20 重定型产物：旧 workflow 是 DAG + pending/accept + `Tool`/`Case` 节点 + `WORKFLOW_NO_TRIGGER`/`WORKFLOW_DAG_CYCLE` 等 aspirational 码；老 domains/workflow.md DOC-128 为旧设计） | 新增 `/api/v1/workflows` 端点组（CRUD + `:edit`/`:revert`/`:activate`/`:deactivate`/`:capability-check` + versions，**无 `:trigger`/execution**）；**8 个 `WORKFLOW_*`** 错误码（NOT_FOUND/NAME_DUPLICATE/VERSION_NOT_FOUND/NO_ACTIVE_VERSION/INVALID_GRAPH/INVALID_OPS/REF_NOT_FOUND/INVALID_LIFECYCLE，替换旧 aspirational 集）；**7 个 `*_workflow` LLM 工具**（Lazy：search/get/create/edit/revert/delete/capability_check，**无 trigger_workflow**）；database `workflows`/`workflow_versions` **2 表**（graph JSON + lifecycle/concurrency CHECK + partial-UNIQUE name）+ `wf_`/`wfv_` 前缀（§1 表索引 + S15 已预登记，本轮落物理表）；relation 第 **12** 类 EntityKind `workflow`（`wf_`，**已登记**），图 node.Refs 产 `workflow → {trg_,fn_/hd_/mcp_,ag_,ctl_,apf_}` equip 边；**pkg/cel 加 `ScopedEnv`/`NewScopedEnv`**（node-id 根编译，供节点 Input 接线，与固定 payload/ctx/input env 并列）；**老 DOC-128 全量重写**为 as-built；database §3.2 Graph Engine 旧 typescript 块（tool/case/variables）改为 as-built（kind=trigger/action/agent/control/approval、node-id input CEL）；error-codes §2.7 旧 workflow 行替换为真 8 码、flowrun/scheduler 段标「前瞻·未建」 | 18/20 图模型「5 节点 × 5 实体」的编排者落地（trg/fn·hd·mcp/ag/ctl/apf 全到齐后收口）；workflow = function 范式套图：版本化 + ops 编辑 + 图存 JSON，**只 STORE+VALIDATE+PIN，不执行**（执行 = durable 解释器/scheduler/flowrun，import 同一批纯 helper ValidateGraph/BackEdges/BuildPinClosure/WorkflowReader 走 pin 版本）；node-addressable scope（model B）让节点按 node id 接线、免全图变量 | **前端**：workflow 获得**图编辑器**——节点引用实体（trg/fn·hd·mcp/ag/ctl/apf）、边 = 控制骨架（不携数据，control 源 fromPort=Branch.Port / approval 源 yes\|no）、节点 `input` = node-id 根 CEL（`reviewer.score`）；lifecycle（active/draining/inactive）+ concurrency 选择器；activate 前跑 `:capability-check` 看 report；**testend**：workflow CRUD / ops〔7 op〕 / `:edit`·`:revert`·`:activate`·`:deactivate` / `:capability-check` 断言 + `WORKFLOW_*` 8 码（**M7 中央装配后**——RefResolver 接真 catalog、Tools 进 Lazy、Handler 注册、Schema 进 migrate）；**deferred**：执行（`:trigger`/解释器/scheduler）、flowrun/journal（`fr_`/`fre_`/`apv_` 表）、ancestor-only CEL lint、`:iterate`（askai 波次 6）|
| 30 | flowrun（波次 4 编排核心 · M4.2 R0048） | **新增 flowrun 执行状态（节点结果记忆化 2 表，非事件日志）** | 旧 backend `flowruns` + `flowrun_events`〔fre_ 事件日志〕 + `approvals`〔apv_ 投影〕GORM；database §1/§2.4 旧 GORM-tag 前瞻 struct + `Generation`/`PinnedCallables` | database **2 表** `flowruns`(fr_ header)+`flowrun_nodes`(frn_ 记忆化真相表)，无 GORM/user_id；`idx_frn_once`=UNIQUE(flowrun_id,node_id,iteration)〔D3 record-once，取代 `idx_fre_record_once`〕；Log 表无软删(D1)；**删 `fre_`(事件日志)/`apv_`(parked 投影)/`Generation`**，`frs_`(agent 子步)不引入；5 错误码 `FLOWRUN_*`(NOT_FOUND/NOT_REPLAYABLE/APPROVAL_NOT_PARKED/INVALID_ENTRY/INVALID_DECISION，替换旧虚构 cancel/pause/subDAG 集)；ControlResult 扁平 emit + `__port` 保留键(对齐 doc 20 §5.4 下游读 gate.feedback) | 记忆化模型(DBOS/Conductor 式)：Forgify 是图解释器、无用户代码可重放，唯一状态 = 节点 result 行表；事件日志/generation 是 Temporal 为重放用户代码设计的冗余机制 | **前端**：run 历史/详情按记忆化模型(节点行 + iteration)；审批收件箱 = parked 行(无 `apv_`)；**testend**：flowrun 表断言改 2 表 / `FLOWRUN_*` 码 / 无 fre_·apv_；**无前端契约破坏**(执行面新建) |
| 31 | scheduler（波次 4 编排核心 · M4.3 R0049 · 🔴 最大重灾区） | **durable 解释器重写(节点结果记忆化，删旧 9302 行事件溯源引擎) + 新 flowrun 端点组 + 手动起 run** | 旧 `app/scheduler` 9302 行：14 dispatcher + `state.go`/`pause.go` topo-walk + generation + Agenda；api §3 虚构端点 `/nodes`·`/failures`·`/trace`·`DELETE`·`GET /approvals` | `app/scheduler` ~1500 行纯解释器(无实体)：幂等 `advance`(读 frn + pin 图 → computeReady → dispatch/内联 → upsert → loop)；**14→2 dispatch**(RunAction[fn/hd/mcp]+RunAgent 粗粒度)+2 内联(control/approval)；join = 从决策重推活跃子图(无 skip 传播，AND-join + simple-merge 统一)；回边→iteration+1；StartRun 两入口(手动 + firing 单事务 claim ADR-021)+overlap(serial/Skip/AllowAll)+Recover/DecideApproval/CheckTimeouts/Replay；DIP 6 端口。**端点** `GET /flowruns`(?workflowId) · **`POST /flowruns`(手动起 run，body{workflowId,entryNode?,payload})** · `GET /flowruns/{id}` · `POST :replay` · `GET /flowrun-inbox` · `POST .../approvals/{nodeId}:decide`；删旧虚构端点。doc 17 事件溯源契约执行层面被 doc 21 取代 | 旧引擎给单进程 SQLite app 背了分布式味偶然复杂度(14 dispatcher/topo-walk/generation)；记忆化 + 从决策重推活跃子图是单进程图解释器的「最小正确」 | **前端**：workflow「Run now」= `POST /flowruns`(payload 表单 schema = 入口 trigger.Outputs)；run 列表/详情/审批 decide/replay 按新端点；**testend**：flowrun 端点断言全改(新 6 端点)、删 `/nodes`·`/failures`·`/trace`·`DELETE`·`GET /approvals`；**装配影响 M7**：scheduler `Dispatcher` 注真(fn/hd/mcp/agent Service)、`FiringInbox` 接 trigger store、ticker(DrainFirings/CheckTimeouts)+boot `Recover`、`trigger_workflow` LLM 工具、eventlog Emitter(flowrun.* 事件) |
| 32 | conversation（波次 5 · M5.1 R0050） | **对话线程容器重写（去 GORM/UserID → pkg/orm 软删；2 错误码；5 端点 as-built）** | 旧 GORM `Conversation`（user_id + `AnyReferencesApiKey` 反查 + 级联删 messages/blocks）；DOC-106 虚构错误码（`ErrTitleTooLong=INVALID_REQUEST`/`ErrDeleteFailed=INTERNAL_ERROR`）；database §2.2 GORM struct 缺 ModelOverride/AutoTitled/时间戳/软删 | pkg/orm `conversations` 表（workspace_id 自动隔离 + 软删 `deleted_at` D1 + `idx_conversations_ws_list` 置顶优先 partial 索引）；**2 错误码** `CONVERSATION_NOT_FOUND`(404)/`CONVERSATION_INVALID_MODEL_OVERRIDE`(422)；**5 CRUD 端点 as-built**（POST `/conversations` · GET 列〔?cursor&limit&search&archived〕 · GET `/{id}` · PATCH `/{id}`〔modelOverride 三态〕 · DELETE `/{id}`〔软删+清边〕）；modelOverride 仅结构校验（照 agent，**对 apikey 零依赖**）；**删 `AnyReferencesApiKey`**（零消费者）；`conversation.*` 经 notification.Emitter | conversation = 持久化容器 + 配置（消息归 chat M5.2）；记忆化模型边界已被 `domain/messages` 前向引用 `conversation.summary` 钉死；弱引用范式 → override 不探 key 存在性（chat 时优雅失败） | **前端**：对话列表/CRUD 端点同形不破坏；置顶优先列表；modelOverride 三态 PATCH（缺/null/object）；**testend**：错误码加 `CONVERSATION_INVALID_MODEL_OVERRIDE`、删 `ErrTitleTooLong`/`ErrDeleteFailed` 断言；**deferred M5.2**：`GET /{id}` 的 `tokensUsed` 富化 + `GET /{id}/system-prompt-preview`（chat 数据） |
| 35 | attachment sandbox 提取（波次 5 · M5.2 前置 R0053，3/3·收官） | **新增 sandbox-python 文档抽取（Extractor 端口 + SandboxExtractor）+ sandbox 加 attachment owner kind** | 旧无独立提取层（attachment 内嵌 chat、仅存路径） | app/attachment 加 `Extractor` 端口（`Extract(mime,data)` + `ErrExtractionUnsupported`）+ `SandboxExtractor`（`SandboxRunner` 端口 EnsureEnv+Spawn、DIP 结构化满足；`python -c <内嵌脚本> <mime>` 跑 pdfplumber/python-docx/openpyxl/python-pptx，stdin 喂字节、打印 `{text\|error}`）；`ToContentParts(att_ids, visionCapable bool)` → `(att_ids, Capabilities{Vision,NativeDocs})`，`document` = NativeDocs ? file part : 抽文本（token 截断 `maxExtractedChars=400K`，无 extractor/失败→占位）；**sandbox domain `OwnerKindAttachment` + `sandbox_envs.owner_kind` CHECK 加 `'attachment'`**（固定共享 owner `{attachment,"extractor"}` 的抽取 venv）。DOC-307 §4/§5/§7 重写 as-built + sandbox.md §2.2 owner_kind 枚举加 attachment。**无新 REST / error-code**；DB 仅 `sandbox_envs.owner_kind` CHECK 多一枚举值（database.md 仅索引该表、无 CHECK 细节，未改）。 | 用户「主线扎实做、音视频可插延后」；提取与渲染解耦（R0052 渲染 / R0053 抽取）；端口可插（音视频后补独立 extractor 不动主干）；无投机缓存（抽出文本由 chat message 持久化天然抽一次）；共享全机 env（无用户数据、对齐 runtime 全机共享） | **前端**：无（内部能力，attachment REST 端点不变）；**testend**：无新端点/码；**M5.2 接线**：chat 拼用户文本 part + 调 `ToContentParts` 传 `Capabilities`、model 目录补 `vision`/`nativeDocs` flag、消息持久化抽取结果；**装配 M7**：`attachment.NewSandboxExtractor(sandboxSvc)` 注入。**attachment 子模块完整收官 🎉 → M5.2 chat** |
| 34 | attachment 多 provider 注入（波次 5 · M5.2 前置 R0052，2/3） | **11 家 LLM provider 多模态 wire 渲染 + `ToContentParts` 桥**（纯内部 wire，无对外契约破坏） | 旧多模态注入基本只 OpenAI `image_url` 一家格式（LibreChat 同病） | infra/llm `ContentPart` 扩 `MediaType`/`Data`/`Filename` 字段 + `PartFile` 常量；**11 家逐一对官方文档渲染**（每家自包含、不共享 wire 基座）：原生内联 PDF **仅** anthropic〔`document`〕/openai〔`file`〕/gemini〔`inlineData`〕；deepseek/qwen/zhipu/doubao/moonshot/openrouter/custom = `image_url` data-URL + PDF 降级跳过；ollama = 原生 `images[]` 裸 base64；moonshot `content` 升 `json.RawMessage`。app/attachment `ToContentParts(att_ids, visionCapable)→[]ContentPart`（保序 + vision 门控 + 降级文字提示）。DOC-307 §4 由「待建」整段重写为 as-built。**无新 REST / DB / error-code**。 | 用户「各家分开做、挨个查官方文档对齐、都做完」；每家自包含延续 R0014-16「重复 < 错抽象」（不造共享 wire 基座，11 个 buildXUserMsg 各自）；**降级即跳过非报错**（新 part 类型不会 400 某家）；vision 门控用形参（调用方按模型能力传，本层不耦合模型目录） | **前端**：无（ContentPart 是 infra/llm 内部 wire、非对外契约）；**testend**：无新端点/码；**M5.2 接线**：model 目录补 `vision` flag〔ToContentParts 的 `visionCapable` 来源〕、chat 拼用户文本 part + 调 ToContentParts。**R0053 sandbox 提取（抽文本补降级家）续** |
| 33 | attachment 子模块（波次 5 · M5.2 前置 R0051，存储核心 1/3） | **新增独立 attachment 多模态域（att_ + CAS blob 存储 + 4 端点）** | 旧 attachment 内嵌 chat domain（`chatdomain.Attachment{UserID,StoragePath}` + SaveAttachment/GetAttachment + 5 个 `ErrAttachment*` 码）；api `POST /attachments→chat.go` 占位 | 提升为**独立 `attachment` 域**：domain（att_ + Kind 6 类 + `KindFromMIME` + MaxBytes 50MB + 3 errorsdomain + Repository）+ **CAS blob 存储**（infra/fs/blob：`~/.forgify/workspaces/<ws>/blobs/<sha[:2]>/<sha>`，内容寻址 dedup + Sweep GC + 64-hex 防穿越）+ orm 元数据表（att_，sha256 **不唯一**=dedup，软删 D1）+ app（Upload/Download/Delete/GC 按 sha refcount）+ **4 端点**（POST multipart / GET / GET content / DELETE）。**error-codes §2.4b 独立**（`ATTACHMENT_NOT_FOUND`/`_TOO_LARGE`/`_EMPTY`/`_BAD_UPLOAD`，取代旧 chatdomain.ErrAttachment* 4 行）。DOC-307 新 + database §2.2b。 | 用户要完整多模态；研究 LibreChat（OSS 最全流水线）+ ChatGPT/Claude 模态矩阵 → 抄骨架 + 3 升级（CAS / sandbox 本地提取引擎无云 OCR / 多 provider 中立注入）；blob 存盘不胀 SQLite；无 RAG（对齐 document） | **前端**：附件上传/预览/删——`POST /attachments`(multipart)→att_id、`GET /{id}/content` 预览、消息引用 att_ids；**testend**：attachment 端点 + `ATTACHMENT_*` 码断言、删旧 chat 内嵌 attachment 断言；**装配 M7**：handler 注册 + `blob.New(forgify-home)` + boot/ticker `:gc`。**R0052 多 provider 注入（ContentPart+渲染器+vision flag）/ R0053 sandbox 提取（PDF/Office；音频/视频/OCR 可插延后）续** |

## 覆盖后兼容清单（从上表自动汇总）

- 前端（`frontend/src/shared/api` + entities）：
- testend（`testend/src`）：
