# backendcleaner — STATE（单一状态源）

> 进度的**唯一**事实源。原 CONCLUSIONS.md / backlog.json 已删；结论并入 SPEC/criteria，轮次索引在 ROUNDS.md，跨模块待办在 deps-todo.md。

## 当前

- **阶段**：Phase 2 逐模块 — 波次 0 全部完成；**波次 1（叶子业务域）进行中：M1.1 workspace ✅ · M1.2 apikey ✅ · M1.3 model ✅ · M1.4 relation ✅ · M1.5 catalog ✅ · M1.6 mention ✅ · M1.7 memory ✅ · M1.8 sandbox ✅ · M1.9 ⏭️解散 · M1.10 document ✅ · M1.11 todo ✅**。**波次 1 收官**；**波次 2（tool + 原语）✅ 收官：M2.1 tool ✅ · M2.2 loop ✅（+建 domain/messages）· M2.3 叶子工具全完成：#1 filesystem ✅ · #2 search ✅ · 搜索配置 ✅（web 前置，建 domain/websearch）· #3 web ✅（删 MCP tier）· #4 toolset ✅（search_tools 检索式懒加载，砍 activate_tools）**。**波次 3（Quadrinity 执行体）进行中：M3.1 function ✅ R0037 · M3.2 handler ✅ R0038 · M3.3 trigger ✅ R0039（独立信号源实体：引用计数生命周期 + 4 source + sensor/CEL，填 polling 坑、用户加站）。skill ✅ R0040 · **mcp ✅ R0041** · **agent ✅ R0043**（压轴：配置好的 LLM worker）· **M3.7 tool 适配器组 ✅ R0044**（memory/document/shell 三叶子工具：砍 type/cwd/auto-route + 危险硬拦截 + shell.md 重写）；**波次 3 完整收官 🎉**。**波次 4 前置（18 图模型重定型）**：用户从「CEL 实体化」纠结推导出 workflow 图模型彻底重定型（`18-graph-model-redesign`：纯编排数据依赖图、5 节点 trigger/action/agent/control/approval 各引用一类实体、边=payload）——**control 逻辑实体 ✅ R0045**（旧 case 节点 CEL 路由逻辑物化成 `ctl_`）· **approval 渲染实体 ✅ R0046**（旧 approval 节点 prompt 模板 + 决策规则物化成 `apf_`，**非 `apv_`**=运行时表；顺带给 `pkg/cel` 加 `{{ CEL }}` 模板地基）。**两个新 AI 工作实体（ctl_/apf_）收齐**。**workflow 静态图实体 ✅ R0047 → flowrun ✅ R0048（记忆化 2 表）→ scheduler ✅ R0049（durable 解释器收官，🔴最大重灾区拿下）**——波次 4 编排核心 domain/store/解释器/handler 全落 + 18 集成测试绿；剩 M7 中央装配（Dispatcher 注真 / ticker / boot Recover / trigger Attach·Detach）。**
- **分支**：`main`（backend-new 平行重写不需要分支）。
- **策略**：`backend-new/` 平行重建 → 覆盖回 `backend/` → 调前端/testend 兼容。

## 已定的关键决策

- 全量重写，**无任何保留**（含本地 SQLite 数据 → schema 可激进重定）。
- **全局命名 `user_id` → `workspace_id`**（本地单机隔离单元=工作区；ctx/middleware/物理列/实体一律 workspace）。从波次 0 reqctx 起生效。
- 契约**可改**：每改对外 API/SSE/error 都 take note 到 `contract-changes.md`；前端/testend 也是 AI 写的，覆盖后一并兼容。
- 架构按 `module-template.md` 统一、**按需取层**；`go.mod` 空起按需生长、版本对齐现有。
- 重写单元 = 垂直切片；顺序见 `order.md`，判据见 `criteria.md`。
- **去 GORM**：自研 `pkg/orm`（链式、类型安全、自动 workspace 双向隔离 + 软删 + 时间戳）+ `glebarez/go-sqlite`（database/sql driver）。R0008 ✅。
- **domain 去 GORM 化（贯穿所有业务模块）**：domain 实体剥 `import gorm` + gorm tag + `TableName` + `gorm.DeletedAt` → 纯 struct + 轻量 `db:"col,..."` tag（无 import）；store 基于 `pkg/orm` 重写。
- **错误体系强化（贯穿所有模块）**：domain 错误升级为结构化 `Error{Kind,Code,Message,Details,cause}`（Is by Code）；错误码契约内聚 domain；各 domain `errors.New(msg)`→`New(kind,code,msg)`；transport errmap 塌缩成 `statusForKind`（M0.7，零 domain import）。R0012 ✅。
- **SSE 三流统一协议（流式树）**：`eventlog/forge/notifications` → `messages`/`entities`(全实体流式总线)/`notifications`；统一信封 `Envelope{seq,scope,id,frame}` + 四动词 frame(open/delta/close/signal) + **通用 Node{Type,Content}**（词表下放各业务、domain 不持）；**id 升信封层**；frame 按可丢性分级(delta/tick=ephemeral 不入 buffer，open/close/signal=durable，close 带快照)；infra **单一 `Bus`×3 实例**。设计蓝本 = `stream-protocol.md`（已拍板 2026-06-03）。
- **workspace（原 user 正名，M1.1）**：本地隔离单元=workspace。**一切 workspace 隔离（业务表持 workspace_id、orm 自动隔离；应用资源 mcp/skills/settings/memory **按 workspace 分桶** `~/.forgify/workspaces/<wsID>/`——R0024 翻转原"不分桶"决策）**——workspace=完整隔离单元。Name 自由展示名 + 全机唯一（去 slug/GetByUsername/EnsureExists）；Language 是第一个 workspace 偏好（不预建 preferences 容器，YAGNI）。orm 顺手补 `ErrConflict`（UNIQUE 翻译）+ handler 地基首建。R0018 ✅。
- **apikey（收窄，M1.2）**：大幅收窄为「加密保险箱 + 哑探针 + 按 id 发钥匙」。**选 key 下放**（LLM→model 的 api_key_id 显式 / 搜索→未来搜索配置，防乱烧钱）；**哑探针**（tester 只判 HTTP 200 + 存 `test_response` 原始返回，砍解析器）；**解析下放**（`models_found→test_response`，model 靠 `ProbeReader` 解析 + 静态目录兜底——Claude 无 list-models 端点故静态目录是其唯一来源、应**可更新推送**）。`KeyProvider` 收窄 2 法全按 id；**首个吃 orm 自动隔离的业务表**。`modelcatalog`/capabilities 移交 M1.3。R0019 ✅。
- **model（重写，M1.3）**：model 退化成「聚合+展示薄层」(无 store/无 Repository)；删中立 `ThinkingSpec`、各家原生旋钮经 `Request.Options` 自包含；模型知识(窗口/上限/旋钮)下沉各家 `infra/llm` provider(`DescribeModels`+静态目录)，弃跨家 `pkg/modelcatalog`；默认模型搬 workspace 3 列(`ModelPicker` 由 workspace 实现)；override 改运行时优雅报错(删删除时保护)；`Resolve` 收口 override 优先；`Knob` 容器统一/内容原生。R0020 ✅。
- **relation（横切，M1.4）**：实体血缘网（有向边图）。**边类型收成 4 动词** `create/edit/equip/link`（两端类型在 from_kind/to_kind 列，kind 只需动词，删 from/to 冗余编码 + DB CHECK 恒 4 值；中央枚举保留=全局拓扑契约，与删中立抽象不矛盾）；**8 节点**(Quadrinity+document+conversation+skill+mcp)；**`KindForID` 收编自 idgen**(8 条前缀，补 agent + 定 `sk_`/`mcp_` 规矩——skill/mcp 归一 id 体系是波次 3，前缀此刻定死使 document 现在就能 wikilink tag)；**显示名读时内存 hydrate**(按 kind 批量 `Namer.NamesByIDs`，不落库/改名即新/无 reader port/孤立节点不显示)；**无删除时引用保护**(对齐 model override 弱引用，best-effort sync)。R0021 ✅。
- **catalog（收窄，M1.5）**：能力概览「实体名录」——只报「名字+描述」按类型分组，告诉 LLM「有哪些实体」。**砍 InvokeTool**（调用是搜索工具/调用层的事）+ 砍花活(handler 方法列表/mcp 合成/Kind/Active) + 砍预留(`Generator`/`GeneratedBy`/`Granularity`/`Category`/`activate_tools`)；**两段式**：概览→`search_*`(波次 2)精确定位，故 id 不进菜单、name 不要求唯一；document 例外(name=文档名/desc=路径)；无 store 派生现查。R0022 ✅。
- **mention（纯契约，M1.6）**：@ 引用快照的 domain 契约——5 种可 @ 类型(四件套+document) + `MentionInput` + `Reference`(含 Content) + `Resolver` 接口 + `IsValidMentionType`。**Freeze-on-Send**(发送瞬间抓内容快照注入、定格)；**纯 domain 无 app/store/handler/error**——resolver 实现(波次 3)、chat 注册表+统一 `<mentions>` 渲染+错误处理(波次 5)。conversation/skill/mcp 不可 @。R0023 ✅。
- **notification（新模块，R0024）**：通知从"内存广播"升格为**持久化实体**——`Notification{ID,Type,Payload,ReadAt}` 存 DB(workspace 隔离)、前端通知中心(列表/badge/标已读)、关机重开仍在。scope=`notification:noti_x`(锚通知实体)，**workspace 是 Bus 分流轴非 scope**(前端按当前 workspace 订阅防多窗口串台)，事件类型在 `node.type`=`<域>.<动作>`，后端只发 type+payload 前端自渲文案。`Emitter` 端口给 producer。连带 stream 清理(删 KindWorkspace/ListReader/list.go)+ **R0018 翻转**(一切 workspace 隔离/~/.forgify 分桶)。业界调研 4 agent 指导 memory(下一步)走文件式。R0024 ✅。
- **memory（文件式，M1.7）**：从重型 SQLite CRUD 改为**按 workspace 的文件式 markdown**(`~/.forgify/workspaces/<wsID>/memories/<name>.md`，frontmatter description/pinned/source + 正文，文件名即 name、**无 mem_ id**)。两段式注入(pinned 全文常驻 + 非 pinned 目录 `read_memory` 按需)；天然去重(注入目录让 LLM 自判 update 否则新建，无向量 pipeline)；发通知用 `notification.Emitter`；用户可直接编辑文件。**backend-new 首个文件式 store**(手写 frontmatter/原子写/slug 防穿越，skills 波次 3 复用)。砍热度/Type 四分类/Metadata/向量/reflection/decay(业界调研判过度)。R0025 ✅。
- **sandbox（三 runtime，M1.8）**：Python+Node+Docker 隔离运行时（GitHub MCP registry 98 调研:Python+Node+remote 覆盖 92%、缺口 7 Docker-only）。**三 runtime 统一双接口**（image=docker 的 runtime、容器=env，零特例共用 manifest/锁/Ensure 流程）；`EnvBin/EnvDir`→`ResolveExec`（spawn 层不持 runtime 知识，docker 返回 `docker run --rm -i` 包装）；**两表系统级不分桶**（orm `meta.ws==nil` 自动跳隔离，runtime 全机共享——相对 memory/skills 分桶的合理例外）；去 GORM+硬删（无 deleted 列）；`docker.go` 新写（探测 daemon+pull+docker run+`-e` env，**不代装** docker，ErrDocker* 从"残留"转正预留）；notifications pkg→`notification.Emitter`（`sandbox.env_status_changed`/`env_deleted`）；路由 hacky(`POST /sandbox/{action}` 前导冒号)→RESTful+N5(`DELETE /runtimes|envs/{id}`、`POST /sandbox:gc`)。**骨架照搬**（旧实现本质复杂度无脂肪、双接口正交故 docker 无缝插入）+重写烂文档（MiseSpec/BootstrapOK 虚构字段、错误码全旧）。Docker 精细化（stop/孤儿/stdio e2e）留 M3.6、注册+base+fetch-mise 留 M7。R0026 ✅。
- **permissions/hooks/settings（M1.9，判定解散 R0027）**：三者整个**不迁 backend-new**。**中央配置门控是错误抽象**（抄 Claude Code 的交互式逐步授权，但 Forgify 单人本地：交互聊天嫌烦 bypass、无人值守 workflow 没人答 ask → 实际闲置）。**hooks 砍**（花活）；**危险控制不做中央门控**（由别处/工具自管）；**protectedPaths** 归危险控制（`pathguard` 已有默认禁区）；**limits 用 `pkg/limits` 默认**（不暴露用户调、不进 settings）；**settings.json + infra/settings 砍**（无内容可存）。连带 **M5.4 tool/permissionsgate 解散**、**M5.2 chat 去 hooks 依赖**、pkg/limits 删 settings-backed `SetProvider` 装配。契约删 permissions.md / api 7 端点 / 2 错误码。对齐 userpath R0004 判删。R0027 ⏭️。
- **document（Notion 树，M1.10）**：去 GORM + workspace 隔离（app 去掉所有 RequireUserID，orm 自动）+ Emitter（document.created/updated/moved/deleted）+ 树 CRUD 照搬（path 级联/防环/重名加后缀/软删子树/COALESCE UNIQUE 兜根级同名）。**砍 attach 子树注入**（AttachedDocument 去 IncludeSubtree、ResolveAttached 只取单篇——挂载有界不炸 context，用户拍板）。**第一个接通 catalog/relation/mention 的实体，4 适配器对齐前三模块收窄后的新地基**（catalog_source 去 Granularity/InvokeTool/Category、relations wikilink.Parse(去 Kind)→KindForID→link 边、mention 微调、Namer.NamesByIDs 给 relation 读时 hydrate）——验证了前三模块端口设计。旧文档腐烂（标题"RAG 引擎"实无 RAG）整篇重写。注入留 M7、attach 消费波次4/5、:iterate 波次6、tool 波次3。R0028 ✅。
- **todo（TodoWrite 重铸，M1.11）**：旧「4 工具 CRUD 数据库」是**错误抽象**（注入没接→LLM 看不见自己清单、前端看板返 `[]` 假、testend 调死链 REST、blocked_by/owner/metadata 三死字段），重铸为 Claude Code 的 **TodoWrite**——**1 工具 `TodoWrite` 整列替换**、item 3 字段(content/activeForm/status)、无逐项 id。**作用域 (conversation, subagent?)** + `scope_id=subagent??conv` **多态键**（同 relation `from_id`、两 id 全局唯一故天然 PK 无 surrogate/COALESCE）、**subagent 各自一张不污染父看板**（旧 owner 字段做对了=作用域非 item 属性）。**两条硬指标钉死**：「LLM 真用」=双层可见(工具结果回显 + 每轮 `SystemReminder` 注入,loop M2.2)、「前端真看见」=REST 初值(`GET /conversations/{id}/todos`)+messages 流 `todo` signal(scope=conversation/node.type=todo/payload 带 conv+subagent，**不走 notifications 收件箱**)。**reqctx 补双种子**(conversation_id+subagent_id，subagent 提前埋、写入方波次3)；**无 HTTP 错误码**(校验只回 tool-result 串供 LLM 自纠、永不冒泡)。工具波次2/3、注入 M2.2、bridge 实接 M7、前端真看板覆盖后。R0029 ✅。
- **tool（基础接口瘦身，M2.1）**：M1.9 权限解散 + M1.5 catalog 落到工具层。**Tool 9→5 方法**（`Name`/`Description`/`Parameters`/`ValidateInput`/`Execute`；删 `CheckPermissions`/`IsReadOnly`/`NeedsReadFirst`/`RequiresWorkspace`——全仓无消费者、仅已解散 permissionsgate 用过 IsReadOnly + 整套 `PermissionMode`/`PermissionResult`）。**`destructive` boolean → `danger` 三级枚举**（safe 静默 / cautious 标记不阻塞 / dangerous 阻塞等用户同意）；**纯信任**——工具不设静态下限、危险全由 LLM 逐次自报。三字段强制注入剥离（`summary` 必填 + `danger` 必填 + `execution_group`；jsonrepair 兜畸形 JSON、danger 缺/坏→safe）。**Toolset 懒加载保留**（resident/lazy + activate_tools）——与 catalog 实体菜单**正交**（catalog 报实体=搜索数据、懒加载解决工具自身巨大 description 烧 token），**不是** catalog 替代的（纠正一度判错）。与 M1.9 不冲突：砍的是**配置门控**、这是**逐次自报 + 一次确认**。无 domain/store/handler（纯 app 接口模块）；14 测试。danger 确认流 + 并行批 → loop M2.2、activate_tools + lazy prompt + host 组装 → chat M5.2、15 工具适配器 → 波次2.3/3。R0030 ✅。
- **web（WebFetch + WebSearch，M2.3#3）**：LLM 上网能力。WebFetch 抓 URL + utility 小模型摘要（**SSRF 双层守卫** guardHostname/classifyIP + CheckRedirect 每跳重查 + Jina/direct 抓取；**摘要链对齐新地基重写** `model.Resolve(utility)→ResolveCredentialsByID→factory.Build→llm.Generate`，删 `llmclient` 残留 + `Thinking`（M1.3），摘要失败/utility 未配降级返原始截断）。WebSearch **单把显式 BYOK**（R0034 `SearchKeyPicker`→`ResolveCredentialsByID`→`IsProvider`→switch brave/serper/tavily/bocha，provider 由 key 隐含、401→`MarkInvalidByID`、**替旧 `SearchProviderPriority` 自动遍历防乱烧钱**）。**改判删 MCP tier**（用户质疑后认同：MCP 搜索经 `tool/mcp`(M3.7) **平级暴露** LLM 直接调、WebSearch 不该代理——删 search_mcp.go/`MCPSearchRouter` + WebTools 去 mcpRouter，**web 零 mcp 依赖**；引导文案提 duckduckgo MCP 是纯文字、零耦合）。9→5 方法、danger 工具侧零逻辑（只读 LLM 自报）、`GetUserID`→`GetWorkspaceID`。fetch 6 + search 7 测试（mock Jina httptest + provider=mock 短路 MockClient + 各 provider httptest）；契约 domains/web.md 整篇重写（DOC-127，删 MCP 备选引擎 / 虚构 wire code / `web_reference` relation）+ contract #15。波次 2 仅剩 #4 toolset。R0035 ✅。
- **搜索配置（建 domain/websearch + workspace 加列，M2.3#3 web 前置）**：补 M1.2 主动推迟的"搜索配置"。考古 web 发现旧 WebSearch BYOK 靠 apikey「按 provider 找搜索 key」(`SearchProviderPriority` 自动遍历)，而 M1.2 收窄全砍(只剩按 id 发钥匙)。研究确认 **apikey 层早就绪**(brave/serper/tavily/bocha 4 搜索 provider 在白名单 + TestMethodSearchPing + CategorySearch)，唯一缺 workspace 的"默认搜索 key 选择"。**建独立 `domain/websearch` 包**(用户要求、对齐 domain/model 便管理:Provider 词表 + IsProvider/Providers + `SearchKeyPicker` 接口 + **无 store**)；**存储借 workspace**(完全同 model default-models:加 `DefaultSearchKeyID` 列、`SetDefaultSearch` 在 workspace.Service、**不建 app/websearch**——model 的 app 是 capability 聚合搜索无此需求)；**单选显式 key 防乱烧钱**(替旧 `SearchProviderPriority` 自动遍历，provider 由 key 隐含)；**不校验 category**(运行时优雅 + workspace 零新依赖，对齐 model `SetDefault`)；端点 `PUT/DELETE /workspaces/{id}/default-search`。websearch 2 + workspace app 4 + store 1 测试；契约 domains/websearch.md 新(DOC-302)+ database 列 + api 端点 + workspace.md §4.4 + contract #14。WebSearch 消费留 R0035 web。R0034 ✅。
- **search（LS/Glob/Grep 三件套，M2.3#2）**：LLM 在整台机器**找文件**的导航能力——对应人找文件三动作:打开文件夹看(LS 新增)/按名找(Glob)/按内容找(Grep)。**确立桌面 agent「全电脑绝对路径、无 cwd」范式**——桌面 agent 无项目根/无当前目录（全电脑范围、靠用户交互定位），照搬 Claude Code 项目级 cwd 是缺陷（进程 cwd=app 安装目录、搜了个寂寞）。**新建 `pkg/fspath.Expand`**（展开 `~`/os home + 必绝对否则拒 + Clean，filesystem 3 + search 3 共六工具的 path 唯一解析点、"永远绝对"物理执行点）。**LS 独立成工具**（列目录是全电脑导航最高频动作，不再 `Glob "*"` 兼任；三件套职责正交）。Glob/Grep 删 `os.Getwd` 默认→path 必填 + `fspath.Expand`。**Grep 双后端保留**（rg 优先 + 纯 Go stdlib 兜底——桌面分发不假设有 rg、不代装二进制，同 sandbox docker；464 行 stdlib 是对齐 rg 功能集的必要代价）。**回溯改 filesystem**（read/write/edit 改走 `fspath.Expand` 支持 `~`、删 IsAbs/ErrPathNotAbsolute——绝对性裁判下沉 fspath）。**cwd 概念全局废弃**（agentstate 永不加 cwd、shell M3.7 也无 cwd 持久化、R0032"cwd→shell"撤销）。加 `doublestar/v4` 依赖；danger 工具侧零逻辑（LLM 自报）；fspath 6 + search 29 + filesystem 回溯 6 测试；契约 search.md 整篇重写（DOC-120，旧版 `grep_search`/`glob` 假名 / `Permissions` / 多线程 / SearchBlock / 5 虚构 sentinel 全删）+ filesystem.md 加 §4.1 路径解析 / 清 cwd + contract #13。R0033 ✅。
- **filesystem（Read/Write/Edit 三件套，M2.3#1）**：LLM 的"读写手臂"——3 工具机械跟进新地基。**首建 `pkg/agentstate`**（只引入 `SeenFiles` 字段，渐进生长：cwd/activeSkill/activatedGroups 各自首个消费者按需追加，反预留）+ 首建 reqctx `WithAgentState/GetAgentState` 种子（接线方=chat/subagent/scheduler 留波次 3-5）。9→5 方法（M2.1 已砍 4 死方法机械跟进）；**写守卫两级分流**：Read 用 `Allow`（R0003 默认 deny 集）、Write/Edit 升级用 `AllowWrite`（再 ∪ `.git/`/`.env*`/`node_modules/` 等"AI 可读不可写"写专属 extras）——把"AI 永不覆盖 git 历史/.env"从无防护升为物理拦截；**写前必读铁律保留**（SeenFiles 跨工具协作 + **fail-closed**：state 缺失时 Write/Edit 显式拒，静默放过会让不变式形同虚设）；Edit 保留 size 漂移检测（廉价外部改动指示器，v1 取舍接受同 size 互换漏）；原子 tmp+rename + mode 保留（防 CreateTemp 默认 0600 静默收紧权限）；danger 不静态声明（M2.1 纯信任）。agentstate 5 + reqctx 3 + 三工具 31 测试；契约 domains/filesystem.md **整篇重写**（DOC-108——旧版腐烂：`settings.json/protectedPaths`/`IsReadOnly` flag/虚构 1MiB 上限/虚构 5 sentinel HTTP code/工具名 `fs_*` 错/`forgify.db` 禁区错全删）+ contract-changes #12。`Glob`/`Grep` 已在 search（R0033）、**cwd 概念 R0033 全局废弃**、activatedGroups 留 toolset/chat host AutoActivator 钩子。R0032 ✅。
- **loop（ReAct 引擎重写，M2.2）**：共享 ReAct 引擎接 stream 统一协议（eventlog→messages 流 `open/delta/close` + node content 词表、close 带快照供 replay）、**danger 纯标记**（tool_call 节点带 danger/summary 前端标记、不阻塞、dangerous 确认留 ask 波次6）、**删 interceptor**（M1.9 中央门控 + hooks 解散）、**todo 注入走 Host `ReminderProvider` 钩子**（每步临时 `<system-reminder>` user 消息、不污染持久历史）。**建 `domain/messages`**——Block/ToolCallData/词表无家可归（backend-new 无 chat/messages domain，M0.4 三流统一成纯传输 stream），loop 依赖中立 messages **修正旧「共享引擎依赖 chat」耦合反向**；本轮只立类型契约，message_blocks 表/落盘/History/Message 实体留 chat M5.2。**executeTool 极简**：删 CheckPermissions/skill-preapproval（M1.9/M2.1）+ 改判删 sanitizeToolErr/enrichWithNextStep（硬编码具体工具名 + 旧 §S16 wrap 规约、违反 loop 中立——工具自负 error 质量，tools.go 323→~150 行）。**agentstate 零依赖**（4 块职责无一属于 loop：skill 预授权随 CheckPermissions 删而失靶、activate_tools 激活归 host 钩子；随 filesystem/shell/chat/skill 各自重建、创建者 chat）。reqctx 补 messageID 种子（host 在 Run 前埋、emitter 锚 block）。domain/messages + loop 6 源 + 17+3 测试。R0031 ✅。
- **function（重写，M3.1）+ 抽 app/envfix**：Quadrinity 第一元。**版本模型方案 A 三正交**（version 单调号写入即分配 / 内容 append-only / `active_version_id` 自由指针）——**删 pending/accept 状态机**（create/edit 立即生效写 `max+1`，**revert 纯移指针**不删"更新的"版本、不动号轴，edit fork-from-active，50 上限裁最老但**绝不裁 active**）。**polling 整个剥离**（kind/polling_interval/PollingAdapter/2 ops 不迁——轮询触发源是独立概念、不寄生 function；顺带修旧 `triggered_by="polling_trigger"` 违反 CHECK 的 bug）。**env-fix 抽独立共享包 `app/envfix`**（用户预判 function/handler/trigger 都要、rule-of-three 已满足；`Provisioner.Provision` 装→失败 utility LLM 看 stderr 改 deps 重试≤3→FinalDeps 由调用方回写版本；链对齐新地基 model.Resolve→ResolveCredentialsByID→factory.Build→llm.Generate + jsonrepair，**删 Thinking/llmclient/llmparse 残留**；**stream-agnostic**——进度经 `Sink` 回调暴露、推流逻辑留 tool 层）。`triggered_by` ∈ **chat/agent/workflow/manual**（执行体轴，非传输层；tool 按 subagent ctx 区分 chat/agent，其余波次接线）。execution **砍 hints**（OutputEmpty/SignificantlySlower 审计剧场）+ aggregates 只 ok/failed。校验诚实轻量（def 检查 + D7 禁 import forgify_handler，不吹 AST/docstring）。domain 去 GORM + errorsdomain（删 5 旧码 + 加 ErrInvalidCode、OpInvalid 改 422）；store orm（trim cutoff 避 offset + 保护 active）；9 工具（5 方法、danger 自报、create/edit forgeSink 折 envFixAttempts）；handler 删 pending 三端点、:iterate 留 M6；3 适配器（catalog Name+ListItems / mention Resolver / relation Namer + forged/edited 4 动词边）。envfix 6 + store 9 + app 8 + tool + domain 测试。契约 function.md 重写(DOC-110) + envfix.md 新(DOC-304) + error-codes/api/database/contract #17。R0037 ✅。
- **handler（重写，M3.2）= function 孪生 + 3 件独有**：Quadrinity 第二元（有状态 Python 类）。**进程生命周期重做成 MCP 式单例常驻**（用户拍板）——每 handler 一个常驻进程（扁平 `map[handlerID]*Instance`）：**boot 开局起 / restart 重置 / shutdown 退出关**，chat/agent/workflow **共享同一实例 + 状态**（真有状态）。**砍旧 per-owner 模型**（`Owner` + `map[Owner]map[name]` 每对话一份 + 两套 call 分叉——**chat 即起即杀使有状态类退化无状态、自我矛盾**——+ lazy-spawn/crash 重生/竞态/owner-teardown/文档吹的 15min idle-GC，全塌成 3 动词）。**restart 双触发**：自动（edit / 改 config → 重启吃新代码/config）+ **手动 `restart_handler` 工具 + `POST :restart`**（对话内"坏了帮我重启"——crash 重生救不了"进程活着但状态坏了"如 DB 断连/session 过期，restart 是有状态服务的「重置按钮」；无状态 function 不需要）。**并发**单例 + 客户端 mutex 串行。**三件独有**：① 类组装（ops set_imports/init/shutdown/init_args_schema/add·update·delete_method → `AssembleClass` 拼 HandlerImpl 类 + Python `DriverScript` stdio RPC：init→ready/call/generator yield→progress/`_` 私有方法拒）② **加密 config**（init-args 值加密存、Sensitive 掩码、ConfigState unconfigured/partial/ready **门控 spawn**、改 config 自动重启）③ MCP 式进程管理器。**其余照 function（R0037）孪生**：方案 A 版本（去 accept/单调号/revert 纯移指针/edit·revert 重启实例）、**复用 `app/envfix`**（第二消费者）、call_log（hcl_、triggered_by chat/agent/workflow/manual、砍 hints）、catalog/mention/relation Namer 适配器（4 动词边）、11 工具（含 call/update_config/restart）、去 GORM + errorsdomain（删 pending/instanceNotFound/envFailed/ast/configInvalid，crashed→HANDLER_CRASHED 502/RPC_TIMEOUT 改名）。infra/handler client 照搬（stdio 行-JSON RPC）。store 5 + app 9（生命周期核心 + 版本模型 + config 门控 + AssembleClass）+ tool + domain 测试。契约 handler.md 重写(DOC-111) + api（删 pending/config POST→PUT/+:restart）+ error-codes + database 前缀 hdenv_/hdi_ + contract #18。R0038 ✅。
- **trigger（独立实体加站，M3.3）**：用户提把 trigger 从「workflow 图节点」提升为**独立实体**（`trg_`，地位同 function：进 catalog + relation **第 9 节点** + 8 工具），顺带填 M3.1 剥离 polling 的坑。**引用计数生命周期**：≥1 个 active workflow 引用才起 listener、N workflow 共享一个 listener、fire 扇出（**淘汰旧 per-workflow 重复 listener**）。**4 source** cron/webhook/fsnotify/**sensor**（砍 manual——手动跑是 workflow 自己的能力）。**sensor**（polling 改名，用户选）= 绑 function 或 handler.method（看 active 版本）+ `intervalSec`(≥5) + CEL `condition`/`output`（替旧 events/nextCursor 协议；**要状态绑 handler、不要状态绑 function**，trigger 自身无状态）。**无版本**（配置实体改即生效）。**Activation 动作日志**（触没触发都记 return_value/detail，「为什么没触发」可查；用户提）。**关键洞察**：旧 trigger 节点是 no-op 透传 `flowrun.TriggerInput`，搬出图不丢信号 → target=workflow（方案 A）。**建 `pkg/cel`**（CEL 编译/求值共享包，workflow M4 复用、避 infra→app 依赖）；listener 回调升级 `ReportFunc`（每动作都报、Fired 与否）统一 4 listener。3 表 triggers/trigger_firings/trigger_activations（`trg_`/`trf_`/`tra_` 取代旧 `ts_`/`tfi_`）；durable firing 保留（persist-before-act + 单事务 claim ADR-021，claim 留波次 4 scheduler）。去 GORM + errorsdomain（12 码）；前 3 listener 照搬去 gorm 改 triggerID 维度 + sensor 新写。store 5 + sensor 3 + app 7 + relation 测试；契约 trigger.md 重写(DOC-125) + api(§2.5) + error-codes(§2.6.1) + database(§4.3) + relation(节点 9) + contract #19。R0039 ✅。
- **subagent 后移波次 5（M3.4 考古判定，2026-06-07）**：subagent **不是锻造实体**而是**递归的 chat**——无自己的表（写**父对话 messages 表**：sub-message + parentBlock 锚点 E3 递归）、无 model 配置（**承袭父 effective model**）。这两个核心行为是 **chat host 的子集**，chat 在波次 5 → 波次 3 单做只能留 stub 端口。故 subagent（连 `tool/subagent`）**移到波次 5、与 chat 同轮做**（共享落盘/model、一次测嵌套子对话）。**对比 agent 为何能现在做**：agent 是实体、有自己的 invocation log 表 + 自己的 model 配置列 → 落盘/model 自洽、不碰 chat。唯一消费者 **skill（M3.5）走 DIP `SubagentService` 端口 + `subagent==nil` 优雅降级**（旧代码已有）→ 不被堵。防递归改用 `SubagentID` 存在性（种子 R0029 已埋）；agentstate 子 run 独立新建（防 SeenFiles 污染父）；旧契约 DOC-123 严重腐烂（虚构 cv_xxx 子对话/深度限 2 实为 1/AgentState 沙箱/4 个虚构错误码）整篇重写。
- **agent 顺序后移 + 砍 pending/accept（M3.4 考古连带，2026-06-07）**：agent 是 Quadrinity **最综合实体**（挂载 skill/mcp/document/function/handler/model 六件），现做则 skill/mcp 挂载只能 stub、半成品。故波次 3 顺序调成 **skill(M3.4) → mcp(M3.5) → agent(M3.6 压轴)**——agent 挂载件齐全后一次做完整、测完整（挂 skill 真激活、挂 mcp 工具真能调）。agent 对 skill/mcp 是**运行时弱引用**（`Skill string` + `mcp:` ref + 白名单过滤，能独立编译），功能完整性依赖。agent 重写**跟进方案 A 砍 pending/accept**（旧 domain 明说 mirrors function/handler 的 pending/accept，但 R0037/R0038 已砍成 create/edit 立即生效 max+1 + revert 移指针）；不跑代码→**不需要 sandbox/envfix**；execution 面（invoke/`agx_` 表/search_executions）对齐 function。
- **skill 文件式指令载体（M3.4 R0040）**：skill 重写为 **memory 的近亲**（文件式注入物），**砍 execution 审计 + search + 常驻轮询 + GORM**——纯文件、零 DB 表、零 LLM 依赖、纯按需扫描（catalog/HTTP 触发现扫）。对齐 function 仅工具命名（create/edit/delete/get/activate）。调研（deep-research 104 agent / 22 源）推翻两倾向：① allowed-tools = **预授权**（免危险确认）非白名单限制，与「无中央门控、危险 LLM 自报」自洽（存 `agentstate.activeSkill`，消费留 ask 波次 6）；② AI 自创 skill 有 Anthropic 软性愿景背书（create/edit/delete 工具，`source=ai` 区分）。**无版本**（编辑即覆盖）；disable-model-invocation 实装（发现层过滤）；fork 经 `SubagentRunner` 端口（subagent 波次 5 注入、nil 降级 ErrSubagentUnavailable）；relation 节点用 **name**（R0021 预留的 `sk_` 前缀作废，文件式无生成 id）；3 处对不齐 function（无版本/不可 @mention/无 DB）。SKILL.md 已是跨厂 open standard。旧 2275→~1000 行；契约 DOC-122 整篇重写。
- **mcp（市场对接 + 加密表 + 双 transport，M3.5 R0041）**：mcp 重写为接外部生态的协议网桥（go-sdk v1.6.0）。**market 从硬编码 24 → 对接 GitHub registry 全量 99**（`api.mcp.github.com/v0/servers`，`go:embed` snapshot + 磁盘 cache 三级兜底离线；调研确认 github.com/mcp 是有标准 API 的 registry，99=38remote+38node+11python+9docker+3.NET，95+ 可装、embed 测试守住）。**`mcp_servers` 加密表**取代 mcp.json + **砍 `mcp_calls`/`mcp_health_history` 两表**（审计/health=校验剧场，承 skill 先例）；`config_enc` AES-GCM 加密 {env,headers}（store 层封装、domain 持明文）。**进程池单例 `map[mcp_id]` 对齐 handler**（Boot per-ws / crash 重连 / 手动 `reconnect_mcp` / Shutdown；id 全局唯一跨 ws 不撞——这是「单例 vs workspace 隔离」自洽的关键）。transport **stdio**（进程由 sandbox `SpawnLongLived` 起、归 sandbox 管，go-sdk 用 handle stdin/stdout 经 `IOTransport` 接协议——省掉 sandbox ResolveExec 暴露）+ **sse/streamable-http**（go-sdk，headers 经自定义 http.Client 注入 Bearer）。安装遍历 registry packages 数组、按 runtime 优先级（node>python>docker>dotnet，轻量优先、有 npm 版就别拉 .NET SDK）挑最优 package（或 remote）+ sandbox EnsureEnv。**catalog 报 server+描述+全工具名**（catalog.Item 加 `Members []string` + mechanical 渲染；容器实体范式，handler 同款）。工具 = **4 系统**（list_mcp_marketplace/install/uninstall/**reconnect_mcp**=重置按钮对齐 restart_handler）+ **动态 `mcp__server__tool`**（每工具独立 lazy、进 search_tools 检索池**不进 Overview**、catalog 引导；冒号非法故双下划线）。**砍** Search LLM rerank/searchrouter/call_mcp_tool/审计工具/curated 硬编码。danger 零逻辑（LLM 自报）。**3 处留口 R0042 全补完**：① **sandbox 物理 runtime-tool 真机端到端跑通**（node→npx / python→uvx / dotnet→dnx ResolveExec + spawn 绝对 runtimeRef + docker 空 Cmd image entrypoint + PATH 注入；真机 e2e context7 v3.1.0 跑通 2 工具 + `mise install dotnet@10.0.300` 验证 dnx 顶层；**uvx 随 uv 已在、先前「需装 uv」是错判已纠正**）② handler catalog 列方法名 ③ trigger sensor 绑 mcp.tool（domain+relation，Invoke 路由随 M7）。go.mod +go-sdk v1.6.0；domain 6+store 6+app 7+infra 3+tool 3 测试全绿；契约 mcp.md 重写（DOC-112）+ database/api/error-codes/catalog.md + contract #21。
- **tool 适配器组（memory/document/shell，M3.7 R0044）= 波次 3 收官**：补齐 backend-new 唯三还没建的工具适配器，皆 **filesystem.md 范式的叶子工具**（无 domain/store/handler/DDL/HTTP，只 5 方法、软失败返串）。**核心教训「不能照抄旧」**——逐项核对 backend-new 真实接口 + 回顾契约文档：① **memory** read/write/forget（**砍 `type` 四类**，memory.md §3 已白纸黑字；write `source=ai` 内定、不暴露 `pinned`）；② **document** 7 工具（`CreateInput/UpdateInput/MoveInput` 已挪 `documentdomain`、app `type=` alias；`edit`→Service.`Update`；`delete` 砍 `destructive`；errorsdomain 转软失败串；真 store 端到端测）；③ **shell** Bash/BashOutput/KillShell + ProcessManager（**砍整个 cwd 机制** R0033；**砍 auto-route** 留波次5、不引 `mvdan.cc/sh`、不预留 sandbox 字段；**新增 `danger.go` 硬拦截 6 条** rm-rf//~/sudo/mkfs/dd-of-dev/fork-bomb 替代解散的 permissionsgate、**非 allow/deny**；砍 Snapshots/ConvID）。**文档纠正**（「别光看代码、回顾文档」揪出）：**shell.md DOC-121 严重 stale**（停 2026-05-03：30s/run_bash/allow-deny/6 个孤岛 HTTP 错误码连 error-codes.md 都没进/auto-route 当核心）→ **整篇重写**对标 filesystem.md；agentstate.go 注释 doc-fix（去 cwd）；database.md 登记 `bsh_`。memory/document/filesystem.md 核对皆 ✅ 准。**memory Lazy + document Lazy + shell Resident**，装入 Toolset 留 M7。三新包全离线测全绿、不破坏任何包。契约 shell.md 重写 + memory/document.md 补 + database/contract #24。R0044 ✅。
- **control（路由逻辑实体，波次 4 前置 R0045）**：`18-graph-model-redesign` 图模型重定型的**首落地实体**。一段长设计对话——用户从「CEL 实体化」纠结，逐步推导出 workflow 图模型**彻底重定型**（**workflow = 纯编排数据依赖图、5 节点 trigger/action/agent/control/approval 各引用一类实体、边 = payload 数据管道**）。control = 旧 `case` 节点内联 CEL 路由（when/emit）物化成独立 `ctl_` **AI 工作实体**。**用户 10 条拍板**：CEL 不实体化/逻辑坨才实体化、强制每个都是实体（清晰心智 > catalog 洁癖）、版本（pin 必需）去 sandbox/env/executions、全量 branches 无 ops、强制末条 `when:"true"` 兜底、无 run/executions、relation 轻（只被引用 + Namer、不产边）、不进 mention、独立孤儿、payload 隐式（对齐 17 §5、不改 17）。domain(`ctl_`/`ctlv_`/`Branch{port,when,emit}`/`ValidateBranches`/8 errorsdomain) + store(两表 orm + TrimProtectsActive) + app(Service 9 法 + validateBranches〔结构 domain + CEL `pkg/cel`〕 + catalog/relation/notif) + 6 Lazy 工具(无 run) + REST(无 `:run`/pending) + relation 第 **10** 类 EntityKind。全离线测 domain/store/app/tool/relation 全绿。契约 control.md(DOC-305) + database §4.5 + api §2.6 + error-codes §2.6b + relation 9→10 + contract #25。装配留 M7。**波次 4 第一块地基**。R0045 ✅。
- **approval（审批渲染实体，波次 4 前置 R0046）**：18 第 2 个落地实体（control 后）。旧 `approval` 节点内联的 prompt 模板 + 决策规则物化成 `apf_` **AI 工作实体**（「把数据渲染成人能看懂的审批点」）。与 control 几乎双胞胎，**3 差异**：①内容 `template`(markdown)+ 决策规则(`allowReason`/`timeout`/`timeoutBehavior`)非 branches ②**`{{ CEL }}` 模板**非裸 CEL → **给 `pkg/cel` 加模板地基**（`Template`/`CompileTemplate`/`Render`，agent.prompt 波次 4 复用）③`timeout` duration(支持 `30d`/`2w`)+ behavior 联动。**前缀分层**：`apf_`/`apfv_`（form=配置），**≠ `apv_`**（`approvals` 运行时表，17/database §1 已占）——18 文档 `apv_`/`apvv_` 撞前缀、全局 doc-fix。domain(apf_/apfv_/ValidateForm/ParseTimeout/7 码) + store(两表 + `allow_reason` bool 往返) + app(9 法 + validateForm〔结构 + CompileTemplate〕 + 适配器) + 6 Lazy 工具(无 run) + REST + relation 第 **11** 类。全离线测 cel + domain/store/app/tool/relation 全绿。契约 approval.md(DOC-306) + database §4.6 + api §2.7 + error-codes §2.6c + relation 10→11 + contract #26 + 18 doc-fix。装配留 M7。**两个新 AI 工作实体（ctl_/apf_）收齐**。R0046 ✅。
- **flowrun + scheduler（durable 执行引擎，波次 4 核心 R0048+R0049）**：旧 9302 行事件溯源引擎（14 dispatcher + topo-walk + generation + Agenda）是重写主因——拆。**核心模型 = 节点结果记忆化（DBOS/Conductor 式），非事件日志**：Forgify 是图解释器、无用户代码可重放，唯一状态 = 「哪些 (节点,轮次) 完成 + result」一张行表正好装。**flowrun（R0048）= 2 表**（`flowruns` fr_ header + `flowrun_nodes` frn_ 真相表，无 GORM/user_id；`idx_frn_once` record-once；删 fre_ 事件日志/apv_ 投影/generation；frs_ 不引入 = agent 粗粒度）。**scheduler（R0049）= ~1500 行纯解释器**：幂等 `advance`（读 frn+pin 图→computeReady→dispatch/内联→upsert→loop；崩溃=再调一次抄 completed）；**join = 从 control/approval 决策重推活跃子图**（判定②，BPMN 状态式/Conductor decider，无 skip 传播，AND-join+simple-merge 统一）；回边→iteration+1；14→2 dispatch（RunAction/RunAgent 粗粒度）+2 内联；CEL 双轨（节点 Input=ScopedEnv node-id 根 / when·emit·template=input 根）；StartRun 两入口（手动 `POST /flowruns` + firing 单事务 claim ADR-021）+overlap+Recover+DecideApproval+CheckTimeouts（approval durable timer）+Replay。**三血泪边界**（replay 确定性/record-once/approval first-wins）+ 崩溃恢复 + at-least-once 诚实——18 集成测试全绿。**两处用户细化**：①**手动 trigger 进 v1**（StartRun 反正 firing 也要、近零成本、dogfood 刚需；payload 表单 schema = 入口 trigger.Outputs）②**agent 降粗粒度删 frs_**（卡在 loop.Run 流式黑盒无 resume 入口、LLM 轨迹非确定无 cheap middle；resume-mid-agent→v2 改 loop.go）。ControlResult 扁平 emit+__port（对齐 doc 20 §5.4 下游读 gate.feedback）。契约 doc 21 重写 + database/api/error-codes/events + domains（flowrun DOC-109 / scheduler DOC-119 重写）+ contract #30/#31。M7 装配（Dispatcher 注真 / FiringInbox / ticker / boot Recover / eventlog Emitter / `trigger_workflow` 工具）+ trigger Attach/Detach + 祖先-only CEL lint 延后。R0048+R0049 ✅。
- **conversation（对话线程容器，波次 5 · M5.1 R0050）**：波次 5「对话与上下文」第一站。**持久化容器 + 线程级配置**——干净的 CRUD 垂直切片（无 LLM/loop/消息；消息归 chat M5.2）。document 范式同构叶子。**去包袱**：GORM/user_id → pkg/orm（自动 ws 隔离 + 软删 `deleted_at` D1 + `Page` keyset）；`notificationspkg.Publisher` → `notification.Emitter`（发 `conversation.*`，document 先例）；`errors.New` → errorsdomain；**删 `AnyReferencesApiKey`**（backend-new 零消费者）。**modelOverride** = `model.ModelRef`，**仅结构校验**（apiKeyId+modelId 非空 → `CONVERSATION_INVALID_MODEL_OVERRIDE`，照 agent 先例）——**不探 key 存在性**（弱引用 + chat 时 `model.Resolve` 优雅失败）→ **conversation 对 apikey 零依赖**；handler `UnmarshalJSON` 三态（缺/null/object）。**全 schema 一次到位**（summary/summaryCoversUpToSeq 由 contextmgr M5.3 写、autoTitled 由 chat M5.2 写、PATCH 不暴露；`summary` 已被 `domain/messages` 前向引用故非投机）。List 置顶优先（`pinned DESC, created_at DESC, id DESC` + partial 索引）、游标只键 (created_at,id)（单用户务实取舍）、archived 三态过滤。relation 第 8 节点（Namer/PurgeEntity nil-tolerant）。**deferred M5.2**：`tokensUsed` 富化 + `/system-prompt-preview`（chat 数据，不引 chat 类型）；M5.1 不级联删 message_blocks。store 13 + app 8 测试全绿；契约 DOC-106 重写 + database §2.2 + api + error-codes + contract #32。R0050 ✅。
- **attachment 多模态子模块（波次 5 · M5.2 前置 R0051，存储核心 1/3）**：用户要「完整支持所有模态」。研究 LibreChat（OSS 最全流水线：存储/处理解耦 → 类型路由 OCR>STT>解析 → 提取 → token 截断 → 投递；无 RAG 纯提取+直接注入）+ ChatGPT/Claude 模态矩阵 → **抄骨架 + 3 Forgify 升级**：①**CAS 内容寻址存储**（dedup+完整性，胜 LibreChat 普通 uploads/）②**sandbox 当本地提取引擎**（python pdfplumber/python-docx/whisper，离线、无云 OCR/API key，胜 LibreChat 调 Mistral OCR 云）③**中立 ContentPart + 各 provider 渲染器**（多家，LibreChat 基本只 OpenAI）。**无 RAG**（对齐 document 域）。**主线**（图/文本/PDF/Office）扎实做、**音频/视频/OCR 经可插 `Extractor` 端口留插槽**按需补（dogfood 再上，按 ship-over-polish）。**R0051 存储核心**：domain（att_ + Kind 6 类 + `KindFromMIME` + MaxBytes 50MB + 3 码 + Repository）+ **infra/fs/blob**（CAS `<ws>/blobs/<sha[:2]>/<sha>`，Put 内容寻址 dedup + Sweep GC + 64-hex 防穿越，ws 取自 ctx 同 memory）+ orm 元数据表（sha256 不唯一、软删 D1）+ app（Upload〔校验→sha→blob.Put→Insert，blob 先于行〕/Download/Delete/GC〔按 sha refcount〕）+ **4 端点**（POST multipart〔MaxBytesReader+DetectContentType〕/GET/GET content/DELETE）。提升为独立 attachment 域（取代旧 chat 内嵌 + 5 旧码）。blob 9 + store 6 + app 8 测试全绿（含 dedup 幂等、ListLiveSHAs 软删-aware、GC sha refcount）。契约 DOC-307 新 + database §2.2b + api 4 端点 + error-codes §2.4b + contract #33。R0051 ✅。**R0052 多 provider 注入**：`ContentPart` 扩 MediaType/Data/Filename + `PartFile` → **11 家逐一对官方文档渲染**（每家自包含、不共享 wire 基座；原生内联 PDF **仅** anthropic/openai/gemini；7 家 openai 兼容 `image_url` data-URL + PDF 降级跳过；ollama 原生 `images[]` 裸 base64；moonshot content 升 `json.RawMessage`）+ `attachment.ToContentParts(att_ids,visionCapable)` 桥（按 att_ids 保序 + vision 门控 + 降级文字提示，本层不持模型目录）；multimodal_test 11 家 + ToContentParts 4 测全绿；契约 attachment.md §4 重写 + contract #34（**无新 REST/DB/码**——ContentPart 是 infra/llm 内部 wire）。R0052 ✅ → **R0053 sandbox 提取**随后。

## 模块进度（编号见 order.md）

状态：⬜ pending ｜ 🔧 doing ｜ ✅ done ｜ ⏭️ 判定删除/合并

- **Phase 1 骨架** ✅：`backend-new/` + 空 go.mod + health server + smoke。
- **波次0 地基**：M0.1 pkg ✅（**reqctx/idgen/pagination ✅** R0001；**tokencount ✅** R0002；**pathguard ✅** R0003；**userpath ⏭️删** R0004；**wikilink ✅** R0005；**jsonrepair ✅** R0006；**limits ✅** R0007；modelcaps/modelcatalog 移交 M1.3）· M0.2 数据库层 ✅（**pkg/orm R0008 · db 网关 R0009**；业务表 DDL 分散各模块）· M0.3 ✅（**logger R0010 · crypto R0011**）· M0.4 ✅：**errors R0012** · **stream 统一协议 R0013**（单一 domain/stream：信封+四动词Frame+通用 Node{Type,Content}+Bridge/ListReader；词表下放业务）· M0.5 ✅ infra **stream bus（单一 Bus）R0014**（实例化三次=三流；frame 分级；D2 全量推；infra/chat extractor 移交 M5.2）· M0.6 llm ✅（11 家 provider）· **M0.7 transport ✅ R0017**（response N1+errmap 塌缩+SSE marshal · middleware workspace · router 框架；完整 New→M7）· **波次 0 收官 ✅**
- **波次1 叶子域**：M1.1 workspace(原 user) **✅ R0018** · M1.2 apikey **✅ R0019** · M1.3 model **✅ R0020** · M1.4 relation **✅ R0021** · M1.5 catalog **✅ R0022** · M1.6 mention **✅ R0023** · notification(基础) **✅ R0024** · M1.7 memory **✅ R0025** · M1.8 sandbox **✅ R0026** · M1.9 permissions/hooks **⏭️ 解散 R0027** · M1.10 document **✅ R0028** · M1.11 todo **✅ R0029** · **波次 1 收官 ✅**
- **波次2 tool+原语**：tool **✅ R0030** · loop **✅ R0031**（+建 messages domain） · tool/**filesystem ✅ R0032**（+首建 pkg/agentstate）· tool/**search ✅ R0033**（+建 pkg/fspath、cwd 全局废弃）· **搜索配置 ✅ R0034**（+建 domain/websearch、workspace 加列）· tool/**web ✅ R0035**（WebFetch 摘要链 + WebSearch 单把 BYOK、删 MCP tier）· tool/**toolset ✅ R0036**（search_tools 检索式懒加载，砍 activate_tools，+agentstate discoveredTools）· **波次 2 收官 ✅**
- **波次3 Quadrinity**：**M3.1 function ✅ R0037**（+建 `app/envfix`）· **M3.2 handler ✅ R0038**（MCP 式单例常驻）· **M3.3 trigger ✅ R0039**（独立信号源实体 + 建 `pkg/cel`）· **skill ✅ R0040**（文件式指令载体，砍 execution/search/polling）· **mcp ✅ R0041**（GitHub registry 全量市场 + 加密表 + 双 transport）+ **R0042 三留口补完**（sandbox 物理 runtime-tool **真机跑通** / handler catalog 列方法 / trigger sensor+mcp）· **agent ✅ R0043**（压轴：配置好的 LLM worker，挂载六件弱引用、invoke 接 loop + InvokeDeps 端口、砍 pending、search 子串）· **M3.7 tool 适配器组 ✅ R0044**（memory/document/shell 三叶子工具：砍 type/cwd/auto-route + 危险硬拦截 6 条 + shell.md 整篇重写）· **波次 3 完整收官 ✅**（**subagent + tool/subagent 后移波次 5，贴 chat**）
- **波次4 编排核心**：workflow **✅ R0047** · **flowrun ✅ R0048**（记忆化 2 表）· **scheduler ✅ R0049**（durable 解释器，🔴最大重灾区拿下）· trigger(Attach/Detach) ⬜（M7 装配）· tool/workflow **✅ R0047**（并入）
- **波次5 对话**：**conversation ✅ R0050**（线程容器 + 配置 CRUD 叶子，document 范式同构）· **attachment 子模块（M5.2 前置）：R0051 存储核心 ✅**（CAS blob + 元数据 + 4 端点）· R0052 多 provider 注入 ⬜ · R0053 sandbox 提取 ⬜（PDF/Office；音频/视频/OCR 留插槽）· chat ⬜ 🔴核心（+ subagent + tool/subagent：递归子对话，共享落盘/model）· contextmgr ⬜ · tool/permissionsgate ⏭️解散
- **波次6 顶层编排**：askai ⬜ · ask+tool/ask ⬜(强残留嫌疑)
- **波次7 wiring**：cmd/server 装配 ⬜ · cmd/desktop+工具 ⬜

## 下一步

- **波次 2 ✅ 全部完成**：M2.1 tool → M2.2 loop（+messages domain）→ M2.3 #1 filesystem（+agentstate）→ #2 search（+fspath、cwd 废弃）→ 搜索配置（R0034，+websearch domain）→ #3 web（删 MCP tier）→ #4 toolset（R0036，search_tools 检索式懒加载，砍 activate_tools）。
- **波次 3 Quadrinity 执行体进行中**：function ✅ R0037 · handler ✅ R0038 · **trigger ✅ R0039**（用户加站：独立信号源实体 + 建 `pkg/cel`）。**skill ✅ R0040**（文件式指令载体；fork 走 `SubagentRunner` 端口、subagent 波次 5 注入）。**mcp ✅ R0041 + 留口补完 R0042**（GitHub registry 全量市场 + 加密表 + stdio/remote 双 transport + 容器实体 catalog；**3 留口 R0042 全补完**：sandbox 物理 runtime-tool **真机跑通**（e2e context7 + dotnet 验证）/ handler catalog 列方法 / trigger sensor+mcp）。**agent ✅ R0043**（压轴：配置好的 LLM worker，挂载六件弱引用；invoke 接 `app/loop.Run` + InvokeDeps 三端口注入 DIP（LLMResolver/ToolsProvider/KnowledgeProvider）；砍 pending/accept；search 子串；relation 5 出边；fake LLM 测 invoke 跑通）。**波次 3 Quadrinity 执行体全部完成 🎉**。**M3.7 tool 适配器组 ✅ R0044**（memory/document/shell 三叶子工具补齐——砍 type/cwd/auto-route + 危险硬拦截 + shell.md 整篇重写；function/handler/trigger/mcp/agent/skill 工具早随各模块完成）。**波次 3 完整收官 🎉**。下一站 **波次 4 编排核心**（workflow / flowrun / scheduler —— durable execution 引擎）。**subagent + tool/subagent 后移波次 5**（贴 chat，共享落盘/model；唯一消费者 skill 走 DIP 端口 + nil 降级不被堵）。
- **波次 4 编排核心进行中**：**workflow 静态图实体 ✅ R0047**（Quadrinity 编排者：节点引用实体、边=控制骨架、按名引用祖先〔model B〕、ops 编辑、ValidateGraph+BackEdges、CapabilityCheck+BuildPinClosure、7 工具、REST——**只 pin 不执行**；建前扣模型收敛〔承重墙/per-port control/agent JSON〕 + doc 20 校准到落地；**pkg/cel 加 ScopedEnv** 接住 node-id 接线〔修 subagent 用固定 env 拒 `reviewer.score` 的 model B 反面〕；ctl/apf 删冗余 Outputs 收尾 R0047②、Inputs 全留）。**flowrun ✅ R0048 + scheduler ✅ R0049 收官**（记忆化 2 表 + ~1500 行 durable 解释器，非 journal；删旧 9302 行引擎；走图四形态 + 三血泪边界 18 集成测试全绿）。**波次 4 编排核心拿下 🎉**——剩 M7 中央装配（Dispatcher 注真 / FiringInbox / ticker DrainFirings·CheckTimeouts / boot Recover / eventlog Emitter / `trigger_workflow` 工具 / Toolset/Register/migrate/SetResolver）+ trigger Attach·Detach + 祖先-only CEL lint 延后。
- M1.1 遗留 → M7：boot 默认 workspace（`Count==0→Create`）+ `WorkspaceResolver` 注入 `IdentifyWorkspace` + `~/.forgify/` 共享资源布局落地（不分桶）。
