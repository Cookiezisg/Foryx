# backendcleaner ROUNDS

| Round | 日期 | 阶段 | 目标 | 结果 |
|---|---|---|---|---|
| 0058 | 2026-06-09 | 波次5 · M5.2+ | subagent（递归子对话） | ✅ 先**三路 Explore 考察**（旧 subagent 必留/必废 + 当前复用面 + 持久化模型）+ 用户拍板**持久化 B**，再建。**app/subagent**：registry（Explore[Read/LS/Glob/Grep] / Plan[+WebFetch/WebSearch] / general-purpose[父全集]，`filterTools` 白名单交集 + 总剔 Subagent 防递归）+ `Service.Spawn(ctx,agentType,prompt)→(result,err)` 实现 `skilldomain.SubagentRunner`（**混血 host**：LoadHistory=任务 prompt〔agentHost〕/ Tools=静态白名单〔agentHost〕/ WriteFinalize=Detached 落 sub-message + message_stop〔chatHost〕）+ emit（subagent 自己的 message 节点 `Open.ParentID`=派它的 tool_call，E3）。**app/tool/subagent**：`Subagent` 工具 5 方法 `{subagent_type enum, prompt}`，Execute **防递归**（`GetSubagentID` 在则拒）+ 调 Spawn。**持久化 B**：`Message += SubagentID`（sub-message 落**父对话** messages、`Attrs.parentBlockId` 锚；chat `LoadHistory` 排除 `SubagentID!=""`〔不污染父历史，父只见 tool_call + tool_result=最终答案〕；`ListMessages` 返回 → reload 重建子树）+ store DDL `subagent_id` 列。**E3 gap 补**：loop `runOneTool` 执行工具前 `reqctx.SetToolCallID(tc.ID)`（+ reqctx `Set/GetToolCallID`），使工具知自己 call id 作锚。**model 承袭** = 自有 `ModelResolver.Resolve`（M7=`Resolve(ScenarioDialogue,nil,picker)`=workspace 默认=父常见 effective；override 承袭越 pkg→domain 延后）。**防递归**双保险（SubagentID 存在性 + filterTools 剔 Subagent，深度 1，废 DOC-123「限 2」）。**无 catalog/relation/REST/新 error-code**（运行时机制；派发失败软返 tool-result）；DB 仅 1 列。subagent 4 测（registry/filterTools / Spawn 端到端落 sub-message SubagentID+anchor+result / 防递归 / 未知类型）+ tool 4 测（Execute/防递归/spawn 错/validate）+ chat 1 测（LoadHistory 排除 subagent）。契约 DOC-123 整篇重写（砍 cv_xxx/深度2/沙箱/4 虚构码）+ messages.md/database SubagentID + chat.md §4.1 过滤 + contract #40。gofmt/build ./.../vet/test **101 ok/0 FAIL**。**M7 装配**：skill 注入 runner（替 nil）+ Subagent 工具入 Toolset + Deps 注真。**波次 5 仅剩 contextmgr** |
| 0057 | 2026-06-09 | 波次5 · M5.2 子轮4/4·**收官** | chat 收尾（auto-title + usage + preview） | ✅ 补齐累积 deferred，**chat 模块完整收官 🎉**。**auto-title**：`ModelResolver` += `ResolveUtility`（utility 场景）+ `Deps{Titler, Notifier}`；`autotitle.go` 首回合（conv 无标题 + 未 AutoTitled）`processTask` 后台 detached + 10s + best-effort：`LoadThread` 取首 user + assistant 摘要 → `ResolveUtility` + `llm.Generate`（System=5-10 词标题）→ `cleanTitle`（去引号/首尾标点/单行/截断）→ `ConversationTitler.SetAutoTitle` → `conversation.auto_titled` 通知；s.wg 追踪、失败静默（标题非关键路径）。`conversationapp.Service` += `SetAutoTitle`（写 Title+AutoTitled，PATCH 不暴露，**不覆盖用户已设标题**）。**`GET /system-prompt-preview`**（`Service.SystemPromptPreview` 复用 buildSystemPrompt、不解析模型 → `{systemPrompt}`）。**`GET /usage`**（`messages.Repository` += `SumTokens` 聚合 + `Service.Usage` 透传 → `{inputTokens,outputTokens,totalTokens}`；**专用端点解耦 conversation←messages**，非富化 conversation GET——记 R0050 契约微调）。**砍 export/llm-trace**（投机占位、YAGNI，从 api.md 删）。chat +3 测试（auto-title fake utility 首回合触发 + 标题清洗 / SumTokens 求和 / SystemPromptPreview 含 Section）全绿。契约 chat.md 交付分轮+§11 as-built + api.md 2 端点 + contract #39；**无新 error-code**；DB 无变。**待 model 域补**：attachment caps 的 vision/nativeDocs flag（model DescribeModels）。gofmt/build ./.../vet/test **99 ok/0 FAIL**。**chat 收官**（R0054 落盘 + R0055 引擎 + R0056 可用面/mention + R0057 收尾）→ 波次 5 剩 subagent / contextmgr |
| 0056 | 2026-06-09 | 波次5 · M5.2 子轮3/3 | chat 外围（可用面 + mention） | ✅ 给 R0055 引擎补对外可用面 + @ mention。**HTTP handler**（`transport/httpapi/handlers/chat.go` 3 端点照 conversation 范式）：`POST /conversations/{id}/messages`（**202** `{messageId}`，body `{content,attachmentIds?,mentions?}`，回合经 messages SSE）/ `GET /conversations/{id}/messages`（Paged 最新在前 + blocks，N4）/ `DELETE /conversations/{id}/stream`（**204**）。**mention 整套**：Service += `RegisterMentionResolver`（map[Type]Resolver，各域 M7 注册）+ **freeze-on-send**（`resolveMentions`：Send 时经注册表抓 `Reference` 快照存 `Attrs["mentions"]`，缺 resolver/失败→stub `(unavailable)` 不阻断）+ **渲染**（`renderMentions`：LoadHistory 从快照渲 `<mentions>` 前置 user 文本）；`SendInput` += `Mentions`；**补 backend-new 缺失的 `workflow`/`agent` 两个 `AsMentionResolver`**（5 类 document/function/handler/workflow/agent resolver 全在）。**`Service.Cancel`**（取 queue.cancel〔R0055 已存〕触发运行回合 ctx Done → loop 中断 → WriteFinalize 落 cancelled；drain 积压回合逐个 `finalizeCancelled` 防孤儿；无队列 no-op）+ `ListMessages` 透传（handler→app→store）。**复用 R0055 已存 queue.cancel**（无新机制）。chat +4 测试（mention resolve/freeze + 缺 resolver stub / render / Cancel no-queue / finalizeCancelled 落 cancelled + message_stop）全绿。契约 chat.md §8 mention freeze + §11 端点 + api.md 3 端点 as-built + contract #38；**无新 error-code**（EMPTY_CONTENT/STREAM_IN_PROGRESS 已在 §2.4）。**范围克制**：auto-title〔需 utility resolver + conversation 写〕/ conversation tokensUsed / system-prompt-preview / export / llm-trace 留 **R0057 chat 收尾**。gofmt/build ./.../vet/test **99 ok/0 FAIL**。**chat 端到端可用**（HTTP 发消息→SSE 流式→历史→取消 + @ mention） |
| 0055 | 2026-06-09 | 波次5 · M5.2 子轮2/3 | chat 引擎核心（对话 runner） | ✅ 先**三路 Explore 考察**（旧 chat 必留行为 + backend-new 12 依赖当前接口 + SSE 发射机制）+ 逐个源码核实精确签名，再建 `app/chat`。**chatHost = `loop.Host` 持久化版**（骨架照 agentHost，两改写）：`LoadHistory`（`messages.LoadThread` + conv.Summary 前置 + user 回合 `attachment.ToContentParts` 多模态 parts + assistant `loop.BlocksToAssistantLLM` 投影 + 跳过在飞回合）/ `Tools`（resident + `search_tools` + agentstate 已 discovered 的 lazy，每步重算）/ `WriteFinalize`（**Detached** `context.Background()` + `FinalizeMessage` + 推 message_stop，防 streaming 孤儿）；可选 `AutoActivator`（`FindLazy`→`MarkToolDiscovered`）+ `ReminderProvider`（todo），不实现 StepRecorder。**convQueue** per-conv 串行（容量 5 → `STREAM_IN_PROGRESS`、idle GC 5min 自毁注销、agentState 挂 queue、cancel 存储留 R0056）。**Send**（202 语义：落 user 回合 `CreateMessage` + emit user echo + 开 assistant 回合 message_start + 入队，携 wsID/locale 给脱离的队列 goroutine）。**System Prompt** 10 Section `<section name>` 容器、cache-friendly 顺序、**静态段重写**（identity/how_to_work/tools/architecture_rules/critical_rules，高密度/去 fluff/去 safety theater）、动态段调现成 provider（catalog/memory/document/conv.SystemPrompt/environment locale）。**SSE "message" 顶层节点**（messages.md §3 加：chat 第二 producer，message_start=`Open{role}` / message_stop=`Close{Result: tokens+终态元数据}`，block 挂其下、token 进 message 元数据不进 block）。**model resolve** `ResolveChat(ScenarioDialogue, conv.ModelOverride)→Bundle{Client,Request,Caps,Provider}`（M7 适配器对标 agent runLoop）。**DIP 端口** `Deps`（9 协作者，可选 nil 降级）。**复用 loop 不重写循环**（熔断/maxSteps=25 在 loop）。**范围克制**：mention 整套 / HTTP handler / auto-title / cancel 端点 / tokensUsed 富化 留 R0056。chat 5 测试全绿（fake LLM 端到端 Send→落盘+流帧序 / EMPTY_CONTENT / STREAM_IN_PROGRESS / System Prompt Section / LoadHistory 拼装含跳过在飞）。契约 chat.md DOC-104 引擎 as-built 重写 + messages.md §3 message 节点 + contract #37；**无新 REST/error-code**（EMPTY_CONTENT/STREAM_IN_PROGRESS 已在 §2.4）；events.md 全量重写随覆盖阶段。gofmt/build ./.../vet/test **99 ok/0 FAIL**。**R0056 chat 外围收官** 续 |
| 0054 | 2026-06-09 | 波次5 · M5.2 子轮1/3 | messages 持久化（chat runner 落盘地基） | ✅ 给 chat runner 建消息落盘层。**domain/messages 加 `Message` 实体**（turn 记录：role/status/stop_reason/error/tokens/provider·model 溯源/attrs〔attachments·mentions 快照〕+ Blocks 非列；**归 domain/messages 非复活 domain/chat**——与 Block 同中立内容模型，agent/subagent/chat 共享、修正旧 loop→chat 反向耦合）+ `Repository` 5 法 + `ErrMessageNotFound`；**Block 补 `workspace_id,ws`**（D2，R0031 漏的隔离列）。**infra/store/messages 两表**（messages + message_blocks，**append-only** 无 deleted_at·D1、workspace 隔离、role/status/type/context_role CHECK、`idx_blocks_conv_seq`=UNIQUE(conversation_id,seq) seq 单调）。**两段式写对齐 loop.Host**：`CreateMessage`（开回合，tx：行 + seq + blocks）/ `FinalizeMessage`（收终态 + 追加 blocks，缺失→ErrMessageNotFound）/ `GetMessage` / `ListMessages`（orm Page **最新在前** N4 分页）/ `LoadThread`（全量**最旧在前**供 chat LoadHistory）；seq = 对话内 `MAX+1`（靠 chat convQueue per-conv 串行写、非 DB 序列）；block id 落盘补全（loop text/reasoning 无 id→mint `blk_`，tool_call/result 已带）。store 9 测试全绿（round-trip / seq 跨消息单调 + 新对话重起 / block 树 parent / finalize 终态 + not-found / 分页最新在前 / LoadThread 时序 + blocks 按 seq / ws 隔离）。契约 messages.md §1+§5+§6 + database §2.2 as-built + error-codes（MESSAGE_NOT_FOUND 出处 chatdomain→messagesdomain）+ contract #36；**无新 REST/SSE**（store 层）。gofmt/build ./.../vet/test **98 ok/0 FAIL**。**R0055 chat Service+host+runner 核心** 续 |
| 0053 | 2026-06-09 | 波次5 · M5.2前置 | attachment sandbox 提取（多模态子模块 3/3，**收官**） | ✅ 给不能原生读文档的模型把 PDF/Office **在 sandbox 跑 python 抽文本**内联。可插 `Extractor` 端口（`Extract(mime,data)`，不认的 mime 返 `ErrExtractionUnsupported` 降级）+ `SandboxExtractor`（依赖 `SandboxRunner` 端口 EnsureEnv+Spawn、DIP 结构化满足；`python -c <内嵌脚本> <mime>`、字节走 stdin、pdfplumber/python-docx/openpyxl/python-pptx 分派、打印 {text\|error}）。**强化地基**：sandbox 加 `attachment` owner kind（domain 常量 + envs.owner_kind CHECK），固定全机共享 owner `{attachment,"extractor"}` 单 venv 跨 ws 复用（无用户数据、字节流过）。`ToContentParts` 签名 `visionCapable bool`→`Capabilities{Vision,NativeDocs}`：document = **NativeDocs?file part:抽文本**（token 截断 maxExtractedChars=400K，无 extractor/失败→占位）。**无独立缓存**（抽出 text 由 chat M5.2 落 message_blocks 天然抽一次）。音频/视频/OCR 留独立 extractor 延后。extractor 路径 3 + SandboxExtractor 4 + R0052 测改 caps 全绿；契约 attachment.md §4/§5/§7 as-built + sandbox.md owner_kind + contract #35；**无新 REST/码**。gofmt/build/vet/test **97 ok/0 FAIL**。**attachment 子模块完整收官 🎉 → M5.2 chat** |
| 0052 | 2026-06-09 | 波次5 · M5.2前置 | attachment 多 provider 注入（多模态子模块 2/3） | ✅ 用户「各家分开做、挨个查官方文档对齐、都做完」——**11 家 LLM provider 多模态 wire 逐一对官方文档渲染**（每家自包含、不共享 wire 基座，延续 R0014-16「重复<错抽象」）：原生内联 PDF **仅** anthropic〔`document` 块〕/openai〔`file` part〕/gemini〔`inlineData`〕三家；deepseek/qwen/zhipu/doubao/moonshot/openrouter/custom = `image_url` data-URL + **PDF 降级跳过**（非报错，靠 R0053 抽文本补）；ollama = 原生 `images[]` 裸 base64（剥 data-URL 前缀）；moonshot `content` string→`json.RawMessage` 容纳 parts 数组。`ContentPart` 扩 MediaType/Data/Filename + `PartFile` 常量。**app/attachment `ToContentParts(att_ids,visionCapable)→[]ContentPart`**：image→image_url（仅 visionCapable，否则降级文字提示）/text→内联/pdf→file part/audio·video·other→占位（R0053）；缺失 id 跳过 + **按 att_ids 保序**（GetBatch WHERE-IN 不保序、按 id 重排）；vision 门控用形参（本层不持模型目录）。multimodal_test 11 家渲染 + 9 家纯文本回归 + ToContentParts 4 测全绿；修 ollama `+=`→strings.Builder。契约 attachment.md §4 重写 as-built + §3/§7 修 + contract #34；**无新 REST/DB/码**。gofmt/build ./.../vet/test 全绿。**R0053 sandbox 提取** 续 |
| 0051 | 2026-06-09 | 波次5 · M5.2前置 | attachment 存储核心（多模态子模块 1/3） | ✅ 用户要完整多模态——抄 LibreChat（OSS 最全流水线）+ **3 Forgify 升级**：**CAS 内容寻址存储**（dedup+完整性）/ **sandbox 当本地提取引擎**（python 无云 OCR/key）/ **中立 ContentPart 多 provider 渲染**；**无 RAG**；主线 图/文本/PDF/Office 扎实做、音频/视频/OCR 经可插 `Extractor` 端口留插槽（dogfood 再上）。**R0051 存储**：domain(att_ + Kind 6 类 + KindFromMIME + 50MB + 3 码 + Repository) + infra/fs/blob(CAS `<ws>/blobs/<sha[:2]>/<sha>` + 内容寻址 dedup + Sweep GC + 64-hex 防穿越) + orm 元数据表(sha256 不唯一=dedup、软删 D1) + app(Upload/Download/Delete/GC 按 sha refcount) + 4 端点(POST multipart / GET / GET content / DELETE)；提升为独立 attachment 域(取代旧 chat 内嵌 + 5 旧 ErrAttachment* 码)；blob 9 + store 6 + app 8 测试全绿(dedup 幂等 / ListLiveSHAs 软删-aware / GC sha refcount)；契约 DOC-307 + database §2.2b + api 4 端点 + error-codes §2.4b + contract #33；gofmt/build ./.../vet/test 全绿。**R0052 多 provider 注入 → R0053 sandbox 提取** 续 |
| 0050 | 2026-06-09 | 波次5 · M5.1 | conversation 线程容器（波次 5 第一站） | ✅ 对话线程的**持久化容器 + 线程级配置**（CRUD 叶子，document 范式同构，无 LLM/loop/消息）；去 GORM/user_id → pkg/orm（自动 ws 隔离 + 软删 `deleted_at` D1 + `Page` keyset 置顶优先）；**modelOverride 仅结构校验照 agent、对 apikey 零依赖**；删 `AnyReferencesApiKey`（零消费者）；`conversation.*` 经 notification.Emitter；relation 第 8 节点 Namer/PurgeEntity；**全 schema 一次到位**（summary/autoTitled 列声明、后续轮写——summary 已被 messages domain 前向引用）；2 错误码 `CONVERSATION_NOT_FOUND`/`_INVALID_MODEL_OVERRIDE`；5 CRUD 端点（system-prompt-preview + tokensUsed 延后 M5.2）；store 13 + app 8 测试全绿；契约 DOC-106 重写 + database/api/error-codes + contract #32；gofmt/build/vet/test 全绿 |
| 0049 | 2026-06-09 | 波次4 · M4.3 | scheduler durable 解释器（🔴最大重灾区拿下，删旧 9302 行事件溯源引擎）| ✅ ~1500 行纯解释器(无实体)取代旧 14-dispatcher 屎山。**幂等 advance**(读 frn+pin 图→computeReady→dispatch/内联→upsert→loop until 无 ready→finalize；崩溃=再调一次，completed 抄不重跑)；walk.go **从 control/approval 决策重推活跃子图**(判定②，BPMN 状态式/Conductor decider；readiness 统一 AND-join+simple-merge、无 skip 传播；回边→iteration+1 仅源 completed+port 命中才走；MaxIterations 帽；scopeFor model-B 按 node-id 取祖先 max-iter≤k)；dispatch **14→2**(RunAction[fn/hd/mcp]+RunAgent 粗粒度)+2 内联(control first-true-wins/approval 渲染 park)；CEL 双轨(节点 Input=ScopedEnv node-id 根 / when·emit·template=input 根)；run.go StartRun 两入口(手动 POST /flowruns 直调 + firing 单事务 claim ADR-021 via SeedRunOnTx)+overlap(serial defer/Skip/AllowAll)+Recover(跨 ws boot)+DecideApproval(first-wins)+CheckTimeouts(approval durable timer)+Replay；DIP 6 端口；handler 6 端点；ControlResult 改扁平 emit+__port(对齐 doc 20 §5.4)；**集成测试 18 个全绿**(走图线性/AND-join/XOR+simple-merge/回边循环 + 三血泪边界 + 崩溃恢复/at-least-once 丢行重跑 count=2 + park/resume/timeout + firing claim + overlap + replay)；契约 database/api/error-codes/events + domains/flowrun.md(DOC-109)+scheduler.md(DOC-119)重写 + contract #31 + doc 21 §3.2 doc-fix；gofmt/build ./.../vet/test 全绿 |
| 0048 | 2026-06-09 | 波次4 · M4.2 | flowrun 记忆化 2 表 domain+store | ✅ durable 执行的持久化状态(节点结果记忆化，非事件日志)。domain/flowrun:FlowRun(fr_)+FlowRunNode(frn_ 真相表)**2 实体无 frs_**(agent 粗粒度)；result 形状契约(ControlResult 扁平+__port / ApprovalDecision)；5 错误码 FLOWRUN_*；Log 表无软删(D1)。store:2 表手写 DDL + `idx_frn_once`=UNIQUE(flowrun_id,node_id,iteration) record-once(D3)；InsertNodeResult first-wins(ErrConflict→inserted=false)；ResolveParkedNode 条件 UPDATE WHERE status=parked=approval first-wins；SeedRunOnTx/CreateRunWithTrigger 原子建 run+seed trigger 节点；DeleteFailedNodes 物理删(Log 唯一允许)；ReopenForReplay failed→running+replay_count++；CountRunningByWorkflow(overlap)；ListRunningRuns 跨 ws(boot)；inbox=parked 行(无 apv_)。测试 record-once/approval first-wins/replay/ws 隔离+跨 ws boot 全绿；契约随 R0049 整体落 |
| 0047 | 2026-06-09 | 波次4 · M4.1 | workflow 静态图实体（波次 4 第一块：Quadrinity 编排者落地） | ✅ doc 20「function 范式套图」落地——`wf_`/`wfv_` 静态编排图：节点按 id 引用 trg_/fn_·hd_·mcp_/ag_/ctl_/apf_、边画控制骨架、图 JSON 存版本行，**只 STORE+VALIDATE+PIN 不执行**。**建前先扣模型**（用户「先扣模型」）：承重墙=一切数据皆某祖先 result + `ctx.runId`、边不搬数据、下游按名引用（model B）；**control 输出定 per-port**→删先前为对称给 ctl/apf 加的 `Outputs`（输出已被各 `Branch.Emit` keys/`{decision,reason}` 常量描述、单存冗余且并集丢精度），**Inputs 全留**（run/invoke 表单 + 接线需入参清单）；agent input=结构化 JSON 喂 LLM 非插值；据此 **doc 20 校准到落地**（FieldSpec→schema.Field、InputSchema→Inputs、Branch.Port 保留、agent JSON、mcp FromJSONSchema）。domain(Graph/Node/Edge/RetryConfig + ValidateGraph〔形状/良构/≥1 trigger/可达/环=control·approval 可归约回边/端口形状〕 + **BackEdges 纯函数解释器复用** + 7 ops〔ApplyOps RFC7396 + 级联删边〕 + 8 errorsdomain) + store(两表 orm·partial-UNIQUE·CHECK·trim 护 active) + app(Service CRUD/ops/CapabilityCheck/**BuildPinClosure**〔agent 挂载 depth-2〕 + **RefResolver 端口 nil 容忍** + **WorkflowReader DIP** + catalog/relation) + 7 Lazy 工具(无 trigger/执行类) + handler(REST + :activate/:deactivate/:capability-check，无 :trigger) + relation 第 **12** 类 `wf_`(早预登记)。**关键修复**：subagent 首版用固定 env(payload/ctx/input)编译节点 Input→拒 `reviewer.score` 即 model B 反面；**pkg/cel 加 `ScopedEnv`**(根=图 node ids + ctx)，引用非节点名→编译失败=白送「引用存在节点」校验(「只可祖先」lint 仍 deferred)。全离线测 domain/store/app/tool + ScopedEnv 全绿(build/vet/gofmt/test exit 0)。契约 workflow.md 重写(DOC-128 纠 stale pending/gorm/tool·case)+database §4.7/§3.2/S15+api §2.3+error-codes 8 码+contract #29。**执行(解释器/scheduler/flowrun)+祖先-only lint+M7 装配延后**。下一站 flowrun + scheduler(durable 解释器) |
| 0046 | 2026-06-08 | 波次4前置 · 18重定型 | approval 渲染实体（旧 approval 节点的 prompt 模板 + 决策规则 → 独立 AI 工作实体；18 第 2 个落地，control 后） | ✅ 与 control 几乎双胞胎，**3 处真实差异**：①内容是 `template`(markdown) + 决策规则(`allowReason`/`timeout`/`timeoutBehavior`)，非 branches ②**`{{ CEL }}` 模板**非裸 CEL → **给 `pkg/cel` 加模板地基**（`Template`/`CompileTemplate`/`Render`，agent.prompt 波次 4 复用同一套）③`timeout` duration(支持 `30d`/`2w`)+ `timeoutBehavior` 联动。**前缀分层修正**：`apf_`/`apfv_`（form=配置），**≠ `apv_`**（`approvals` 运行时表，17/database §1 已占）→ 18 文档 `apv_`/`apvv_` 撞运行时前缀、全局 doc-fix。domain(`apf_`/`apfv_`/`ValidateForm`/`ParseTimeout`/7 errorsdomain) + store(两表 + `allow_reason` bool 往返) + app(Service 9 法 + `validateForm`〔结构 domain + `CompileTemplate`〕 + 适配器) + 6 Lazy 工具(无 run) + REST(无 `:run`/pending) + relation 第 **11** 类 EntityKind `approval`。全离线测 `pkg/cel` + domain/store/app/tool/relation 全绿。契约 approval.md(DOC-306) + database §4.6 + api §2.7 + error-codes §2.6c 7 码 + relation 10→11 + contract #26 + 18 doc-fix。装配留 M7。**两个新 AI 工作实体（ctl_/apf_）收齐** |
| 0045 | 2026-06-08 | 波次4前置 · 18重定型 | control 逻辑实体（旧 case 节点的 CEL 路由逻辑 → 独立 AI 工作实体） | ✅ 一段长设计对话的产物：用户从「CEL 实体化」纠结，逐步推导出 workflow 图模型**彻底重定型**（`18-graph-model-redesign` 同步起草：**workflow = 纯编排数据依赖图、5 节点 trigger/action/agent/control/approval 各引用一类实体、边 = payload 数据管道**）。control 是首个落地新实体——旧 `case` 节点内联的 CEL 路由（when/emit）物化成独立 `ctl_`。**用户 10 条拍板**：①CEL 不实体化/control 逻辑坨才实体化 ②强制每个都是实体（清晰心智 > catalog 洁癖）③版本（pin 必需）去 sandbox/env/executions ④全量 branches 无 ops ⑤强制末条 `when:"true"` 兜底 ⑥无 run/executions ⑦relation 轻（只被引用 + Namer、不产边）⑧不进 mention ⑨独立孤儿 ⑩payload 隐式（对齐 17 §5、不改 17）。domain(`ctl_`/`ctlv_`/`Branch{port,when,emit}`/`ValidateBranches`/8 errorsdomain) + store(两表 orm + TrimProtectsActive) + app(Service 9 法 + validateBranches〔结构 domain + CEL `pkg/cel`〕 + catalog/relation/notif) + 6 Lazy 工具(无 run) + REST(无 `:run`/pending) + relation 第 **10** 类 EntityKind `control`。全离线测 domain/store/app/tool/relation 全绿。契约 control.md(DOC-305) + database §4.5 + 前缀 + api §2.6 + error-codes §2.6b 8 码 + relation 9→10 + contract #25。装配留 M7。**波次 4 第一块地基** |
| 0044 | 2026-06-08 | 波次3 · M3.7 | tool 适配器组（memory/document/shell 三叶子工具，波次 3 收官） | ✅ 补齐 backend-new 唯三还没建的工具适配器，皆 **filesystem.md 范式叶子工具**（无 domain/store/handler/DDL/HTTP，只 5 方法、软失败返串）。**核心教训「不能照抄旧」**——逐项核对 backend-new 真实接口 + 回顾契约文档：**memory** read/write/forget（**砍 `type` 四类**，memory.md §3 已白纸黑字；write `source=ai` 内定、不暴露 `pinned`）· **document** 7 工具（`CreateInput/UpdateInput/MoveInput` 已挪 `documentdomain`、app `type=` alias；`edit`→Service.`Update`；`delete` 砍 `destructive`；errorsdomain 转软失败串；**真 SQLite store 端到端测**）· **shell** Bash/BashOutput/KillShell + ProcessManager（**砍整个 cwd 机制** R0033；**砍 auto-route** 留波次5、不引 `mvdan.cc/sh`、不预留 sandbox 字段；**新增 `danger.go` 硬拦截 6 条** rm-rf//~/sudo/mkfs/dd-of-dev/fork-bomb 替代解散的 permissionsgate、**非 allow/deny**；砍 Snapshots/ConvID）。**文档纠正（用户「别光看代码、回顾文档」揪出）**：**shell.md DOC-121 严重 stale**（停 2026-05-03：30s/run_bash/allow-deny/6 个孤岛 HTTP 错误码连 error-codes.md 都没进/auto-route 当核心）→ **整篇重写**对标 filesystem.md；agentstate.go 注释 doc-fix（去 cwd）；database.md 登记 `bsh_`。memory/document/filesystem.md 核对皆 ✅ 准。**memory Lazy + document Lazy + shell Resident**，装入 Toolset 留 M7。三新包全离线测全绿（memory fake repo · document 真 SQLite store · shell 真子进程 + `checkDangerous` 纯函数测灾难命令）、不破坏任何包。契约 shell.md 重写 + memory/document.md 补 + database/contract #24。**波次 3 完整收官 🎉**（Quadrinity 执行体 + 全部工具适配器）。gofmt/build/vet/test 全绿 |
| 0043 | 2026-06-08 | 波次3 · M3.6 | agent 重写（配置好的 LLM worker，波次 3 压轴收官） | ✅ Quadrinity 第四元、最综合实体（挂载 skill/mcp/doc/fn/hd/model 六件，跑 ReAct loop）。全身唯一与 function/handler 范式实质差异是 **pending/accept，砍掉即对齐**。两层 Agent(`ag_`)+AgentVersion(`agv_`，max+1 无 status)+AgentExecution(`agx_`，无 deleted_at)；挂载六件全 JSON 弱引用（tools 禁 `ag_`=员工不调员工）。**invoke 接 `app/loop.Run` + InvokeDeps 三端口注入 DIP**（LLMResolver/ToolsProvider/KnowledgeProvider，M7 注真/测试注 fake——agent 是 backend-new 第一个跑完整 ReAct loop 的实体）；**SSE 白捡**（loop emitter ctx-driven，chat 内嵌套 subagent 子树，零 stream 代码）。**用户参与设计**：① outputSchema 三态语义（不是 agent 分三种、是输出形态配置项三选一；free_text+json_schema 覆盖任意输出，enum 常用特例+coerce 规整下游 case）② **search 子串**（内存 Contains、无 LLM rerank，对齐全实体统一范式——用户揪出的对齐点）③ invoke 选 A（连 loop + 端口注入，fake 测）。relation 5 出边(KindEquip)+forged 入边(create/edit)；execution 对齐 function 简化(OK/Failed)；9 工具(无 accept)+REST 砍 3 pending。**砍** pending/accept · LLM rerank · AttentionReason · CheckPermissions · p95/avg · dispatch 内联路径。store/app(invoke fake LLM 跑真 loop+execution 落表)/tool 测试全绿；`TestService_InvokeRunsLoopAndRecords` 是选 A 核心保证。契约 agent.md(DOC-129 重写)+database/api/error-codes+contract。boot+SetInvokeDeps 留 M7。**波次 3 Quadrinity 执行体全部完成 🎉**。gofmt/build/vet/test 全绿 |
| 0042 | 2026-06-07 | 波次3 · M3.5 | M3.5 三处留口补做 + sandbox 物理 runtime-tool 真机跑通 | ✅ R0041 mcp 留的 3 处留口全补完。**留口2 handler catalog 列方法名**（已 commit 071e8425）：handler source 填 active version 方法名（`GetVersion(ActiveVersionID).Methods[].Name`），与 mcp 同款容器实体范式（catalog item 摊平内部成员名）；测试 `TestCatalogSource_ListsMethodNames`。**留口3 trigger sensor 绑 mcp.tool**（已 commit 071e8425）：回改 trigger R0039，sensor target 加第三类 `SensorTargetMCP`（TargetID=server / Method=tool，mcp 同 handler 要求 Method 非空）+ relation `syncSensorBinding` 加 mcp 分支（trigger→mcp 的 `equip` 出向边）；测试 `TestValidateConfig_SensorTargets`；实际 Invoke 路由（SensorInvoker）随 M7 装配，function/handler/mcp 三路由现都未接。**留口1 sandbox 物理 runtime-tool**（待提 commit）：R0041 stdio transport 用 `SpawnLongLived` 起 MCP 进程，但 sandbox R0026 的 `ResolveExec` 只认 env 内依赖、不认 MCP 启动方式 `npx/uvx/dnx`（runtime 自带包运行器）。5 点回改：① **node ResolveExec** 认 `npx/npm/node/corepack`→`<runtimeRef>/bin/<cmd>`（runtimeRef 现为绝对 install dir）② **python ResolveExec** 认 `uvx/uv`→`EnsureTool("uv")` 同目录（**修正 R0041 留口「python uvx 需装 uv」之误——sandbox python 本就是 uv-backed（uv venv/uv pip 经 EnsureTool），uv 早在、uvx 随 uv 自带**）③ **dotnet.go 新 `DotnetEnvManager`（第 4 runtime）**：CreateEnv/InstallDeps no-op（dnx 拉包即跑无 env），ResolveExec `dnx/dotnet`→`<runtimeRef>/dnx`（**顶层**非 bin/）④ **prepareSpawn**：空-Cmd 检查移到 runtime 查之后 + docker 放行（image entrypoint 无命令），非-docker 传**绝对 runtimeRef** ⑤ **prependPATH**：非-docker spawn 把 `<rt>/bin`+`<rt>` 塞 PATH 最前——**端到端真跑逼出的硬集成点**（npx shebang `#!/usr/bin/env node`，运行时须能找到 node；dnx 顶层同理调 dotnet）。**真机验证（darwin/arm64）**：`mise install dotnet@10.0.300` 真装成功确认 dnx 在顶层；e2e（`infra/mcp/e2e_test.go`，build tag `e2e`）embed mise→装 node 22.22.3→ResolveExec npx→SpawnLongLived（PATH 注入）→go-sdk Initialize+ListTools→**context7 v3.1.0 真起来、列出 2 工具（resolve-library-id/query-docs），PASS 17.91s**。**注册留 M7**：dotnet Installer/EnvManager + node/python/docker/dotnet 全 runtime 注册留 cmd/server（现都没人注册）。单测 `infra/sandbox/resolveexec_test.go`（3 runtime ResolveExec）+ `app/sandbox/path_test.go`（prependPATH）；契约 contract #22 + sandbox.md（DOC-118：dotnet 第 4 runtime + runtime-tool 解析 + PATH 注入 + docker 空 Cmd）；gofmt/build/vet/test 全绿（e2e 离线不跑）。**波次 3 M3.5 收官** |
| 0041 | 2026-06-07 | 波次3 · M3.5 | mcp 重写（GitHub registry 全量市场 + 加密表 + stdio/remote 双 transport + 容器实体 catalog） | ✅ 调研 github.com/mcp 确认是有标准 API 的 registry（99 个 server，`api.mcp.github.com/v0/servers`），改判市场从「硬编码 24 curated」→**对接 GitHub registry 全量**（`go:embed` snapshot + 磁盘 cache 三级兜底离线）。99 个：38 remote + 38 node + 11 python + 9 docker + 3 .NET（2 有 npm 备选），95+ 可装（embed 测试守住）。**`mcp_servers` 表**取代 mcp.json + 砍 `mcp_calls`/`mcp_health_history` 两表，`config_enc` AES-GCM 加密 {env,headers}（store 层封装）。**进程池单例 `map[mcp_id]`**（对齐 handler：Boot per-ws/crash 重连/手动 reconnect/Shutdown）。transport **stdio(sandbox SpawnLongLived + go-sdk IOTransport，进程归 sandbox)** + **sse/streamable-http**(go-sdk，headers 注入)。安装遍历 packages 按 runtime 优先级(node>python>docker>dotnet)挑最优。**catalog 报 server 名+描述+全工具名**(Item.Members 新字段 + mechanical 渲染)。工具 = 4 系统(list_marketplace/install/uninstall/**reconnect_mcp**) + 动态 `mcp__server__tool`(进检索池不进 Overview，catalog 引导)。**砍** Search LLM rerank/searchrouter/call_mcp_tool/审计工具/curated 硬编码。danger 零逻辑(LLM 自报)。domain(实体+Plan 纯逻辑+10 errorsdomain)+infra/mcp(client 双 transport+GitHubRegistrySource+embed+config)+store(加密表)+app(进程池+install+catalog/relation)+5+4 工具+handler+回改 catalog(Item.Members+渲染)。go.mod +go-sdk v1.6.0。domain 6+store 6+app 7+infra 3+tool 3 测试全绿；契约 mcp.md 重写(DOC-112)+database/api/error-codes/catalog.md+contract #21。**留口**(deps-todo)：sandbox 物理 runtime-tool(npx/uvx/dnx)+docker 空Cmd+dotnet runtime(需真实环境)、handler catalog 列方法、trigger sensor+mcp.tool。gofmt/build/vet/test 全绿 |
| 0040 | 2026-06-07 | 波次3 · M3.4 | skill 重写（文件式指令载体，对齐 memory，砍 execution/search） | ✅ 用户拍板把 skill 从「LLM 搜索 + 执行审计 + 常驻轮询 + GORM」收敛为**纯文件式指令载体**（memory 的近亲，非 function 的近亲）。**对齐 function 工具命名**（create/edit/delete/get/activate）但**砍 execution 审计 + search**（对 skill 是抽象错配/校验剧场）+ **纯按需扫描**（砍 polling.go/缓存/fingerprint）+ **无版本**（编辑即覆盖）。调研（deep-research 104 agent / 22 源 / 19 confirmed）推翻两倾向：① allowed-tools = **预授权**（免危险确认）非白名单限制，与「无中央门控」自洽；② AI 自创 skill 有 Anthropic 愿景背书。allowed-tools 存 `agentstate.activeSkill`（消费留 ask 波次 6）；AI 可创建（`source=ai`）；disable-model-invocation 实装；fork 经 `SubagentRunner` 端口（subagent 波次 5，nil 降级）；relation 节点用 name（`sk_` 作废）。domain（Frontmatter 全字段镜像 + 6 errorsdomain，无 id）+ infra/fs（目录式 SKILL.md，`go.yaml.in/yaml/v3`，按需扫描，原子写）+ app（scan/activate[inline·fork]/mutate + catalog/relation 适配器）+ 5 工具 + handler（REST + `:activate`）+ agentstate activeSkill 槽。store 6 + app 8 + agentstate 1 测试；契约 skill.md 重写(DOC-122) + api(删 :import/:refresh/:body、:invoke→:activate) + error-codes(改名 + 加 2 码 + 删 ExecutionNotFound) + database(删 skill_executions/`ske_`、`sk_` 作废) + contract #20；gofmt/build/vet/test -race 全绿 |
| 0039 | 2026-06-07 | 波次3 · M3.3 | trigger 升格为独立实体（信号源 + 引用计数 + sensor/CEL，填 polling 坑） | ✅ 用户提**加站**：把 trigger 从「workflow 图节点」提升为**独立实体类型**，顺带填 M3.1 剥离 polling 的坑。研讨 6 轮拍板：**①独立实体**（`trg_`，地位同 function：进 catalog + relation **第 9 节点** + 8 工具）**②引用计数生命周期**（≥1 个 active workflow 引用才起 listener、N workflow 共享一个、fire 扇出，**淘汰旧 per-workflow 重复 listener**）**③polling→`sensor`**（用户选名：绑 function 或 handler.method 看 active 版本 + interval + CEL condition/output；**要状态绑 handler、不要状态绑 function**，trigger 自身无状态）**④无版本**（配置实体改即生效）**⑤砍 manual**（手动跑是 workflow 能力）**⑥Activation 动作日志**（触没触发都记 return_value/detail，「为什么没触发」可查）。关键洞察：旧 trigger 节点是 no-op 透传 `flowrun.TriggerInput`，**搬出图不丢信号**→ target=workflow。4 source（cron/webhook/fsnotify/sensor）+ 3 表（triggers/trigger_firings/trigger_activations，`trg_`/`trf_`/`tra_`）。**建 `pkg/cel`**（CEL 编译/求值共享包，workflow M4 复用，避 infra→app 依赖）。回调升级 `ReportFunc`（每动作都报、Fired 与否）让没触发也落 Activation、统一 4 listener。domain 去 GORM+errorsdomain（12 码）；store orm 3 表（firing dedup 幂等 + 单事务 claim ADR-021 留波次 4）；infra 4 listener（前 3 照搬去 gorm 改 triggerID 维度 + sensor 新写 invoke→CEL→fire）；app 引用计数 Attach/Detach + 扇出 + CRUD + catalog/relation 适配器；8 工具；REST + `:fire` + activations。relation 加第 9 节点。go.mod +cron/fsnotify/cel-go。store 5 + sensor 3 + app 7 + relation 测试；契约 trigger.md 重写(DOC-125) + api(§2.5) + error-codes(§2.6.1) + database(§4.3 三表 + 前缀 trg_/trf_/tra_ 取代 ts_/tfi_) + relation(节点 9) + contract #19；gofmt/build/vet/test -race 全绿 |
| 0038 | 2026-06-07 | 波次3 · M3.2 | handler 重写：MCP 式单例常驻生命周期 + 复用 app/envfix | ✅ 用户拍板**进程生命周期重做成 MCP 式**（开局常驻/一直在线/退出软件优雅关闭）——砍旧 `Owner`+`map[Owner]map[name]` per-owner + 两套 call 分叉（**chat 即起即杀使有状态类退化无状态、自我矛盾**）+ lazy/crash/race/owner-teardown/(文档吹的)idle-GC，塌成 **3 动词 boot/restart/shutdown** + 扁平 `map[handlerID]`（chat/agent/workflow 共享同一实例+状态、真有状态）。**restart 双触发**（自动 edit/改 config 重启 + **手动 `restart_handler` 工具/`:restart`**——crash 重生救不了"活着但状态坏了"，有状态服务的「重置按钮」，用户特别要求加 LLM 工具）。其余 = function 孪生：方案 A 版本（去 accept/单调号/revert 纯移指针）、**复用 `app/envfix`**（第二消费者）、类组装（AssembleClass HandlerImpl + DriverScript stdio RPC）、加密 config（门控 spawn + 改配置自动重启）、call_log（triggered_by chat/agent/workflow/manual、砍 hints）、catalog/mention/relation 适配器、11 工具（含 call/update_config/restart）。domain 去 GORM+errorsdomain（crashed→HANDLER_CRASHED 502）；infra/handler client 照搬；store 三表 orm；handler HTTP（删 pending、config POST→PUT、+:restart）。store 5 + app 9（生命周期核心：create 不 spawn/call spawn+复用+记录/restart 停+重生/crash 下次调用重生/edit 重启+号+1/revert 纯移指针/config 门控 spawn/shutdown 停全部/AssembleClass）+ tool + domain 测试；契约 handler.md 重写(DOC-111) + api/error-codes/database 前缀 hdenv_/hdi_ + contract #18；gofmt/build/vet/test -race 全绿 |
| 0037 | 2026-06-06 | 波次3 · M3.1 | function 重写 + 抽 app/envfix（**波次 3 起步**） | ✅ 用户拍板三大简化：**①版本号去 accept**（方案 A：单调号 / 不可变内容 / 自由 active 指针三正交，无 pending/accept；create/edit 立即生效写 max+1，**revert 纯移指针**不删"更新的"版本，edit fork-from-active，50 上限裁最老但**绝不裁 active**）**②polling 剥离**（删 kind/polling_interval/PollingAdapter/2 ops——独立概念不寄生；顺带修旧 `triggered_by="polling_trigger"` 违反 CHECK 的 bug）**③env-fix 抽共享 `app/envfix`**（function/handler/trigger 复用，Provision 装→失败 utility LLM 改 deps 重试≤3→回写 FinalDeps；链对齐新地基删 Thinking/llmclient/llmparse；Sink 回调推流留 tool 层、包 stream-agnostic）；triggered_by ∈ chat/agent/workflow/manual（执行体轴）；execution 砍 hints、aggregates 只 ok/failed；domain 去 GORM + errorsdomain（删 5 旧码 + 加 ErrInvalidCode）；store orm（trim cutoff 避 offset + 保护 active）；9 工具（5 方法、danger 自报、create/edit forgeSink 折 envFixAttempts）；handler 删 pending 三端点；envfix 6 + store 9 + app 8（含 revert 纯移指针 + fork-from-active 核心）+ tool + domain 测试；契约 function.md 重写(DOC-110) + envfix.md 新(DOC-304) + error-codes/api/database 前缀 fnenv_ + contract #17；gofmt/build/vet/test -race 全绿 |
| 0001 | 2026-06-03 | 波次0 · M0.1 | pkg reqctx/idgen/pagination 重写 | ✅ stdlib-only，测试绿（含 R0001.1：reqctx 按 concern 拆 workspace.go/reqctx.go） |
| 0002 | 2026-06-03 | 波次0 · M0.1 | tokencount 迁移 | ✅ 原样保留（干净叶子），测试绿 |
| 0003 | 2026-06-03 | 波次0 · M0.1 | pathguard 迁移 + #7 清理 | ✅ 逻辑不动，删 V1.2 叙述/死变量/过时注释，测试绿 |
| 0004 | 2026-06-03 | 波次0 · M0.1 | userpath 去留判定 | ⏭️ 判定删除（多用户分桶+历史迁移，新架构不存在）；登记 M1.1/M7.1 |
| 0005 | 2026-06-03 | 波次0 · M0.1 | wikilink 剥成纯抽取 | ✅ 去 Kind/去 idgen 依赖，Kind 映射归 relation(M1.4)，测试绿 |
| 0006 | 2026-06-03 | 波次0 · M0.1 | jsonrepair 迁移 + 补测试 | ✅ 实现原样（高质量），补 10 unit，gofmt 归一，测试绿 |
| 0007 | 2026-06-03 | 波次0 · M0.1 | limits 迁移(搬+清+补测试) | ✅ 保留全局 getter，清 P0/P3+adhoc 叙述，补 4 测试，绿。**M0.1 完成** |
| 0008 | 2026-06-03 | 波次0 · M0.2 | 自研 pkg/orm（去 GORM） | ✅ 链式/类型安全/自动 workspace+软删+时间戳，9 源+21 测试绿；domain 去 GORM 化成全局方针 |
| 0009 | 2026-06-03 | 波次0 · M0.2 | infra/db 网关 GORM→database/sql | ✅ glebarez/go-sqlite + 单连接 + DDL 迁移机制；schema_extras 删（分散各模块）；orm 补 Exec/Close。**M0.2 完成** |
| 0010 | 2026-06-03 | 波次0 · M0.3 | infra/logger | ✅ zap.go 保留+简化(去 extras)；broadcast.go 删(日志 SSE 违反 E1)；2 测试绿 |
| 0011 | 2026-06-03 | 波次0 · M0.3 | crypto 切片(domain port + infra adapter) | ✅ AES-GCM + 机器指纹，原样保留，13 测试绿；port-adapter 范本。**M0.3 完成** |
| 0012 | 2026-06-03 | 波次0 · M0.4 | domain/errors 结构化强化 | ✅ Error{Kind,Code,Details,cause}+Is by Code；根除 errmap 293 行+27 import 巨耦合；6 测试绿；契约 UNAUTH_NO_WORKSPACE |
| 0013 | 2026-06-03 | 波次0 · M0.4 | SSE 三流统一协议 domain（改名 + 流式树重构） | ✅ 单一 domain/stream：信封+四动词Frame+**通用 Node{Type,Content}**+Bridge/ListReader；id 升信封层、frame 可丢性分级、close 带快照；**node 词表下放业务、砍三流 domain 包**；5 源 3 测试绿 |
| 0014 | 2026-06-04 | 波次0 · M0.5 | infra/stream 单一 Bus（三流底座） | ✅ per-workspace seq + frame 分级 buffer（durable 入环/ephemeral seq0 不入不卡）+ replay/ErrSeqTooOld + List；旧三抄 Bridge 收敛成 1 份、实例化三次=三流；D2 全量推；3 源 3 测试 -race 绿；infra/chat 移交 M5.2 |
| 0015 | 2026-06-04 | 波次0 · M0.6 | infra/llm 核心框架 + openai | ✅ Provider 接口+providerClient 铁律+共享传输+类型+factory+mock+openai(完整自包含)；error 内聚 domain/errors(+S20 守则)；**每家 provider 完整自含 wire、不共享基座**；删死代码、strip 历史；8 源 6 测试 -race 绿；trace 推迟；其余 10 provider=R0016 |
| 0016 | 2026-06-04 | 波次0 · M0.6 | infra/llm 其余 10 provider（各自完整自包含） | ✅ anthropic+gemini 原生方言 + deepseek 模板 + 7 家 OpenAI-compat（**workflow 并行 7 agent ~424k tok**）；每家自包含 wire、error sentinel、去 modelcatalog(→Request.MaxTokens)/slog、strip 历史；11 家 -race+合规 grep 全绿。**M0.6 完成** |
| 0017 | 2026-06-04 | 波次0 · M0.7 | transport 框架（response/middleware/router） | ✅ **errmap 293行27import → statusForKind ~50行1import**(零业务 domain)；envelope N1 + SSE marshal(M0.4 推迟，frame/node 判别注入) + pagination Parse；auth **user→workspace 改名落地**(本地 WorkspaceResolver 接口)；6 middleware + router chain；13 源 6 测试绿。完整 New→M7。**波次 0 收官** |
| 0018 | 2026-06-04 | 波次1 · M1.1 | workspace（原 user 正名）+ orm ErrConflict + handler 地基 | ✅ user→workspace 全量正名垂直切片(domain/store/app/handler 7 源)；**多 workspace 数据隔离 + 资源不分桶**定型、Name 自由名 UNIQUE(去 slug/GetByUsername/EnsureExists)；**orm 补 ErrConflict**(对称收口 UNIQUE 翻译，store 不碰 SQLite 字符串)；handler 地基首建(registrar/util/decode)；`Validate` 实现 WorkspaceResolver 端口；去 GORM+S20；store 8+app 10+orm 2 测试 -race 绿；契约 4 件同步 |
| 0019 | 2026-06-04 | 波次1 · M1.2 | apikey（收窄 + 首个 orm 隔离表）| ✅ 收窄为「加密保险箱 + 哑探针 + 按 id 发钥匙」：**选 key 下放**(model/搜索配置，防乱烧钱)、**哑探针**(只判 200 + 存 test_response 原始，砍解析器)、**解析下放**(models_found→test_response，model 靠 ProbeReader + 静态目录兜底/可推送)；KeyProvider 收窄 2 法全按 id；**orm 自动隔离首验**；去 IsDefault/GetByProvider/ErrNotFoundForProvider；modelcatalog/capabilities 移交 M1.3；store 8+app 10 测试 -race 绿；契约 4 件同步 |
| 0020 | 2026-06-04 | 波次1 · M1.3 | model 重写(聚合薄层) + infra/llm 11 家旋钮重构 + apikey 探针 + workspace 默认 | ✅ 删 ThinkingSpec 中立抽象、各家原生 Options 自包含、模型知识下沉 provider(DescribeModels)、model 退化无 store、默认搬 workspace 3 列、Resolve 收口、override 改运行时优雅报错；10 家官方调研；修 anthropic budget_tokens→400 bug；gofmt/build/vet/test -race 全绿 |
| 0021 | 2026-06-05 | 波次1 · M1.4 | relation（横切关系图）+ KindForID 收编 + wikilink doc-fix | ✅ 边类型收成 4 动词(create/edit/equip/link，删 from/to 冗余、CHECK 恒 4 值)、8 节点、KindForID 8 条(补 agent + 定 sk_/mcp_ 规矩)、显示名读时内存 hydrate(不落库/改名即新/无 reader port)、孤立节点不显示、override 式弱引用无删除保护；domain+store+app+handler+测试；契约 4 件同步(relation.md 整篇重写删 ErrInUse/runs_in/mentions)；gofmt/build/vet/test -race 全绿 |
| 0022 | 2026-06-05 | 波次1 · M1.5 | catalog（能力概览聚合层） | ✅ 职责收窄到「只报名字+描述、按类型分组」；砍 InvokeTool(调用下放搜索)/Generator/GeneratedBy/Granularity/Category 全部预留；两段式概览→搜索故 id 不进菜单+name 不要求唯一；document 例外(name=文档名/desc=路径)；无 store 派生现查；domain+app+handler+6 测试；契约 3 件同步(catalog.md 整篇重写)；gofmt/build/vet/test -race 全绿 |
| 0023 | 2026-06-05 | 波次1 · M1.6 | mention（@ 引用快照契约） | ✅ 纯 domain 契约一个文件：5 种可 @ 类型(四件套+document) + Resolver 接口 + IsValidMentionType；Freeze-on-Send 发送即冻结快照；无 app/store/handler/error(resolver 波次3、chat 渲染+错误波次5)；契约 domains/mention.md 整篇重写(删虚构 relation 边/活跃度)、api/db/error-codes 不涉及；gofmt/build/vet/test -race 全绿 |
| 0024 | 2026-06-05 | 波次1 · notification(基础) | notification 模块新建 + stream scope 厘清 + R0018 分桶翻转 | ✅ 通知升格为持久化实体(DB+通知中心+SSE durable signal, scope=notification:noti_x, workspace 是 Bus 分流轴非 scope, 事件类型 node.type=<域>.<动作>, Emitter 端口); stream 删 KindWorkspace/加 KindNotification/删 ListReader+list.go(零外部牵连); **R0018 翻转**→一切 workspace 隔离+~/.forgify 分桶; 业界调研 4 agent 指导 memory 文件式; domain+store+app+handler+5 测试; 契约 6 件(notification.md 新+events.md 重写); gofmt/build/vet/test -race 全绿 |
| 0025 | 2026-06-05 | 波次1 · M1.7 | memory 文件式重设计 | ✅ SQLite→按 workspace markdown(~/.forgify/workspaces/<wsID>/memories/*.md, frontmatter description/pinned/source, 文件名即 name 无 mem_ id); 两段式注入(pinned 全文+目录 read_memory 按需); 天然去重(注入目录 LLM 自判 update); 发通知用 notification.Emitter; 砍热度/type/向量/reflection(业界调研); backend-new 首个文件式 store(skills 复用); domain+fs store+app+handler+12 测试; 契约 5 件(memory.md 重写+删 memories 表/mem_); gofmt/build/vet/test -race 全绿 |
| 0026 | 2026-06-05 | 波次1 · M1.8 | sandbox 三 runtime 隔离运行时 | ✅ Python+Node+Docker(GitHub MCP registry 98 调研:Python+Node+remote 覆盖 92%、缺口 7 docker-only); 三 runtime 统一双接口(image=docker runtime/容器=env、零特例)、EnvBin/EnvDir→ResolveExec(spawn 不持 runtime 知识); 两表系统级不分桶(orm meta.ws==nil 自动跳隔离); 去 GORM+硬删; docker.go 新写(探测+pull+docker run+-e env、不代装); notifications→Emitter(sandbox.env_*); 路由 hacky→RESTful+N5; 骨架照搬(本质复杂度无脂肪)+重写烂文档(虚构字段/旧错误码); domain+store+infra(照搬+docker)+app+handler+25 测试; 契约 5 件; Docker 精细化留 M3.6、注册+base 留 M7; gofmt/build/vet/test -race 全绿 |
| 0027 | 2026-06-05 | 波次1 · M1.9 | permissions/hooks/settings 判定解散 | ⏭️ 三者整个不迁——中央配置门控是错误抽象(抄 Claude Code 交互授权，Forgify 单人本地闲置:交互嫌烦 bypass/无人值守没人答 ask); hooks 砍(花活)、危险控制别处管(不做中央门控)、protectedPaths 归危险控制(pathguard 已有默认)、limits 用 pkg/limits 默认、settings.json+infra/settings 砍; 连带 M5.4 permissionsgate 解散+M5.2 chat 去 hooks; 契约清(删 permissions.md/api 7 端点/error 2 码); 对齐 userpath R0004 判删 |
| 0030 | 2026-06-06 | 波次2 · M2.1 | tool 基础接口瘦身（**波次 2 起步**） | ✅ M1.9 权限解散 + M1.5 catalog 落到工具层：**Tool 9→5 方法**(删 CheckPermissions/IsReadOnly/NeedsReadFirst/RequiresWorkspace 全仓无消费者 + PermissionMode/Result)；**destructive boolean→danger 三级枚举**(safe 静默/cautious 标记不阻塞/dangerous 阻塞等同意)，**纯信任**工具不设下限、危险 LLM 逐次自报；三字段强制注入剥离(summary+danger 必填 + execution_group，jsonrepair 兜畸形、danger 缺坏→safe)；**Toolset 懒加载保留**(resident/lazy + activate_tools，与 catalog 实体菜单**正交**、解决工具自身巨大 description 烧 token——纠正我一度判错)；无 domain/store/handler；14 测试；契约 CLAUDE.md S18 + contract-changes #10；danger 确认流+并行批→loop M2.2、activate_tools+lazy prompt→chat M5.2、15 适配器→波次2.3/3；gofmt/build/vet/test -race 全绿 |
| 0029 | 2026-06-06 | 波次1 · M1.11 | todo TodoWrite 式重铸（**波次 1 收官**） | ✅ 旧「4 工具 CRUD 数据库」错误抽象(注入没接 LLM 看不见清单/前端看板返 `[]` 假/testend 调死链/blocked_by·owner·metadata 死字段)重铸为 Claude Code TodoWrite：**1 工具整列替换**、item 3 字段(content/activeForm/status)、`scope_id=subagent??conv` 多态键(同 relation from_id、天然 PK、subagent 各自一张不污染父看板)；**两硬指标**「LLM 真用」双层可见(工具回显 + 每轮 `SystemReminder` 注入) + 「前端真看见」(messages 流 `todo` signal scope=conversation/node.type=todo/payload 带 conv+subagent + REST 初值，不走 notifications)；**reqctx 补双种子**(conversation_id+subagent_id、subagent 提前埋)、无 HTTP 错误码(校验只回 tool-result 串)；domain+store+app+handler+11 测试; 契约 6 件(todo.md 整篇重写/database 注册表去 td_+4.4 段/error-codes 删 3 码/events+api 加端点); 工具波次2-3·注入 M2.2·bridge M7·前端看板覆盖后; gofmt/build/vet/test -race 全绿 |
| 0028 | 2026-06-06 | 波次1 · M1.10 | document Notion 树知识库 | ✅ 去 GORM+workspace 隔离(app 去 uid)+Emitter+砍 attach 子树注入(AttachedDocument 去 IncludeSubtree、ResolveAttached 只取单篇)+树 CRUD 照搬(path 级联/防环/重名加后缀/软删子树/COALESCE UNIQUE 兜根级同名); **4 适配器对齐前三模块新地基**(catalog 去 Granularity/InvokeTool/Category、relations wikilink→KindForID→link 边、mention 微调、Namer.NamesByIDs); domain+store+app+4 适配器+handler+17 测试; 契约 5 件(domains 去 RAG 标题/database 4.1 去 GORM); 注入留 M7/attach 消费波次4-5/iterate 波次6; gofmt/build/vet/test -race 全绿 |
| 0036 | 2026-06-06 | 波次2 · M2.3#4 | tool/toolset:砍 activate_tools → search_tools 检索式懒加载（**波次 2 收官**） | ✅ 深度调研(103 agent / 25 声明对抗式核实)后**改判砍按类激活**(业界淘汰的 legacy:整组激活仍灌整组 schema + 锁定整对话 + 粗粒度);换**检索式**——`Toolset.Lazy` 扁平 `[]Tool` + `Overview()`(name+一句话目录卡,不含 Parameters)+ **`search_tools` 工具**(query→`rankLazy` 关键词打分→top-5→返完整定义含 Parameters + `MarkToolDiscovered`,纯关键词无 embedding)+ agentstate **`discoveredTools`**(替 activatedGroups,按工具名细粒度);**与 catalog 正交**(工具能力地图 vs 实体实例地图;概览常驻补纯 defer 的 ~64% 召回);删旧 activate.go;Toolset/search_tools/agentstate 测试 5+5+2;契约 domains/toolset.md 新(DOC-303)+ contract #16;lazy 实体工具/目录卡注入/host 组装/跨 run 从历史重建留波次 3/5;gofmt/build/vet/test -race 全绿。**波次 2(tool + 执行原语)全部完成** 🎉 |
| 0035 | 2026-06-06 | 波次2 · M2.3#3 | tool/web:WebFetch 摘要 + WebSearch 单把 BYOK + 删 MCP tier | ✅ WebFetch(SSRF 双层 guardHostname/classifyIP/CheckRedirect + Jina/direct 抓取 + **摘要链对齐新地基重写**:model.Resolve(utility)→ResolveCredentialsByID→factory.Build→llm.Generate,删 llmclient 残留 + Thinking,失败降级原始截断);WebSearch **单把 BYOK**(R0034 SearchKeyPicker→ResolveCredentialsByID→IsProvider→switch brave/serper/tavily/bocha,provider 由 key 隐含、401→MarkInvalidByID、替旧 SearchProviderPriority 自动遍历防乱烧钱);**改判删 MCP tier**(用户质疑后认同——MCP 搜索经 tool/mcp 平级暴露 LLM 直接调,WebSearch 不代理;删 search_mcp.go/MCPSearchRouter + WebTools 去 mcpRouter,**web 零 mcp 依赖**);9→5 方法、danger 工具侧零逻辑、GetUserID→GetWorkspaceID;fetch 6 + search 7 测试(mock Jina httptest + provider=mock 短路 MockClient + 各 provider httptest);契约 domains/web.md 整篇重写(DOC-127)+ contract #15;gofmt/build/vet/test -race 全绿 |
| 0034 | 2026-06-06 | 波次2 · M2.3#3前置 | 搜索配置:建 domain/websearch + workspace 加 default_search_key_id | ✅ web 前置——补 M1.2 主动推迟的"搜索配置"。**建独立 domain/websearch 包**(用户要求、对齐 domain/model 便管理:Provider 词表 brave/serper/tavily/bocha + IsProvider/Providers + SearchKeyPicker 接口 + **无 store**);workspace **加 default_search_key_id 列**(完全同 model default-models:存储借 workspace、`SetDefaultSearch` 在 workspace.Service、**不建 app/websearch**——model 的 app 是 capability 聚合搜索无此需求);**单选显式 key 防乱烧钱**(替旧 `SearchProviderPriority` 自动遍历)、provider 由 key 隐含、**不校验 category**(运行时优雅+workspace 零新依赖);apikey 层早就绪(4 搜索 provider 在白名单+TestMethodSearchPing+CategorySearch);websearch 2 + workspace app 4 + store 1 测试;契约 domains/websearch.md 新(DOC-302)+ database 列 + api 端点(PUT/DELETE default-search)+ workspace.md §4.4 + contract #14;WebSearch 消费留 R0035 web;gofmt/build/vet/test -race 全绿 |
| 0033 | 2026-06-06 | 波次2 · M2.3#2 | tool/search 三件套 + 新建 pkg/fspath + cwd 全局废弃 | ✅ search(LS/Glob/Grep)文件导航——对应人找文件:看一眼(LS 新增)/按名找(Glob)/按内容找(Grep);**确立桌面 agent「全电脑绝对路径、无 cwd」范式**(照搬 Claude Code 项目级 cwd 是缺陷——进程 cwd=app 安装目录,搜了个寂寞);新建 `pkg/fspath.Expand`(展开 `~`/os home + 必绝对否则拒,六文件工具共用、"永远绝对"唯一物理执行点);**LS 独立成工具**(列目录是全电脑导航最高频动作,不再用 `Glob "*"` 兼任);Glob/Grep 删 `os.Getwd` 默认→path 必填+`fspath.Expand`;**Grep 双后端保留**(rg 优先+stdlib 兜底——桌面不假设有 rg/不代装);**回溯改 filesystem**(read/write/edit 改走 `fspath.Expand` 支持 `~`、删 IsAbs/ErrPathNotAbsolute);**cwd 概念全局废弃**(agentstate 永不加 cwd、shell M3.7 也无、R0032"cwd→shell"撤销);加 `doublestar/v4` 依赖;danger 工具侧零逻辑(LLM 自报);fspath 6+search 29+filesystem 回溯 6 测试;契约 search.md 整篇重写(DOC-120)+filesystem.md 加 §4.1/清 cwd+contract #13;gofmt/build/vet/test -race(含 rg 集成)全绿 |
| 0032 | 2026-06-06 | 波次2 · M2.3#1 | tool/filesystem Read/Write/Edit 三件套 | ✅ 机械跟进新地基 + 首建 `pkg/agentstate`(SeenFiles 渐进、其他字段后续消费者按需追加)+ 首建 reqctx `WithAgentState/GetAgentState` 种子;9→5 方法(删 IsReadOnly/NeedsReadFirst/RequiresWorkspace/CheckPermissions——M2.1 已砍);Read 用 `Allow`、Write/Edit 升级用 `AllowWrite`(消费 R0003 写专属 extras `.git/`/`.env*`/`node_modules/`——AI 永不覆盖物理拦截);**写前必读铁律保留**(SeenFiles + fail-closed:state 缺失 Write/Edit 显式拒);Edit 保留 size 漂移检测(廉价外部改动指示器);原子 tmp+rename + mode 保留;danger 不静态声明(M2.1 纯信任);agentstate 5 + reqctx 3 + 三工具 31 测试; 契约 domains/filesystem.md 整篇重写(DOC-108)+ contract-changes #12;跨波次:cwd→shell M3.7、activeSkill→skill M3.5、activatedGroups→toolset、WithAgentState 调用方→chat/subagent/scheduler;gofmt/build/vet/test -race 全绿 |
| 0031 | 2026-06-06 | 波次2 · M2.2 | loop ReAct 引擎重写 + 建 messages domain | ✅ 共享 ReAct 引擎接 stream 统一协议(eventlog→messages 流 open/delta/close + node content 词表、close 带快照供 replay)、danger 纯标记(tool_call 节点带 danger/summary 前端标记、不阻塞、确认留 ask 波次6)、删 interceptor(M1.9 解散)、todo 注入走 Host `ReminderProvider` 钩子(每步临时 system-reminder 不污染历史); **建 `domain/messages`**(Block/ToolCallData/词表无家可归——backend-new 无 chat/messages domain，修正共享引擎依赖 chat 的耦合反向)、reqctx 补 messageID 种子; **executeTool 极简**(删 CheckPermissions/skill-preapproval + 改判删 sanitizeToolErr/enrichWithNextStep——硬编码工具知识违反 loop 中立、工具自负 error 质量，323→~150 行); **agentstate 零依赖**(4 块职责无一属于 loop，随 filesystem/shell/chat/skill 各自重建); domain/messages + loop 6 源 + reqctx 种子 + 17+3 测试; 契约 domains/messages.md 新建(DOC-301) + contract-changes #11; gofmt/build/vet/test -race 全绿 |
