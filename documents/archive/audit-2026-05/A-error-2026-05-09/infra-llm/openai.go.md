# audit: backend/internal/infra/llm/openai.go

LOC: 637
Read: full file (lines 1-637)

## Trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | openai.go:44-47 | `body, err := buildOpenAIBody(req); if err != nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: build body: %w", err)}) }` | A.4 | EDGE | §S16: prefix `llm/openai:` (slash-separated) not canonical `infra/llm.<Type>.<Method>:` form. Has `%w` ✓ + sentinel chain preserved through buildOpenAIBody → toOpenAIMsgs → toOpenAIMsg internal errors. | LOW | identical UX (caller in chat runner sees error message); call-site grep slightly weaker since `llm/openai:` doesn't include method | switch to `llm.openAIClient.Stream: build body: %w` for consistency with §S16 spec literal | FOUND |
| 2 | openai.go:50-54 | `httpReq, err := http.NewRequestWithContext(...); if err != nil { yield... fmt.Errorf("llm/openai: new request: %w", err) }` | A.4 | EDGE | same prefix issue as #1 | LOW | same | same | FOUND |
| 3 | openai.go:59-66 | `resp, err := c.http.Do(httpReq); if err != nil { if ctx.Err() != nil { return }; yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: do: %w", err)}) }` | A.1/A.4 | OK | §S3 `if ctx.Err() != nil { return }` is intentional cancel detection — when ctx is done, the err from http.Do is the ctx error itself, returning silently is correct (caller already knows about cancel via ctx). Otherwise wraps with `%w`. Prefix style same as #1. | N-A | — | — | — |
| 4 | openai.go:67 | `defer resp.Body.Close()` | A.1 | OK | §S3 spec carve-out for read-only body Close — error from Close after consume is unactionable. Compliance literal. | N-A | — | — | — |
| 5 | openai.go:69-73 | `if resp.StatusCode != http.StatusOK { raw, _ := io.ReadAll(io.LimitReader(resp.Body, 4096)); yield(StreamEvent{Type: EventError, Err: classifyHTTPError(resp.StatusCode, raw)}) }` | A.1/A.4 | EDGE | (a) `_ = err` from io.ReadAll discarded — body might fail to read (network truncation mid-error-body), but the error is best-effort context only; classifyHTTPError handles empty body gracefully. NOT a §S3 violation since classifyHTTPError returns clear error from status code regardless. (b) classifyHTTPError errors are non-sentinel `fmt.Errorf("llm: %s (%d): %s", ...)` — see #15 below. | LOW | (a) acceptable; (b) HTTP errors lack sentinel chain — see #15 cluster | (a) optional comment; (b) introduce sentinels per status class — see #15 | FOUND |
| 6 | openai.go:96-100 | `func parseOpenAINonStreaming: raw, err := io.ReadAll(io.LimitReader(body, 8<<20)); if err != nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: read non-streaming body: %w", err)}) }` | A.4 | EDGE | same prefix issue as #1 | LOW | same | same | FOUND |
| 7 | openai.go:103-106 | `if err := json.Unmarshal(raw, &resp); err != nil { yield... fmt.Errorf("llm/openai: parse non-streaming response: %w", err) }` | A.4 | EDGE | same prefix issue as #1 | LOW | same | same | FOUND |
| 8 | openai.go:107-110 | `if resp.Error != nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm: provider returned error: %s", resp.Error.Message)}) }` | A.4 | VIOLATION | §S16 violation: `%s` not `%w` — even though resp.Error.Message is a string (not an error wrap target), this loses the categorization signal. Also prefix `llm:` (no method/type qualifier) — bare. **More important**: provider-side errors (auth fail / bad model / rate limit) have NO sentinel mapping. errmap.go can't discriminate "401 from upstream" vs "500 internal" — both fall through to "unmapped domain error". This is the same gap as #15's classifyHTTPError set. | MED | LLM call → provider returns logical error → chat layer sees generic LLM_STREAM_ERROR/500; cannot distinguish `apikeydomain.ErrInvalid` (would call apikey.MarkInvalid) from transient retry-worthy errors. Currently swallows discriminative info for the entire OpenAI-compat fleet. | introduce sentinel `llm.ErrProviderError` (or per-class — see #15); wrap with `%w`; map to errmap as 502 LLM_PROVIDER_ERROR. Better: classify by resp.Error.Code/Type — 401→ErrInvalidKey (already in apikeydomain), 429→rate-limit sentinel, etc. | FOUND |
| 9 | openai.go:111-113 | `if len(resp.Choices) == 0 { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: non-streaming response has no choices")}) }` | A.4 | EDGE | §S16 prefix style + no sentinel + no `%w` (no inner err to wrap; "no choices" is a structural assertion). Same family as #15 — provider returned malformed response, caller can't `errors.Is` discriminate. | LOW | malformed response surfaces as generic LLM_STREAM_ERROR; rare in practice (real providers always return choices) | introduce sentinel like `llm.ErrEmptyResponse` if discriminating worth the noise; otherwise WAIVE | FOUND |
| 10 | openai.go:172-174 | `var chunk oaiChunk; if err := json.Unmarshal([]byte(data), &chunk); err != nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: malformed SSE chunk: %w", err)}) }` | A.4 | EDGE | same prefix style as #1 | LOW | same | same | FOUND |
| 11 | openai.go:180-182 | `if err := scanner.Err(); err != nil && ctx.Err() == nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm/openai: scan: %w", err)}) }` | A.1/A.4 | OK | §S3: scanner.Err() is checked + ctx.Err()==nil guard means we only surface the error when it's NOT a cancel-induced read failure. Wrap with `%w`. The non-canonical prefix is the only nit. | N-A | — | — | — |
| 12 | openai.go:248-253 | `if chunk.Error != nil { yield(StreamEvent{Type: EventError, Err: fmt.Errorf("llm: provider returned in-stream error: %s", chunk.Error.Message)}) }` | A.4 | VIOLATION | same as #8 — `%s` not `%w`, no sentinel, no method prefix. **TE-23 fix added this for OpenRouter mid-stream errors**; the structural detection is correct but the error type is generic. | MED | same as #8 — loses discriminative info for upstream provider errors | introduce sentinel + classify by chunk.Error.Code/Type per #8's recommendation | FOUND |
| 13 | openai.go:316-326 | `func buildOpenAIBody(req Request) ([]byte, error) { req.Messages = SanitizeMessages(req.Messages); msgs, err := toOpenAIMsgs(req.Messages, req.System); if err != nil { return nil, err }; ... return json.Marshal(body) }` | A.4 | EDGE | bare-return — toOpenAIMsgs wraps internally (see #14, #15); no `infra/llm.buildOpenAIBody:` prefix. json.Marshal at line 340 returns `(nil, err)` directly without wrap (Marshal of in-process basic struct is provably unfailable, but no inline comment; same pattern as the loop fork's resolved comments). | LOW | identical UX (sentinel preserved); slight grep weakness | wrap: `return nil, fmt.Errorf("llm.buildOpenAIBody: %w", err)` | FOUND |
| 14 | openai.go:343-356 | `func toOpenAIMsgs(msgs, system) ([]oaiMessage, error) { ... om, err := toOpenAIMsg(m); if err != nil { return nil, err } ... }` | A.4 | EDGE | bare-return; toOpenAIMsg wraps internally (see #15) | LOW | same | same | FOUND |
| 15 | openai.go:358-373 | `func toOpenAIMsg(m): switch m.Role { ... default: return oaiMessage{}, fmt.Errorf("llm/openai: unknown role %q", m.Role) }` | A.4 | EDGE | §S16: prefix style + **NO sentinel** + **NO `%w`** (no inner err). Same defensive-validation pattern as the apikey/mcp/sandbox sentinel decisions. Reachability: m.Role is filled by chat / loop layers from chatdomain.Role* constants (User/Assistant/Tool); empty here would be unreachable. | LOW | hits "unmapped domain error" alarm if triggered (programmer-side bug — e.g. someone added a new chatdomain.Role* without updating this switch) | panic per "config-time invariant" (consistent with apikey.HTTPTester / mcp.AddServer / sandbox.EnsureEnv); OR introduce sentinel + register errmap as 500 INTERNAL_ERROR | FOUND |
| 16 | openai.go:375-397 | `buildOpenAIUserMsg: ... default: return oaiMessage{}, fmt.Errorf("llm/openai: unknown part type %q", p.Type) ... raw, err := json.Marshal(parts); if err != nil { return oaiMessage{}, fmt.Errorf("llm/openai: marshal parts: %w", err) }` | A.4 | EDGE | (a) line 389: same NO-sentinel pattern as #15 — reachable via ContentPart.Type = "text" | "image_url" enums; future variant addition would trigger. (b) line 394: `%w` ✓ but Marshal of parts (basic types) is provably unfailable — same family as loop's resolved Marshal-comment polish. | LOW | (a) same as #15 (panic vs sentinel); (b) Marshal err comment polish | (a) panic per pattern; (b) inline comment "Marshal of basic-type parts is unfailable" | FOUND |
| 17 | openai.go:475-478 | `func jsonString(s string) json.RawMessage { b, _ := json.Marshal(s); return b }` | A.1 | EDGE | §S3: `_ = err` discarded with no inline comment. **In practice**: json.Marshal of a Go string cannot fail. **However**, §S3 spec is explicit that `_ = err` requires inline justification. Same pattern as searchrouter.go:#4 (resolved with comment). | LOW | none in practice — string Marshal is a Go stdlib invariant | add inline comment: `// json.Marshal of plain string is unfailable per encoding/json invariant; ignore err` | FOUND |
| 18 | openai.go:486-503 | `classifyHTTPError(status, body): switch status { case 401: return fmt.Errorf("llm: authentication failed (401): %s", msg); case 429: return fmt.Errorf("llm: rate limited (429): %s", msg); case 400: ...; case 404: ...; default: return fmt.Errorf("llm: provider error (%d): %s", status, msg) }` | A.4/A.5 | VIOLATION | **§S16 + §S17 dual gap**: 5 case branches each return plain `fmt.Errorf("llm: ... %s", ...)` — NO `%w`, NO sentinel. **Critical impact** for 401: this is the path where apikey errors should propagate to apikey.MarkInvalid (apikeydomain.ErrInvalid is registered errmap.go:52 → 401). Currently chat runner can't `errors.Is(err, apikeydomain.ErrInvalid)` to trigger MarkInvalid because there's no sentinel chain — it'd have to string-match "(401)" which is fragile. **Pattern**: 401 → apikeydomain.ErrInvalid; 429 → ErrRateLimited (new); 400/404/other → ErrProviderError (new) or per-class. | MED | API key invalidation path doesn't fire — user's bad key stays "OK" in UI even after multiple 401s. Same defect class as the apikey.MarkInvalid §S9 we just fixed (e36f890), one layer higher in the chain. | introduce sentinels: `llm.ErrAuthFailed` (or reuse apikeydomain.ErrInvalid); `llm.ErrRateLimited`; `llm.ErrBadRequest`; `llm.ErrModelNotFound`; `llm.ErrProviderError`. Wrap each with `%w`. Register all in errmap. Then chat runner can `errors.Is(err, apikeydomain.ErrInvalid)` to call MarkInvalid. | FOUND |

## Sub-check（必显式，不许 silence）

A.1 §S3 错误吞没:
  - violations: site #17 (jsonString silent json.Marshal — LOW EDGE)
  - acceptable suppressions: site #3 (ctx.Err() pre-check), #4 (defer Body.Close), #5a (best-effort body read for error context), #11 (scanner.Err with ctx guard)

A.2 §S9 detached ctx 终态写:
  - terminal-state writes identified: none
  - 各自 ctx 来源: N/A
  - violations: N/A — file performs no DB writes; HTTP client only.

A.3 §S15 ID 生成:
  - ID generation calls: none (ToolID is provider-supplied LLM tool-call ID, not Forgify-issued)
  - violations: N/A — file generates no business IDs.

A.4 §S16 错误 wrap 格式:
  - violations: sites #1, #2, #6, #7, #10 (LOW EDGE — `llm/openai:` prefix style not canonical), #13, #14 (bare-return chain), #16a (panic-or-sentinel wiring), #17 (Marshal comment); MED at #8 #12 (`%s` not `%w` for upstream errors); MED at #18 (entire classifyHTTPError set has no `%w` + no sentinels)

A.5 §S17 sentinel 登记 errmap:
  - sentinels defined: none (file declares no `var Err... = errors.New(...)`)
  - 已登记 errmap: N/A
  - missing: **#18 cluster has the highest impact** — HTTP 401/429/400/404 paths produce sentinel-less errors that callers (chat runner / apikey.MarkInvalid path) can't discriminate via errors.Is. Recommend introducing 4-5 new sentinels. Same family as the gap fixed by mcp install.go:#5 `%w: %w` (505d6e3) but here at the LLM transport layer.

## Cross-cutting note (out-of-scope for this file but documented in _summary)

The 401/429 sentinel-less classification at #18 is the upstream cause of why **apikey.MarkInvalid is rarely called in practice** — chat runner gets a "provider error" but can't discriminate "auth failed" without string-matching. Worth coordinating with the apikey audit's MarkInvalid §S9 fix (e36f890) for a complete repair: that fix made MarkInvalid work *if* invoked; this fix would make it actually get invoked.
