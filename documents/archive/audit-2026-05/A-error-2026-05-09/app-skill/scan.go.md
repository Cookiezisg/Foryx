# audit: backend/internal/app/skill/scan.go

LOC: 259
Read: full file (lines 1-259)

## Trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | scan.go:48-50 | `if s.skillsDir == "" { return fmt.Errorf("skillapp.Scan: skillsDir is empty") }` | A.4 | EDGE | §S16: pkg.Method prefix ✓; **NO sentinel + NO %w**. Defensive validation (skillsDir always set by main.go); same pattern as mcp.go:#6 + apikey.tester.go:#4. Wiring-bug invariant: every caller (main.go / harness) sets non-empty skillsDir. | LOW | hits "unmapped domain error" alarm if triggered (programmer-side bug, never user input) | introduce `skilldomain.ErrSkillsDirUnconfigured` + register errmap, OR panic per "config-time invariant" pattern (consistent with mcp.AddServer cfg.Name resolution) | FOUND |
| 2 | scan.go:59-63 | `if _, err := os.Stat(s.skillsDir); !errors.Is(err, fs.ErrNotExist) { entries, err := os.ReadDir(s.skillsDir); if err != nil { return fmt.Errorf("skillapp.Scan: read skillsDir: %w", err) } ... }` | A.4 | OK | §S16 canonical: pkg.Method + sub-tag + %w. ENOENT (missing dir) intentionally falls through to empty cache + publish (per design — user hasn't installed any skills yet). | N-A | — | — | — |
| 3 | scan.go:71-76 | `sk, err := s.parseSkillDir(dir); if err != nil { s.log.Warn("skill skipped", zap.String("dir", dir), zap.Error(err)); continue }` | A.1 | OK | §S3 documented soft-fail: per-skill error logged at Warn with dir + Error so author can debug. **One bad skill must not silence the catalog** (file header §S3 rationale at lines 32-36 explicitly cites this). Audit trail: zap.Warn satisfies §S10 "异步必须打" + §S3 "documented intent with rationale". | N-A | — | — | — |
| 4 | scan.go:77-88 | `if existing, dup := loaded[sk.Name]; dup { s.log.Warn("skill name collision; later one ignored", zap.String("name", sk.Name), zap.String("kept", existing.DirPath), zap.String("rejected", dir)); continue }` | A.1 | OK | §S3 documented soft-fail: name collision logged with both paths so user can deconflict. Same rationale as site #3. | N-A | — | — | — |
| 5 | scan.go:103-108 | `last, _ := s.lastFP.Load().(string); s.lastFP.Store(fp); if last != fp { s.notif.Publish(ctx, "skill", "*", map[string]any{"changed": true, "count": len(loaded)}) }` | A.2 | OK | publish call uses request ctx — but Publisher contract is fire-and-forget (notificationspkg.Publisher.Publish returns no error and is intended to be best-effort). Not a §S9 terminal write — the cache update at line 95 already happened under Lock; the publish is signal-out, ctx cancel just means the SSE subscriber misses one snapshot which the next tick will resend. | N-A | — | — | — |
| 6 | scan.go:128-133 | `fmtBytes, err := yaml.Marshal(&skills[n].Frontmatter); if err != nil { h.Write([]byte(n)); h.Write([]byte{0}); continue }` | A.1 | EDGE | §S3: yaml.Marshal err discarded silently — but rationale is fingerprint-stability (failing one skill's frontmatter shouldn't poison the entire fingerprint, just hash the name). Marshal of frontmatter struct is provably unfailable in practice (basic types only). Missing inline ritual comment per §S3 example. | LOW | fingerprint may become inconsistent across runs if marshal somehow fails — but it can't with current types | add inline comment: `// _ = err — yaml.Marshal of basic-type struct is unfailable; fingerprint stays stable across runs even on theoretical failure` OR pre-flight check at construction time | FOUND |
| 7 | scan.go:151-153 | `raw, err := os.ReadFile(bodyPath); if err != nil { return nil, fmt.Errorf("read SKILL.md: %w", err) }` | A.4 | EDGE | §S16: prefix is `read SKILL.md:` not canonical `skillapp.parseSkillDir: read SKILL.md:`. Caller (Scan) wraps the per-skill error into Warn log (line 73) so call-site context preserved at log level; helper-style prefix loses §S16 literal grep value but UX is intact. Same pattern as forge audit's many `<helper>:` prefix LOWs. | LOW | identical UX (error gets logged at Warn with dir context anyway) | wrap: `fmt.Errorf("skillapp.parseSkillDir: read SKILL.md: %w", err)` for grep | FOUND |
| 8 | scan.go:155-158 | `if len(raw) > skilldomain.MaxBodyBytes { return nil, fmt.Errorf("%w: %d bytes (cap %d)", skilldomain.ErrBodyTooLarge, len(raw), skilldomain.MaxBodyBytes) }` | A.4 | OK | §S16: %w wraps sentinel ErrBodyTooLarge; size + cap context after sentinel for diagnostic. Sentinel registered errmap.go:151 → 422. Missing pkg.Method prefix — but sentinel is the call-target so grep works via sentinel name. Borderline; consistent with the `fmt.Errorf("%w: ...", sentinel, ...)` pattern used elsewhere in package. | N-A | — | — | — |
| 9 | scan.go:160-162 | `yamlPart, _, err := splitFrontmatter(raw); if err != nil { return nil, fmt.Errorf("%w: %v", skilldomain.ErrInvalidFrontmatter, err) }` | A.4 | **VIOLATION** | **§S16 sentinel-chain truncation**: `%v` collapses the inner err (from splitFrontmatter — `errors.New("missing opening --- fence")` etc.) into a string. errors.Is correctly catches ErrInvalidFrontmatter outermost, but the inner detail (which fence missing) is no longer programmatically discriminable. **Same defect class as mcp install.go:#5 (resolved 505d6e3)** + infra/sandbox sites (resolved d6b626f). Go 1.20+ supports multi-`%w`. | MED | inner-error introspection broken (e.g. UI wanting to differentiate "missing opening fence" vs "missing closing fence"); current callers only string-match the message which works but is fragile | switch to `fmt.Errorf("%w: %w", skilldomain.ErrInvalidFrontmatter, err)` to preserve both sentinel (registered errmap.go:150) and the inner string-error chain | FOUND |
| 10 | scan.go:166-168 | `if err := yaml.Unmarshal(yamlPart, &fm); err != nil { return nil, fmt.Errorf("%w: yaml: %v", skilldomain.ErrInvalidFrontmatter, err) }` | A.4 | **VIOLATION** | **§S16 sentinel-chain truncation**: same defect class as site #9. yaml.Unmarshal returns its own typed errors (`*yaml.TypeError` etc.); collapsing via `%v` breaks future `errors.As` discrimination. | MED | same as #9 | switch to `fmt.Errorf("%w: yaml: %w", skilldomain.ErrInvalidFrontmatter, err)` | FOUND |
| 11 | scan.go:169-171 | `if err := validateFrontmatter(fm); err != nil { return nil, err }` | A.4 | OK | bare return — `validateFrontmatter` already wraps with sentinel + %w (lines 240, 243, 255). Sentinel chain preserved through bare propagation. | N-A | — | — | — |
| 12 | scan.go:204-207 | `content = bytes.TrimPrefix(content, []byte{0xEF, 0xBB, 0xBF})` (BOM strip) — **no error** | A.1 | OK | TrimPrefix is pure byte-slice op; no error to handle. | N-A | — | — | — |
| 13 | scan.go:212, 224 | `errors.New("missing opening --- fence")` / `errors.New("missing closing --- fence")` | A.4/A.5 | EDGE | §S16/§S17: bare `errors.New` instead of sentinel. These are inner errors caller wraps with `ErrInvalidFrontmatter` (sites #9 #10) so the sentinel chain at outer layer is OK. **However**: with the `%v` truncation in #9 #10, these inner messages become strings only. Once #9 #10 fixed to `%w: %w`, these become introspectable through errors.Is — could promote to module-level sentinels (`var errMissingOpenFence = errors.New(...)`) for callers wanting to discriminate. Pure refactor though, no behavior change. | LOW | none currently (only referenced via sentinel chain at outer layer) | once #9 #10 fixed: promote these to package-level vars for inner-error discrimination if a UX need arises | FOUND |
| 14 | scan.go:240-241, 243-245, 255-256 | `validateFrontmatter: return fmt.Errorf("%w: ...", skilldomain.ErrInvalidFrontmatter, ...)` (3 sites) | A.4 | OK | §S16 canonical: %w wraps sentinel + diagnostic context after. Sentinel registered errmap.go:150 → 422. Same shape as #8. | N-A | — | — | — |

## Sub-check

A.1 §S3 错误吞没:
  - violations: not present (sites #3 #4 are documented soft-fails with audit log per spec carve-out; #6 is borderline EDGE LOW for missing inline ritual comment but Marshal is provably unfailable for basic-type structs)

A.2 §S9 detached ctx 终态写:
  - terminal-state writes identified: site #5 (notif.Publish using request ctx)
  - 各自 ctx 来源: request ctx (or boot ctx via Service.Start)
  - violations: N/A — Publisher contract is best-effort fire-and-forget signal-out; cache update (line 95) is the actual state mutation and uses internal Lock not ctx. Cancel just means one SSE subscriber misses a snapshot which next tick (1s) resends.

A.3 §S15 ID 生成:
  - ID generation calls: none in this file
  - violations: N/A — skill names are user-supplied (frontmatter.name or dir basename), not idgen-generated business IDs

A.4 §S16 错误 wrap 格式:
  - violations: site #9, #10 (`%w: %v` truncates inner err; same defect class as mcp install.go:#5 commit 505d6e3 / infra/sandbox commit d6b626f)
  - LOW EDGE: site #1 (no sentinel for empty skillsDir defensive validation — wiring-bug; #7 (helper-style prefix `read SKILL.md:` instead of canonical `skillapp.parseSkillDir:`); #13 (bare `errors.New` for inner-fence errors — discoverable through outer sentinel after #9 #10 fixed)

A.5 §S17 sentinel 登记 errmap:
  - sentinels defined in this file: none (consumed `skilldomain.ErrInvalidFrontmatter`, `skilldomain.ErrBodyTooLarge`)
  - 已登记 errmap: `skilldomain.ErrInvalidFrontmatter` → errmap.go:150; `skilldomain.ErrBodyTooLarge` → errmap.go:151 ✓
  - missing: N/A — file defines no new sentinels
