# dev.go audit trace (handlers-B4)

**File**: `backend/internal/transport/httpapi/handlers/dev.go` (490 LOC)
**Scope**: §S3 / §S9 / §S15 / §S16 / §S17
**Note**: All endpoints under `/dev/*` — only registered when server starts with `--dev`. Production users never hit these. §S3 / §S16 expectations relaxed (per dev-only stance — same posture as B3 dev_mock_llm.go / B2 dev_info.go). Other DevHandler methods (Info / ForgifyHome / Runtime / Routes / BashProcesses / MockLLM* / LLMTrace) live in sibling `dev_*.go` files (dev_mock_llm.go + dev_info.go audited in B3).

## 9-column trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | dev.go:70 | `buildID: fmt.Sprintf("%d", time.Now().Unix()),` | A.3 | EDGE | Build cache-buster ID — uses `time.Now().Unix()` (NOT `idgenpkg.New`). **Not a business ID** (per §S15 N/A); pure cache invalidation token. **OK** — strict §S15 doesn't apply. | N-A | — | — | — |
| 2 | dev.go:138-158 | `ServeIndex` — `data, err := os.ReadFile(...); if err != nil { http.Error(...) }; ...; _, _ = w.Write([]byte(body))` | A.1 | OK | `_, _ = w.Write` has inline comment lines 154-156 explaining client-disconnect rationale — §S3 allowed exception. Same pattern as forge.go:216 (Export). | N-A | — | — | — |
| 3 | dev.go:138-141 | `data, err := os.ReadFile(filepath.Join(h.integrationDir, "tester.html")); if err != nil { http.Error(w, "tester.html not found — check --integration-dir", http.StatusNotFound); return }` | A.4 | EDGE | Uses `http.Error` (stdlib, plain `text/plain` body) instead of `responsehttpapi.Error`. **Dev-only** — testend bootstrap; cannot reasonably go through wire-code envelope (browser asks for HTML, not JSON envelope). OK as transport-layer divergence for non-API endpoint. | LOW | None — dev-mode tester misconfiguration; minor message divergence. | EDGE — non-API HTML serving naturally diverges. | handled-this-batch (see _summary status) |
| 4 | dev.go:169-185 | `StreamLogs` — `ch, unsub := h.broadcaster.Subscribe(); defer unsub(); responsehttpapi.StreamSSE(w, r, ..., ch, func(out io.Writer, data []byte) error { _, err := fmt.Fprintf(...); return err })` | A.1/A.4 | OK | SSE stream — `unsub()` deferred (no-error close). `Fprintf` errors propagated up via SSE writer callback. Standard pattern. | N-A | — | — | — |
| 5 | dev.go:176 | `fmt.Fprintf(out, "event: log\ndata: %s\n\n", entry)` (within ring-buffer replay closure) | A.1 | EDGE | **Ring-buffer replay**: `fmt.Fprintf` return values discarded (no `_ = ...` and no err handling). This is a `func(out io.Writer)` callback whose return is `void` — caller (StreamSSE) cannot observe per-line write fail. **OK** for SSE replay path: if browser drops mid-replay, ctx cancels → StreamSSE shuts down. Late write fail = client gone, by design. **§S3 acceptable** in this SSE shape (analogous to forge.go:216 Export late-write). | N-A | — | Could add inline comment "Fprintf err during replay = client disconnected; ctx will cancel imminently" to anchor §S3 audit — but pattern is well-known. | — |
| 6 | dev.go:209-212 | `if err := json.NewDecoder(r.Body).Decode(&req); err != nil { writeDevJSON(w, http.StatusBadRequest, sqlErrorResponse{Error: "invalid JSON"}); return }` | A.4 | EDGE | Inline `json.NewDecoder().Decode` instead of `decodeJSON` helper — **dev-only** SQL endpoint with custom error envelope (`sqlErrorResponse{Error: ...}` per dev-frontend contract). Cannot use FromDomainError because dev console expects `{"error":"..."}` not `{"error":{"code","message",...}}`. **OK as dev-only divergence** (same posture as dev_mock_llm in B3 site 3 EDGE). | LOW | Wire-style drift — minor; dev-only. | EDGE — accept dev-only divergence. | handled-this-batch (see _summary status) |
| 7 | dev.go:215-218 | `if !strings.HasPrefix(upper, "SELECT") { writeDevJSON(w, http.StatusBadRequest, sqlErrorResponse{Error: "只允许 SELECT 语句 / only SELECT statements allowed"}); return }` | A.4 | EDGE | Dev-only validation theater — single allow-list check. Inline error message. OK as dev-only. | LOW | None functional. | EDGE — dev-only. Note: prefix-check is naive (won't catch `WITH ... SELECT` or comment tricks); but per-design dev-only, attacker == operator. | handled-this-batch (see _summary status) |
| 8 | dev.go:220-224 | `sqlDB, err := h.db.DB(); if err != nil { writeDevJSON(w, http.StatusInternalServerError, sqlErrorResponse{Error: err.Error()}); return }` | A.4 | EDGE | Raw `err.Error()` to user — **dev-only** intentional (testend operator wants the SQLite/GORM err verbatim for debugging). Production handlers must NOT leak err.Error() (errmap.go:255 strips `internal error`); dev path explicitly does. **OK by design**. | LOW | Internal err visible to dev console — by design. | EDGE — dev-only deliberate. | handled-this-batch (see _summary status) |
| 9 | dev.go:226-230 | `rows, err := sqlDB.QueryContext(r.Context(), req.SQL); if err != nil { writeDevJSON(w, http.StatusBadRequest, sqlErrorResponse{Error: err.Error()}); return }` | A.4 | EDGE | Same dev-only verbatim err.Error() path. OK by design. | LOW | Same as site 8. | EDGE. | handled-this-batch (see _summary status) |
| 10 | dev.go:231 | `defer rows.Close()` | A.1 | OK | Read-only path Close — §S3 example exception. | N-A | — | — | — |
| 11 | dev.go:240-260 | `for rows.Next() { ...; if err := rows.Scan(ptrs...); err != nil { continue } ...}` | A.1 | VIOLATION | **§S3 silent skip**: `rows.Scan` err triggers `continue` — silently drops row. No log, no diagnostic. Per §S3 "if err != nil { /* nothing */ }" violation pattern. Even dev-only, hiding scan errors gives **misleading row count** (e.g., user runs `SELECT * FROM forges`; some rows scan-fail → result claims fewer rows exist than reality). | LOW | Dev SQL console silently shows incomplete result set if any row's Scan fails (e.g., type mismatch in dynamic schema). Operator may diagnose wrong issue. | At minimum log: `if err := rows.Scan(ptrs...); err != nil { h.log.Warn("dev: row scan", zap.Error(err)); continue }`. **Dev-only context tempers severity to LOW**, but the silent skip is a real §S3 violation. | handled-this-batch (see _summary status) |
| 12 | dev.go:289-291 | `sqlDB, err := h.db.DB(); if err != nil { writeDevJSON(...InternalServerError, sqlErrorResponse{Error: err.Error()}); return }` | A.4 | EDGE | Same dev-only verbatim err pattern as site 8. OK by design. | LOW | Same. | EDGE. | handled-this-batch (see _summary status) |
| 13 | dev.go:293-298 | `rows, err := sqlDB.QueryContext(r.Context(), \`SELECT name FROM sqlite_master ...\`); if err != nil { ... }` | A.4 | EDGE | Same dev-only verbatim pattern. | LOW | Same. | EDGE. | handled-this-batch (see _summary status) |
| 14 | dev.go:300-305 | `for rows.Next() { var n string; if err := rows.Scan(&n); err == nil { names = append(names, n) } }` | A.1 | VIOLATION | **§S3 silent skip** — same defect as site 11: scan err drops table name. If `sqlite_master` row has unexpected column type / charset issue, entire table is hidden from schema view. Operator sees fewer tables than exist. | LOW | Dev schema view silently incomplete on scan err. | `if err := rows.Scan(&n); err != nil { h.log.Warn("dev: schema scan", zap.Error(err)); continue }; names = append(names, n)`. | handled-this-batch (see _summary status) |
| 15 | dev.go:306 | `rows.Close()` (no err handling) | A.1 | EDGE | Direct Close call without `defer` and without err handling. Read-only path — Close-err inconsequential. **§S3 read-path Close exception** OK. | N-A | — | — | — |
| 16 | dev.go:311-313 | `// Row count — best effort; ignore errors (e.g. virtual tables). _ = sqlDB.QueryRowContext(...).Scan(&t.RowCount)` | A.1 | OK | `_ = ...` with **inline comment** explaining why ignored ("best effort; ignore errors e.g. virtual tables"). Per §S3 example exception "_ = err with inline comment". OK. | N-A | — | — | — |
| 17 | dev.go:317-335 | `colRows, err := sqlDB.QueryContext(... PRAGMA table_info ...); if err == nil { for colRows.Next() { ...; if err := colRows.Scan(...); err == nil { ... } }; colRows.Close() }` | A.1 | VIOLATION | **§S3 multiple silent skips** in nested block: (a) outer `if err == nil` silently skips entire table's column listing if PRAGMA fails — no log, no warning, schema view shows table with **empty columns** as if it had none; (b) inner `if err := colRows.Scan(...); err == nil` silently drops malformed column rows. Per §S3 pattern "if err != nil { return nil }" / "static fallback masking failure". | LOW | Schema view: tables show with empty column list when PRAGMA fails (e.g., virtual table); operator may think table really has no columns. Misdiagnosis risk. | Add log on PRAGMA fail: `if err != nil { h.log.Warn("dev: pragma fail", ...); continue }`. Add log on Scan fail. **Dev-only context tempers to LOW**, but `if err == nil { ... }` pattern (lines 318/326) is cleaner inverted-with-log. | handled-this-batch (see _summary status) |
| 18 | dev.go:334 | `colRows.Close()` (no err handling) | A.1 | OK | Read-path Close per §S3 exception. OK. | N-A | — | — | — |
| 19 | dev.go:377-405 | `ListCollections` — `entries, err := os.ReadDir(...); if err != nil { writeDevJSON(...OK, []Collection{}); return }` | A.1 | VIOLATION | **§S3 silent fallback**: ReadDir err → return empty collection list with **HTTP 200**. User sees "no collections" with no indication that the directory was unreadable (e.g., wrong `--collections-dir` flag, permission issue). Same pattern as `if err != nil { return nil }` violation in §S3 list. **Dev-only**, but operator UX is degraded. | LOW | Operator runs `--dev`, sees empty collections list, has no clue dir is misconfigured / unreadable. | Log + return empty: `if err != nil { h.log.Warn("dev: read collections dir", zap.String("dir", h.collectionsDir), zap.Error(err)); writeDevJSON(w, http.StatusOK, []Collection{}); return }`. **Note**: silent fallback to empty list on missing dir is intentional in some interpretations (dev mode works without collections) — but **adding the log is nearly free** and removes the silence. | handled-this-batch (see _summary status) |
| 20 | dev.go:391, 396 | `h.log.Warn("dev: read collection file", ...)` / `h.log.Warn("dev: parse collection yaml", ...)` then `continue` | A.1 | OK | **Logged silent skip** — file-level read or yaml-parse fail logs and skips. Operator can find the cause in logs. **§S3 acceptable** when log emits + continues (vs. truly silent). Same pattern as skill scan. | N-A | — | — | — |
| 21 | dev.go:419-425 | `ListTools` — pure read of in-memory `h.tools` slice. No errors possible. | A.4 | OK | Pure data read; no err path. | N-A | — | — | — |
| 22 | dev.go:446-455 | `InvokeTool` — `if err := json.NewDecoder(r.Body).Decode(&req); err != nil { writeDevJSON(...BadRequest, invokeResponse{Error: "invalid JSON"}); return }; if req.Tool == "" {...}` | A.4 | EDGE | Same dev-only inline JSON decode + custom envelope. OK by design (same posture as site 6). | LOW | Wire-style drift — dev-only. | EDGE. | handled-this-batch (see _summary status) |
| 23 | dev.go:460-471 | `ctx := r.Context(); ...; for _, t := range h.tools { if t.Name() == req.Tool { target = t; break } }; if target == nil { writeDevJSON(...NotFound, ...); return }` | A.4 | OK | Tool lookup fallback — explicit 404 with dev envelope. OK. | N-A | — | — | — |
| 24 | dev.go:473-481 | `start := time.Now(); output, err := target.Execute(ctx, req.Args); elapsed := time.Since(start).Milliseconds(); if err != nil { writeDevJSON(w, http.StatusOK, invokeResponse{Output: output, OK: false, ElapsedMs: elapsed, Error: err.Error()}); return }` | A.2/A.4 | EDGE | Tool execution — could be terminal write (e.g., `create_forge` tool from dev console). `r.Context()` (variable `ctx`) pass-through. Tool err wrapped into envelope with HTTP 200 (caller checks `OK:false`). Per **mcp.go site 6 same MED concern** — 200 + Error field is "success-shaped failure", but **dev-only** + tool framework standard contract (tool err is data, not HTTP error). OK as dev-tool framework convention. | LOW | If user disconnects mid-tool-execute, tool still runs (good in some cases, bad if it locks). Standard handler-layer pass-through — detached responsibility lives in tool framework / app layer. | EDGE-FLAG (analogous to forge-app long-running terminal). Tool framework's own ctx-handling is the audit point. | handled-this-batch (see _summary status) |
| 25 | dev.go:486-490 | `func writeDevJSON(w http.ResponseWriter, status int, v any) { w.Header().Set(...); w.WriteHeader(status); _ = json.NewEncoder(w).Encode(v) }` | A.1 | EDGE | `_ = json.NewEncoder(w).Encode(v)` — **no inline comment** explaining why err is ignored. Per §S3 strict reading, naked `_ = err` without comment is violation. **Dev-only helper** + analogous to `_, _ = w.Write(...)` late-write pattern; status code already sent. Mild violation in form (no comment). | LOW | None functional. | Add inline comment: `// Late JSON encode err = client gone; status already sent.` Brings into §S3 example-exception territory. | handled-this-batch (see _summary status) |
| 26 | dev.go (entire file) | No `var Err...` / sentinel definitions | A.5 | OK | File defines zero sentinels. Defines structs (sqlRequest, sqlResponse, sqlErrorResponse, schemaTable, schemaColumn, Collection, StepConfig, ExpectConfig, devToolSummary, invokeRequest, invokeResponse) but no domain errors. **§S17 N/A**. | N-A | — | — | — |
| 27 | dev.go (entire file) | No `idgen.New` / `crypto/rand` calls. `time.Now().Unix()` for buildID (not a business ID — site 1) | A.3 | OK | Handler does not generate business IDs. buildID is a cache-buster token, not subject to §S15 (§S15 explicitly: business IDs only). | N-A | — | — | — |

## Sub-check summary

**A.1 §S3 错误吞没**:
- violations: site 11 (rows.Scan continue silent), site 14 (Scan continue silent in Schema), site 17 (PRAGMA + colRows.Scan double silent skip), site 19 (ReadDir silent fallback to empty list, dev-mode UX hostile), site 25 (writeDevJSON no comment for ignored Encode err)
- EDGE notes: site 2 (Write late err — comment OK), site 5 (SSE replay Fprintf — by SSE design), site 10/15/18 (read-path Close exception OK), site 16 (`_ = QueryRowContext...Scan` with explicit comment OK), site 20 (Warn-log + continue — proper pattern OK)

**A.2 §S9 detached ctx 终态写**:
- terminal-state writes identified: site 24 (InvokeTool — could trigger any tool's terminal write, e.g., create_forge / write_file)
- ctx 来源: `r.Context()` pass-through (variable `ctx`)
- violations: not present at handler layer — handler-layer pass-through is standard §S9 stance. **EDGE-FLAG → tool framework audit batch** (Tool.Execute responsibility for detached ctx).
- N/A reason for read-only handlers: ServeIndex / StreamLogs / QuerySQL (read-only SELECT) / Schema (read-only) / ListCollections / ListTools — pure reads / SSE pull paths.

**A.3 §S15 ID 生成**:
- ID generation calls: 0 in handler. `buildID = fmt.Sprintf("%d", time.Now().Unix())` is cache-buster, not business ID
- violations: N/A — file does not generate business IDs

**A.4 §S16 错误 wrap 格式**:
- violations: not present in this file (no `fmt.Errorf` constructions — only `Sprintf` for non-error formatting)
- EDGE: dev-only wire-style divergence (sites 6/7/8/9/12/13/22) using custom `sqlErrorResponse` / `invokeResponse` envelope vs. project-standard FromDomainError. **OK by dev-only design** — same posture as B2 dev_info.go / B3 dev_mock_llm.go.

**A.5 §S17 sentinel 登记 errmap**:
- sentinels defined in file: none
- consumed sentinels: none surface to FromDomainError — dev-only handlers use custom error envelopes instead
- missing: N/A — file defines no sentinels

## File-level findings summary

| Severity | Count | Sites |
|---|---|---|
| HIGH | 0 | — |
| MED | 0 | — |
| LOW | 4 (concrete §S3 violations) + 9 (EDGE LOW dev-only divergence/pass-through) = 13 | sites 11, 14, 17, 19 (§S3 silent skip — concrete); sites 3, 6, 7, 8, 9, 12, 13, 22, 24, 25 (EDGE — dev-only / pass-through) |

**Net dev.go**: 0 HIGH / 0 MED / 13 LOW.

**§S3 silent-skip cluster (sites 11/14/17/19)** is the file's only concrete dev-non-pattern violation pattern:
- All 4 sites involve "iterate-then-skip-on-err with no log" — common dev-tool laziness, but turns dev console into an unreliable diagnostic surface (hides the very errors the operator is trying to find).
- **1-line fix per site**: add `h.log.Warn("dev: <op>", zap.Error(err))` before `continue` / before `writeDevJSON`. Consistent with site 20's existing logged-skip pattern in the same file (lines 391/396).
- **§S20 "no留下次"** applies — the fix is trivial; 4 lines added, defects gone.

**Dev-only divergences (sites 3, 6, 7, 8, 9, 12, 13, 22, 25)** are accepted patterns per project posture (B2 / B3 cross-cutting): dev console doesn't go through wire-code envelope. Optional polish: add inline comment to `_ = json.NewEncoder(w).Encode(v)` (site 25) to anchor §S3 audit, but no functional impact.

**Site 24 (InvokeTool tool-execute pass-through)** is the only terminal-write site — handler-layer pass-through is standard; tool framework owns detached-ctx semantics.
