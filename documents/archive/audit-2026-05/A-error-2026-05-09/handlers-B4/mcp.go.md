# mcp.go audit trace (handlers-B4)

**File**: `backend/internal/transport/httpapi/handlers/mcp.go` (483 LOC)
**Scope**: §S3 / §S9 / §S15 / §S16 / §S17

## 9-column trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | mcp.go:105-108 | `func (h *MCPHandler) ListServers(...) { servers := h.svc.ListServers(r.Context()); responsehttpapi.Success(w, http.StatusOK, servers) }` | A.4 | EDGE | `svc.ListServers` returns no error — interface is no-fail (returns slice + status from in-memory). Looks weird but matches mcpapp surface (servers cached in mem; status fields capture per-server health). | LOW | None — by design. | No fix; consider inline comment "ListServers is no-fail by mcp service contract" to anchor audit. | handled-this-batch (see _summary status) |
| 2 | mcp.go:120-132 | `tail, err := h.svc.Stderr(name); if err != nil { responsehttpapi.FromDomainError(...) }; ...` | A.4 | OK | Standard read; svc returns mcpdomain.ErrServerNotFound on miss (errmap.go:158 → 404). | N-A | — | — | — |
| 3 | mcp.go:137-145 | `GetServer` read-only via FromDomainError | A.4/A.5 | OK | mcpdomain.ErrServerNotFound registered in errmap. | N-A | — | — | — |
| 4 | mcp.go:159-171 | `if err := json.NewDecoder(http.MaxBytesReader(w, r.Body, importMaxBytes)).Decode(&body); err != nil { responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "failed to parse request body: "+err.Error(), nil); return }` | A.4 | VIOLATION | Inline `json.NewDecoder(...).Decode` bypasses the project's standardized `decodeJSON` helper (apikey.go:217). The helper produces `fmt.Errorf("handlers.decodeJSON: %w", joinInvalidRequest(err))` which lets FromDomainError handle the 400. Here the literal `"INVALID_REQUEST"` wire-code + free-form message duplicates errmap responsibility. **Same cross-cutting pattern called out in handlers-B3 _summary §1 (decodeJSONLimit refactor — HIGH-ROI)**. | LOW | Wire-code style drift; INVALID_REQUEST routing diverges between handlers (some via errmap, some inline). | Replace with `if err := decodeJSONLimit(w, r, importMaxBytes, &body); err != nil { responsehttpapi.FromDomainError(w, h.log, err); return }` (after decodeJSONLimit refactor in B3 lands). | handled-this-batch (see _summary status) |
| 5 | mcp.go:172-176 | `if strings.TrimSpace(body.Command) == "" { responsehttpapi.Error(w, http.StatusBadRequest, "MCP_COMMAND_REQUIRED", "command field is required", nil); return }` | A.5 | EDGE | Inline ad-hoc wire-code `"MCP_COMMAND_REQUIRED"` — no sentinel registered. Per handlers-B3 _summary §5 (wire-code ad-hoc cross-cutting). Could potentially be `errorsdomain.ErrInvalidRequest` wrap or a new `mcpdomain.ErrCommandRequired` sentinel. | LOW | Wire-code style drift. | EDGE-FLAG → cross-cutting wire-code policy decision. Lean: declare `mcpdomain.ErrCommandRequired` + register in errmap so all field-validation paths flow through FromDomainError. | handled-this-batch (see _summary status) |
| 6 | mcp.go:184-198 | `if err := h.svc.AddServer(...); err != nil { st, gErr := h.svc.GetServer(...); if gErr == nil && st != nil { h.log.Warn(...); responsehttpapi.Success(w, http.StatusOK, st); return }; responsehttpapi.FromDomainError(w, h.log, err); return }` | A.1 | VIOLATION | **§S3 silent fallback**: AddServer error is swallowed when GetServer succeeds — `err` becomes a zap.Warn (logged but not surfaced to caller). The HTTP response is 200 OK with status row — caller has to parse `status` field to discover the connect failure. Comment (lines 187-189) documents this as intentional ("status row to caller along with warning"). **Borderline §S3** — error reaches user (via status field) but warning-log + 200 OK is "success-shaped failure". Per spec "silent fallback by design" debate — here the design is to return status row regardless. mcp.md §10 ("PUT returns 200 with ServerStatus regardless of connect succeed — caller checks status field") explicitly designs this. | MED | Caller MUST inspect status field; bare 200 OK + zap.Warn means logs are the only place the original error appears. If subsequent observability is filter-tuned to ERROR only, AddServer fails silently from the user's perspective. | Add `responsehttpapi.Header.Set("Warning", "...")` or include `error` field in response payload so caller sees it without status-field parsing. **Defensible by design** per mcp.md §10 — debatable; recommend re-design discussion w/ PM. Alternatively: log at `Error` level (not `Warn`) so log filters catch it. | handled-this-batch (see _summary status) |
| 7 | mcp.go:200-205 | `st, err := h.svc.GetServer(r.Context(), name); if err != nil { responsehttpapi.FromDomainError(...) }; responsehttpapi.Success(w, http.StatusOK, st)` | A.2/A.4 | EDGE | Successful PUT path. Terminal-write is `AddServer` (line 184) which writes mcp.json + connects subprocess. `r.Context()` pass-through to service — handler-layer §S9 pass-through is standard stance (per handlers-B3 _summary §3). | LOW | Mid-PUT cancel risk: mcp.json may be written but Connect skipped. | EDGE-FLAG → mcp-app audit batch (AddServer terminal write — must use detached ctx for mcp.json save + Connect). | handled-this-batch (see _summary status) |
| 8 | mcp.go:211-218 | `DeleteServer` — `h.svc.RemoveServer(r.Context(), name)` then 204 | A.2/A.4 | EDGE | Terminal write — removes from mcp.json + disconnects subprocess. r.Context() pass-through. | LOW | Mid-delete cancel risk (could leave mcp.json edited but subprocess running). | EDGE-FLAG → mcp-app audit. | handled-this-batch (see _summary status) |
| 9 | mcp.go:230-234 | `name, action := splitAction(r.PathValue("nameAction")); if name == "" { responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "missing server name in path", nil); return }` | A.4 | EDGE | Same inline `INVALID_REQUEST` wire-code pattern as site 4. Duplicate of validation theater that could be FromDomainError-routed. | LOW | Wire-code style drift. | EDGE-FLAG → cross-cutting. Could wrap `errorsdomain.ErrInvalidRequest` with descriptive message. | handled-this-batch (see _summary status) |
| 10 | mcp.go:236-258 | switch action: reconnect / health-check / default; reconnect calls `h.svc.Reconnect(r.Context(), name)` then `h.svc.GetServer(...)` | A.2/A.4 | EDGE | Reconnect = terminal write (subprocess kill + spawn). r.Context() pass-through. Long-running (subprocess kill + handshake). | LOW | Mid-reconnect cancel risk (could leave subprocess in inconsistent state — old killed, new not yet up). | EDGE-FLAG → mcp-app audit (long-running subprocess restart MUST use detached ctx — else cancel between kill and spawn = no MCP server). | handled-this-batch (see _summary status) |
| 11 | mcp.go:248-253 | `case "health-check": res, err := h.svc.HealthCheck(r.Context(), name); ...` | A.4 | OK | Probe — non-mutating active call to subprocess. r.Context() pass-through correct. | N-A | — | — | — |
| 12 | mcp.go:255-258 | `default: responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "unknown action "+action, nil)` | A.4/A.5 | EDGE | Inline INVALID_REQUEST literal. Same cross-cutting pattern. | LOW | Wire-code drift. | EDGE-FLAG → cross-cutting. | handled-this-batch (see _summary status) |
| 13 | mcp.go:280-284 | `if err := r.ParseMultipartForm(importMaxBytes); err != nil { responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "failed to parse multipart form: "+err.Error(), nil); return }` | A.4 | EDGE | Inline INVALID_REQUEST wire-code; multipart parse fail. Same cross-cutting pattern. Hard to clean up cleanly without `decodeMultipart` helper. | LOW | Wire-code drift. | EDGE-FLAG → consider helper `parseMultipartLimit(w, r, importMaxBytes)` analogous to decodeJSONLimit. | handled-this-batch (see _summary status) |
| 14 | mcp.go:285-291 | `file, _, err := r.FormFile("config"); if err != nil { responsehttpapi.Error(...); return }; defer file.Close()` | A.1 | EDGE | `defer file.Close()` — Close error swallowed. Per §S3 example exception "defer f.Close() in only-read path". File is read in loop below; closing read-only file Close-err is benign. **OK per §S3 read-path Close exception**. | N-A | — | — | — |
| 15 | mcp.go:295-310 | `raw = make([]byte, 0, 4096); buf := make([]byte, 4096); for { n, rerr := file.Read(buf); if n > 0 { raw = append(...); if int64(len(raw)) > importMaxBytes { responsehttpapi.Error(w, http.StatusRequestEntityTooLarge, "INVALID_REQUEST", "config file exceeds 1MB limit", nil); return }}; if rerr != nil { break } }` | A.1 | VIOLATION | **§S3 silent break on non-EOF io error** — same defect as **handlers-B3 skills.go site 11 (MED)**. The loop breaks on any `rerr != nil` without distinguishing `io.EOF` (legitimate) from real errors (disk fail, connection drop). Truncated `raw` is treated as legitimate JSON below. The downstream `json.Unmarshal` will fail noisily on bad input, but real cause (read err) is hidden — debug breadcrumb lost. **Identical pattern, identical fix as B3 skills.go**. | MED | If multipart upload encounters mid-read I/O error (e.g., temp file truncated, disk full), user sees "failed to parse JSON" with cryptic message instead of the actual read error. | Replace hand-rolled loop with `io.ReadAll(io.LimitReader(file, importMaxBytes+1))` — single line, errors explicit, no silent EOF-vs-real-error confusion. **§S20 — same line of fix as B3 skills.go MED.** | handled-this-batch (see _summary status) |
| 16 | mcp.go:317-322 | `body := http.MaxBytesReader(w, r.Body, importMaxBytes); var err error; raw, err = readAll(body); if err != nil { responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "failed to read body: "+err.Error(), nil); return }` | A.1/A.4 | VIOLATION | Calls **`readAll` (mcp.go:451)** — see site 24 below. The helper itself uses `err.Error() == "EOF"` string comparison (✗ instead of `errors.Is(err, io.EOF)`), and the call site uses inline `INVALID_REQUEST` wire-code. Both feed into the same §S3 + §S16 cross-cutting concern. | MED | If `io.EOF` ever wraps in some custom Reader that produces `*MyEOFErr{Inner: io.EOF}`, `err.Error() == "EOF"` returns false, and what was actually a clean EOF is treated as a real error. Today it works because http.MaxBytesReader returns plain io.EOF — but it's fragile. | Replace `readAll` calls with `io.ReadAll(body)` (stdlib does the right thing). Or fix `readAll` internals: `if errors.Is(err, io.EOF) { return out, nil }`. | handled-this-batch (see _summary status) |
| 17 | mcp.go:335-339 | `if err := json.Unmarshal(raw, &fragment); err != nil { responsehttpapi.Error(...); return }` | A.4 | EDGE | Inline INVALID_REQUEST wire-code. Inline json.Unmarshal here is unavoidable (raw bytes already collected) — can't easily route to decodeJSON helper. | LOW | Wire-code drift. | Could wrap `errorsdomain.ErrInvalidRequest` and FromDomainError, but lose specific message context. EDGE — drift acceptable unless cross-cutting policy formalizes. | handled-this-batch (see _summary status) |
| 18 | mcp.go:340-344 | `if len(fragment.MCPServers) == 0 { responsehttpapi.Error(w, http.StatusBadRequest, "INVALID_REQUEST", "no servers found in payload (mcpServers map is empty or missing)", nil); return }` | A.4 | EDGE | Inline INVALID_REQUEST wire-code; defensive validation (empty payload). | LOW | Wire-code drift. | EDGE-FLAG → cross-cutting policy. | handled-this-batch (see _summary status) |
| 19 | mcp.go:357-362 | `res, err := h.svc.Import(r.Context(), configs, overwrite); if err != nil { ...FromDomainError... }; responsehttpapi.Success(w, http.StatusOK, res)` | A.2/A.4 | EDGE | Terminal write — bulk import writes mcp.json + connects each new server (long-running). r.Context() pass-through. **Same long-running cancel concern as PUT** but worse: bulk operation could partially complete (some servers written, others not). | LOW | Mid-import cancel may leave mcp.json with partial set of servers committed. | EDGE-FLAG → mcp-app audit (Import is the largest §S9 risk on this file — must use detached ctx OR be transactional). | handled-this-batch (see _summary status) |
| 20 | mcp.go:373-380 | `ListRegistry` — `entries, err := h.svc.ListRegistry(r.Context()); if err != nil { ...FromDomainError... }` | A.4 | EDGE | Read-only. **However**: V3 (per file comment lines 367-372) returns curated catalog; the service may attempt remote registry fetch (see mcpdomain.ErrMarketplaceUnavailable registered errmap.go:170). The handler properly surfaces err — service-layer responsibility for "fall back to cached / return ErrMarketplaceUnavailable" is checked separately. | LOW | If service silently falls back when registry unreachable (silent fallback §S3), user sees stale catalog with no indication. **handler-layer pass-through OK** — concern is mcp-app behavior. | EDGE-FLAG → mcp-app audit (ListRegistry must surface ErrMarketplaceUnavailable when applicable, not silent-fallback). | handled-this-batch (see _summary status) |
| 21 | mcp.go:387-395 | `GetRegistryEntry` read | A.4 | OK | Standard read; ErrRegistryEntryNotFound registered (errmap.go:163 → 404). | N-A | — | — | — |
| 22 | mcp.go:407-417 | `name, action := splitAction(...); if name == "" {...INVALID_REQUEST...}; if action != "install" {...INVALID_REQUEST...}` | A.4 | EDGE | Inline INVALID_REQUEST literals (twice). Same cross-cutting wire-code pattern. | LOW | Wire-code drift. | EDGE-FLAG → cross-cutting. | handled-this-batch (see _summary status) |
| 23 | mcp.go:419-432 | `var body struct {...}; if err := json.NewDecoder(http.MaxBytesReader(w, r.Body, importMaxBytes)).Decode(&body); err != nil { if !errors.Is(err, errEmptyBody) { body.Env = nil; body.Args = nil } }` | A.1 | VIOLATION | **§S3 silent swallow of non-empty-body decode err.** The errors.Is check against errEmptyBody is **always false** because `json.NewDecoder().Decode` never returns errEmptyBody (it returns io.EOF on empty body, or other JSON errors on malformed). So `if !errors.Is(err, errEmptyBody)` is always true → execution always sets `body.Env = nil; body.Args = nil` and falls through. **The errEmptyBody sentinel (line 477) is never produced — comment at line 470-476 admits it's "currently unused".** The behavior is "any decode error → treat as no-body, swallow err". This is silent fallback masking real errors (malformed JSON, oversize body via MaxBytesReader). InstallFromRegistry below then runs with empty env/args, hits ErrRequiredEnvMissing/ErrRequiredArgsMissing — user sees "missing env" instead of "your JSON was malformed". | MED | User submits broken `{env: ...}` JSON → response says `MCP_REQUIRED_ENV_MISSING` (lying — env was provided, just unparseable). Debugging UX hostile. | Replace with: try decode; if `err == io.EOF` → empty body OK, body stays zero; else → reject with INVALID_REQUEST (let user fix their JSON). The errEmptyBody sentinel + comment about "future strict mode" is dead — should be deleted now. | handled-this-batch (see _summary status) |
| 24 | mcp.go:437-442 | `st, err := h.svc.InstallFromRegistry(r.Context(), name, body.Env, body.Args); if err != nil {...FromDomainError...}; responsehttpapi.Success(w, http.StatusCreated, st)` | A.2/A.4 | EDGE | Terminal write — sandboxapp runtime install + mcp.json write + Connect. **Long-running** (mise install can take minutes; npm install too). r.Context() pass-through. **High §S9 risk**: if browser cancels during runtime install → mcp.json written but runtime gone, server can't connect. | LOW | Mid-install cancel risk; **most §S9-critical site in file** because install can take minutes. | EDGE-FLAG → mcp-app audit (InstallFromRegistry MUST use detached ctx — long enough for browser hard refresh / tab close to be likely). | handled-this-batch (see _summary status) |
| 25 | mcp.go:451-466 | `func readAll(r interface{ Read([]byte) (int, error) }) ([]byte, error) { ...; for { n, err := r.Read(buf); ...; if err != nil { if err.Error() == "EOF" { return out, nil }; return out, err } } }` | A.1 | VIOLATION | **§S3 stringly-typed EOF check**. `err.Error() == "EOF"` should be `errors.Is(err, io.EOF)`. Currently fragile: any custom Reader that wraps EOF (e.g., `&MyError{wrapped: io.EOF}` returning `"my custom: EOF"`) breaks the check, and a real EOF is mistaken for a real error. **Also a subtle reinventing of `io.ReadAll` for no apparent benefit** (claimed "single line" but stdlib already provides this). | LOW (it works today w/ http.MaxBytesReader returning plain io.EOF — but is fragile). | If anyone wraps a Reader, EOF detection silently breaks → "failed to read body: EOF" surfaces as a 400 to user. | Delete `readAll`; use `io.ReadAll(body)` everywhere it's called (sites 16/317). 2-line removal, stdlib does the right thing. | handled-this-batch (see _summary status) |
| 26 | mcp.go:477 | `var errEmptyBody = errors.New("empty body")` | A.5 | VIOLATION | **Dead sentinel**: per file comment (lines 470-476), errEmptyBody is "currently unused" — kept for "future strict mode". `errors.Is(err, errEmptyBody)` at site 23 (line 428) is always false because nothing produces it. Per §S3 spirit of "no silent dead branches" + §S17 "register sentinels that reach handler" — this sentinel reaches handler **never**, so it's not a §S17 violation per se, but it's a **dead-code §S3 problem**: the false sense of "we're handling empty-body specifically" masks the real swallow at site 23. | LOW | Code reads as if empty-body is handled distinctly, but it's not — masks the silent swallow at site 23. | Delete `errEmptyBody` + the dead branch at site 23. Replace with explicit `errors.Is(err, io.EOF)` check for empty body. | handled-this-batch (see _summary status) |
| 27 | mcp.go:483 | `var _ = mcpinfra.MergeResult{}` | A.1 | EDGE | Compile-time keep-alive — explicit "silence unused-import lint". Comment provides reason. **§S3 example exception** (`_ = err with inline comment`). Note though: typing `var _ = mcpinfra.MergeResult{}` to keep an import alive is unusual — usually `var _ mcpinfra.MergeResult` (zero-allocation) or just import as `_ "..."`. Allocates a zero MergeResult struct at package init. | LOW | None functional; minor allocator noise at init. | Replace with `var _ mcpinfra.MergeResult` (no allocation) or `_ = mcpinfra.MergeResult` if the value form is preferred. | handled-this-batch (see _summary status) |
| 28 | mcp.go (entire file) | No `mcp_<16hex>` ID generation (servers keyed by user-supplied name) | A.3 | OK | mcp servers identified by user-given `name` (mcp.json map key) — no `<prefix>_<hex>` business ID. Per mcp.md §5, name is the canonical key. Same rationale as skill.md (handlers-B3 §S15 N/A). | N-A | — | — | — |
| 29 | mcp.go (sentinels) | `var errEmptyBody = errors.New(...)` only; no Err* sentinels | A.5 | EDGE | File defines 1 sentinel (`errEmptyBody`) which is package-private and never reaches handler (because never produced — see site 26). All consumed mcpdomain sentinels (14 total) are registered in errmap.go (lines 158-173). | N-A (coverage) | — | Plus delete dead errEmptyBody (see site 26). | — |

## Sub-check summary

**A.1 §S3 错误吞没**:
- violations: site 6 (AddServer error → zap.Warn + 200 OK silent fallback), site 15 (multipart silent break on non-EOF — MIRRORS B3 skills.go MED), site 16 (readAll usage), site 23 (`errors.Is(err, errEmptyBody)` always false → silent swallow of all decode errs as "no body"), site 25 (readAll EOF stringly-check), site 26 (dead errEmptyBody sentinel masking site 23)
- EDGE notes: site 14 (defer file.Close in read path — OK per §S3 exception), site 27 (compile-time keep-alive with comment — OK)

**A.2 §S9 detached ctx 终态写**:
- terminal-state writes identified: site 6 (AddServer = mcp.json save + Connect), site 7 (PUT post-success path), site 8 (DeleteServer = mcp.json edit + disconnect), site 10 (Reconnect = subprocess kill+spawn), site 19 (Import = bulk mcp.json + Connect each), site 24 (InstallFromRegistry = sandboxapp install + mcp.json + Connect — **most critical**)
- ctx 来源: 全部 `r.Context()` 透传
- violations: not present at handler layer — handler-layer pass-through is standard §S9 stance per handlers-B3 _summary §3. **All 6 EDGE-FLAG → mcp-app audit batch.** Especially site 24 (InstallFromRegistry: mise + npm install can take minutes; cancel is highly likely) and site 19 (Import bulk).

**A.3 §S15 ID 生成**:
- ID generation calls: 0 in handler
- violations: N/A — MCP servers identified by user-supplied name (per mcp.md §5)

**A.4 §S16 错误 wrap 格式**:
- violations: site 4 (inline json.NewDecoder bypass of decodeJSON — should be decodeJSONLimit per B3 cross-cutting fix), site 5 (`MCP_COMMAND_REQUIRED` ad-hoc), sites 9/12/13/17/18/22 (inline INVALID_REQUEST literals)
- All trace to same cross-cutting wire-code drift / decodeJSONLimit refactor recommendation from B3 _summary §1.

**A.5 §S17 sentinel 登记 errmap**:
- sentinels defined in file: `errEmptyBody` (dead, never produced — see site 26)
- mcpdomain sentinels consumed (via FromDomainError): all 14 registered (errmap.go:158-173: ErrServerNotFound, ErrServerNotConnected, ErrToolNotFound, ErrToolCallFailed, ErrToolCallTimeout, ErrRegistryEntryNotFound, ErrRuntimeMissing, ErrRequiredEnvMissing, ErrRequiredArgsMissing, ErrInstallFailed, ErrMarketplaceUnavailable, ErrAlreadyInstalled, ErrUnsupportedRuntime, ErrHandshakeFailed)
- missing: none from errmap. Plus dead `errEmptyBody` should be deleted (not registered, not produced — fine; just dead code).

## File-level findings summary

| Severity | Count | Sites |
|---|---|---|
| HIGH | 0 | — |
| MED | 3 | site 6 (AddServer silent fallback to 200 OK + Warn — design-debatable), site 15 (multipart silent break on non-EOF — MIRRORS B3 skills.go MED), site 23 (errEmptyBody dead-branch silent swallow → all decode errs treated as "no body") |
| LOW | 12 | sites 1, 4, 5, 7, 8, 9, 10, 12, 13, 16, 17, 18, 19, 20, 22, 24, 25, 26, 27 (mostly EDGE: terminal-write pass-through + wire-code drift + readAll fragility) |

**Net mcp.go**: 0 HIGH / 3 MED / 12 LOW.

**MED priorities (per §S20 "no留下次")**:

1. **Site 23 (errEmptyBody dead-branch)** — critical UX bug masking real JSON errors with "missing env" wire-code. **1-line fix**: replace `if !errors.Is(err, errEmptyBody)` with explicit `if !errors.Is(err, io.EOF)`, and delete `errEmptyBody` sentinel + comment.
2. **Site 15 (multipart silent break)** — same defect as B3 skills.go MED, same fix (`io.ReadAll(io.LimitReader(file, importMaxBytes+1))`).
3. **Site 6 (AddServer 200 OK + Warn)** — design call: mcp.md §10 explicitly says "PUT returns 200 with status row regardless of connect success". May be intentional. **Recommend**: PM/design discussion + at minimum log at Error level (not Warn) or include `error` field in response payload so caller doesn't have to inspect status field.

**LOW priorities** are mostly cross-cutting (decodeJSONLimit refactor from B3 + wire-code policy) — defer to that batch.
