# File trace: backend/internal/pkg/llmclient/llmclient.go

LOC: 104

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | llmclient.go:23-27 | `var ( ErrPickModel = errors.New("llmclient: pick model failed"); ErrResolveCreds = errors.New("llmclient: resolve credentials failed"); ErrBuildClient  = errors.New("llmclient: build client failed") )` | A.5 | OK | §S17: 3 sentinels declared; chat runner.go:108-117 manually `errors.Is(err, ErrPickModel)` / `ErrResolveCreds` to translate to wire codes (`MODEL_NOT_CONFIGURED` / `API_KEY_PROVIDER_NOT_FOUND`) inside async agent loop, then calls `emitFatalError` (writes status=error assistant message + event log). Other 7 callsites (skill / forge / mcp / subagent / catalog / web fetch) wrap with their own domain prefix or absorb. **None of these sentinels reach `responsehttpapi.FromDomainError`** — they live entirely in async chat / catalog flows that don't return errors to handler. errmap registration correctly absent. | — | — | — | — |
| 2 | llmclient.go:49-52 | `provider, modelID, err := picker.PickForChat(ctx); if err != nil { return nil, fmt.Errorf("%w: %v", ErrPickModel, err) }` | A.4 | EDGE | §S16: Format is `<sentinel>: <inner>`, **missing `<pkg>.<func>:` prefix** (`llmclient.Resolve:` would satisfy strict reading). However: (a) sentinel name itself encodes "llmclient: pick model failed" — the `<pkg>:` token is literally embedded in the sentinel (line 24); (b) `%v` on the inner `err` is the documented violation pattern but here is **deliberate** — the inner `err` from picker may itself be a sentinel (`modeldomain.ErrNotConfigured`), and the **outer** sentinel `ErrPickModel` is what callers `errors.Is` on. If both were `%w`, `errors.Is(err, ErrPickModel)` AND `errors.Is(err, modeldomain.ErrNotConfigured)` would both succeed, and the chat runner switch at runner.go:111-115 picks `MODEL_NOT_CONFIGURED` first via `ErrPickModel`. Net behaviour is correct, but the `%v` here loses the underlying picker sentinel for downstream `errors.Is`. **Open question**: is `modeldomain.ErrNotConfigured` ever needed via `errors.Is` at the resolve callsite? runner.go:108-119 only checks for `ErrPickModel` / `ErrResolveCreds`, so the answer is "no for chat path". For the `ResolveForWebSummary` path (line 69), the function explicitly handles `modeldomain.ErrNotConfigured` BEFORE the wrap, so loss of inner sentinel after wrap doesn't matter. **Verdict**: works as-is; pattern is `%w: %v` chained-sentinel-to-string-tail, common in Go. Not a strict violation given the 7-callsite empirical check. | LOW | None visible — chat shows correct error code; web summary fallback works | Optional: change to `fmt.Errorf("llmclient.Resolve: %w (%w)", ErrPickModel, err)` (Go 1.20+ multi-`%w`) for full sentinel preservation. Not required for current callsites. | — |
| 3 | llmclient.go:62-76 | `func ResolveForWebSummary(...) ... { provider, modelID, err := picker.PickForWebSummary(ctx); if errors.Is(err, modeldomain.ErrNotConfigured) { provider, modelID, err = picker.PickForChat(ctx) } if err != nil { return nil, fmt.Errorf("%w: %v", ErrPickModel, err) } ... }` | A.1 | OK | §S3: `errors.Is(err, modeldomain.ErrNotConfigured)` then **silently** falls back to `PickForChat`. This **is** a documented design (godoc line 56-61: "Tries the web_summary scenario first; falls back to chat when unconfigured so summarisation works out of the box"). Per §S3 example list, this is a "documented design" silent fallback explicitly authorized at the spec level. Annotation present (godoc); behaviour matches documentation. Not a violation. | — | — | — | — |
| 4 | llmclient.go:84-87 | `creds, err := keys.ResolveCredentials(ctx, provider); if err != nil { return nil, fmt.Errorf("%w: %v", ErrResolveCreds, err) }` | A.4 | EDGE | §S16: same `%w: %v` pattern as site #2 — outer sentinel preserved (callers `errors.Is(err, ErrResolveCreds)`), inner picker/key err is string-tail. runner.go:114 only checks `ErrResolveCreds`, so behaviour correct. Same as site #2 — works as-is, optional improvement. | LOW | None visible | Optional: Go 1.20+ multi-`%w` wrap | — |
| 5 | llmclient.go:88-96 | `client, baseURL, err := factory.Build(...); if err != nil { return nil, fmt.Errorf("%w: %v", ErrBuildClient, err) }` | A.4/A.5 | EDGE | §S16: same `%w: %v` pattern. §S17: `ErrBuildClient` is **never** referenced via `errors.Is` by any caller (grep across all 7 llmclient callsites: only `ErrPickModel` + `ErrResolveCreds` are checked; `ErrBuildClient` defined but unused at error-discrimination level). Code-shape suggests it was added for symmetry with the other two stages. Not strictly wrong (defensive sentinel; future callers may want to discriminate Build failures), but currently dead-weight at the discrimination level. errmap absence correct: never reaches handler. | LOW | None | Optional: either wire a callsite that branches on `ErrBuildClient` (e.g., chat runner could add `case errors.Is(err, ErrBuildClient): code = "LLM_PROVIDER_ERROR"`) for symmetry, or document it as "reserved for future stage-discrimination". Not required. | — |

## File summary

5 sites total. 2 OK, 3 EDGE (LOW). 0 violations.

The `%w: %v` pattern at sites 2/4/5 is the **only systematic concern** — it matches §S16's "禁止 `%v`" rule literally but the empirical chain works because callers only `errors.Is` on the **outer** sentinel (which is preserved via `%w`). The inner-sentinel loss is invisible because no caller depends on it. Score: LOW × 3.

`ErrBuildClient` is defined but unreferenced via `errors.Is` — design intent (3 stages = 3 sentinels) is clearer than the code requires, but harmless.
