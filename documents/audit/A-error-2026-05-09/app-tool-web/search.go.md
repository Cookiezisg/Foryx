# audit: backend/internal/app/tool/web/search.go

LOC: 372
Read: full file (lines 1-372)

## Trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | search.go:75 | `var ErrEmptyQuery = errors.New("query is required and must be non-empty")` | A.5 | OK | Tool framework sentinel — consumed via ValidateInput, not handler errmap path. Same carve-out as fetch.go sentinels. | N-A | — | — | — |
| 2 | search.go:175-180 | `if err := json.Unmarshal(args, &a); err != nil { return fmt.Errorf("WebSearch.ValidateInput: %w", err) }; if strings.TrimSpace(a.Query) == "" { return ErrEmptyQuery }` | A.4 | EDGE | §S16 prefix missing pkg qualifier (`WebSearch.` not `webtool.WebSearch.`); ErrEmptyQuery direct return is correct | LOW | same as fetch.go | same | FOUND |
| 3 | search.go:181-184 | `if a.Limit < 0 { return errors.New("limit must be non-negative") }` | A.4 | EDGE | §S16: no prefix + no sentinel + uses errors.New for ad-hoc error. Reaches Tool framework as friendly string. Could promote to `ErrNegativeLimit` sentinel for symmetry with ErrEmptyQuery, but rarely user-actionable (LLM-side bug). | LOW | identical UX | introduce `ErrInvalidLimit` sentinel for symmetry; or accept | FOUND |
| 4 | search.go:204-206 | `if err := json.Unmarshal([]byte(argsJSON), &args); err != nil { return "", fmt.Errorf("WebSearch.Execute: %w", err) }` | A.4 | EDGE | same prefix issue | LOW | same | same | FOUND |
| 5 | search.go:210-220 | BYOK iteration: `for _, provider := range apikeydomain.SearchProviderPriority { ... results, source, ok := t.tryBYOKProvider(...); if ok && len(results) > 0 { return marshalSearchResponse(...) } }` | A.1 | OK | Iteration with explicit ctx-cancel guard at line 212. Per-provider failures fall through with audit log via tryBYOKProvider's warnf (#13). | N-A | — | — | — |
| 6 | search.go:223-234 | MCP tier: `errors.Is(err, ErrMCPSearchUnavailable)` (silent fall-through), `case err != nil: t.warnf(...)` (logged fall-through), `case len(results) > 0: return ...` | A.1 | OK | Three-way switch on err shape: documented soft-fail (silent for "not configured") with explicit log path for non-Unavailable errors. Per §S3 spec example carve-out for documented soft-fail with audit log. Cross-fork verified `mcp.searchrouter` site #2 (audit doc 2026-05-09) correctly translates ErrServerNotFound→ErrMCPSearchUnavailable so this branch lights up properly. | N-A | — | — | — |
| 7 | search.go:236-244 | `return fmt.Sprintf("No results for %q. No search backend is currently available.\n\nTo enable web search, do ONE of the following:\n  • Configure a search-category API key in Settings → API Keys ...\n  • Install the duckduckgo-search MCP server from the marketplace ...\n\n(The previous Bing CN HTML scrape fallback was removed because Bing now renders results via JavaScript, making server-side HTML scraping return 0 results.)", args.Query), nil` | A.1 | EDGE | **§S3 / "教学式 result" anti-pattern noted in earlier conversation**: tool result text contains 1) data ("no results"), 2) installation instructions to user, 3) implementation history detail (Bing CN scraper removal rationale). Items 2-3 are LLM-facing copy that pollutes tool result. Per the "tool-result-as-copy" anti-pattern discussion: implementation detail leak + self-promoting fix instructions ("install our marketplace MCP"). LLM will faithfully repeat to user including the historical Bing scraper note. **However**: this is functional behavior, not a §S3 silent-failure violation per se. Out-of-scope as a §S3 finding; out-of-scope concern flagged separately. | LOW (separate phase) | LLM repeats stale implementation history to user | trim to: result + minimal next-step hint; move history note to comment | FOUND |
| 8 | search.go:257-263 | `creds, err := t.keys.ResolveCredentials(ctx, provider); if err != nil { ... return nil, "", false }` | **A.1** | **VIOLATION** | **§S3 silent fallback**: doc comment 259-262 says "ErrNotFoundForProvider is the silent path. Other errors (e.g. decryption fail) are still silent here — the next tier covers it." This is **explicit silent error swallowing for non-NotFoundForProvider cases** — same defect class as B2 bash auto-route silent fallback (commit 888739c). Decryption failures, ctx errors, store errors all silently route as "no key configured". User with corrupted apikeys DB sees web search just "not work" with zero log trail. The "next tier covers it" claim is hollow if MCP also unconfigured → user gets the cryptic "No results" message at site #7 with no operator clue why. | **MED** | apikey corruption / decryption failures appear as "BYOK not configured" silently; debugging requires reading the DB directly | replace with explicit shape match: `if errors.Is(err, apikeydomain.ErrNotFoundForProvider) { return nil, "", false }; if err != nil { t.warnf(fmt.Sprintf("WebSearch BYOK %q: ResolveCredentials failed unexpectedly", provider), err); return nil, "", false }` — narrow the silent path to ErrNotFoundForProvider only; log other errors with audit context | FOUND |
| 9 | search.go:266-272 | `baseURL := strings.TrimRight(creds.BaseURL, "/"); if baseURL == "" { ... return nil, "", false }` | A.1 | EDGE | Comment 268-270 documents intent ("Defensive — meta.DefaultBaseURL should be merged by the keyProvider"). Falls through silently. **Defensive case**: if reached, indicates upstream apikey provider misconfig. **No log** — missing audit trail for the wiring bug. | LOW | rare misconfig path, silent | add `t.warnf(...)` log | FOUND |
| 10 | search.go:278-291 | switch provider { case "brave": ... case "bocha": ... default: return nil, "", false } | A.1 | EDGE | default branch silent fall-through with comment "Defensive — providers list and switch must stay in sync". If apikeydomain.SearchProviderPriority adds a new provider but switch isn't extended, user sees "no results" silently. **No log**. | LOW | wiring bug surfaces as silent miss; rare | log Warn or panic (config-time invariant) | FOUND |
| 11 | search.go:292-298 | `if runErr != nil { t.warnf(...); t.markInvalidIfAuthErr(...); return nil, "", false }` | A.1/A.4 | OK | Provider call failure: audit-logged via warnf + auth detection via markInvalidIfAuthErr + fall-through. Documented best-effort soft-fail with audit trail per §S3 carve-out. | N-A | — | — | — |
| 12 | search.go:308-329 | `markInvalidIfAuthErr(ctx, provider, err) { msg := err.Error(); if !strings.Contains(msg, "HTTP 401") && !strings.Contains(msg, "HTTP 403") { return }; ...; uid, _ := reqctxpkg.GetUserID(ctx); mctx := ctx; if uid != "" { mctx = reqctxpkg.SetUserID(context.Background(), uid) }; if merr := t.keys.MarkInvalid(mctx, provider, msg); merr != nil { t.debugf(...) }` | **A.2/A.4/A.5** | **MED EDGE** | Multiple concerns: **(a) §S17 sentinel-less detection**: line 310-312 uses `strings.Contains(msg, "HTTP 401")` instead of `errors.Is(err, llm.ErrAuthFailed)` or a webtool-specific `ErrAuthFailed`. Fragile to error message format changes (e.g. doSearchHTTP at search_byok.go:201 would need to keep producing "HTTP 401" substring). **The recently-added llm.ErrAuthFailed (commit 363b084) doesn't apply here** because BYOK calls bypass infra/llm — they do direct HTTP to Brave/Serper/etc. So a parallel `webtool.ErrAuthFailed`/`ErrRateLimited` sentinel chain in search_byok.go would let this be `errors.Is`-driven. **(b) §S9 detached ctx**: lines 322-325 — correct detached pattern (model §S9 example: `reqctxpkg.SetUserID(context.Background(), uid)` for terminal-write apikey.MarkInvalid). POST-FIX OK ✓. The MarkInvalid wraps internally per apikey audit ff8fd77. **(c) §S3**: merr (MarkInvalid failure) goes to t.debugf — debug not warn for "best-effort". Acceptable per "marker failure" docs. | MED (§S17 string match) + N-A (§S9 OK) | string-matching auth detection is **a real fragility** — if doSearchHTTP changes the error format (e.g. drops "HTTP" prefix), MarkInvalid silently stops firing and stale BYOK keys keep failing without UI flip | introduce `webtool.ErrAuthFailed` + `ErrRateLimited` sentinels in search_byok.go; wrap doSearchHTTP HTTP-status errors with %w; rewrite markInvalidIfAuthErr to `if errors.Is(err, webtool.ErrAuthFailed) { ... }`. Mirrors the llm sentinel pattern (commit 363b084) one layer up. | FOUND |
| 13 | search.go:334-339 | `func (t *WebSearch) warnf(msg, err) { if t.log == nil { return }; t.log.Warn(msg, zap.Error(err)) }` | A.1 | OK | Tests-friendly nil-log guard — documented soft-no-op | N-A | — | — | — |
| 14 | search.go:341-346 | `func (t *WebSearch) debugf(msg, err)` | A.1 | OK | same as #13 | N-A | — | — | — |
| 15 | search.go:352-368 | `marshalSearchResponse: ...; body, err := json.MarshalIndent(...); if err != nil { return "", fmt.Errorf("WebSearch.Execute: marshal: %w", err) }` | A.4 | EDGE | §S16: prefix `WebSearch.Execute: marshal:` — has sub-tag but missing pkg qualifier per #2/#4 pattern. Marshal of constructed response struct is unfailable in practice (no NaN/Inf/cyclic possible from these types) — sentinel chain matters less. | LOW | same | same | FOUND |

## Sub-check

A.1 §S3 错误吞没:
  - violations: site #8 (MED — silent swallow of non-ErrNotFoundForProvider errors in tryBYOKProvider; defect class same as B2 bash auto-route)
  - documented soft-fails with audit log (OK): sites #6, #11, #13, #14
  - silent fall-throughs without log (LOW): sites #9 (defensive baseURL empty), #10 (defensive switch default)
  - "教学式 result" pollution (LOW, separate phase): site #7 — implementation history leak in tool-result string

A.2 §S9 detached ctx 终态写:
  - terminal-state writes identified: site #12 (MarkInvalid via markInvalidIfAuthErr)
  - 各自 ctx 来源: detached ctx via `reqctxpkg.SetUserID(context.Background(), uid)` ✓
  - violations: not present — site #12 implements §S9 model pattern correctly

A.3 §S15 ID 生成:
  - ID generation calls: none
  - violations: N/A — file does not generate business IDs

A.4 §S16 错误 wrap 格式:
  - violations: sites #2, #4, #15 (`WebSearch.<Method>:` missing pkg qualifier — same scheme as fetch.go); site #3 (errors.New no prefix no sentinel)
  - canonical: ErrEmptyQuery direct return (#2 nested case)

A.5 §S17 sentinel 登记 errmap:
  - sentinels defined in this file: ErrEmptyQuery (line 75)
  - 已登记 errmap: N/A — Tool-framework-internal sentinel
  - missing registrations: N/A — but **MED conceptual gap at site #12**: webtool relies on `strings.Contains` for HTTP 401/403 detection because search_byok.go produces sentinel-less HTTP-status errors. Should introduce `webtool.ErrAuthFailed` + `ErrRateLimited` sentinels paralleling llm.ErrAuthFailed (commit 363b084). Then markInvalidIfAuthErr can use errors.Is.
