# `backend/internal/pkg/eventlog/eventlog.go` — Phase A trace

**LOC**: 486 (sole file in package; `_test.go` excluded per fork instructions)
**Role**: ergonomic Emitter helper around the eventlog Bridge — auto-mints msg/blk IDs from `pkg/idgen`, reads `conversationId` + parent linkage from `reqctx`, and dual-writes block lifecycle to `chatdomain.Repository` (message_blocks_v2 table) for history replay. Phase 2B of the eventlog龙骨 refactor (event-log-protocol.md).

## Trace table

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | eventlog.go:139-148 | `func New(bridge eventlogdomain.Bridge, repo chatdomain.Repository, log *zap.Logger) Emitter { ... if log == nil { log = zap.NewNop() } ... }` | A.1 | OK | log==nil 默认 zap.Nop() 是 documented contract（godoc 行 140："log may be nil (zap.Nop)"）。bridge / repo 为 nil 时无校验——godoc 显式说"repo is optional"，bridge 也由调用方负责，缺失会在 publish 时 nil-deref 但那是接线 bug 不是吞错 | — | — | — | — |
| 2 | eventlog.go:156-164 | `func (em *emitter) requireConv(ctx context.Context, op string) (string, bool) { convID, ok := reqctxpkg.GetConversationID(ctx); if !ok { em.log.Warn("emit skipped: no conversationID in ctx", zap.String("op", op)); return "", false } ... }` | A.1 | OK | "missing conversationID = wiring bug" — godoc 行 53-55 显式说"methods log + return without emitting (do not panic — too disruptive for streaming code paths)"。这是**有据可查的 silent skip**——streaming 路径 panic 会撕掉整个流，且 conversationID 一定来自 chat handler 入口（接线缺失会被 dev mode 立刻撞到）。**记 Warn + 跳过** = 让接线 bug 暴露但不毁流，§S3 例外的"清理资源失败且不影响业务"的反向类比 | — | — | — | — |
| 3 | eventlog.go:174-184 | `func (em *emitter) publish(...) (int64, bool) { env, err := em.bridge.Publish(ctx, convID, e); if err != nil { em.log.Warn("emit failed", ...); return 0, false } return env.Seq, true }` | A.1 | EDGE | Bridge.Publish 失败 = SSE 事件没发出去——前端会缺这一帧。godoc 行 167-173 说"failed publish (logged); callers may still proceed (DB writes keyed off blockID, not seq, are still safe)"，但实际上这条路径覆盖了 ctx-cancel / Bridge buffer 阻塞错 / ErrInvalidEvent 三种语义截然不同的 err 全 warn 一刀切。**ErrInvalidEvent 是 producer bug**（domain/eventlog/eventlog.go:286-352 "caller bug, not recoverable"），应当 panic 或至少 Error 级 log 而非 Warn——否则形状错的事件被 emitter 静默吞，前端缺整段事件链。**ctx-cancel** 又是预期的（用户关 tab），Warn 也偏激。**不算 §S3 violation**——错误确实被记了，但是粒度过粗导致"形状错的事件被淡化成 Warn"是条架构-级缺陷 | LOW | producer bug 被淡化成 Warn，前端缺事件链找不到根因 | 按 err 类型分流：`errors.Is(err, eventlogdomain.ErrInvalidEvent)` → `em.log.Error` + 调用方应 fail-loud（producer bug）；`errors.Is(err, ctx.Err())` → debug 级（client gone）；其他 → Warn | FIXED (this commit) |
| 4 | eventlog.go:186-200 | `func (em *emitter) StartMessage(...) string { convID, ok := em.requireConv(ctx, "StartMessage"); if !ok { return "" }; msgID := idgenpkg.New("msg"); em.publish(ctx, convID, eventlogdomain.MessageStart{...}); return msgID }` | A.3 | OK | `idgenpkg.New("msg")` 走 §S15 统一生成（pkg/idgen 内部 `crypto/rand` 失败 panic）。`msg_<16hex>` 前缀符合 CLAUDE.md §S15 list | — | — | — | — |
| 5 | eventlog.go:186-200 | （同 site 4） | A.1 | EDGE | publish 返 ok=false 时，Emitter 仍**返新生成的 msgID** 给调用方——调用方拿这个 ID 做后续 StopMessage / DeltaBlock 调用都是错的（事件链断了，后续事件 publish 也会因为前面没 message_start 被 frontend 视为 dangling parentId）。**§S21 invariant**: "block_start.parentId 必须先于本事件出现过——任何 dangling parentId 都是 producer bug"。这里 publish 失败但 ID 给了调用方 = 主动制造 dangling parentId。**与 §S3 silent fallback 对应**——upstream 失败但仍返成功。但实际上 chat loop 调用方拿到 msgID 也没法判断"这个 ID 实际未发出"。文档承诺"DB writes keyed off blockID, not seq, are still safe"对消息层不成立——message 不双写（行 259-261），失败就是丢了 | LOW | publish 失败时调用方拿到无效 msgID，后续事件都 dangling parent 触发前端 producer-bug warning | 失败时返 `""` 让调用方早 fail；或在 godoc 显式说"返 ID 不保证已 publish——bridge 失败时仍返但事件链断" | FIXED (this commit) |
| 6 | eventlog.go:202-242 | `func (em *emitter) saveBlockRow(...) { if em.repo == nil \|\| seq == 0 { return }; ... attrsJSON := ""; if len(attrs) > 0 { if b, err := json.Marshal(attrs); err == nil { attrsJSON = string(b) } } ... if err := em.repo.SaveBlock(ctx, &chatdomain.Block{...}); err != nil { em.log.Warn("blockV2 dual-write failed (block_start)", ...) } }` | A.1 | EDGE | 两个错误吞点：(a) 行 222 `json.Marshal(attrs); err == nil { ... }` 失败时 `attrsJSON = ""` 静默继续——attrs 本身是 `map[string]any`，json.Marshal 几乎不会失败（仅 unsupported type 如 channel/func 才失败）。但项目 CLAUDE.md §S3 typical violation #4 "如果 err != nil { /* nothing */ }" 是禁止——这里的 `if ... err == nil` 等价于"err != nil 时啥也不干，丢 attrs"。**实际工程影响低**（attrs 是 producer 自己塞的 map 不会含 channel），但**违反 §S3 字面**。(b) 行 238-241 SaveBlock 失败 `Warn + 继续`——godoc 行 210-211 显式说 "Best-effort: failures log + continue (Bridge already shipped the SSE event; DB miss only affects history replay)"。**这条 OK**——SSE 已发，DB 失误只影响回放。属 §S3 例外的 "清理资源失败且不影响业务"。（注：但这与 §S21 invariant 冲突——若 DB 失败而 SSE 成功，重连用 Last-Event-ID 重发期间 DB 没记此 block，replay 会缺。Phase 2B 双写"事实源"语义 vs godoc "best-effort" 不一致——见 _summary.md cross-cutting） | LOW | (a) attrs json.Marshal 失败时静默丢 attrs（极少触发但违 §S3 字面）；(b) DB 失败时 SSE 已发但 history replay 缺 block（Phase 2B 当前承诺仅是事实源） | (a) `if b, err := json.Marshal(attrs); err != nil { em.log.Warn("attrs marshal failed", zap.Error(err)) } else { attrsJSON = string(b) }`；(b) 不动，但 godoc 注一下与 §S21 关系 | FIXED-(a) (this commit) / WAIVED-(b) (best-effort dual-write 设计契约符合 §S3 例外) |
| 7 | eventlog.go:244-261 | `func (em *emitter) StopMessage(...) { convID, ok := em.requireConv(ctx, "StopMessage"); if !ok { return }; em.publish(ctx, convID, eventlogdomain.MessageStop{...}) // No DB dual-write for messages ...}` | A.1/A.2 | OK | 不双写 messages（注释行 259-261 解释 Phase 4 cutover 才统一）。当前 messages 走 legacy chat repo 持久化。message_stop 是终态事件——但本函数仅 emit；终态写库由调用方（chatapp.Service）用 detached ctx 完成（CLAUDE.md §S9 example）。本 emit **本身**不是终态写——它是事件发布，DB 写入 owner 在他处。requireConv 返 false 时 silent skip 同 site 2 reasoning | — | — | — | — |
| 8 | eventlog.go:263-299 | `func (em *emitter) StartBlock(...) string { convID, ok := em.requireConv(ctx, "StartBlock"); if !ok { return "" }; parentID, _ := reqctxpkg.GetParentBlockID(ctx); if parentID == "" { parentID, _ = reqctxpkg.GetMessageID(ctx) }; msgID, _ := reqctxpkg.GetMessageID(ctx); if parentID == "" \|\| msgID == "" { em.log.Warn("emit skipped: missing parent or message in ctx", ...); return "" } ... blockID := idgenpkg.New("blk"); seq, ok := em.publish(...); if ok { em.saveBlockRow(...) } return blockID }` | A.1/A.3 | OK | 三处错误处理：(a) `parentID, _ := reqctxpkg.GetParentBlockID(ctx)` 第二返忽略——按 §S3 例外，reqctx Get-form 返 (string, bool)，bool 单纯表示"key 存在"不是 error，丢弃合法（pkg-reqctx audit 已确认 event-filter 类合法静默）。(b) `msgID, _ := reqctxpkg.GetMessageID(ctx)` 同理。(c) parentID/msgID 都空时 Warn + return ""——返 "" 让调用方早 fail（vs site 5 那种返 ID 但事件丢的 silent fallback）。`idgenpkg.New("blk")` §S15 合规 | — | — | — | — |
| 9 | eventlog.go:268-285 | `parentID, _ := reqctxpkg.GetParentBlockID(ctx); if parentID == "" { parentID, _ = reqctxpkg.GetMessageID(ctx) }; msgID, _ := reqctxpkg.GetMessageID(ctx)` | A.1 | EDGE | **§S21 invariant 隐患**: 当 parentID == "" 时回退到 messageID（行 270-278）；但若 parentID 取自 reqctx 是早前 turn 残留（context 复用 bug），会指向**已 stopped** 的 block_id——前端 router 仍能找到（map 有），但块 status=completed 接 child = 违反 §S21 "block.status 单向 streaming → terminal" 暗含的 "stopped block 不再接 child"。这条 audit 不在 §S3-S17 scope（属 §S21 invariant 静态难证），但应在 _summary.md cross-cutting 提一下 | — | — | — | — |
| 10 | eventlog.go:301-324 | `func (em *emitter) StartBlockUnder(...) { convID, ok := em.requireConv(ctx, "StartBlockUnder"); if !ok { return "" }; if parentID == "" \|\| messageID == "" { em.log.Warn("emit skipped: empty parent or message", ...); return "" } ... blockID := idgenpkg.New("blk"); seq, ok := em.publish(...); if ok { em.saveBlockRow(...) } return blockID }` | A.1/A.3 | OK | 同 site 8 模式，校验 + 返 "" 让调用方早 fail。`idgenpkg.New("blk")` §S15 合规 | — | — | — | — |
| 11 | eventlog.go:326-343 | `func (em *emitter) EmitMessageStart(ctx, id, role, parentBlockID, attrs) { convID, ok := em.requireConv(ctx, "EmitMessageStart"); if !ok { return }; if id == "" \|\| role == "" { em.log.Warn("emit skipped: empty id or role", ...); return }; em.publish(...) }` | A.1 | OK | 调用方提供 id（msgID 上游已铸场景）。空 id 跳过 = 同 requireConv 逻辑。但**未校验 id 是否符合 `msg_<16hex>` 格式**——若调用方传错前缀（如 `blk_xxx` 当 msgID）会污染前端 router。属架构层 producer-bug，§S15 不强制 emit 层校验"传入 id 格式"——idgen.New 由 owner 调，emit 层信任 caller。**这条不是 §S3-S17 violation**，仅 cross-cutting note | — | — | — | — |
| 12 | eventlog.go:345-367 | `func (em *emitter) EmitBlockStart(ctx, id, parentID, messageID, blockType, attrs) { ... if id == "" \|\| parentID == "" \|\| messageID == "" { em.log.Warn("emit skipped: empty id / parent / message", ...); return }; seq, ok := em.publish(...); if ok { em.saveBlockRow(...) } }` | A.1 | OK | 同 site 11，调用方提供 ID（如 LLM 自报 tool-call ID）。校验三字段非空 + Warn + return | — | — | — | — |
| 13 | eventlog.go:369-390 | `func (em *emitter) DeltaBlock(ctx, blockID, delta) { ... if blockID == "" { return // upstream skipped (e.g. StartBlock returned "" due to missing ctx) }; em.publish(...); if em.repo != nil { if err := em.repo.AppendDelta(ctx, blockID, delta); err != nil { em.log.Warn("blockV2 dual-write failed (delta)", ...) } } }` | A.1 | EDGE | (a) `blockID == ""` 静默 return（行 374-376）——注释解释 "upstream skipped"。这条**OK**——是 site 8/9 那套早 fail 链的 downstream 自然消化（如果 StartBlock 返 ""，调用方拿 "" 调 DeltaBlock 也是 ""）。属 documented silent skip。(b) AppendDelta 失败 Warn + 继续——同 site 6(b) reasoning，best-effort dual-write。但 **delta 失败的影响比 SaveBlock 大**——block 行已存（site 6 写过），delta 没 append 上 = DB content 缺一段，replay 时 block content 不完整。**§S21 "deltas 按 seq 顺序 append-only——前端永不重写、永不重排"** 强调 delta 顺序——若 DB 的 AppendDelta 失败而 SSE 发了，replay 出来的 content 缺，但 SSE buffer 内事件还在的话重连能补回来。Phase 2B 当前 buffer 30s 的窗口外事件就丢了。 **不算 §S3 violation**（错误确实记了），但**与 §S21 一致性的 trade-off** 应在 godoc 提一笔 | LOW | AppendDelta 失败时，replay 出来 block content 缺一段；30s 窗口外重连看不到这段 delta | godoc 加一句"DB 失败时 replay 缺 delta；buffer 内重连 SSE 仍能补，window 外才丢"——明确"事实源"边界 | WAIVED (best-effort dual-write 已 Warn log；godoc 边界澄清属 doc cleanup，跨 Phase 2B↔3 cutover 时一并改) |
| 14 | eventlog.go:392-418 | `func (em *emitter) StopBlock(ctx, blockID, status, err) { ... errStr := ""; if err != nil { errStr = err.Error() }; em.publish(...); if em.repo != nil { if e := em.repo.FinalizeStop(ctx, blockID, status, errStr); e != nil { em.log.Warn("blockV2 dual-write failed (stop)", ...) } } }` | A.1/A.2 | EDGE | (a) `errStr := err.Error()` 仅取 message——不保留 sentinel/wrap 链。**OK**——这是 wire format（SSE 事件 payload 字符串字段），不是 Go 错误传播路径。SSE 看不到 sentinel。(b) FinalizeStop 失败 Warn + 继续——但 **block_stop 的 status 是 §S21 "block.status 单向 streaming → terminal" 的终态写**！如果 FinalizeStop 失败而 SSE 发了，DB 的 block 行 status=streaming 永久卡住。replay 会显示 streaming block 永远没结束。**这是 §S9 终态写**——但 ctx 是调用方传入（chat loop / tool 内的 r.Context() 派生），如果上游 cancel，FinalizeStop 也 cancel，导致 SSE 已 stop 但 DB 卡 streaming。**应该用 detached ctx 落 finalize**（§S9 典型例：流被 cancel 后写 assistant final message）。当前实现把"是否 detached"留给调用方——但 emit 层是 fire-and-forget 不等待，且 emit 调用栈很深，调用方很少意识到该传 detached。属 §S9 隐患 | MED | 终态 FinalizeStop 在 ctx-cancel 路径下失败 = DB block 永留 streaming（前端 history replay 永远看到加载中），与 SSE 上 status=completed 矛盾 | StopBlock 内部对 FinalizeStop 用 detached ctx：`detached := reqctxpkg.SetUserID(context.Background(), ...)` 同时保留 conversationID，确保终态总能落库。或在 godoc 显式契约"调用方负责传 detached ctx"并在 chat loop 等终态写位点改写 | FIXED (this commit — 用 context.Background() 落 FinalizeStop) |
| 15 | eventlog.go:422-429 | `type emitterKey struct{}; func With(ctx context.Context, em Emitter) context.Context { return context.WithValue(ctx, emitterKey{}, em) }` | A.1/A.2/A.3/A.4 | OK | ctx-key + Emitter ferry。无错误路径 | — | — | — | — |
| 16 | eventlog.go:437-443 | `func From(ctx context.Context) Emitter { em, ok := ctx.Value(emitterKey{}).(Emitter); if !ok \|\| em == nil { return noopEmitter{} } return em }` | A.1 | OK | godoc 行 432-435 显式说返 no-op (vs nil) 让调用方无 nil-check。"missing emitter logs a warning so wiring bugs surface"——但实际**没 log warning**（return 路径无 log）。**轻度 §S3 issue**——godoc 撒谎了。但这不是吞错（无 error 概念），仅是文档 vs 行为不一致。属 doc-fix 不是 audit issue | — | — | — | — |
| 17 | eventlog.go:450-456 | `func MustFrom(ctx context.Context) Emitter { em, ok := ctx.Value(emitterKey{}).(Emitter); if !ok \|\| em == nil { panic(fmt.Sprintf("eventlog.MustFrom: no emitter in ctx")) } return em }` | A.1/A.4 | OK | 缺 emitter panic——godoc 行 444-449 显式说"Use only at places where missing emitter is unambiguously a wiring bug"。**注意**：`fmt.Sprintf` 但无 format-arg——staticcheck S1039 会捞，但属 stylelint 非 §S3-S17 scope。panic 信息未带 ctx 信息但接线 bug 不需 | — | — | — | — |
| 18 | eventlog.go:465-467 | `func WithParent(ctx context.Context, blockID string) context.Context { return reqctxpkg.WithParentBlockID(ctx, blockID) }` | — | OK | 薄 wrapper，无错误路径 | — | — | — | — |
| 19 | eventlog.go:471-486 | `type noopEmitter struct{} ... func (noopEmitter) StartMessage(...) string { return "" }; func (noopEmitter) StopMessage(...) {}; func (noopEmitter) StartBlock(...) string { return "" } ...` | A.1 | OK | 11 个 no-op 方法。`StartMessage` / `StartBlock` / `StartBlockUnder` 返 ""；其他 silent。**契约**：调用方接到 "" ID 后，DeltaBlock/StopBlock 看到 "" 早 return（site 13/14 已检），整个链失败-but-safe。这就是设计原则——missing emitter 不毁流 | — | — | — | — |

## Sub-check

**A.1 §S3 错误吞没**:
  - violations: site 6(a) attrs json.Marshal silent drop 字面违 §S3 typical #4（实际工程低概率，map[string]any 几乎不会失败）—— LOW
  - EDGE 未升级 violation: site 3 (publish err 粒度过粗 ErrInvalidEvent 被淡化), site 5 (publish 失败仍返 msgID 制造 dangling parentId), site 6(b) (SaveBlock 失败 silent—godoc 称 best-effort), site 13(b) (AppendDelta 失败 silent—replay 缺 delta), site 14(b) (FinalizeStop 失败 silent—§S9 + §S21 隐患)

**A.2 §S9 detached ctx 终态写**:
  - terminal-state writes identified: 
    - site 14 `StopBlock → FinalizeStop`（block 终态 streaming → terminal）—— **是终态写**
    - site 7 `StopMessage`（仅 emit，不双写——message 终态写责任在调用方 chatapp.Service）—— **不是本包的终态写**
  - 各自 ctx 来源: 
    - site 14: ctx 来自调用方（emit 层不主动构造 detached）。chat loop / tool framework 传入 r.Context() 派生 ctx，cancel 路径下终态丢失
  - violations: site 14 — MED。**emit 层不主动构造 detached ctx 让调用方负责**这一设计在 streaming 路径下脆弱——上游 cancel 让本应保证落库的 block_stop 持久化失败。Phase 2B "持久化与实时分离" 原则要求 DB 是事实源，FinalizeStop 失败 = 事实源不一致

**A.3 §S15 ID 生成**:
  - ID generation calls: `idgenpkg.New("msg")` (site 4 行 191), `idgenpkg.New("blk")` (site 8 行 286, site 10 行 311)
  - violations: not present — 全走 §S15 统一 helper，前缀 `msg_` / `blk_` 在 CLAUDE.md §S15 list 内

**A.4 §S16 错误 wrap 格式**:
  - violations: not present — 本包不上抛错误（emit 全 silent skip + log），唯一 panic 是 site 17 MustFrom 的 wiring-bug panic（不是 error path）

**A.5 §S17 sentinel 登记 errmap**:
  - sentinels defined: 0 — 本包不定义任何 `var Err...`
  - 已登记 errmap: N/A
  - missing: N/A — 本包是纯 emitter helper，错误全经 `bridge.Publish` 返 `eventlogdomain.ErrInvalidEvent` / `chatdomain.Repository.{SaveBlock|AppendDelta|FinalizeStop}` 的 GORM err。这些 sentinel 的登记是 domain/eventlog + chat repo 的责任，本 audit 跨包不审。
    - 备注：`eventlogdomain.ErrSeqTooOld` / `ErrInvalidEvent` 经 `transport/httpapi/handlers/eventlog.go:107` 直接 `errors.Is` 处理（返 410 + `code=SEQ_TOO_OLD` 按 §N7），**不走 FromDomainError**——故无须登记 errmap。这是合法设计（CLAUDE.md §N7 明确该路径）

## Severity rollup

| Severity | Count |
|---|---|
| HIGH | 0 |
| MED | 1 (site 14 — FinalizeStop 终态写无 detached ctx) |
| LOW | 3 (site 3 publish err 粒度, site 5 publish 失败仍返 ID, site 6(a)+13(b) dual-write 字面违 §S3 / replay 一致性) |
| — | site 6(a) 与 13(b) 合并为 1 LOW（同性质 best-effort dual-write 但 godoc 边界不清）；故 LOW = 3 |

**Net trace sites = 19** (some sites split A.1 + A.2 / A.3 双 category)
**Net violations = 4** (1 MED + 3 LOW)
