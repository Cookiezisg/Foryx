# V1.2 Backend 进展记录

**关联**：
- [`backend-design.md`](./backend-design.md) — 总体设计 + 规范（相对稳定，很少动）
- [`service-contract-documents/`](./service-contract-documents/) — 每个 domain 的服务契约索引（一眼清单）
- [`service-design-documents/`](./service-design-documents/) — 每个 domain 的详细设计
- [`desktop-packaging-notes.md`](./desktop-packaging-notes.md) — 桌面端分发方向（Wails / 打包 / 常驻后台）
- [`claude-code-research-documents/`](./claude-code-research-documents/) — Claude Code 内部机制调研（9 份主题报告）

**本文档定位**：所有"正在发生"的状态都在这里。开发日志 / 完成快照 / 待办清单 / 原则演化。规范/架构/愿景这些"相对不变"的放 `backend-design.md`。

---

## 1. 当前快照（截止 2026-05-13）

| Phase | 主题 | 状态 | 里程碑 |
|---|---|---|---|
| **Phase 0** | 骨架(go mod + main + /health) | ✅ | 2026-04-22 |
| **Phase 1** | 基础 infra 7 件套(GORM / logger / crypto / events / middleware / response / pagination) | ✅ | 2026-04-23 |
| **Phase 2** | 基础对话能力(apikey / model / conversation / chat) | ✅ | 2026-04-25 |
| **Phase 3** | 工具锻造(forge → trinity:function / handler / workflow) | ✅ | 2026-04-26 |
| **Phase 3 后优化轮 + forge_redesign** | chat 重构 / 驱动迁移 / 打包方向 + trinity 重做(forge→function/handler + Plan 03 SSE 三流) | ✅ | 2026-05-12 |
| **Phase 4** | 工作流(workflow + flowrun + scheduler + trigger + 13 节点 dispatcher) | ✅ | 2026-05-13 |
| **Phase 5** | 智能化(knowledge / intent / mcp / skill / chat 终极版) | ⬜ 未开工 | — |

**当前测试规模**:~315 单元/集成测试全绿(Plan 05 加 ~118 + Plan 06 加 ~10 个:3 filterTools strip + 3 multi-agent prompt + 2 catalog trinity + 3 approval E2E)。5 个 LLM 集成测试因 DeepSeek API key 环境失效,与代码无关。
**当前驱动**:modernc.org/sqlite(纯 Go,无 CGO),跨平台编译一行命令。
**当前依赖体系**:完全摆脱 Eino(chat 重构后)。新增 `robfig/cron/v3 v3.0.1`(Plan 05 首次引入);`fsnotify/fsnotify` 提 direct v1.10.1。
**forge_redesign ✅ 全交付**(2026-05-13):Plan 01-06 全部 merge — function trinity + handler trinity + eventlog/forge 三流 + workflow authoring + execution plane(scheduler/trigger/flowrun + 14 hardening + 4 张新表 D22)+ subagent forger D21 + 主 agent multi-agent forging 教学 + trinity catalog 源 + approval lifecycle E2E。trinity architecture 完工。下一阶段:V1.2 桌面端 Wails 迁移 + Phase 5 智能化。

---

## 2. 开发日志

按时间顺序（旧 → 新）。每个时间块按 phase 或专题分组。

### Phase 0-1：地基（2026-04-22 ~ 2026-04-23）

| 日期 | 内容 |
|---|---|
| 2026-04-22 | 全面契约审计（45 API 端点 + 10 DB 表 + 21 SSE 事件），一致性评分均低 |
| 2026-04-22 | 确定 12 条契约标准（N1-N5 API + D1-D5 DB + E1-E2 SSE） |
| 2026-04-22 | 确定 4 层架构：domain / app / infra / transport，GORM，单份结构带 tag |
| 2026-04-22 | Phase 0 完成：`backend-new/` 骨架，`/api/v1/health` 返回 envelope，优雅退出 |
| 2026-04-22 | 立 **S11 双语注释规范**（英文 + 中文），backend-new 全套代码/注释必须遵守 |
| 2026-04-22 | 日志框架定为 **zap**（dev 彩色 / prod JSON），`infra/logger/zap.go` 封装 |
| 2026-04-22 | transport 层结构升级：`http/` → `httpapi/`（避免包名冲突），拆出 `response/` / `middleware/` / `handlers/` 3 子包 |
| 2026-04-22 | **Phase 1 Step 2** 完成：`response/envelope.go`（Success / Created / NoContent / Paged / Error）+ `response/errmap.go`（FromDomainError）。N1 标准落地为强制 API |
| 2026-04-23 | **Phase 1 Step 3** 完成：`pagination/cursor.go`（Parse / EncodeCursor / DecodeCursor），cursor 分页 + 10 单测 |
| 2026-04-23 | **Phase 1 Step 4a** 完成：`middleware/recover.go`，panic → 500 INTERNAL_ERROR + 6 单测（含敏感信息不泄漏守卫）|
| 2026-04-23 | **Phase 1 Step 4b** 完成：`middleware/logger.go`（method/path/status/bytes/elapsed）+ 6 单测 |
| 2026-04-23 | **Phase 1 Step 4c** 完成：`middleware/notfound.go`，envelope 格式 404 fallback + 4 单测 |
| 2026-04-23 | 模块名纠正：`github.com/sunweilin/forgify-new` → `github.com/sunweilin/forgify/backend`（Go multi-module repo 标准命名）|
| 2026-04-23 | **Phase 1 Step 4d** 完成：`middleware/cors.go`，白名单 CORS（拒绝 `*`）+ 7 单测 |
| 2026-04-23 | **Phase 1 Step 4e** 完成：`router/` 子包 + `handlers/health.go` Register pattern 模版，4 个集成测试验证端到端中间件链 |
| 2026-04-23 | Phase 1 地基 4/7，37 测试零失败；envelope、CORS、访问日志全链路通 |
| 2026-04-23 | **Phase 1 Step 5** 完成：crypto 接口化（`domain/crypto/Encryptor`）+ AES-GCM 实现。修 4 个老代码安全问题（fallback 密钥共享灾难 / decrypt 返 nil nil / 无版本标识 / shell 脆弱）。密文 `v1:` 前缀给 KMS 留兼容位。14 新测试 |
| 2026-04-23 | **Phase 1 Step 6** 完成：`infra/db/`（db.go / migrate.go / schema_extras.go）。WAL / FK / PrepareStmt / UTC。AutoMigrate + schema_extras 模式，4 个 schema 业务问题推迟到 Phase 3 |
| 2026-04-23 | **Phase 1 Step 7** 完成：`domain/events/` 接口 + `infra/events/memory/` 内存实现。强类型事件（禁 `map[string]any`）、扇出 pub-sub、buffer 满非阻塞丢弃、ctx 自动 cancel、sync.Once 幂等 |
| 2026-04-23 | **路线图升级**：定位从"V1.0 重写"→ Agentic Workflow Platform 完整愿景。引入 6 新 domain（workflow / flowrun / scheduler / knowledge / mcp / skill / intent），对标 Dify+Coze 桌面版 |
| 2026-04-23 | 文档目录重组：`Documents/` → `documents/`；按版本分 `version-1.0` / `1.1` / `1.2`；文件名 kebab-case |
| 2026-04-23 | 加 auth middleware `InjectUserID`（硬编码 `DefaultLocalUserID = "local-user"`），Phase 2 多租户字段就绪 |
| 2026-04-23 | 加 locale middleware `InjectLocale` + 跨层共享包 `internal/pkg/reqctx/`（只 stdlib、无状态、单一职责） |
| 2026-04-23 | **全量注释瘦身**：15 个生产文件共砍 ~420 行冗余注释。S11 规范扩展为"双语 + 节制" |
| 2026-04-23 | **Phase 2 路线图修正**：新增 `model` domain（"场景 → provider/model"策略层）。立第 5 条设计原则 **"端到端推演先行"** |

### Phase 2：基础对话能力（2026-04-24 ~ 2026-04-26）

| 日期 | 内容 |
|---|---|
| 2026-04-24 | **apikey domain 层**完成。试过扁平 / 按角色子包 / Go 社区味子包多种结构，最终定**平铺**：`apikey.go`（entity + 常量 + errors + Credentials + ListFilter + Repository + KeyProvider）+ `providers.go`（11 provider 白名单）。立 **S12 包结构**（domain 平铺按概念拆，禁子目录）|
| 2026-04-24 | **apikey Repository + 18 集成测试**（CRUD / 跨用户隔离 / 分页 / GetByProvider 排序）。立 **S13 包命名**（三层同名 + `<name><role>` 别名：apikeydomain / apikeyapp / apikeystore）|
| 2026-04-24 | **apikey ConnectivityTester + HTTPTester + 21 httptest 用例**。4 种 HTTP 模式分派（openai-compatible `/models` / anthropic `/v1/messages` 1-token / google `/v1beta/models?key=` / ollama `/api/tags`）。立 **"spec 优先于邻居文件"** 审计纪律 |
| 2026-04-24 | **apikey Service + KeyProvider + 18 单测**。Service 拥有加密边界（repo 见密文、tester 见明文）。Test 编排：`repo.Get → decrypt → tester.Test → repo.UpdateTestResult → log` |
| 2026-04-24 | **apikey 5 个 HTTP 端点 + 15 个 E2E 契约测试**。`:action` URL 规范通过 `POST /{idAction}` 通配符 + `strings.Cut(":")` 拆分实现。`:test` 失败 → 422 `API_KEY_TEST_FAILED` |
| 2026-04-24 | **apikey 装配**。`router/deps.go` 加 `APIKeyService` 字段；`main.go` 串起 `MachineFingerprint → DeriveKey → AES-GCM → Store → HTTPTester → Service`。curl 实机冒烟 4/5 通过 |
| 2026-04-24 | **立设计原则 #6 "反校验剧场"**：Forgify 是本地 Electron + 单用户 + 同人写前后端。跳过"前端下拉已筛 + 下游自然报错"式的 backend 校验 |
| 2026-04-24 | **model domain 设计定档**：Q1 `/model-configs/{scenario}` 复数 path + path param；Q2 不校验 provider 白名单；Q3 不校验 hasKey。4 sentinel |
| 2026-04-24 | **文档结构重组**：`backend-rewrite.md` → `backend-design.md`；分册迁入 `service-contract-documents/`；详设计迁入 `service-design-documents/` |
| 2026-04-24 | **文档大审计 + 重写**：apikey.md 与实际代码对齐（14 处失真）。立 **设计原则 #7 + S14 "文档同步纪律"（最高优先级）**：每次代码改动联动三处文档，发现不符立刻修 |
| 2026-04-25 | **[arch-fix] providers.go 归属修正**：从 `domain/apikey/` 迁到 `app/apikey/`。理由：所有消费者都在 app 层，符合 Go "接口在消费方" 原则 |
| 2026-04-25 | **[arch] S12 文件命名规范扩展**：主文件用包名的规则从 domain 层扩展到 app / infra/store 全部三层。`service.go` → `apikey.go` / `model.go` |
| 2026-04-25 | **[arch] app/apikey 文件整合**：`keyprovider.go` + `mask.go` 合并入 `apikey.go`；测试同步合并 |
| 2026-04-25 | **model domain 完成**：7 步套路全跑完。domain（ModelConfig + 4 sentinel）→ store（9 集成测试）→ app（Service + PickForChat，12 单测）→ handler（GET + PUT，7 E2E 测试）→ errmap 4 条 → curl 冒烟 6 场景全通 |
| 2026-04-25 | **conversation domain 完成**：7 步套路全跑完。domain → store（11 集成测试）→ app（Create/List/Rename/Delete，11 单测）→ handler（POST/GET/PATCH/DELETE，6 E2E 测试）|
| 2026-04-25 | **chat domain 完成（Phase 2 版）**：Eino ReAct Agent 驱动，per-conversation 队列化（buffered channel 5）；SSE 15s keep-alive；ContentExtractor 7 种格式 + Vision；auto-titling；FTS5 全文索引（`CGO_CFLAGS="-DSQLITE_ENABLE_FTS5"`）；8 sentinel + errmap 全覆盖 |
| 2026-04-25 | **目录重组**：`backend-new/` → `backend/`；旧 Electron 代码移入 `legacy/`；`.gitignore` 按标准 Go 重写。Phase 6 原子切换内嵌完成，从路线图移除 |
| 2026-04-25 | **[doc-fix] 文档补全**：model.md / conversation.md 完整详设计；api-design.md / database-design.md / error-codes.md 同步 |
| 2026-04-26 | **[feat] apikey.ModelsFound 持久化**：`APIKey` entity 新增 `ModelsFound []string`（GORM `serializer:json`）。前端配模型时直接用作下拉选项 |
| 2026-04-26 | **[fix] SSE buffer 扩容**：`infra/events/memory/bridge.go` `defaultBufferSize` 64 → 2048，解决 DeepSeek 等快速 LLM 大量 token 事件被丢弃导致回复不完整的问题 |

### Phase 3：工具锻造（2026-04-26）

| 日期 | 内容 |
|---|---|
| 2026-04-26 | **Phase 3 开工：tool domain layer**。`domain/tool/tool.go`：5 个 entity + ExecutionResult（定义在 domain 避免循环依赖）+ 9 sentinel + Repository（30 方法）。ToolVersion 合并 pending 职责 |
| 2026-04-26 | **`infra/sandbox/python.go`**：PythonSandbox 实现，Python subprocess + 30s 超时；driver 模板追加 __main__ 桥接；Python 异常返回 ok=false 不上升为 Go error。8 测试全绿 |
| 2026-04-26 | **`domain/events/types.go` 追加 6 个 tool SSE 事件**：`tool.code_streaming` / `tool.created` / `tool.pending_created` / `tool.test_case_generated` / `tool.test_cases_done` / `tool.test_cases_not_supported` |
| 2026-04-26 | **`infra/db/schema_extras.go` 重构**：单列表 → 按 table 分组的 extraGroup 结构。追加 tools 部分唯一索引 `UNIQUE(user_id, name) WHERE deleted_at IS NULL` |
| 2026-04-26 | **[arch] 工具搜索方案切换**：chromem-go 向量搜索 → LLM 排序（SearchTool 把全部工具发给 LLM 选最相关 N 个）。删除 `infra/vectordb/`，移除 chromem-go 依赖 |
| 2026-04-26 | **`infra/store/tool/tool.go`**：完整 Repository 实现，30 个方法，覆盖 Tool CRUD / Version+Pending / TestCase / RunHistory / TestHistory。11 集成测试全绿 |
| 2026-04-26 | **`app/tool/ast.go`**：Python subprocess AST 解析，提取函数名/参数（含 required/description/default）/返回值。Google-style docstring 解析，无 docstring 不报错 |
| 2026-04-26 | **`app/tool/tool.go`**：Service 完整实现，含 CRUD / 版本管理 / pending 生命周期 / sandbox 执行 / 测试用例 / LLM 生成测试用例（emit callback 解耦 HTTP）/ 导入导出 |
| 2026-04-26 | **`app/agent/forge.go`**：5 个 System Tool（SearchTool/GetTool/CreateTool/EditTool/RunTool）+ ForgeTools 工厂。SearchTool 用 LLM 排序；Create/EditTool 流式推 ToolCodeStreaming SSE；RunTool att_id 解析 |
| 2026-04-26 | **Phase 3 装配 + 冒烟**：handlers/tool.go（22 端点）+ errmap 9 条 + main.go（Migrate 5 表、创建 sandbox/toolService、ForgeTools 注入 chatService.SetTools）。curl 验证 create / list / :run / versions / run-history / delete 全通 |
| 2026-04-26 | **[feat] testend 工具面板**：新增 Tools tab（System + User Tools 子面板）。`GET /dev/tools` 列出注册 tool；`POST /dev/invoke` 直接调用任意 system tool（绕过 LLM agent，用于冒烟） |
| 2026-04-26 | **[feat] testend SSE 双视图 + chat tool 步骤卡片**：SSE 标签页加 Stream/Raw 切换；chat 面板 assistant 消息内嵌 tool step collapsible 卡片（⚙ running → ✓ ok/✗ error） |
| 2026-04-26 | **[feat] chat tool call 可见性**：`app/chat/chat.go` 拆分为 4 文件（chat / pipeline / interceptor / util）。新增 `toolInterceptor` 包装所有 tool，发布 `chat.tool_call` / `chat.tool_result` SSE（含 `summary` 人类可读）。`Summarizable` 接口 + `CoreInfo` 方法 |

### Phase 3 后基础设施优化轮（2026-04-27 起）

Phase 3 完成后未直接开工 Phase 4，而是进入一轮深度优化与调研——chat 架构重构、生产 bug 收尾、开发体验改进、Claude Code 内部机制调研、SQLite 驱动迁移、桌面端分发方向定型。

#### Chat 基础设施重构（2026-04-27）

| 日期 | 内容 |
|---|---|
| 2026-04-27 | **[refactor] 重构决策**：审计 chat 管线发现 3 处系统性设计债——DB schema 拍扁多列 / Eino 黑盒渗透 app 层 / collectStream 收完再推。新增 `archaved/refactor-chat-infra.md` 设计文档 |
| 2026-04-27 | **[arch]** 自实现 ReAct Loop 替换 `react.NewAgent + Callback`：Eino v0.8.11 `OnEnd` 对流式不触发，改直接调 `ToolCallingChatModel.WithTools().Stream()` |
| 2026-04-27 | **[refactor Step 1]** 新建 `internal/infra/llm/`（4 文件 OpenAI-compat + Anthropic 原生）替代 Eino，`iter.Seq[StreamEvent]` 替代 channel |
| 2026-04-27 | **[refactor Steps 2-11]** chat 基础设施全量重构完成：Tool 接口 4 方法、Message 拆 Block 模型（5 类型）、message_blocks 新表、自实现 ReAct 替 Eino agent、`app/chat/` 拆 5 文件、Eino import 全清 |
| 2026-04-27 | **[refactor 测试补全]**：infra/llm 21 / app/agent 35 / app/chat 18 / store/chat Block 模型适配 + 3 新增。22 包全绿 |
| 2026-04-27 | **[fix]** 修 3 处 ReAct 严重 bug：多步循环 DB 覆盖（统一 allBlocks 累积一次保存）/ maxSteps 退出 stopReason 错 / 用户消息附件 block 缺元数据 |
| 2026-04-27 | **[refactor]** 代码清理：删 `app/agent/summarizable.go`；统一 `blocksToAssistantLLM`；修 S13 alias 违规 |
| 2026-04-27 | **[fix] T15-T19 补丁 5 条**：forge.go ctx helpers / GenerateTestCases 改 json.RawMessage / extractJSON 剥 markdown fence / extractTextContent 取最后 text block / chatRepo 共享单例 |
| 2026-04-27 | **[feat] Thinking 可见性**：新增 `chat.reasoning_token` SSE + `Message.ReasoningContent` 字段（DeepSeek-R1 history 重建必需）；testend 加 `🤔 Thinking…` 折叠块 |
| 2026-04-27 | **[fix]** 集成测试拍出 4 个生产 bug：created_at=0001 错排（OnConflict.DoUpdates 修）/ 取消流后助手消息丢失（detached ctx）/ web_search 返 null（切 lite.duckduckgo）/ 快速连发历史顺序错 |
| 2026-04-27 | **[test]** 集成测试 13 组（A-M）全通（真实 DeepSeek API），覆盖 CRUD / API Key / 分页 / 工具 / ReAct / Attachment / Auto-title / SSE messageId 等 |
| 2026-04-27 | **[doc-sync] events-design.md / database-design.md / chat.md** 全量同步：messages 表精简、message_blocks 新表、chat.tool_call_start / chat.reasoning_token 新增 |

#### Chat pipeline 二次重构（2026-04-27 后）

| 日期 | 内容 |
|---|---|
| 2026-04-27+ | **[refactor]** 移除 pipeline.go，引入 runner.go（commit b6a9199）：chat 执行管道二次拆分，为后续 context compaction 预留接口 |

#### 开发体验工程化

| 日期 | 内容 |
|---|---|
| 2026-04-27+ | **[devx]** Brewfile + Makefile setup target + 11 testend YAML collections（commit 6113d16）：`make setup` 一键检查 Xcode CLT / 装 Homebrew / 装依赖 |

#### Claude Code 内部机制调研

| 日期 | 内容 |
|---|---|
| 2026-04-28 | **[research]** Claude Code 内部机制调研：产出 `claude-code-research-documents/` 9 份主题报告 + `agent-core-upgrade.md` + `summary.md`，为 Phase 4-5 chat 终极版设计提供参考 |

#### SQLite 驱动迁移（2026-05-01）

| 日期 | 内容 |
|---|---|
| 2026-05-01 | **[infra]** SQLite 驱动 mattn → modernc.org/sqlite（纯 Go），三平台一行交叉编译，删 CGO_CFLAGS。性能慢 1.5-2x（本地无感） |

#### 桌面端分发方向定型（2026-04-30 ~ 2026-05-01）

| 日期 | 内容 |
|---|---|
| 2026-04-30 | **[doc]** 桌面端分发方向定型 + `desktop-packaging-notes.md` 落地：目标 Wails 原生桌面 app（窗口外壳 + 复用 httpapi，不走 binding）；分发 dmg/setup.exe/AppImage（v0.1 起 L3）；Python 沙箱短期 A、中期 C |
| 2026-05-01 | **[doc]** 常驻后台模式 + Notifier 接口决策：scheduler 不退出（关窗 ≠ 退出）。Phase 4 写 scheduler 时 `domain/notification/Notifier` 接口先行；桌面壳代码限 `internal/infra/desktop/` |
| 2026-05-01 | **[doc]** 决定不走 Wails binding：HTTP 等价但能复用 v1.2 transport（middleware/errmap/curl）；SSE 天然契合；binding 只换"类型同步"一项收益，OpenAPI 也能做到 |
| 2026-05-01 | **[refactor]** `schema_extras` guard 改 `db.Migrator().HasTable()` 替代 raw `sqlite_master` 查询；真正 GORM 写不出的 SQL（partial UNIQUE 等）仍走 raw exec |
| 2026-05-01 | **[refactor]** message_blocks 复合索引 `(MessageID, Seq)` 迁到 GORM tag（`index:idx_mb_msg_seq,priority:N`），删 schema_extras 对应 group |
| 2026-05-01 | **[cleanup]** 死代码清扫：删 3 个未发布的 `ToolTestCase*` event 类型（SSE 实际走 callback 不经 Bridge）；events-design.md 同步 |
| 2026-05-01 | **[arch]** pagination 迁到 `pkg/pagination` + S13 全代码补别名：4 store 反向 import 各自抄一份合并删 ~64 行；S13 加 `httpapi` 后缀全代码补 `*httpapi` 别名 |
| 2026-05-01 | **[fix] staticcheck 全套 5 修**：恢复误删 ListProviders/ListScenarios（deadcode 默认不扫测试）；SA1029 改 `//lint:ignore`；S1016 改类型转换。staticcheck 0 |
| 2026-05-01 | **[fix]** 5 处 `_ = err` 静默吞错改正：tool.newID 加 panic 与其他 newID 一致；tool.Import/Export 加 log.Warn；2 处 w.Write 加注释保留 |
| 2026-05-01 | **[review]** TODO 扫描：全代码仅 3 处 TODO 全是合法前瞻性标记（A1 中流执行 / context compaction 钩子点），无历史包袱 |
| 2026-05-01 | **[refactor]** `userID(ctx)` helper 统一到 `pkg/reqctx`：合 11 处重复；新增 `ErrMissingUserID` sentinel + `RequireUserID` helper。事故：sed 清空 apikey store，立教训"项目内禁用 sed 改 import / 函数名" |
| 2026-05-01 | **[review]** errmap 完整性反查：32 个 domain sentinel 全部已映射 ✅；补登记 `reqctxpkg.ErrMissingUserID` + `cryptoinfra.ErrUnsupportedVersion`（均 500） |
| 2026-05-01 | **[arch]** S5 / S6 降级为参考线：行数当硬规则会噪音（main.go DI / SSE 状态机 / Service 956 行都是结构必要）；改措辞"可读性优先于行数"。同步 backend-design.md |
| 2026-05-01 | **[review]** S13 别名全代码验证：176 处 internal import 0 处无别名 ✅，32 个别名全部规范后缀，100% 合规 |
| 2026-05-01 | **[refactor]** 跨 store 共享 Cursor 类型：4 store 的 `pageCursor` JSON tag 漂移，`pkg/pagination` 加共享 `Cursor` 类型，4 store 删本地副本统一为 `c` |
| 2026-05-01 | **[doc]** V1.2 文档全量校对：11 份反查代码 drift——testend-design 整体重写 / backend-design tree 更新 / chat.md pipeline→runner / 5 份 service-design 去 Eino 残留 |
| 2026-05-01 | **[arch]** backend-design.md 规范补完：新增 N6 / D6 / D7 / S15 / S16 / S17，扩 S9 detached context；新增 **T 系列测试规范**（T1-T4）+ **开发期工具纪律**（staticcheck / deadcode -test / 禁 sed 改 import） |
| 2026-05-01 | **[doc]** 创建项目根 `CLAUDE.md` + `backend-design.md` 拆分：把全部代码规范从后者搬到前者（自动加载进 context）；后者退化为"项目说明书"。前者 378 行 / 后者 304 行 |

#### Tool 系统大重构（2026-05-02 起，Phase 0-8 计划，Phase 0-7 完成，Phase 8 进行中）

对照 Claude Code 调研后认定当前 tool 实现"基础设施过于薄"。原 7 阶段计划中途扩成 8 阶段（Phase 5/6 改造为 DB schema 统一 + SSE 3-event entity-state 模型，原 Phase 5 重建 system tools 撤销）。

**关键决策**：(1) 推流仍 `bridge.Publish` 直调不引 emit 抽象；(2) agent 包改 tool / 原 app/tool 改 app/forge；(3) §S12 例外允许 tool/ 嵌套子包；(4) 每 user-facing domain 一个 SSE entity-state 事件；(5) Phase 5 数据库统一（forge_executions 合表 / Forge.Pending / Message errorCode 等）。

| 日期 | 内容 |
|---|---|
| 2026-05-03 | **[devx]** 测试包 `internal/e2e/` → `backend/test/`（build tag `e2e` → `pipeline`）；Makefile 加 `test-pipeline` / `test-console`。`forgeapp.Service.PublishSnapshot` 是 forge SSE 唯一发布点（与 chat.runner 同模式） |
| 2026-05-03 | **[test]** Step 3 chat E2E 5 场景全绿（~11s）：SimpleText / MissingModelConfig / ToolCall / CancelMidStream / ReasoningModel |
| 2026-05-02 | **[test]** Step 2 E2E harness：`internal/e2e/` 3 文件（harness/seed/sse），real DI + entity-state SSE 解析。smoke 680ms |
| 2026-05-02 | **[fix]** Step 1 防御代码大摸排：全 backend `_ = err` 扫描，修 6 处真问题 + 2 加 Warn + 1 conversation 时戳 flake。`.env` 注入 + Makefile 3 targets |
| 2026-05-02 | **[doc]** Phase 7 文档同步 #2：8 份跟齐 Phase 5/6 改造（events / database / forge / chat / api） |
| 2026-05-02 | **[refactor]** Phase 6 SSE 12→3 entity-state 事件（chat.message / forge / conversation），载荷 = entity GET 完整快照；`runner.go` 三 helper 是 chat.message 唯一发布源。22 包绿 |
| 2026-05-02 | **[refactor]** Phase 5 DB schema 重构 4 领域：forge_executions 统一表 / Forge.Pending 计算字段 / Message 加 errorCode/Message/updatedAt / attachments 软删。22 包绿 |
| 2026-05-02 | **[refactor]** Phase 0 清理过时 tool：删 `app/agent/{system,web}.go` 共 8 件（read_file/write_file/list_dir/run_shell/run_python/datetime/web_search/fetch_url），Phase 5 重建 |
| 2026-05-02 | **[refactor]** Phase 0 GenerateTestCases 去流改普通 HTTP（底层 `llm.Generate` 本就非流式）。`GenerateEvent` 删；新增 `GenerateResult{NotSupported, Reason, TestCases}` |
| 2026-05-03 | **[refactor]** 重复实现 8 项整改：新建 `pkg/idgen` / `pkg/llmparse` / `pkg/llmclient`；`forgeapp.PublishSnapshot` 收敛 6 处；`response.StreamSSE[T]` 泛型 helper |
| 2026-05-02 | **[fix]** testend 前端跟齐 Phase 0-3：tab-sse `forge.*` 事件 / tab-tools generateTestCases 改普通 fetch / tab-sql f_/fv_ 前缀 / destructive 红色徽章 |
| 2026-05-02 | **[doc]** Phase 4 文档同步 #1：Phase 0-3 跟齐 6 文件（CLAUDE.md §S15/§S18 / events / database / api / backend-design / forge / chat） |
| 2026-05-02 | **[refactor]** Phase 3 Tool 接口扩 10 方法 + forge tool 重写：3 静态元字段 + 3 钩子；destructive per-call AI 自报落 DB；5 forge tool 移 `tool/forge/`；runTools 改 IsConcurrencySafe 分批。22 包绿 |
| 2026-05-02 | **[arch]** Phase 2 `agent/` → `tool/` 包重组：CLAUDE.md §S12/§S13 加例外条款（tool 是 framework meta-namespace 允许嵌套子包） |
| 2026-05-02 | **[refactor]** Phase 1 大重命名 tool → forge："用户造的 Tool" 全语义改 Forge：6 entity / 5 表 / ID 前缀 t_→f_ / 22 端点 / 5 LLM-facing 名 / 3 Bridge 事件 / testend 161 处。**保留** Tool 接口 / ChatToolCall / tc_ 前缀 |

#### 沙箱方向迭代设计（2026-05-02）

| 日期 | 内容 |
|---|---|
| 2026-05-03 | **[devx]** devbox + Makefile 二轮整理：`$HOME/go/bin` 入 PATH；加 `_require-devbox` / `_refuse-inside-devbox` 两守卫；help 加 Setup/Daily 分组 |
| 2026-05-03 | **[devx]** Makefile 收成 5 核心命令 + help 默认 target：`environment` / `test-console` / `test-unit` / `test-pipeline` / `stop`。删 `make dev` / `logs` / `LOG_FILE` |
| 2026-05-03 | **[devx]** 依赖基线 + devbox 落地：Go 1.25.5；modernc.org/sqlite v1.23→v1.50（27 minor）；devbox 锁 go/python/uv/make；bootstrap 装 staticcheck/deadcode + 沙箱资源。~190 单测全绿 |
| 2026-05-03 | **[fix]** draft forge 首拒后该消失但留下空壳：`Service.RejectPending` 末尾若 `ActiveVersionID==""` 触发 `s.Delete(forgeID)`；已 active 的 forge 行为不变。新增 2 测试 + forge.md §8.5 |
| 2026-05-03 | **[fix/devx]** 沙箱迭代 1 出场 bug：`parse()` 不再裸吞 cause；加 `ensure-resources` target；新增 `smoke_bootstrap_test.go`（§T3 门控） |
| 2026-05-03 | **[doc]** 沙箱迭代 1 Phase G 收尾：8 份契约文档全量同步（forge.md ~360 行新内容为最大份） |
| 2026-05-03 | **[refactor]** 沙箱迭代 1 实施完工（Phase A-F）：sandbox 包 10 文件重写；Forge/ForgeVersion 加 env 字段；Sandbox 接口 6 方法；Service lifecycle 改造（CreateDraft / env 守卫 / LRU）。~80+13+19+11 测全绿 |
| 2026-05-03 | **[doc]** 沙箱迭代 1 MVP "punt 给 AI 自救" 哲学：砍 5 个自动恢复机制，保留 2 个真兜底（mac codesign / EnvError 收集） |
| 2026-05-03 | **[doc]** 沙箱迭代 1 反查 5 处认知偏差（wheel clone/hardlink / Python embed.FS / uv stage 名 / macOS `com.apple.provenance` codesign 重签）。详见 `desktop-packaging-notes.md §六` |
| 2026-05-02 | **[doc]** 沙箱迭代 1 设计文档 v2 重写：EnvID 算法（sha256+排序）/ 磁盘布局 / N=3 LRU / EnvStatus 5 态。**vs v1**：砍异步 sync worker / 不引新 SSE 事件类 / create_forge 进 pending / 删 SandboxTimeout |

#### 测试流水线迭代设计（2026-05-03）

| 日期 | 内容 |
|---|---|
| 2026-05-03 | **[fix]** sandbox `uv sync` 加 `--no-workspace` 试图阻止 `.venv` 外溢（事后查 flag 不存在，05-04 回退） |
| 2026-05-03 | **[infra/test]** 流水线迭代 1 Phase G 收尾：Makefile 加 `test-pipeline` / `-quick` / `-live` 三 target；CLAUDE.md 加 T6（fake LLM 约定）。**迭代 1 全 7 段完工** |
| 2026-05-03 | **[infra/test]** Phase E+F：`chat_forge` (3) / `errcodes` (16+3) / `isolation` (3) pipeline tests。**68 测全绿，2.6s** |
| 2026-05-03 | **[infra/test]** Phase D：`forge_http` (12) + `forge_lifecycle` (4) pipeline tests，`RequireForgeResources` gate |
| 2026-05-03 | **[infra/test]** Phase C：5 个 chat 场景 pipeline tests（basic/react/autotitle/queue/attachment）；harness `SetMaxOpenConns(1)` 修 in-mem SQLite 多连接 bug。**30 测全绿，2.3s** |
| 2026-05-03 | **[infra/test]** Phase B：`apikey` (5) / `model` (4) / `conversation` (4) pipeline tests；fake_llm 加 `/v1/models`。**19 测全绿，1.1s** |
| 2026-05-03 | **[infra/test]** Phase A：fake LLM 基础设施（`fake_llm.go` httptest + 5 scripts + 5 helpers）；harness 修 sandbox drift；4 chat 测试切 fake LLM 离线可跑。**5 测，1.0s** |
| 2026-05-03 | **[doc]** 流水线迭代 1 设计文档：`adhoc-topic-documents/test-pipeline-iteration-documents/01-foundation-and-coverage.md`，~13h / 7 phase / fake LLM + 真 sandbox 双层 / ~80 测覆盖目标。完整方案见该文件 |

#### Claude Code tool 抄录研究（2026-05-03）

| 日期 | 内容 |
|---|---|
| 2026-05-03 | **[research]** CC tool 抄录启动：v2.1.88→v2.1.126 delta + 41 工具 inventory（8 P0 / 7 P1 / 13 P2 / 10 Skip）。新建 `02-tools-deep/00-inventory.md` |
| 2026-05-03 | **[research]** deep-dive `01-file-ops.md`：Read/Write/Edit Piebald 原文 + Go 实现 6 节。MultiEdit 已下线（issue #11125 not planned），inventory P1 7→6 |
| 2026-05-03 | **[research]** deep-dive `02-search.md`：Grep（ripgrep wrapper 12 字段）+ Glob（doublestar + mtime-desc + 1000 cap）。LS 已下线，文档留 A/B 两方案待决 |
| 2026-05-03 | **[research]** deep-dive `03-shell.md`：Bash 描述全 41 子文件抓取 + Go 实现（cwd 状态机 + dangerous detect + background + 30K 截断）。**v1 不做 OS-level sandbox**，用 PathGuard + Ask pattern 替代 |
| 2026-05-03 | **[research]** deep-dive `04-web.md`：WebFetch（HTML→md + 小模型摘要 + 15min cache，独立 context）+ WebSearch（CC 美国限制改接 Tavily）。Forgify 走 Jina Reader 优先 + html-to-markdown fallback |
| 2026-05-03 | **[research]** deep-dive `05-ux-tasks.md`：AskUserQuestion + TaskCreate 族 4-in-1 + TodoWrite legacy + EnterPlanMode 简评。**02-tools-deep 系列收官**，5 篇覆盖 15 P0/P1 |
| 2026-05-03 | **[research]** **02-tools-deep 13 决策复审 + V1 清单**：8 P0（Read/Write/Edit/Glob/Grep/Bash/WebFetch/WebSearch）+ 5 P1（Task 族 4 + AskUserQuestion）+ 框架重构（execution_group / AgentState / PathGuard）= **13 工具 + 0.6d 框架，~7 天**。详 13 决策见 `02-tools-deep/` 各篇 |
| 2026-05-03 | **[refactor]** 框架重构 F1-F10（V1 工具前置）：新增 `pkg/agentstate` + `pkg/pathguard` + `pkg/reqctx/agentstate.go`（共 15 测）；Tool 接口 10→9 方法；`StandardFields` 加 `ExecutionGroup`；分批改按 LLM 自报 group 调度 |
| 2026-05-03 | **[fix/devx]** `llm_integration_test.go::testKey()` 从 `"shabi"` placeholder 改 `requireKey(t)+t.Skip`（per §T3）；CLAUDE.md 加"测试命令选择"小节（禁直跑 `go test ./...`） |
| 2026-05-03 | **[feat]** O1 Read tool：`app/tool/filesystem/{filesystem,read}.go`，9 方法 + 19 单测。chat 层 wire AgentState（convQueue 字段 + ctx 注入）让 must-Read-first 守卫工作 |
| 2026-05-03 | **[feat]** O2 Write tool：`write.go`，9 方法 + 13 单测。原子写 `CreateTemp+Rename`，覆写保留原 mode |
| 2026-05-04 | **[feat]** O3 Edit tool：`edit.go`，9 方法 + 19 单测，含 `#51986 markdown bold 5 处全替`。信任 stdlib `strings.Replace`，显式报 N occurrence 比 CC "All replaced" 透明 |
| 2026-05-04 | **[fix]** sandbox `sync.go` 删 `--no-workspace`——uv 0.11.8 无此 flag（昨日加错），真正建 `.venv` 的源头是 devbox python venvShellHook（已修） |
| 2026-05-04 | **[feat/test]** O4 file-ops 装配 + pipeline test：`main.go` + `harness.go` 装 PathGuard + FilesystemTools；新建 `test/filesystem/` 3 场景（ReadEditClosedLoop / WriteWithoutReadDenied / PathGuardDeniesSensitivePath）。29.7s 通过 |
| 2026-05-04 | **[feat]** S1 Grep tool：`search/{grep,grep_rg,grep_stdlib}.go`，9 方法 + 28 单测。双后端 rg shell out（10-100× 加速）+ stdlib 兜底，surface 一致。装到 main + harness |
| 2026-05-04 | **[feat]** S2 Glob tool：`search/glob.go`，9 方法 + 19 单测。`doublestar.Glob` over `os.DirFS`，mtime 降序，limit 100/1000。决策 D3：pattern `*` 即 LS 替代，无单独 LS tool |
| 2026-05-04 | **[test]** S3 search pipeline test：`test/search/` 3 场景（GrepFindsMatches / GlobListsDirectoryWithMetadata / GrepPathGuardDeniesSensitivePath）。40s 通过；errmap 无变更——tool 错误返友好字符串不到 handler |
| 2026-05-04 | **[feat]** W1 model `web_summary` scenario：domain 加 `ScenarioWebSummary` 常量 + `IsValidScenario`/`ListScenarios` 扩展；`ModelPicker` 接口加 `PickForWebSummary`，`*Service` 实现。WebFetch 工具未配置时 fallback 到 `PickForChat`。model.md 同步 4 处（清单/接口/字段/方法签名）|
| 2026-05-04 | **[feat]** W2 WebFetch tool：`web/{web,fetch}.go`，9 方法 + 24 单测。两段抓：Jina r.jina.ai → 直 GET fallback；30s timeout / 1MB cap；SSRF 守卫（loopback/private/link-local/unspecified/multicast 全拒，含 DNS rebinding 防御：解析全 IP 任一禁区即拒）。`pkg/llmclient` 加 `ResolveForWebSummary`（透明 fallback 到 chat 场景）|
| 2026-05-04 | **[feat]** W3 WebSearch tool：`web/{search,search_bing}.go`，9 方法 + 21 单测。3 层 fallback：SearXNG 公共池（随机洗牌 + JSON 解析）→ Bing HTML 抓 → Bing CN HTML 抓；每后端 10s 单超时。Bing 解析用 `x/net/html` visitor（`<li class="b_algo">` 提 title/url/snippet，含 `b_caption` 缺失时 fallback 到首个 `<p>`）。`FORGIFY_SEARXNG_INSTANCES` env 可覆盖实例池 |
| 2026-05-04 | **[test]** W4 web 装配 + pipeline test：`main.go` + `harness.go` 装 WebTools；`test/web/` 2 场景（WebFetchBlocksLoopback / WebSearchRejectsEmptyQuery）故意 short-circuit 不打外网，验 LLM ↔ tool 接线。11s 通过；errmap 无变更——tool 错误返友好字符串不到 handler |
| 2026-05-04 | **[feat]** B1 shell tools 三件套：`shell/{shell,manager,bash,output,kill}.go`，3 工具 × 9 方法 + 47 单测。Bash 含 cwd 状态机（AgentState 加 `Cwd()/SetCwd()`，`cd <path>` 整命令短路更新；链式 `cd && ...` 不追踪——与 subshell 语义一致）+ 前后台双模式（前台 sh -c 带 timeout，120s 默认 / 600s 上限；后台 spawn + ProcessManager 注册返 `bsh_<16hex>`）。BashOutput 增量游标 + 可选 regex filter；KillShell SIGKILL 幂等。256 KB 环形输出缓冲，溢出丢头 + 游标 rewind。Forgify 故意不带 banned-command 表（本地单用户） |
| 2026-05-04 | **[test]** B2 shell 装配 + pipeline test：`main.go` 装 ShellTools + `defer Manager.Stop()` 优雅关停杀子，`harness.go` 走 `t.Cleanup`；`test/shell/` 3 场景（BashEchoForeground / CdStateMachinePersistsAcrossCalls / BashOutputAndKillShellHandleUnknownID）。19s 通过；errmap 无变更——tool 错误返友好字符串 |
| 2026-05-04 | **[feat]** U1 task mini-domain + 4 tools：新建 `domain/task` + `infra/store/task` + `app/task` + `app/tool/task`（factory + 4 工具）。Task entity 含 ConversationID 作用域、status 白名单（pending/in_progress/completed/deleted）、`tk_<16hex>` ID。Service 跨 conv 报 ErrNotFound 防泄漏。每次变更发 `task` SSE 事件（entity-state，与 forge/conversation 同模式）。`pkg/reqctx` 加 `ErrMissingConversationID` + `RequireConversationID`。装到 main + harness + DB migrate。共 60+ 单测 + store 集成测试全绿 |
| 2026-05-04 | **[feat]** U2 AskUserQuestion 后端：新建 `app/ask`（in-memory 会合 Service：Wait 阻塞 + Resolve 原子摘条目防双答竞态）+ `app/tool/ask`（AskUserQuestion 工具，5 分钟超时，问题坐 chat.message SSE）+ HTTP `POST /api/v1/conversations/{id}/answers`（handler + Deps.AskService + router 装配）。errmap 加 4 行（task ×3 + ask ×3 + reqctx.ErrMissingConversationID）。决策 D11 落地——不新建事件家族 |
| 2026-05-04 | **[test]** U3 ux-tasks 装配 + pipeline test：harness `eventsBridge` 笔误修正为 `bridge`；`test/uxtask/` 3 场景（TaskCreateAndList / AskUserQuestionAnswerDelivered / AnswerEndpointUnknownCallID_404，旁路 goroutine POST 答案验真实接线）。20s 通过；pipeline 全 12 suite 全绿 |
| 2026-05-04 | **[doc]** Z1 V1 batch 文档全量同步：新建 `service-design-documents/task.md`（task mini-domain 完整设计 + §10 附 ask 服务）；4 契约文档全更——api-design 加 Phase 5 工具家族表 + answers endpoint，database-design 加 `tasks` 表行 + 关系图，error-codes 加 task ×3 + ask ×3 + reqctx.ErrMissingConversationID，events-design 加 `task` 事件（entity-state 4 个）；chat.md §2.3 / §4.1 / §4.2 / §4.3 / §4.4 同步 9 方法 + execution_group + AgentState 注入 + 20 工具 |
| 2026-05-03 | **[devx]** 项目根 + Makefile + devbox 瘦身：删 `.githooks/` / `.air.toml` / `tmp/` / `scripts/`；Makefile 砍 4 项；devbox 删 `python@3.12`（venvShellHook `.venv` 坑）+ `uv@0.11`（装饰） |

#### Phase 4 准备件 — 4 domain 设计批（2026-05-05，待实施）

为 Phase 4-5 的 workflow / 智能化 提前打地基。整批仅设计文档落档，代码未动；预估 ~10 天实施周期（mid-month deadline）。

| 日期 | 内容 |
|---|---|
| 2026-05-05 | **[doc]** 4 份 service-design 文档落档 ~2700 行：`subagent.md` / `mcp.md` / `skill.md` / `catalog.md`。完整方案见各文件 |
| 2026-05-05 | **[doc]** 4 份 contract 文档全量同步（api-design 加 ~25 端点 / database-design 加 subagent_runs + subagent_messages 两表 / error-codes 加 ~20 sentinel / events-design 加 subagent + mcp + skill + forge.persisted 4 事件 + chat.message 加 subagentRunId 字段）；backend-design.md Architecture 树加 4 新 domain |
| 2026-05-05 | **[arch]** 关键设计决策：① **subagent 双流 SSE**——subagent 事件传 run lifecycle，chat.message 带 subagentRunId 复用主对话 schema 流式传内容；② **MCP search/call 模式**不 flat 注册，避 70k token 启动开销；③ **Skill progressive disclosure 三层**（L1 metadata always-on / L2 body Read-on-trigger / L3 resources LLM 自取）；④ **Capability Catalog 接口反转**——CatalogSource port 让新 source 接入 0 行修改 catalog；⑤ MCP 内置 8 server Registry（everything/memory/sequential-thinking/fetch/time/filesystem/git/sqlite）+ marketplace 安装流程 |
| 2026-05-05 | **[arch]** 一轮自检 + 多轮纠偏：(a) 删 `enabled` 字段（catalog 解决 token 爆炸已无须装而不起）；(b) 删 skill/mcp 的项目级目录概念（自包含原则、单用户场景用户级足够）；(c) 删 catalog 的 routing-hints 用户文件（应由 generator 自动推断而非用户维护）；(d) 修订 catalog debouncer→1s BurstCoalescer + singleflight（5min 是过度防御）；(e) catalog 仅订阅 `forge.persisted` 不订阅 streaming `forge`，加 fingerprint dedup（name/description 不变跳过 LLM 调用）|
| 2026-05-05 | **[arch]** P0 生产级缺口补全：MCP per-call 30s timeout + per-server override / Cancel 通过 `notifications/cancelled` 级联 / in-flight RPC 在 disconnect 时清理 / stderr 256KB ring buffer / mcp.json 损坏 fail-soft；Subagent cancel ctx 级联 / 总超时 5min / panic recover / 并发 isolation by RunID / conv 删除不级联 subagent_runs；Skill fork-in-fork 强制忽略 fork directive / symlink 循环防护 / 并发 activate 栈结构 / allowed-tools scan 时校验；Catalog 全 source fail 保留旧 cache / generator output 2k token cap / cache 损坏移 .bak |
| 2026-05-05 | **[arch]** Catalog 触发机制大简化：从"事件订阅 + BurstCoalescer + singleflight + dirty-loop + forge.persisted 专用事件"改成 **1s polling + atomic.Bool 单 flight + fingerprint dedup**——~150 行复杂度删 ~30 行替代；MCP 加载中间态、forge streaming 噪音、并发竞态、启动顺序烦恼全自然消化。CatalogSource 接口砍掉 `EventTopics()` 方法；events-design 删 `forge.persisted` 行 |
| 2026-05-05 | **[arch]** Subagent SSE 大简化：原"双流（自己事件 + chat.message+subagentRunId）"改成 **单流 chat.message** —— subagent 上下文的 chat.message 载荷里嵌套完整 `subagentRun` 快照（含 lifecycle / token 累计 / lastTool 等），一个事件同时承载消息 + run 状态。**不再有独立 `subagent` SSE 事件类型**。事件总数从 7 减到 6。前端单一渲染逻辑（chat.message），按 subagentRunId 字段分流到主对话区 / 流式小窗 |
| 2026-05-05 | **[arch]** 终轮自审 5 处 stale + 2 处过度设计修复：catalog.md `:refresh` 描述去掉 BurstCoalescer 残留；catalog.md source 总览表删 EventTopics 列；subagent.md 关联文档 / 端到端推演 / 测试 desc 3 处独立 `Subagent` 事件残留改为 chat.message 嵌入 subagentRun；**Skill ActiveSkill 栈结构改 `atomic.Pointer[Skill]` last-write-wins**（单用户场景过度防御）；**Subagent LastTool* 5 字段从 `subagent_runs` 表移到 in-memory `gorm:"-"`**（瞬时字段重启丢失无所谓，DB schema 干净）|
| 2026-05-05 | **[arch]** Catalog 失败策略最终落定："**用户活跃度驱动重试**"——LLM 失败 → mechanical fallback 顶上 + lastFP 照常更新。用户不动东西就不再耗 LLM；改了东西 fp 自然变 → LLM 重新机会跑。无须后台 backoff，无须无限重试。Generator 内部重试改 1 次→ **2 次（共 3 次 attempt）**；key 轮训改成"**真跑 LLM 调用**"（不再只是 build client，单 key Generate fail 立即试下一个 key）|
| 2026-05-05 | **[devx]** 排程从 D1-D10 砍到 **D1-D8**，binary 打包 + 上手文档 + demo 预演由用户自己解决 |
| 2026-05-05 | **[arch]** Subagent 整体从 catalog 移除：原设计有 SubagentCatalogSource 列举 3 个内置类型，但 Subagent system tool 自身 description 已覆盖 subagent 类型说明，catalog 再列一遍冗余。CatalogSource 实现方从 4 个收为 **3 个：forge / skill / mcp** |
| 2026-05-05 | **[arch]** Subagent spawn 工具改名 **`Task` → `Subagent`**——避开与已有 `task` mini-domain（TaskCreate/List/Get/Update 管 TODO）的 LLM-facing 命名撞车；Go struct `TaskTool` → `SubagentTool`，包 `app/tool/agent` → `app/tool/subagent` |
| 2026-05-05 | **[arch]** ⭐ Sandbox v2 大重构：原 `infra/sandbox` forge-only（uv + python-build-standalone 直 bundle）改为统一 PluginSandbox 服务。Bootstrap 极简（仅 mise ~10MB，go:embed），所有语言 runtime（Python/Node/Rust/Java/Go/Ruby/PHP/...）lazy install；mise 通配 installer + Playwright/dotnet/static 专用 installer；per-plugin env 隔离（forge / mcp / skill / **conversation** 4 类 owner）；SQLite 双表 manifest（sandbox_runtimes + sandbox_envs）取代 JSON 文件。完整方案见新建的 `service-design-documents/sandbox.md`（~940 行）|
| 2026-05-05 | **[arch]** Bash tool 自动路由 + 对话 scratch env：LLM 通过 Bash 跑 `pip install pandas` / `python script.py` 等命令时，sandbox 检测命令意图自动路由到该对话的 scratch env（`Owner{Kind:"conversation", ID:"<conv>:<runtime>"}`）。**denylist 整套机制不再需要**——靠基础设施收口。conversation env 30d auto-GC（forge/mcp 默认手动）|
| 2026-05-05 | **[arch]** 摆脱 OOTB 预装：原 mcp.md 的 cmd/resources 扩展预装 5 个 server + Chromium 设计**全部废弃**——改 lazy install via sandbox v2。Forgify 总安装 ~25 MB binary + ~10 MB mise bootstrap = ~35 MB，比原 ~250 MB 砍 85%。用户首装某 server 才触发 runtime + 包的下载（带进度条）|
| 2026-05-05 | **[devx]** 配套改造：`Makefile clear` target 加清 `~/.forgify/sandbox/`；`devbox.json` bootstrap 删 `cmd/resources` 调用、删 `FORGIFY_DEV_RESOURCES` env；`cmd/resources/main.go` 重写为 mise binary fetcher（per-platform，给 go:embed 用）；`cmd/server/main.go` sandbox 装配段重写——sandboxapp.New + 注册 10+ installer + forge service 切到新接口；`app/tool/shell/bash.go` 加 detectRuntime + 自动路由逻辑 |
| 2026-05-05 | **[doc]** 沙箱 v2 文档同步：sandbox.md 新建（943 行）+ database-design 加两表 + error-codes 加 8 sentinel + api-design 加 sandbox endpoints + backend-design 加 domain/app/infra 三层 + forge.md / mcp.md 改为指向 sandbox.md（forge sandbox 接口保留作 adapter；mcp 整段 OOTB 预装设计删除）|
| 2026-05-05 | **[arch]** 包管理器共享机制 + GC 简化：Node 由 npm 改 **pnpm**（content-addressable global store，多 conv 共装同包磁盘 ≈ 1×）；uv 已自带 hardlink wheel cache。**v1 全 owner 默认手动 GC**（共享让磁盘开销极小，auto-GC 价值低）；用户主动点 `:gc` 端点或 plugin 卸载时触发 |
| 2026-05-05 | **[arch]** Sandbox bootstrap 失败 + Degraded Mode：bootstrap 失败（mise 解出 / chmod / exec 失败）→ app 仍启动；纯文本 chat + 不需 runtime 的工具（Read/Write/Edit/Bash 跑 ls/cat/git）仍可用；needs-runtime 操作（forge/mcp install/Bash 跑 pip etc.）fail-fast 返友好错；UI banner + retry 按钮；新增 GET /sandbox/bootstrap-status + POST /sandbox:retry-bootstrap |
| 2026-05-05 | **[arch]** ⭐ Windows v1 加入：5 平台 binary（macOS arm64/amd64 + Linux amd64/arm64 + Windows amd64）；Python/Node 类 MCP 全 Windows 可用；Ruby/PHP/长尾 plugin 通过 RegistryEntry.UnsupportedPlatforms 在 Windows marketplace 隐藏；Bash tool 加 Windows 分支用 PowerShell 替代 sh；进程 cancel 用 Job Object；flock 用 sys/windows.LockFileEx；fsnotify ReadDirectoryChangesW；mise binary per-platform embed（build tag 控制）。覆盖 99% 用户需求 |
| 2026-05-05 | **[devx]** 排程从 D8 → **D15**：Windows 适配 + 测试加 D10-D15（~6 天）；总周期内仍能赶投资人月中回来 demo |
| 2026-05-05 | **[arch]** mcp.md §5.5 内置 marketplace 大改——基于 web research 重选 5 个（4 用户可见 + 1 hidden test）：**Playwright / MarkItDown / Context7 / DuckDuckGo / SQLite + everything**。砍掉与内置工具重复的（fetch / filesystem / git / time）；砍掉跟原生计划撞车的（memory）；不要 OAuth 系列（github/notion/slack v2+）。RegistryEntry 加 `Bundled` / `Hidden` / `PostInstallSteps` / `OnlineOnly` / `Notes` 5 字段。**OOTB 预装机制**：cmd/resources/ 扩展抓 Node binary + npm/uvx 预装 5 个 MCP 包 + Playwright Chromium，~250 MB 总成本；首次启动一次性 setup，之后 marketplace 全绿可用 |

#### Phase 4 准备件 — D1 sandbox v2 实施（2026-05-05~）

| 日期 | 内容 |
|---|---|
| 2026-05-05 | **[feat]** D1-2 sandbox domain 包落地：`internal/domain/sandbox/`（sandbox.go + installer.go，~410 行）—— Runtime/Env 两实体 + Owner/RuntimeSpec/EnvSpec/SpawnOpts/ExecutionResult/LongLivedHandle/ProgressFunc 值对象 + 8 sentinels + Repository + RuntimeInstaller/EnvManager 双端口 |
| 2026-05-05 | **[feat]** D1-3 sandbox store 落地：`internal/infra/store/sandbox/`（sandbox.go + sandbox_test.go）—— Repository GORM 实现（Runtime/Env CRUD + FindDefaultRuntime/FindRuntime/FindEnvByOwner/ListEnvsByRuntime/ListEnvsByOwnerKind/TotalSizeBytes/ListEnvsLastUsedBefore），系统级表不按 userID 过滤，19 集成测试全绿 |
| 2026-05-05 | **[feat]** D1-4/5/6 sandbox 装配三件：(a) AutoMigrate 注册 Runtime + Env 两表；(b) errmap 加 8 sandbox sentinels（4xx/5xx 按 error-codes.md）；(c) `bootstrap_mise.go` 骨架定 v2 mise embed 方案（per-platform `//go:embed`，darwin 沿用 macCodesign ad-hoc 签绕 Gatekeeper，待 Apple Developer ID 后切 notarization；D2 填二进制 + extractMise 函数体）|
| 2026-05-05 | **[feat]** D2-1 mise binary fetcher：`cmd/resources` 重写——旧的 uv + python-build-standalone fetcher 整体替换为 jdx/mise per-platform 下载器（默认当前平台，`--all-platforms` 拉全 5 平台），SHA256 校验 + 原子写 + 幂等。输出到源码树 `backend/internal/infra/sandbox/mise/<goos>-<goarch>/mise[.exe]` 给 D2-2 的 `go:embed` 用。Makefile target 由 `fetch-mise` 改为 `resources`，devbox bootstrap 自动跑。**v1 dev resources 目录** (`~/.forgify-dev-resources` + `FORGIFY_DEV_RESOURCES` env) 不再被消费；forge sandbox v1 在 D2-5 之前 fail-soft 返 ErrSandboxUnavailable |
| 2026-05-05 | **[feat]** D2-2 mise embed.FS + ExtractMiseBinary：5 个 per-platform `embed_mise_<goos>_<goarch>.go` build-tag 文件 + 1 个 `embed_mise_unsupported.go` fallback（freebsd/linux-386 等返空 miseBinary）；D1-6 placeholder 替换为真实 `ExtractMiseBinary(ctx, dataDir, log)`——写到 `<dataDir>/sandbox/bin/mise` + chmod 0755 + darwin ad-hoc codesign（沿用 preflight.go::macCodesign）+ SHA256 hash 文件幂等。3 单测全绿（embed sanity / happy path / "binary 删除但 hash 残留" 恢复路径）。6 平台 cross-compile 全过（含 freebsd unsupported fallback） |
| 2026-05-05 | **[feat]** D2-3a mise generic Installer + Python EnvManager：`installer_mise.go` 通配 RuntimeInstaller（python/node/rust/go/java/ruby/php/...）共享 `<sandboxRoot>/mise-data/` MISE_DATA_DIR + `mise install -y` + 用 `mise where` 解析实际 install path（处理 `3.12` → `3.12.5` 部分版本约束）；`envmanager_python.go` 用 `uv venv --python` + `uv pip install --python` 建 venv + 装 deps（uv hardlink wheel 缓存让多 env 共享磁盘）。**RuntimeInstaller 接口签名调整**：`Install(ctx, version, dest, stream) error` → `Install(ctx, version, sandboxRoot, stream) (relPath string, err error)`，因 mise 用全局 MISE_DATA_DIR 不接受任意 dest——sandbox.md §7-8 同步改。6 pure-function 单测；真 install/venv 由 D9 pipeline 测试覆盖（避免单测下载几十 MB python） |
| 2026-05-05 | **[doc]** D2-3 范围澄清：sandbox.md §7 加详细的 v1 EnvManager 矩阵 + 实施顺序——**全部 11 个 EnvManager + 4 个 Installer 进 D2，不延后到 v2**（Python ✅ + Node + Rust + Java + Go + Ruby + PHP + Playwright + dotnet + Static + Generic fallback；Mise + Playwright + dotnet + Static 4 个 Installer）。投资人承诺。Java 选**方案 A**（每 env 独立 Maven local repo，跟 venv 哲学一致；磁盘大但 v1 demo 不显著）。D2-3 拆为 a/b/c/d/e/f 6 子任务，按消费方+复杂度分组逐 commit |
| 2026-05-05 | **[feat]** D2-3b Node + Playwright 全栈：`envmanager_node.go`（pnpm + 写最小 private package.json + content-addressable global store 让多 env 共享 npm 包磁盘）+ `installer_playwright.go`（`playwright install <browser>` + 全 env 共享 `<sandboxRoot>/playwright-browsers/` 缓存避免重复下 ~300 MB Chromium）+ `envmanager_playwright.go`（委托 Node CreateEnv/InstallDeps；InstallExtras 跑浏览器下载）。3 个文件 ~440 行 + 11 单测全绿（Kind / package.json 写出 + 幂等 / EnvBin Windows 后缀 / 浏览器频道列表 / 共享缓存路径 / Node 委托）。真 npm + Playwright 网络 install 推到 D9 pipeline 套 |
| 2026-05-05 | **[feat]** D2-3c Generic + Static binary：`envmanager_generic.go`（兜底 EnvManager；mkdir 后 InstallDeps/InstallExtras no-op；给 mise 长尾 600+ 语言 Erlang/Elixir/Lua/Zig/Deno/...）+ `installer_static.go`（HTTP GET URL → 写 `<sandboxRoot>/static-binaries/<kind>/` → chmod 0755 → darwin macCodesign；version 支持 `sha256:<64hex>@<URL>` 格式做下载校验；给 GitHub MCP 等纯静态二进制 plugin 用）+ `envmanager_static.go`（CreateEnv mkdir，InstallDeps/Extras no-op；EnvBin 反查共享 binary 路径）。13 单测全绿（含 parseStaticVersion 6 子用例）|
| 2026-05-05 | **[feat]** D2-3d Rust + Go：`envmanager_rust.go`（CARGO_HOME=<env>/.cargo + `cargo install --root=<env>` 编到 `<env>/bin/`；目标是 binary CLI 工具不是库 deps）+ `envmanager_go.go`（GOPATH=<env>/gopath + GOBIN=<env>/bin 跑 `go install <pkg@ver>`；同样针对 binary 工具）。两者都"每 env 独立 cache"——v2 视磁盘压力可考虑共享 GOMODCACHE（Go content-addressable 干净；Cargo 不行）。7 pure-function 单测全绿 |
| 2026-05-05 | **[feat]** D2-3e Java + Ruby + PHP：`envmanager_java.go`（方案 A：MAVEN_OPTS=-Dmaven.repo.local=<env>/m2 per-env Maven local repo + `mvn dependency:get -Dartifact=<GAV>`；EnvBin 故意只返 binName 不前缀 envPath，让调用方经 JDK PATH 解析）+ `envmanager_ruby.go`（BUNDLE_PATH=<env>/bundle + 写最小 Gemfile + `bundle add <gem>`；EnvBin 返 bundle/bin/）+ `envmanager_php.go`（composer --working-dir=<env> + COMPOSER_HOME=<env>/.composer + 写最小 composer.json + `composer require <pkg> --no-interaction`；EnvBin 返 vendor/bin/）。9 pure-function 单测全绿 |
| 2026-05-05 | **[feat]** D2-3f .NET（D2-3 收尾）：`installer_dotnet.go`（unix 走 dotnet-install.sh，Windows 走 PowerShell + dotnet-install.ps1，不走 mise——sandbox.md §4 明确"v1 单独走专用 installer"；脚本运行时拉，不预 embed；--install-dir=<sandboxRoot>/dotnet-installs/<version>/）+ `envmanager_dotnet.go`（写最小 env.csproj + nuget.config 钉 `./packages` 让 NuGet 包本地化 + DOTNET_CLI_HOME=<env>/.dotnet + `dotnet add package <pkg>`）。7 单测全绿。**D2-3 整组完成**——4 Installer + 11 EnvManager 全部进 v1（Mise/Playwright/Static/Dotnet 四 installer + Python/Node/Rust/Java/Go/Ruby/PHP/Playwright/Dotnet/Static/Generic 十一 EnvManager），投资人承诺达成 |
| 2026-05-05 | **[arch]** D2-4 part 1 — `domain/sandbox/tooling.go` 新加 **ToolRegistry 抽象**解耦 EnvManager 与"支持工具怎么装"。改 5 个 EnvManager（Python/Node/Java/Ruby/PHP）构造从持具体 binary path（uvBin / pnpmBin / mvnBin / bundleBin / composerBin）改为持 sandboxdomain.ToolRegistry，操作时 `tools.EnsureTool(ctx, "uv", "")` 懒解析。app/sandbox/Service 实现 ToolRegistry（EnsureTool 内部链 EnsureRuntime + Installer.Locate），main.go 把 service 自己注入作 registry。**好处**：(a) EnvManager 接口纯净不依赖具体实现细节；(b) 支持工具懒装让 boot 极快（首次用某 plugin 时才装该 lang 的支持工具）；(c) unit test 用 fakeToolRegistry 注入，无需真起 mise。sandbox.md §7 加 ToolRegistry 接口说明 + §7-8 main.go 装配示例改为分两步注册（RegisterInstaller / RegisterEnvManager），ToolRegistry 注入 |
| 2026-05-05 | **[feat]** D2-4 part 2 — `app/sandbox/sandbox.go` Service 落地：Bootstrap (extract mise + atomic.Bool ready) + IsReady + RetryBootstrap + BootstrapError（degraded mode 暴露给 HTTP）+ EnsureRuntime（per-kind 锁 + 双重检查）+ EnsureEnv（per-(ownerKind,ownerID) 锁 + deps drift 触发 destroy + rebuild）+ Destroy + EnsureTool（实现 ToolRegistry）+ ListRuntimes/ListEnvs/TotalDiskUsage 查询。`disk.go` 辅助：computeDirSize（best-effort 求和；失败返 0 不挡 install）+ removeAll（防灾难性路径如 "/" 守卫）。无外部 spawn 还（D2-4 part 3 来）|
| 2026-05-05 | **[feat]** D2-4 part 3a — 进程树管理 leak 防御加强（**D10 Job Object 提前到 D2**）：拆 `proc_unix.go` → `proc_linux.go`（Setpgid + **PR_SET_PDEATHSIG=SIGTERM** 让 child 在 Forgify 死时收 SIGTERM）+ `proc_darwin.go`（Setpgid，无原生等价靠层 A+B 兜底）；重写 `proc_windows.go` 用 **Job Object + JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE**（master Job 创建时 assign Forgify 自身，所有 child 自动继承；Forgify 死时 OS 强 kill 所有成员，**v1 平台集中最强 leak 防御**）。golang.org/x/sys/windows 转 direct dep。3 平台 cross-compile 全过。日常 ctx-cancel 仍用 SIGKILL/taskkill 不变 |
| 2026-05-05 | **[feat]** D2-4 part 3b — `infra/sandbox/spawn.go` 进程 spawn helper：`SpawnOnce(ctx, opts) → ExecutionResult`（一次性，跑完拿 stdout/stderr/exitCode；非零退出返 Ok=false 不上抛 Go error 让调用方传 LLM）+ `SpawnLongLived(ctx, opts) → LongLivedHandle`（长生命周期，stdio 管道接好返 handle 给调用方驱动 stdin/stdout/wait/kill）+ `SpawnOptions`（Cmd / Args / Cwd / Env / Stdin 全由调用方解析）。两者都套 setupProcessGroup + cmd.Cancel = killProcessGroup（接 ctx-cancel）。8 端到端单测全绿（echo / cat / sleep / 不存在 binary / ctx-cancel 真杀 sleep 30 → 100ms 内返）。infra 把 exec.Cmd 编排藏在两个简洁函数后，app 层不直接碰 exec |
| 2026-05-05 | **[feat]** D2-4 part 3c — `app/sandbox/spawn.go` Service.Spawn / SpawnLongLived + **层 A leak 防御**（active handle 注册表 + Service.Shutdown）：Service.Spawn 解析 Owner → env → EnvManager.EnvBin（裸 cmd 名翻译）/ EnvManager.EnvDir（cwd）/ os.Environ() + opts.Env overlay，调 sandboxinfra.SpawnOnce；SpawnLongLived 类似但返 trackedHandle 自动注册到 activeHandles，Wait/Kill 自动反注册。Service.Shutdown 并发 Kill 所有 active handle 等所有 done 或 ctx 过期。10 端到端单测全绿（happy / 非 ready / 空 cmd / owner mismatch / 绝对 cmd 跳 EnvBin / env overlay / handle 注册 + Wait 反注册 / Kill 反注册 / Shutdown 杀 3 个 / 空 Shutdown）。**层 A 完成**——投资人 demo macOS crash 时 leak 概率从"每次"降到"仅 SIGKILL Forgify 时"|
| 2026-05-05 | **[feat]** D2-4 part 3d — **层 B leak 防御 + D2-4 收尾**：Env struct 加 `RunningPID` + `RunningStartedAt` 列（GORM 显式 `column:running_pid` 防 NamingStrategy 把 PID 拆成 `p_id`）；Repository 加 SetEnvRunningPID / ClearEnvRunningPID / ListEnvsWithRunningPID 三方法 + 2 store 集成测试。`app/sandbox/restore.go` Service.RestoreOrCleanupOnBoot 启动扫 manifest，对 running_pid > 0 的 env 调 killIfAlive（unix 用 signal 0 探测 + p.Kill；windows 直接 p.Kill）+ ClearEnvRunningPID。Service.Bootstrap 成功后自动调 RestoreOrCleanupOnBoot。SpawnLongLived 启动后 SetEnvRunningPID(pid)，trackedHandle.unregister 同时清 manifest。3 端到端测试全绿（killStaleProcessAndClearsPID 真 spawn sleep 验证 p.Kill 落地 / NoOpWhenNoStalePIDs / HandlesAlreadyDeadPID）。**D2-4 完整收尾**——Bootstrap + Service + Spawn + 三层 leak 防御（A/B/C）全到位 |
| 2026-05-05 | **[refactor]** D2-5a forge sandbox 迁到新 service：`app/forge/sandbox_adapter.go` SandboxAdapter 满足 forge.Sandbox 接口，委托 sandboxapp.Service（Owner ID 用 `<forgeID>:<envID>` 拼合保 v1 N=3 EnvID buffer 行为）；adapter 拥有 main.py + driver 模板写盘（v2 sandbox 不管 forge 文件布局）；非零退出翻译为 Ok=false ExecutionResult 不上抛 Go 错（跟 v1 行为一致）。`SyncRequest` / `RunRequest` / `SyncError` / `ComputeEnvID` 从 sandboxinfra 包**挪到 forge 包内** —— forge.go 不再 import sandboxinfra（D2-5b 可删 v1 sandbox 文件）。main.go 装配段重写：sandboxstore + sandboxapp.New + Bootstrap + `registerSandboxStack` helper（注册 7 mise 主流 + 5 mise 支持工具 + dotnet + 11 EnvManager）；forge 用 `forgeapp.NewSandboxAdapter(sandboxSvc, dataDir)`。fakeSandbox in forge_test 也切到本包 types。所有 ~170 单测全绿 + 3 平台 cross-compile 通过 |
| 2026-05-05 | **[refactor]** D2-5b 清 v1 sandbox 残留：删 8 个 v1 文件（`sandbox.go` v1 Sandbox struct + `paths.go` + `preflight.go` + `sync.go` + `run.go` + `destroy.go` + `pyproject.go` + `progress.go`）+ 6 个 v1 测试。`macCodesign` 提到 `codesign.go` 加 `runtime.GOOS != "darwin"` 早返让所有平台编译干净（保留被 ExtractMiseBinary + StaticBinaryInstaller 用）。infra/sandbox 现仅 v2 内容（installer/envmanager/spawn/embed/proc/codesign）。**D2-5 整组完成** ——forge 完全切到 PluginSandbox v2，v1 代码 0 残留，test 全绿，3 平台 cross-compile 通过 |
| 2026-05-05 | **[feat]** D2-6 sandbox HTTP endpoints：`handlers/sandbox.go` 13 个端点（GET runtimes/envs/envs/{id}/disk-usage/bootstrap-status + POST envs/{id}:destroy/runtimes/{id}:destroy/:gc/:retry-bootstrap/runtimes:install + GET conversations/{id}/sandbox-envs + POST conversations/{id}/sandbox-envs/{kind}:reset + sandbox-envs:reset-all）按 sandbox.md §11 表实施；`:action` 风格用 PathValue + splitAction 派发。Service 加 GetEnv / DeleteRuntime（先检查无 env 引用否则返 ErrEnvInUse=409）/ GC（用 ListEnvsLastUsedBefore 删旧 env，olderThanDays 默认 30）。router/deps 加 SandboxService 字段，main.go 装配 sandboxSvc。`MarkReadyForTest` / `ActiveHandleCountForTest` 从 export_test.go 挪到生产 sandbox.go（带 `ForTest` 后缀 godoc 警告生产禁用）让 cross-package handler 测试可调。dataSlice helper 加 apikey_test.go 共享。12 端到端 httptest 全绿（ListRuntimes empty/seeded / ListEnvs requires ownerKind / GetEnv 404+200 / DiskUsage / BootstrapStatus / RuntimeDestroy 409 conflict / GC default / RetryBootstrap returns status / ListConvEnvs 前缀过滤）|
| 2026-05-05 | **[feat]** D2-7 Bash 自动路由 + 对话 scratch env（**D2 收尾，"基础设施收口"承诺兑现**）：`bash_route.go` detectRuntime 按 sandbox.md §9.5 regex 表分类命令到 runtime kind（python/node/rust/go/ruby/php/java/dotnet）；stripCDPrefix 剥前置 `cd <path> &&`；envBinDirsForKind 派生 per-kind PATH 前置目录（Python `.venv/bin`、Node `node_modules/.bin` 等）；prependPath 把 dirs 前置到 PATH（Windows `Path` case-fold）。Bash struct 加可选 `sandbox *sandboxapp.Service` 字段；Execute 内 maybeAutoRoute 按需 EnsureEnv（懒建对话 scratch env owner=`<convID>:<kind>`）+ 返 binDirs；buildShellCmd 加 extraPath 参数把 env 前置 `os.Environ()` 的 PATH。Sandbox 不可用 / 非 runtime 命令优雅降级 plain shell。NewShellTools 接 sandbox（main.go 传 sandboxSvc，pipeline harness 传 nil）。21 pure-function 单测（30+ 命令分类 / cd 前缀剥 / per-kind bin 派生 / PATH 前置语义 / Windows case-fold）全绿。**D2 整组完成** ——PluginSandbox v2 完整落地：embed mise + 11 EnvManager + Service + 3 层 leak 防御 + forge migration + HTTP API + Bash 收口 |
| 2026-05-05 | **[refactor]** infra/sandbox 按 §S12 重组：从 22 个 `installer_*.go` / `envmanager_*.go` "by kind" 平铺改成 12 个 "by concept" 文件——`mise.go`（installer + ExtractMiseBinary 同概念合并）/ `playwright.go`（installer + envmanager browsers cache 同概念）/ `dotnet.go`（installer + envmanager .NET ecosystem 同概念）/ `static.go`（installer + envmanager 自包含 binary 同概念）+ 8 个语言（python/node/rust/golang/java/ruby/php/generic）单文件 rename。`go.go` 因关键字冲突命名 `golang.go`。infra/sandbox 文件总数 44 → 37（含平台 build-tag 文件 + 测试），全部按概念聚合。0 行为改动（公开 symbol 全部不变），全套测试 + 3 平台 cross-compile 全绿 |

#### Tool 自检 batch 1 — 3 个真 bug（2026-05-05）

| 日期 | 内容 |
|---|---|
| 2026-05-05 | **[fix]** Tool 自检 batch 1 修 3 bug：(1) `grep_stdlib.go` line-mode 多行连续匹配时把 match 行错标为 context（`-`）——镜像 multiline 版本预算 matchLines map 跳过；(2) `web/fetch.go` `fetchClient` 加 `CheckRedirect`——不加守卫则公网 URL 302 到 loopback 能绕过 SSRF；(3) `shell/bash.go::runForeground` 加 `context.Canceled` 分支——父 ctx 取消时不再误报 "exec failed: signal: killed"。+7 回归测试，全套绿 |
| 2026-05-05 | **[doc-fix]** Tool 自检 batch 2 — chat.md 全量同步上周末 tool 改动留下的 drift：§4.4 工具表扩到 20 个 + 加家族级设计要点；§5.1/5.2/5.3/5.4/5.5 把 IsConcurrencySafe 分批改为 execution_group；writeDB 全 → writeAndPublish；旧事件类型（ChatToolCallStart/ChatToken/ChatReasoningToken/ChatDone/ChatError）改为 chat.message 单事件；§10.1 `agentapp.Tool` → `toolapp.Tool`；§10.2/§10.3/§11.1/§11.2/§13 `chat.error` SSE 改为 status=error 的 chat.message stub；§15 实现清单 `app/agent/*` 全删，重写为 `app/tool/{forge,filesystem,search,web,shell,task,ask}/` 七家族子包 + main.go 装配链 |
| 2026-05-05 | **[doc-fix]** Tool 自检 batch 3 — CLAUDE.md 同步 tool 体系演化：§S15 ID 前缀清单删 `frh_`/`fth_`（Phase 5 已合并到 forge_executions/`fe_` 表）+ 加 `fe_`/`b_`/`tk_`/`bsh_` 四个新前缀 + 注明 `pkg/idgen.New(prefix)` 是统一实现；§S18 加 §8 静态元数据 3 字段对照表（覆盖全 20 个工具 + Bash 故意不走 PathGuard 的 trade-off 注脚 + must-Read-first/RequiresWorkspace 是文档性元数据非框架强制的明示） |
| 2026-05-05 | **[fix/security]** Tool 自检 batch 4 — PathGuard 跨平台覆盖 + 包注释明示局限。`DefaultDenyList` 原偏 macOS（Linux/Windows 用户大量关键路径无保护），现加：Linux runtime/secrets（`/proc/`/`/run/secrets/`/`/var/run/secrets/`/`/sys/class/`）、Windows 系统 + 凭据库（`C:/Windows/`、`~/AppData/.../Microsoft/{Credentials,Crypto,Protect,Vault}/`）、跨平台浏览器 Login Data（Chrome/Edge macOS+Linux+Windows 三处）、kubectl + docker config。**包注释加局限段**——明示 Bash 故意不走 PathGuard（`bash cat ~/.ssh/id_rsa` 能成功），PathGuard 是 file-tool 防 LLM 手滑的护栏，不是 LLM 跑 shell 的安全边界。+4 回归测试（Linux runtime / Windows credential store / browser logins / kube+docker config），全套绿 |
| 2026-05-05 | **[doc]** Tool 自检 batch 5A — 新建 5 份 tool 家族 service-design doc 对照 task.md 模式：`filesystem.md`（Read/Write/Edit + PathGuard + must-Read-first + AgentState.SeenFiles）/ `search.md`（Grep rg+stdlib 双后端 + Glob 替 LS 决策 D3）/ `web.md`（WebFetch Jina+直 GET + WebSearch SearXNG/Bing/Bing CN 三层 + SSRF 守卫含逐跳重定向校验）/ `shell.md`（Bash 前后台双模式 + cd 状态机 + ProcessManager 256KB 环形缓冲 + Bash 不走 PathGuard 的 trade-off）/ `ask.md`（in-memory rendezvous + 决策 D11 不新建事件家族 + 原子摘条目防双答竞态）；task.md §10 由 ~80 行 ask 详细设计压缩为指向 ask.md 的指针。完整方案见各文件 |
| 2026-05-05 | **[cleanup]** Tool 自检 batch 6 — 收尾 5 条小 nit：(1) `tool/filesystem/read.go` + `tool/task/task.go` 加 `var _ toolapp.Tool = (*X)(nil)` 编译期接口断言（5 个工具补齐，全 20 工具一致）；(2) `app/task/task.go` 删 `_ = errors.Is; _ = idgenpkg.New` 死代码 + 同步删 `errors` 死 import；(3) 重命名 `TestService_Resolve_DoubleAnswerIsErrAlreadyAnswered` → `_IsErrNoPendingQuestion` 反映 atomic-pop 后的契约；(4) `shell/bash.go::runBackground` 加显式注释解释 `context.Background()` 是切断 ctx 的设计意图；(5) `error-codes.md` Phase 5 段加 NB 注脚明示 fs/search/web/shell 不进 errmap（错误以友好字符串走 tool_result）。`go build` + `make test-unit`（零 FAIL）+ staticcheck（0 警告）三件套全绿 |
| 2026-05-05 | **[doc/refactor]** §S11 注释规范 v1.1 + 注释清理首轮（`internal/pkg/`）。CLAUDE.md §S11 加硬上限 + 内容禁令（设计意图段、决策叙事、Phase 状态、章节横幅、字段 godoc 节制）；执行细则 1+1 行直译可接受、≥4 行段中文必须有信息增量。`internal/pkg/` 10 文件清完：注释比例 21-79% → 21-48%，总行数 1046 → 788（-25%）。pathguard 包注释 41 行 → 4 行（搬 design doc）；reqctx 三文件去 file-header 长 essay + 章节横幅。go build / test-unit / go vet 全绿 |
| 2026-05-05 | **[fix]** Tool 自检 batch 7 — Bash auto-route 改 AST walk（`mvdan.cc/sh/v3`）。覆盖 `bash -c`/`env VAR=`/路径前缀/`which X`/subshell/`$()`（之前静默逃逸到主机）；`Bash.Description` 加 `eval`/`source`/动态 `$()` 警告。drop `stripCDPrefix`（AST 自然处理）。+15 测试，shell 包绿。详 sandbox.md §9.5 |
| 2026-05-05 | **[arch]** D3-0 docs only — subagent 设计大调：删 `domain/subagent.SubRunner` port 接口；新设 `internal/app/loop/` 通用 ReAct 引擎（Host 接口 + Run 函数），chat / subagent / Skill fork / Phase 4 workflow LLM 节点都是调用方。无循环 import 无 DI 接口——纯单向依赖 `chat → loop`、`subagent → loop`。subagent.md §2/§6/§14 重写；backend-design.md domain/app 树同步 + LLM 客户端策略表 ReAct 行更新；CLAUDE.md §S15 ID 前缀加 `sar_`/`smm_` |
| 2026-05-05 | **[feat]** D3-1+D3-2 抽 `internal/app/loop/`（loop.go + stream.go + tools.go + history.go）通用 ReAct 引擎；chat 重构为 loop 调用方。chat.runner.go 从 452 行降到 213 行（剩队列+autoTitle+system prompt），新增 host.go（chatHost 实现 loop.Host），删 stream.go + tools.go。loop.Host 5 方法：LoadHistory / Tools / Publish / WriteCheckpoint / WriteFinalize；BlocksToAssistantLLM 导出供 chat.buildHistory 复用（DB ↔ wire 单一事实源）。stream_test + history_test 迁到 loop 包。chatapp 公开 API 0 变更，~170 单测 + 3 平台 cross-compile 全绿 |
| 2026-05-05 | **[refactor]** 命名审计 #1 — Task → Todo 全栈改名。entity 名"Task"在软件里太宽泛，与计划中的 `Subagent` 工具语义易混；改成专指待办事项的 Todo 单义。包 `domain/task`/`app/task`/`infra/store/task`/`app/tool/task`/`test/uxtask` → `*/todo`+`test/uxtodo`；包别名 `task*` → `todo*`；entity Task→Todo；LLM 工具 `TaskCreate/List/Get/Update` → `Todo*`；JSON arg `task_id`→`todo_id`、List 输出 key `tasks`→`todos`；DB 表 `tasks`→`todos`（GORM 默认复数）+ ID 前缀 `tk_`→`td_` + 索引 `idx_tk_*`→`idx_td_*`；SSE 事件 `task`→`todo`；errmap `TASK_*`→`TODO_*`。`go build` + `make test-unit` + staticcheck 0 + 3 平台 cross 全绿。同步 8 份文档（todo.md / api/db/error/events 契约 / CLAUDE.md §S15 / backend-design tree）|
| 2026-05-05 | **[fix]** test/harness：D2-5 沙箱重构后 pipeline harness 一直编不过（sandboxinfra.{Sandbox,New,Config} 不存在）但被掩盖（dev log 写"全套绿"指单测）。harness 改用 sandboxapp.Service v2，加 sandboxstore + sandbox migrations + registerSandboxStack helper（镜像 main.go）；NewShellTools 接 sandboxSvc；RequireForgeResources 改查 IsReady() / BootstrapError()（v1 FORGIFY_DEV_RESOURCES 路径已死）|
| 2026-05-05 | **[fix]** Sandbox 支持工具版本钉死：`uv@0.11.4`（0.11.9 latest 因 GitHub artifact attestation 缺失安装失败）/ `pnpm@9.15.4` / `maven@3.9.9` 替代原 ""（隐式 latest）。**bundler + composer 从 registerSandboxStack 移除**——它们不在 mise registry（bundler 随 Ruby ≥2.6 stdlib；composer 需 `php composer-setup.php` 或静态下载）。Ruby/PHP EnvManager 调 EnsureTool 会失败——**D2 遗留 bug**，修复在 EnvManager 自身（直定位 bundle / composer 静态装），单独跟踪不阻塞 V1 demo（Python/Node/forge 是 demo 路径） |
| 2026-05-05 | **[fix]** Sandbox §S3 系统性扫错：抽 `infra/sandbox/exec_helper.go::RunWithStderrCapture`（pipe + 4KB 环形缓冲 + 失败时附原 cmd.Wait err + stderr tail），9 处 install/dep 调用站（mise/python/node/rust/go/java/ruby/php/dotnet×2/playwright×2）共 ~150 行重复样板压成 3 行调用。同时修 mise 调用：bootstrap 时落 `<sandboxRoot>/mise.toml` 关全部 attestation（python/aqua 各路径——GitHub 未鉴权 API 60/小时 rate-limit 是 pipeline test 反复 403 的真因）；`Locate` 增加 aqua flat-binary 布局识别（uv 装到 `<installDir>/uv-aarch64-apple-darwin`，walkForBinary 递归找匹配 `<kind>*` 的可执行文件）；`PythonEnvManager.CreateEnv` 保留 uv exec err；`Makefile test-pipeline` 加 `-p 1` 串行（多 mise install 并发会撞磁盘 + plugin cache 竞态）。test-pipeline 12 套 4 分钟全绿（之前一直被 harness 编不过掩盖） |
| 2026-05-05 | **[feat]** D3-3 subagent domain + store + reqctx：`internal/domain/subagent/subagent.go`（SubagentType 注册表项 + SubagentRun 16 持久 + 5 瞬时 gorm:"-" 字段 + SubagentMessage 复用 chatdomain.Block + Repository 7 方法 + 4 sentinels + 5 状态常量）；`internal/infra/store/subagent/subagent.go` GORM 实现（AppendMessage 在事务内 SELECT MAX(seq)+1 防并发撞号）+ 14 集成测试（含并发 append 12 路验证）。reqctx 加 `SubagentDepth` / `SubagentRunID` 两 ctx key（GetSubagentDepth 默认返 0）。AutoMigrate 注册 + errmap 加 ErrTypeNotFound (404) / ErrRecursionAttempt (422)。72 包全绿 + 3 平台 cross + staticcheck clean |
| 2026-05-06 | **[feat]** D4-1 events.ChatMessage 扩 3 字段（SubagentRunID / ParentConversationID / *SubagentRun）+ MarshalJSON 双路径（零 subagent 走快路径字节级与原 Message 一致 wire 向后兼容；非零走 map 合并）。agentstate 加 SubagentTokenLog（per-conv 累加器，32 路并发 append 求和验证）。是 D4 余下子任务（subagentHost 推快照 / Service 终态写）的载体 |
| 2026-05-06 | **[feat]** D4-2 app/subagent service + registry + subagentHost：registry 内置 3 类型（Explore 白名单纯只读 / Plan 含 web tools / general-purpose AllowedTools=nil 继承父注册表去 Subagent 自身）；Service.Spawn 一站式 = 解类型 → 过滤 tools → llmclient.Resolve → CreateRun → ctx 注 RunID + Depth+1 → 5min 总超时 → 注册 cancel func → defer recover → loop.Run via subagentHost → loop.Result → SubagentRun.Status 重映射 → detached-ctx UpdateRun → AddSubagentTokens；subagentHost 满足 loop.Host 5 方法（流式精化策略：单 assistant 行跨多 LLM 事件累加；Publish 推 chat.message 嵌 SubagentRun 快照含 lastTool* 瞬时字段）|
| 2026-05-06 | **[feat]** D4-3 SubagentTool 9 方法 + 装配：双保险防递归（结构性 filterTools 剥 + 运行时 Execute 查 ctx depth）；终态友好注脚 `[note: hit max turns]`/`[note: cancelled]`/`subagent failed: ...`；hard sentinel（recursion / 未知类型）走 Go err。main.go + harness 装配顺序：append SubagentTool 到 tools → svc.SetTools(tools) → chatService.SetTools(tools)（确保 filterTools 看到所有其他 tool）。18 单测覆盖 identity/static/schema/Validate 5 路径/CheckPermissions 跨 mode/depth≥1 拒绝/解析失败/appendNote 三 body 形态 |
| 2026-05-06 | **[feat]** D4-4 HTTP 4 端点：GET /api/v1/conversations/{id}/subagent-runs / GET /subagent-runs/{id}（404 内联映射）/ GET /subagent-runs/{id}/messages（按 Seq）/ GET /subagent-types。router/deps 加 SubagentService 字段，nil-tolerant 注册。8 httptest（含显式 StartedAt 避免 monotonic-clock 偏移影响排序断言）。V1 不出 POST :cancel——主对话 cancel 经父 ctx 自动级联 |
| 2026-05-06 | **[test]** D4-5 pipeline 3 场景（test/subagent/subagent_test.go）：Spawn_EndToEnd（parent → SubagentTool → Service.Spawn → loop.Run → tool_result 回 parent，DB 行 + message 落实，2.2s）/ SSE_CarriesSubagentRunSnapshot（chat.message 带 subagentRunId + parentConversationId + 完整 SubagentRun JSON）/ MaxTurns_Triggered（max_turns=1 + sub-LLM 持续 emit 未知工具 → status=max_turns + parent tool_result 含 "[note: hit max turns]" 注脚）。V1 不写嵌套递归 pipeline——结构性防御已在单测覆盖，sub-runner 嵌套尝试会降级为 "tool not found" 重复证明无价值。13 套 pipeline 全绿 ~4 分钟 |
| 2026-05-06 | **[doc]** D4-6 文档同步收尾：subagent.md §13 测试覆盖表更新（71 测试实际数 vs 设计稿 38，含层细分）+ V1 范围说明（不写嵌套递归 pipeline）；4 契约表 📐→✅（api-design subagent 段 / database-design subagent_runs+messages / error-codes 加 SUBAGENT_RUN_NOT_FOUND / events-design ChatMessage 行）；backend-design.md domain/app 树 5 处 📐→✅。**D4 整组完成**——LLM 现在能在主对话调 Subagent(Explore/Plan/general-purpose) spawn 子 LLM loop，独立 context，sub-runner 进展通过 chat.message 嵌 subagentRun 快照实时回流到对话 SSE。Phase 4 准备件之 #1（subagent）就位 |
| 2026-05-06 | **[feat]** D5-1 domain/mcp + 10 sentinels：`internal/domain/mcp/mcp.go`（ServerConfig 含 TimeoutSec / ServerStatus 含 ConsecutiveFailures+TotalCalls+TotalFailures 用于 degraded 触发 / ToolDef / HealthResult）+ 5 status const（disconnected/connecting/ready/degraded/failed）+ IsCallable helper + 10 sentinels（5 runtime + 5 Registry-flow）。errmap 全 10 行接（404/409/422/502/504 按 mcp.md §11）。8 单测（JSON round-trip / OmitEmpty Claude Desktop 兼容 / IsCallable 全态 / sentinel 唯一性）|
| 2026-05-06 | **[feat]** D5-2 RegistryEntry 类型 + V1 marketplace 6 内置 + GOOS filter：`internal/domain/mcp/registry.go`（RegistryEntry/InstallCmd/PostInstallStep/EnvRequirement/ArgRequirement 类型）+ `internal/app/mcp/registry.go`（V1 内置 6 server：playwright + chromium 后装 / markitdown / context7 OnlineOnly / duckduckgo-search / sqlite + dbPath 模板 / everything Hidden）。Registry.Get 返含 Hidden / Visible() drop Hidden + UnsupportedPlatforms 含 GOOS。10 单测验证 6 内置形状 + 各 server 关键字段 + Hidden 过滤 + GOOS 过滤合成注入 |
| 2026-05-06 | **[feat]** D5-3 ~/.forgify/mcp.json I/O：`internal/infra/mcp/config.go` Load/Save/Merge（Claude Desktop 兼容 schema 让用户配置直接粘贴可用）。Save atomic tmp+rename + 0600 权限（env 含凭证）+ alpha 排序 key + POSIX 末换行（diff 跨次稳定）+ auto mkdir 父目录。Load 文件不存在返空、损坏 wrap ErrConfigCorrupt（mcp.md §5.7：Service.Start 不能 panic）。Merge 默认非覆盖 + Conflicts 列表给前端弹"已存在确认"。17 单测：Load 4 路径 / Save round-trip+排序+权限+原子+无 Name 重复 / Merge 4 路径+Name 从 key 印 |
| 2026-05-06 | **[doc]** D5-4 文档收尾（部分翻 ✅）：mcp.md 状态 📐→🔄（D5 完 + D6 待）；error-codes mcp ×10 全翻 ✅ + 注脚（runtime sentinel 触发点在 D6 接通）；api-design mcp 节 📐→🔄 含 D5 已落地清单；backend-design domain/mcp ✅、app/mcp 🔄、infra/mcp 🔄。**D5 整组完成**——MCP 基础（domain types + 内置 marketplace + 配置 I/O）就位，35 单测全绿。D6 接 stdio Client + Service + system tools + HTTP + pipeline，让 LLM 真正能在主对话 search_mcp + call_mcp |
| 2026-05-06 | **[feat]** D6-1 stdio Client wrapper：`internal/infra/mcp/client.go` 包 `modelcontextprotocol/go-sdk` v1.6 的 CommandTransport（SDK 自处理 SIGTERM→5s→SIGKILL）。Client 接口 5 方法（Initialize/ListTools/CallTool/Close/StderrTail）+ 项目特有 stderr→zap.L().Named("mcp.<server>")+256KB ringBuffer + Tool/Content→domain 类型转换 + ctx.Done 区分超时与其他 RPC 错。joinContent 把多模态展平（image/audio/resource_link 渲染占位符待 V2 inline）|
| 2026-05-06 | **[feat]** D6-2 Service runtime：`internal/app/mcp/{mcp,calltool,install}.go` Service{Start/Stop/Add/Remove/Reconnect/Search/CallTool/HealthCheck/InstallFromRegistry/Import}，单 RWMutex 守 configs/states/clients 三 map。`recordCallResult` 实现 §5.6（连续失败≥3→degraded / 成功后 wasDegraded→ready 自愈），`resolveCallTimeout` 实现 §5.7 precedence（ServerConfig > RegistryEntry > 30s）。Search 用 forge.search 模式 A（≤topK 全返跳过 LLM；> topK 走 LLM ranking + JSON 解析失败回退字母序前 K）。`SetClientFactory` test seam 让单测注入 fake Client 不起子进程 |
| 2026-05-06 | **[feat]** D6-3 search_mcp + call_mcp 系统工具：`internal/app/tool/mcp/{mcp,search,call}.go` 9 方法实现 + DI 接 main.go/harness。call_mcp 内嵌 `mapCallToolErrorToFriendly` 把每个 mcpdomain sentinel 翻成对 LLM 可读字符串（ServerNotFound→"please call search_mcp first" / ToolNotFound→列已知 server.tool / Timeout→建议 retry 或调小输入）。**关键设计**：不 flat 注册 N×M tools 进 LLM tool registry——避 70+ server × 5 tool × 200 token ≈ 70k 启动开销 |
| 2026-05-06 | **[feat]** D6-4 HTTP 10 端点：`handlers/mcp.go` (~330 行) 10 端点全实现（servers GET/PUT/DELETE/{name} + import + reconnect + health-check + registry list/get/install）。stdlib mux 局限workaround：`/api/v1/mcp-servers:import` 因 `:` 前无 `/` 不能 wildcard，注册为 literal path；其他 `:action` 走 `{nameAction}` wildcard + splitAction（同 sandbox.go 模式）。20 httptest 覆盖 happy + error + import multipart/JSON + conflict overwrite |
| 2026-05-06 | **[feat]** D6-5 pipeline + fake stdio MCP server：`backend/test/mcp/fakeserver/main.go` ~70 行 go-sdk 写的最小 stdio server（echo / fail / crash 3 tool 覆盖happy/失败计数/子进程死全码径）。TestMain 一次性 build。`mcp_test.go` 4 离线场景（happy 闭环 / BadCommand→failed / 3 fail→degraded→1 success→ready 自愈）+ 1 Live_ 装 everything 双门控（sandbox.IsReady() + `FORGIFY_LIVE_MCP_INSTALL=1`）。3.6s 跑完 |
| 2026-05-06 | **[doc]** D6-6 文档收尾：mcp.md 状态 🔄→✅；events-design `mcp` 行 📐→✅ + 触发点细化（Service.publishSnapshot 调用点 + HealthCheck 不发的设计理由）；api-design mcp 节 🔄→✅；backend-design domain/app/infra/tool 四处 mcp 树 全翻 ✅。**D6 整组完成**——MCP 端到端（domain types + 内置 marketplace + 配置 I/O + stdio Client + Service + 2 system tools + 10 HTTP + 5 pipeline 场景）就位，88 单测全绿 + cross-platform clean。下一步 D7 skill |
| 2026-05-06 | **[feat]** D7-1 domain/skill + 5 sentinels：`internal/domain/skill/skill.go` Skill / Frontmatter（Anthropic SKILL.md spec 全字段保留 cross-vendor，V1 消费 7 字段，V1 仅解析另 6 字段）+ 5 sentinels（NotFound/InvalidFrontmatter/BodyTooLarge/NameConflict/InvalidName）+ MaxBodyBytes (32 KiB) / MaxDescriptionChars (1536) 常量。errmap 全 5 行接（404/422/422/409/422）。6 单测（YAML 全 spec round-trip + 最小必填 + Skill JSON camelCase + sentinel 唯一 + 'skill:' 前缀审计 + 常量校验）|
| 2026-05-06 | **[feat]** D7-2 agentstate ActiveSkill + matchAllowedTool：`internal/pkg/agentstate/skill.go` (~190 行) 给 AgentState 加 `activeSkill atomic.Pointer[skilldomain.Skill]` + SetActiveSkill / ActiveSkill / ClearActiveSkillIfMatches / IsToolPreApprovedBySkill。matchAllowedTool 3 form：bare name / Bash 任意 args / Bash(wildcard)；malformed pattern fail-closed；paren-form V1 仅 Bash（V2 加 per-tool extractor）；wildcardMatch 单 `*` 多段 anchored。10 单测（含并发 LastWriteWins / 坏 args JSON 不 panic）|
| 2026-05-06 | **[feat]** D7-3 app/skill Service：`internal/app/skill/{skill,scan,activate,search}.go` (~620 行) Service{Scan/Get/List/Search/Activate}。Scan walks ~/.forgify/skills/*/SKILL.md，per-skill 错误 log+跳过（一个坏 skill 不静默 catalog）；splitFrontmatter UTF-8 BOM + LF/CRLF + EOF-fence；validateFrontmatter description-non-empty + length cap + fork-requires-agent。Activate body 不缓存（每次重读 + 100ms ErrNotExist 重试覆盖编辑期 race）；substitute \$1..\$N + \$ARGUMENTS + \$<name> + \${CLAUDE_*}；fork 模式 depth ≥ 1 抑制嵌套（§9.5）。SubagentService 接口注入避循环 import。Skill SSE 事件加进 events/types.go。20 单测 |
| 2026-05-06 | **[feat]** D7-4 fsnotify watcher：`internal/app/skill/watcher.go` (~210 行) 递归 watch ~/.forgify/skills/ + 子目录；500ms debounce 收 burst；EvalSymlinks + visited-set 防 symlink 循环；Linux ENOSPC fail-soft；fsnotify init 失败回 5min poll；fsnotify 健康时仍并行 5min backstop poll 兜 NFS/容器 fs 丢事件场景。SSE 'skill' 由 Service.Scan 发，watcher 不发（单一事实源）。fsnotify v1.9.0 promote indirect→direct。6 单测（DetectsNew/Edit/Delete/SymlinkLoop guard/EmptySkillsDir-fails/NilLogOK）|
| 2026-05-06 | **[feat]** D7-5 search_skills + activate_skill 系统工具 + DI：`internal/app/tool/skill/{skill,search,activate}.go` (~430 行) 9 方法实现 + DI 接 main.go/harness。activate_skill 注 IsReadOnly=false（写 ActiveSkill 旁路，与 state-mutating tool 同 execution_group 串行）。Friendly-string 错误映射 ErrSkillNotFound→建议 search_skills 先 / ErrBodyTooLarge→建议缩 SKILL.md。defaultSkillsDir 同 defaultMCPConfigPath fallback 模式。12 单测 |
| 2026-05-06 | **[feat]** D7-6 framework permission integration：`internal/app/loop/tools.go::executeTool` 在 ValidateInput 后 CheckPermissions 前加 agentstate.IsToolPreApprovedBySkill 短路。集中 framework dispatch 层（§9 line 385：避免改每个 Tool）；新 tool 自动继承预授权机制。Refactor: Execute 抽 executeAfterPermission helper 让两条 permission 路径共用 post-permission code。3 单测（NoActiveSkill 仍走 CheckPermissions / ActiveSkill 预授权 permChecks=0 / NoMatch 退回 CheckPermissions）|
| 2026-05-06 | **[feat]** D7-7 HTTP 9 端点：`internal/app/skill/{mutate,import}.go` (~330 行) Service.Body/Create/Replace/Delete/Import + nameRegexp 校验 + atomic 写（.tmp+rename 防并发 Scan 见半截）+ ImportResult{Imported/Conflicts/Errors} per-row outcome。`handlers/skills.go` (~310 行) 9 端点（List/Get/GetBody/Create/Replace/Delete/Refresh/Import/Invoke）；stdlib mux quirk 同 mcp 模式（literal :import + :refresh / {nameAction} wildcard）。V1 import 限：服务端只接 SKILL.md 直传，ZIP/tar/folder client-side 拆完 POST（V2 服务端 archive 解析）。20 httptest |
| 2026-05-06 | **[feat]** D7-8 pipeline 3 场景：`backend/test/skill/skill_test.go` 全 FakeLLM 离线驱动 chat → loop → tool dispatch 端到端：Activate inline E2E（\$1 substitution + body 进 tool_result）/ Search-then-Activate E2E（双 tool_call 配对 + result 含 substituted body）/ PreApproval Bash after Activate（D7-6 端到端：log 抓到 'tool pre-approved by active skill'，Bash 实际跑出 echo 输出，subagent_runs=0 sanity）。<2.5s 跑完。94 单测全绿 + 3 pipeline 全绿 |
| 2026-05-06 | **[doc]** D7-9 文档收尾：skill.md 状态 📐→✅ + §14 测试覆盖重写为实际矩阵（80 单测 + 3 pipeline）；events-design `skill` 行 📐→✅ + 触发点细化（Service.publishSnapshot 在 boot/fsnotify/poll-backstop/refresh/Create/Replace/Delete/Import 后调）+ 模型小段 "6 个事件全部 ✅"；api-design skill 节 📐→✅ 含 D7 全交付清单；error-codes skill 5 行 📐→✅ + 注脚（fork-requires-agent 加进 InvalidFrontmatter 触发点；allowed-tools 校验未注册 tool 推迟 V2，boot 顺序 race 设计原因）；backend-design domain/app/tool/skill 三处树全翻 ✅。**D7 整组完成**——Skill 端到端（domain + agentstate + Service + watcher + 2 system tools + framework integration + 9 HTTP + 3 pipeline）就位，下一步 D8 catalog |
| 2026-05-06 | **[feat]** D8-1 domain/catalog + 2 sentinels：`internal/domain/catalog/{catalog,source}.go` Catalog（Summary + Coverage + Fingerprint + GeneratedAt + Version + SourcesAt + GeneratedBy）+ CatalogSource port (Name + Granularity + ListItems) + Granularity 枚举（PerItem=0 是新 source 安全默认 / PerServer / PerCollection）+ Item + SystemPromptProvider port (chat 注入接口) + 2 sentinels（ErrCoverageIncomplete / ErrGenerationFailed 内部消化不进 errmap）。5 单测：JSON 全 7 字段 round-trip + Granularity String() + 枚举值 pin + Item omitempty Category + sentinel 唯一性 + 'catalog: ' 前缀审计 |
| 2026-05-06 | **[feat]** D8-2 app/catalog Service + pollLoop + cold-start cache：`internal/app/catalog/{catalog,polling,disk,mechanical}.go` (~620 行) Service{Start/Stop/Refresh/RegisterSource/GetForSystemPrompt}。并发模型：cache atomic.Pointer 单写多读零锁；lastFP atomic.Value；busy atomic.Bool 单 flight 守 Refresh；sources sync.RWMutex（writer-only）。Start 加载 disk cache（cold-start 优化）；pollLoop 每 1s tick + 启动立即一次。Refresh 走 sources（per-source 失败隔离）→ fingerprint 短路（~99% tick）→ Generator nil 或失败 → mechanicalFallback。lastFP 总更新 § 3 用户活动驱动重试。disk cache 原子写（.tmp+rename）+ 损坏移 .bak。18 单测 |
| 2026-05-06 | **[feat]** D8-3 LLMGenerator + 3-attempt retry：`internal/app/catalog/generator.go` (~210 行) 实现 Generator 接口让 Service 接 LLM。每 attempt：buildPrompt（含 missingHint 重试）→ llmGenerate → 字符上限 ~10 KB → extractJSON → 解码 → coverage 校验。覆盖未漏（extras 允许：LLM 可合并/改名）。LLM 传输失败不内部重试（同 client/key 同样失败）→ 冒泡 ErrGenerationFailed。3 attempt 用尽 → ErrCoverageIncomplete → Service.Refresh 切 mechanical。V1 简化：用 llmclient.Resolve 取 chat scenario，多 key 轮训推迟 V2（单人开发典型 1 key 等价）。8 单测 |
| 2026-05-06 | **[feat]** D8-4 3 CatalogSource + DI 装配：forge / skill / mcp 各加 catalogsource.go 实现 CatalogSource port + AsCatalogSource() 方法。Granularity 矩阵 §12：forge=PerItem / skill=PerItem / mcp=PerServer。forge.ListItems 用 Forge.Description（创建时 LLM 生成），空退化 simplifyTagsJSON；skill.ListItems 用 frontmatter.description；mcp.ListItems 跳过非 ready/degraded server（V1 契约）+ 合成 per-server description（前 3 tool 名+desc，maxToolsInDescription=3）。main.go + harness 装配：catalog Service + LLMGenerator + 3 source RegisterSource + Start |
| 2026-05-06 | **[feat]** D8-5 chat runner SystemPromptProvider 集成：`chat.Service.catalog catalogdomain.SystemPromptProvider` 字段（nil-tolerant）+ SetSystemPromptProvider setter。`buildSystemPrompt` 在 base intro 与 locale hint 之间插 catalog summary（仅 provider 非 nil 且 GetForSystemPrompt 非空时——空跳防 boot 窗口空行泄漏）。main.go + harness 调 chatService.SetSystemPromptProvider(catalogService)。5 chat 单测：NilProvider 跳 / EmptyProviderText 跳 / NonEmpty 注入正确顺序 / per-conv SystemPrompt 独立 / setter 真 mutate |
| 2026-05-06 | **[feat]** D8-6 HTTP 2 端点：`handlers/catalog.go` (~80 行) 仅 2 字面路径端点（catalog 多为内部组件）。GET /api/v1/catalog 返当前缓存（null in envelope 当 cache 未构造）；POST /api/v1/catalog:refresh 强制立即 Refresh 返结果（让 UI "立即刷新" 无需再 GET）。Wiring：Deps.CatalogService nil-tolerant 注册。4 httptest：Get empty / Refresh builds + 返 / Get after Refresh / 短路 unchanged fingerprint |
| 2026-05-06 | **[feat]** D8-7 pipeline 3 场景 + 关键 bug 修：`backend/test/catalog/catalog_test.go` 全 FakeLLM 离线驱动 forge/skill/mcp → catalog → chat 端到端：AllSourcesCovered（Coverage + Summary + GetForSystemPrompt）/ ForgeDescriptionChange triggers regen（Version + Fingerprint + Summary 都变）/ NoLLMKey FallsBackToMechanical（mech-fallback + 第二次 no-op Refresh 短路）。**实施发现 bug**：catalog 在后台 goroutine 跑（无 HTTP middleware ctx）→ source ListItems + LLMGenerator 都 fail 'reqctx: missing user id'。修复：Service.Refresh 入口注入 DefaultLocalUserID（单人 app 安全），一处覆盖全 pipeline。harness catalog Start 改用 cancellable ctx 绑 t.Cleanup 防 goroutine 跨测试泄漏 |
| 2026-05-06 | **[doc]** D8-8 文档收尾：catalog.md 状态 📐→✅ + §11 测试覆盖重写为实际矩阵（40 单测 + 3 pipeline）+ 记 "user-id 注入 bug" 实施发现；api-design catalog 节 📐→✅ 含 D8 全交付清单；backend-design domain/catalog + app/catalog 两处树全翻 ✅ + Service 描述展开。error-codes catalog 已记 "内部消化"（无需 flip）；events-design catalog "不发 SSE"（无需 flip）。**D8 整组完成**——Capability Catalog 端到端（domain + Service + LLMGenerator + 3 source + chat 注入 + 2 HTTP + 3 pipeline）就位。100 单测全绿，跨平台 clean，staticcheck clean。下一步 D9 集成 demo |
| 2026-05-06 | **[feat]** D9 跨切集成 + 2 harness 修：`backend/test/integration/d9_test.go` (~250 行) 3 离线场景：(1) CatalogReachesLLM——seed forge + skill → drive chat turn → 断言 fake.LastSystemPrompt() 真含 catalog 块（关上 D8 单测留的 "GetForSystemPrompt 返对但有没有进 wire" gap）；(2) DynamicSkillUpdate——atomic rename SKILL.md 进 watched root（避 mkdir-then-write 竞态丢内部 Create 事件）→ 等 fsnotify debounce → trigger Catalog.Refresh → 断言新 skill 进 Summary + fp 变；(3) BootSmoke——投资人 demo 前 confidence check：每个 service handle 非 nil + 24 个 tool 家族都注册 + Catalog.Refresh 出非空 Summary + MCP.ListServers 返空 slice。**2 个 harness 修复**：FakeLLMServer 加 LastSystemPrompt() 捕 role:system 内容（让 pipeline 测试能验真到 LLM 看到啥）；harness skillWatcher 启动 + skills 根 mkdir 预建（之前 main.go 启 watcher 但 harness 没启，pipeline 无 fsnotify 路径） |
| 2026-05-06 | **[doc]** D9-4 文档收尾：progress-record 加 D9 dev log；backend-design Phase 路线图加 "Phase 4 准备件 (D2-D9) ✅ 2026-05-06" 行 + Phase 5 备注 mcp 已提前交付。**D9 整组完成**——Phase 4 准备件（sandbox v2 + subagent + mcp + skill + catalog）端到端集成验证就位，100 单测全绿 + 3 D9 pipeline 全绿。Phase 4 工作流真启动前的所有准备件齐了。下一步 D10-D15 Windows runtime 适配 |
| 2026-05-06 | **[fix]** Pipeline 跑全套发现 2 个 D8 引入的回归并修：(1) catalog 后台 polling 调 Generator 抢 FakeLLM 脚本队列让 test/chat 挂——harness 不接 Generator,catalog 走 mechanical fallback;(2) catalog goroutine 与 t.TempDir cleanup 竞态让 test/cross 挂"directory not empty"——加 Service.Stop() 同步 drain。生产 main.go 完全不动,只 harness + Service.Stop API。第三次完整 pipeline 跑全绿（17 套全 ok,8 分钟）|
| 2026-05-06 | **[feat]** D10+D11 Windows 代码层硬化:bash.go 删"out of scope"道歉、把 Windows 升为一等公民（cmd.exe /c 故意不用 PowerShell + LLM-facing description 说明跨平台行为）;8 个 bash_test.go skip 消息从"Unix shell semantics — Windows out of scope"改"pending real Windows test environment"。MCP marketplace 6 entries 全部审 Windows 兼容（playwright/markitdown/context7/duckduckgo/sqlite/everything）——结论全兼容,无 UnsupportedPlatforms 需填,审计理由记 registry.go 包文档 |
| 2026-05-06 | **[verify]** D12 mise Windows binary embed 验证全套就绪:`embed_mise_windows_amd64.go` 用 `windows && amd64` build tag + `go:embed mise/windows-amd64/mise.exe`（92 MB on disk）+ `miseExeName()` 返 `mise.exe` + `cmd/resources` 知道 windows-amd64 用 `.zip` 归档 + `extractZip` 提取 mise.exe。GOOS=windows go build 干净通过 |
| 2026-05-06 | **[doc]** D13 Windows 平台支持文档:在 `desktop-packaging-notes.md` 加第十二节,含 "代码层就绪情况"表格（10 个模块各自 Windows 状态）+ 用户家目录 layout（%USERPROFILE%\.forgify\）+ Bash 在 cmd.exe /c 下的行为 + `wails build -platform windows/amd64 -nsis` 打包命令 + "当前缺什么"5 项待真 Windows 机验清单 + 打包前流程 |
| 2026-05-06 | **[feat]** D14 `make check-cross` Makefile target:5 平台（darwin/linux/windows × amd64/arm64）vet + build 一键跑。CGO_ENABLED=0 给非 host 平台（cgo 会让 Mac SDK linker 错对 Linux/Windows 头文件——modernc.org/sqlite 纯 Go 选型让此行得通）。本地通过 ✓ |
| 2026-05-06 | **[doc]** D15 Phase 4 准备件 + Windows 代码层全交付收尾:backend-design 路线图加 "Windows 代码层适配 (D10-D15) ✅ 2026-05-06" 行(注明真 Windows runtime 验证待物理机)；progress-record 6 条 dev log。**整组完成**——5 平台代码层 vet/build 干净,Wails Windows 打包文档齐,真机验证清单留好。下一步 Phase 4 工作流真启动 |
| 2026-05-07 | **[fix]** TE-15 forge run 路径 3 项打包修：(1) `ErrEnvNotReady` 拆分——新增 `ErrNoActiveVersion` sentinel + `FORGE_NO_ACTIVE_VERSION` 错码；原 `ErrEnvNotReady` 收窄回"venv 状态非 ready"，原文案"forge: env not ready"误导 LLM 把"未 accept pending"当成 venv 问题。(2) Run 类方法统一守卫——抽 `ensureRunnable(ctx, forgeID)` helper，`RunForge` / `RunTestCase` 共享同一 (ActiveVersionID + EnvStatus) 双检查；之前两者各写一份且仅查 ActiveVersionID。(3) 首次创建 auto-accept——`CreatePending` 检测到 `f.ActiveVersionID == ""` + post-sync EnvStatus=ready 时自动 AcceptPending；edit_forge 流仍走手动 accept（diff review 有价值）。新增测试 `TestCreatePending_FirstCreate_AutoAccepts`；rename `TestRunForge_DraftRejected` → `TestRunForge_NoActiveVersion`；改 `TestRejectPending_DraftFirstPendingDeletesForge` 用 `syncFunc` 注 sync 失败绕过 auto-accept。100/100 单测包绿 |
| 2026-05-07 | **[perf]** TE-16 长流式消息不再打死浏览器（3 项一组）：(1) 后端 `loop/stream.go` 给 `publish()` 加 60fps 节流（16ms），避免 7000-token reasoning 推 7000 次 ~10KB snapshot；EventToolStart 走立即推（低频里程碑），stream 结束前若有 pending 强制 flush。(2) 前端 chat.js SSE handler 改 rAF batch——`_pendingMsgs` Map 累积 + `requestAnimationFrame` 下一帧统一 apply；同 messageId 后到的覆盖前到的（中间帧用户看不到）；`_closeSSE` 取消 rAF + 清缓冲防止跨对话残留。(3) 前端 reasoning + tool step 默认折叠——`expanded` per-block state，`x-if` 控制大体内容（不是 `x-show`，DOM 节点根本不存在），summary 行带 toolName / summary / status / elapsed 信息密度足够。组合效果：现状 ~50MB SSE/对话 → ~15MB；render 6940 次 → ~1800 次；单次 render DOM 量降到 ~20%。**架构说明**：当前仍是 entity-snapshot 模型（每事件推完整 message snapshot），没切 delta —— 单用户本地 Forgify 体量 ABC 够用一两年，delta 化（D）留到真前端 / subagent 多并发 / 长跑会话时再做。100/100 单测包绿 |
| 2026-05-07 | **[refactor]** 屎山拯救计划 #1：skill watcher fsnotify → 1s 轮询。删 `app/skill/watcher.go`(271 行) + `watcher_test.go`，加 `polling.go`(~80 行) 复用 catalog/polling.go 同模子（Service.Start/Stop + 1s ticker）；Scan 内加 sha256 fingerprint(排序后 name+frontmatter YAML) 短路 publishSnapshot 避免每秒一次 SSE 噪音。fsnotify 从 direct → indirect dep（仅 air dev tool 拉）。同步改 main.go / harness / handler+router/domain 注释 6 处 + skill.md 8 处。净 ~-200 行 + 删 4 类边界条件（symlink loop / Linux fd 上限 / debounce / 5min 兜底）。go build + staticcheck + make test-unit + GOOS=windows/linux 全绿 |
| 2026-05-07 | **[fix]** TE-22 reasoning-only assistant 消息致后续轮 LLM 400 锁死对话。根因：`loop/history.go::BlocksToAssistantLLM` 转换时 reasoning block → ReasoningContent / text block → Content，DeepSeek V3.x reasoning 模式偶发把整段回复放 reasoning_content 而 content 为空，下轮 history 含此空 assistant message → DeepSeek API 拒（"Invalid assistant message: content or tool_calls must be set"）。修：转换器末尾兜底 —— 仅有 reasoning + 无 content + 无 tool_calls 时把 reasoning_content 拷给 content，丢 wire 区分胜于 400 锁死。新增 3 测试覆盖：reasoning-only promote / reasoning+text 不 promote / reasoning+toolcall 不 promote。100/100 单测包绿 |
| 2026-05-07 | **[refactor]** TE-23 引入 `infra/llm/Adapter` 抽象集中 provider 级 wire 适配；同时把 TE-22 fallback 归位 + 修 2 个 OpenAI baseline bug。**Adapter 层**：`infra/llm/adapter.go` 新建，11 个 provider 实现（openai/anthropic/google/deepseek/openrouter/qwen/zhipu/moonshot/doubao/ollama/custom/mock）+ `lookupAdapter(name)` + `adapterWrappedClient`；factory 用 Adapter.DefaultBaseURL 替代之前散在 factory.go::resolveBaseURL 的 switch（与 apikey/providers.go 表去重）。**TE-22 归位**：reasoning-only fallback 从 `app/loop/history.go::BlocksToAssistantLLM` 撤回（恢复为纯 schema 转换器）→ 搬到 `openai.go::buildOpenAIAssistantMsg`，对 Anthropic 路径不再误污染。**Baseline bug #1**：assistant content 即使空也强制 emit `""`（之前 `if m.Content != ""` 结合 `omitempty` 让 content 字段被 omit → 严格 provider 拒 `content:null`）。**Baseline bug #2**：OpenAI client SSE chunk 顶层 `error` 字段检测——OpenRouter mid-stream error 抵达时 HTTP 锁 200 但 chunk.error 填充，之前流静默终止用户看不到原因；现 fire EventError。**新发现**：Gemini base URL `https://generativelanguage.googleapis.com` 没带 `/v1beta/openai/` 路径，OpenAI client 拼出 404，本次 `geminiAdapter.DefaultBaseURL` 修。新加 ~10 测试覆盖 adapter 路由 + 3 个 reasoning fallback（搬到 openai_test.go）+ content always emit + OpenRouter SSE error。100/100 单测包绿 |
| 2026-05-07 | **[fix]** TE-24 收尾 LLM provider 兼容 audit 剩余项 + 确立 §S20「禁止留问题无理由」原则。**问题清理**：audit 表里之前列"⏳ 待修"的 4 项重审——#1 Anthropic thinking round-trip 与 #3 Moonshot 温度 clamp 实为「当前 0% 触发」（前者未启用 extended thinking、后者根本不暴露温度参数），从风险表删而非挂着。**真 bug 全修**：(2) Ollama tool_calls.index 缺失 → 加 `toolCallState.resolveIndex` 兜底，按 tool ID 合成 index 避撞键；(4) Ollama OpenAI-compat 在 streaming+tools 下静默吞 tool_calls（ollama #12557/#9632）→ Request 加 `DisableStream` 字段，OpenAI client 加非流式接收路径 `parseOpenAINonStreaming`，`ollamaAdapter.BeforeRequest` 在 `len(Tools)>0` 时自动关流式。**CLAUDE.md 加 §S20**：发现 bug 默认必须当场修，"留下次"必须同时具备（a）结构性硬约束，（b）当场清晰说明，否则禁止；触发场景明示——风险表"⏳ 待修"、任何"以后再说"措辞、"知道在哪知道怎么修就行"自我安慰式收尾。新加 4 测试覆盖 Ollama index 兜底 + 非流式响应解析 + Ollama adapter DisableStream 行为 + 其它 adapter 不误关流式。100/100 单测包绿 |
| 2026-05-07 | **[fix]** TE-25 二轮 audit 真陷阱修复（聚焦"会永久死锁对话"类）。**真 bug**：(1) **Orphan tool block sanitizer**——`infra/llm/sanitizer.go` 新建 `SanitizeMessages` 强制 `assistant.tool_calls ↔ tool message` 配对不变量；缺 tool message 合成 stub `[interrupted: tool call did not complete]`，游离 tool message 丢；OpenAI + Anthropic client 的 `buildXBody` 入口都调用，防 ESC 取消 / backend 崩 / DB 脏数据导致整段对话永久 400 锁死（Claude Code 真实事故 #14132/#10693/#40305）。(3) **DeepSeek turn-type reasoning round-trip**——`deepseekAdapter.BeforeRequest` 按 turn 类型分支：纯文字 turn 剥 `reasoning_content`（不剥 400），含 `tool_calls` turn 保留（V3.2+ 不留 400 "Missing reasoning_content"，规则在 V3.2 反过来）。**留下次 + 理由**：(2) Anthropic 5MB 图片守门 — 真正修复属未来上下文优化层（图片 resize / 摘要替换），wire 层加守门会与优化器双处理；按 §S20 (a)+(b) 当场说明留下次。新加 9 测试（sanitizer 7 + DeepSeek roundtrip 2 + 1 兜底覆盖）。100/100 单测包绿 |
| 2026-05-07 | **[refactor]** 屎山拯救计划 #3：subagent host 三方法去复制粘贴。`subagent/host.go::Publish/WriteCheckpoint/WriteFinalize` 原各 ~30 行干同样 4 步（懒建 streaming row / refine / refresh / publish），仅 ctx / log 等级 / 是否 UpdateMessage / status 字符串 4 处真差异——抽 `ensureStreamingRow(ctx, blocks, in, out, persistRefine, fatal) bool` + `logPersistErr(msg, err, fatal)` 两个 helper，3 公开方法各收成 ~10 行的"拼参数 + 调 helper + 各自后续"。Host 接口不动（loop.Run 阅读流仍清晰）。行数 +9（注释展开占大头），但建/refine 逻辑现在**只有一处**——加新字段 / 改持久化策略只改一个 helper，不再三处同步。go build + staticcheck + subagent/loop/chat 单测 + GOOS=windows/linux 全绿 |
| 2026-05-07 | **[refactor]** 屎山拯救计划 #4：WebSearch SearXNG 公共池 + Bing 国际抓取 → BYOK + MCP + Bing CN 三层路由。删 SearXNG 4 实例池 + Bing 国际 HTML 抓 + `FORGIFY_SEARXNG_INSTANCES` env + `defaultSearXNGInstances` / `resolveSearXNGInstances` / `runSearXNG` / `querySearXNG` / `runBing`；保留 search_bing.go 的 parseBingHTML（Bing CN 复用）。**新架构**：(1) **BYOK** —— 4 个搜索 provider 加进 apikey 域（brave / serper / tavily / bocha），共享 `TestMethodSearchPing` 探测，新加 `Category=CategorySearch` 字段让前端 LLM/搜索分组；search_byok.go 新建 4 个 client（每个 ~30-40 行）。(2) **MCP 路由** —— `MCPSearchRouter` 端口在 web 包定义；`app/mcp/searchrouter.go` 适配器检查 hardcoded `duckduckgo-search` server 是否 ready 并 CallTool；端口反转避免 web ↔ mcp 互相 import。(3) **Bing CN 兜底** —— scrapeBingCN 国内国际通用，零配置零 key。**主要变化**：apikey 域 +1 Category 字段 + 4 search providers + tester 4 个 testXxxPing；domain/apikey 加 SearchProviderPriority；main.go MCP service 构造提前到 WebTools 之前让 SearchRouter 注入；harness.go 测试不接 MCP 路由（nil router）。**统计**：净 ~-200 行 + 删一类公共池维护 + 删 HTML 抓取脆弱面（仅保 Bing CN 一份）+ 保护国内用户（duckduckgo / bing 国际版都被墙，bocha + bingCN 是国内可用兜底）。新加 11 测试覆盖 BYOK 路由 + MCP 路由 + 优先级 + fallthrough + Bing CN 兜底 + MCP 解析器 + provider 注册表完整性。100/100 单测全绿，staticcheck clean，跨平台 windows/linux 编译过 |
| 2026-05-07 | **[feat]** 屎山拯救计划 #4 收尾：testend 接入 GET /providers。后端加 `transport/httpapi/handlers/providers.go` 一个端点 `GET /api/v1/providers[?category=llm\|search]` 返 ProviderMeta 注册表（name / displayName / category / defaultBaseUrl / baseUrlRequired），4 个 handler 测试覆盖（含分类过滤 + 排序 + 未知 category 返空）。testend tab-config.js 删硬编码 12 项 allProviders 数组改成 init 时 fetch /providers 缓存 providersMeta；llmProviders() / searchProviders() 计算属性给 add-key 下拉做 optgroup 分组；okProviders() 按 category=llm 过滤避免搜索 key 误进 chat model picker；needsURL() 改读 providersMeta 的 baseUrlRequired 字段（之前是 hardcoded ollama/custom）。tester.html 加 `<optgroup label="LLM"/Search">` 让 16 个 provider 视觉分两组。前端不再两边硬编码——加 provider 只改后端。同步 api-design.md 加端点行 |
| 2026-05-07 | **[fix]** 屎山拯救计划 #4 第二次收尾：删 Bing CN HTML 抓取这层"假兜底"。dogfood 实测发现 `cn.bing.com/search` → 301 重定向到 `www.bing.com/?q=...` 主页（不是搜索结果页）；www.bing.com/search 也只返页面 chrome（0 个 b_algo 块）—— modern Bing/Bing CN 全 JS 渲染，HTML 抓取无论用啥 UA 都返 0 命中。此前 #4 第一轮收尾保留 Bing CN 是基于"前作者保留它就是因为有用"的假设，没实测；扛着这层伪兜底比删了更糟（用户期望落空，且代码中暗示"有兜底"）。删 `app/tool/web/search_bing.go` (174 行 parseBingHTML + helpers) + search.go 里 scrapeBingCN/bingCNURL/browserUA/io+url import；WebSearch.Execute 收成 BYOK → MCP 两层；失败消息升级成 LLM-actionable 多行提示（配 BYOK key 或装 duckduckgo-search MCP，附"为啥不再有 Bing CN"原因解释）。测试同步：删 sampleBingHTML/parseBingHTML 三个测试 + Bing 相关 helper；BingCN/AllFail 测试改为 NoBackends/MCPUnavailable。同步 web.md 4 处 + dev log。新增 LLM 工具 `list_mcp_marketplace` + `install_mcp_server` 让 LLM 失败时能引导用户装 duckduckgo-search 留作下次（待用户确认实现细节）。100 单测全绿 staticcheck clean GOOS=windows/linux 编译过 |
| 2026-05-08 | **[feat]** Sandbox 加 Docker runtime 支持（marketplace V2 准备件）。新加 `infra/sandbox/docker.go`：DockerInstaller 探活 daemon（`docker version --format {{.Server.Version}}`）+ DockerEnvManager（CreateEnv mkdir host 目录，InstallDeps `docker pull <image>`，EnvBin 返 "docker"）+ BuildDockerRunArgs helper（拼 `run -i --rm -v env:/workspace -e ... image args` 给调用方）。Forgify **不替用户装 Docker**——是系统服务要 root/admin（Mac/Win Docker Desktop ~1.2 GB GUI + 内嵌 Linux VM，Linux dockerd systemd），探活失败返 2 个新 sentinel `ErrDockerNotInstalled` / `ErrDockerDaemonDown` + 平台对应安装/启动链接。registerSandboxStack 注册新 installer + env manager（main.go 加 log 入参）。**默认安全**：仅 envPath 自动 bind 挂载（防 LLM 通过 docker MCP 偷 host 文件）；network bridge 默认；无资源限制；image 永不自动删（卸 server 也不删，可能复用）；`-i --rm` stdio 容器生命周期。新加 17 单测覆盖 Identity / no-op / arg 装配 / 平台引导。**与 marketplace V2 衔接**：下一轮 marketplace adapter 处理 oci/docker package 时调 `sandbox.EnsureEnv(kind=docker, deps=[image])` + `BuildDockerRunArgs` 拼 ServerConfig.Args；mcp 内部 exec docker 起 stdio 容器作 MCP server。同步 sandbox.md（加 §5b 整章）+ error-codes.md（2 个新 sentinel） + dev log。100 单测全绿 staticcheck clean GOOS=windows/linux 编译过 |
| 2026-05-08 | **[refactor]** Marketplace V2：删 6 hardcoded MCP 条目 → 接官方 Registry。**Domain**：加 `mcpdomain.RegistrySource` 端口（List/Get/Refresh）；瘦身 `RegistryEntry` 删 7 字段（Category/License/Notes/Bundled/OnlineOnly/UnsupportedPlatforms/Hidden）+ 加 Version；加 5 个新 sentinel（ErrMarketplaceUnavailable / ErrAliasCollision / ErrAlreadyInstalled / ErrUnsupportedRuntime / ErrHandshakeFailed）。**Infra**：新建 `infra/mcp/official_registry.go`（HTTP fetch `registry.modelcontextprotocol.io/v0.1/servers` cursor 分页全量拉 + 进程内存缓存 + 适配官方 packages[] → 单 InstallCmd 优先级 npm > pypi > docker + RequiredEnv/RequiredArgs 按 ALL_CAPS 启发拆分）+ `fake_registry.go`（测试用，注入预定 entries）。**App**：mcp.Service 持 RegistrySource 取代内嵌 Registry struct；ListRegistry/GetRegistryEntry 改 ctx 参 + delegate 给 source；新加 RefreshRegistry；InstallFromRegistry 加 alias 入参（双命名 namespace+alias，alias 默认 = name 末段，冲突报 ErrAlreadyInstalled）。删 `app/mcp/registry.go` + `registry_test.go`。**LLM 工具**：rename `search_mcp` → `search_mcp_tools`、`call_mcp` → `call_mcp_tool`；新增 `search_mcp_marketplace`（LLM 重排，无 alpha fallback，截 200 字 description，total ≤ topK 时短路）+ `install_mcp_server`（两阶段契约：第一次返 needs_confirmation 让 LLM 走 ask 工具收集用户同意 + 必填值，第二次带 confirmed=true 真装；alias_collision/already_installed/missing_required_args/install_failed/handshake_failed 全部结构化错误返给 LLM 自处理）+ `uninstall_mcp_server`。MCPTools 工厂从 2 工具变 5 工具（marketplace search 在 LLM 依赖 nil 时跳过注册）。**修 mcp.calltool::Search 也去 alpha fallback**——返清晰错让 LLM 重试，与 forge.search / search_mcp_marketplace 一致。**Wiring**：main.go 接 `OfficialRegistrySource`；harness 接 `FakeRegistrySource` 含 `everything` + `sqlite-test` 2 条；handlers/mcp.go 端点透 ctx 走 source；errmap 加 5 个新 sentinel → 5 个 HTTP error code。**dogfood 体验路径**：用户问 "搜新加坡天气" → WebSearch 失败 → LLM 调 search_mcp_marketplace → 找到 duckduckgo-search → install_mcp_server 第一次返 needs_confirmation → LLM 用 ask 问用户 → confirmed=true 第二次真装（30-60s）→ 重试 WebSearch → 走 MCP 路由返结果。新增 6 测试覆盖 install 两阶段 + alias 派生 + not_in_registry + uninstall + 描述自洽。100 单测全绿 staticcheck clean GOOS=windows/linux 编译过 |
| 2026-05-08 | **[doc]** 起草 `documents/version-1.2/event-log-protocol.md` ——SSE 龙骨级重构设计文档。当前 entity-snapshot 6 事件类型 + 60fps 节流 + rAF batch + subagent 借 chat.message 壳被定为屎山根因；新协议参考 Anthropic + Vercel AI SDK 收敛形态，统一 5 事件（message_start/stop / block_start/delta/stop）+ 6 block 类型（text/reasoning/tool_call/tool_result/progress/message）+ parentId 递归嵌套表达 subagent / 并行 / 嵌套 LLM 全部场景。flag-day 切换，无外部 SSE 消费者。完整方案见该文件（6 设计原则 / 数据模型 / 事件协议 + 37 事件交叉示例 / 后端三层架构 / 前端 30 行循环 / 完整 migration SQL / 5 phase × ~2700 行 / 风险 + dogfood 清单）|
| 2026-05-08 | **[feat]** 事件日志协议 Phase 1 基础设施（~930 行）。新建 4 包共存于 legacy events：(1) `domain/eventlog/`：5 事件 + 6 block 类型 + 4 status 常量 + Bridge 接口 + ValidateEvent + 2 sentinel；(2) `infra/eventlog/`：in-process Bridge 实现 per-conv 单调 seq + replay buffer 4096 + 慢订阅者**阻塞** publisher（§A2 反转，delta 不能丢）+ Last-Event-ID 重连 + ErrSeqTooOld；(3) `pkg/eventlog/`：Emitter 接口 + ctx-injected impl + 自动 mint msg_/blk_ ID + ParentBlockID 自动从 reqctx 取 + WithParent/From/MustFrom 三个 ctx helper + noop fallback；(4) `pkg/reqctx/`：加 WithParentBlockID/GetParentBlockID（§A5）。**DB 层**：`domain/chat.BlockV2` 7 新字段（ConversationID/MessageID/ParentBlockID/Content/Status/Error/UpdatedAt）+ Type/Status CHECK + (conv_id, seq) UNIQUE 走 GORM tag；`infra/store/chat.BlockV2Store` 5 方法（Save 走 OnConflict/AppendDelta SQL `content \|\| ?` 原子拼/FinalizeStop/GetByID/ListByConversation/ListByMessage）；ErrBlockNotFound sentinel；message_blocks_v2 表 AutoMigrate（Phase 4 cutover 改名回 message_blocks 后删 legacy）。**HTTP 层**：`/api/v1/eventlog?conversationId=` SSE 端点（与 legacy `/api/v1/events` 共存）+ `Last-Event-ID` header 重连 + 410 Gone 当 seq 已淘汰；wire 格式 `event:<type>\nid:<seq>\ndata:<event JSON>`。**初次设计**：infra/eventlog 拆 memory 子包被反认作过度设计（仅 1 子目录 1 文件违 §S12），拍平到 `infra/eventlog/bridge.go`。Phase 1 共 38 测覆盖 Bridge 并发单调性 + 慢订阅者阻塞 + Last-Event-ID 重连 + 410 + Emitter 父链 + 6 block CRUD + CHECK + UNIQUE。**未消费**：本 phase 后端 producer 还没切换（chat loop / subagent / tools 还推 legacy），前端也没切（Phase 4 完成）。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[feat]** 事件日志协议 Phase 2A producer dual-write（~280 行）。Phase 1 基础设施完工后接入 producer——chat 主管线现在与 legacy bridge 同时给新 eventlog bridge 推 5 类事件。**Emitter 扩展**：加 EmitMessageStart / EmitBlockStart 显式-ID 变体（chat msgID 上游已铸 / tool_call block ID = LLM 自带 tc_xxx）。**chat Service**：构造函数加 emitter 参数（nil 回退 no-op 给 legacy 测试用），新加 emitUserMessage(msg)（user message 同步落库后一次性 burst：MessageStart → 每 block 的 BlockStart/DeltaBlock/StopBlock → MessageStop），Send 流程在 user 落库后就调；processTask 入口塞 ctx.With(emitter) + EmitMessageStart(role=assistant)；emitFatalError 同步推 StopMessage(error)。**chatHost.WriteFinalize**：legacy bridge.Publish 后追加 emitter.StopMessage（mapStatus 把 chat.Status* → eventlog.Status*）。**streamLLM**：跟踪 textBlockID/reasonBlockID/toolBlockIDs；EventText 首次 emit BlockStart text，后续 token DeltaBlock；EventReasoning 同（与 text 互斥转换）；EventToolStart 用 LLM tc_id 直接当 block id（attrs.tool=name）；EventToolDelta 给 tool block emit DeltaBlock；流结束按 stopReason 关闭所有 open block（cancelled/error/completed）。**runOneTool**：toolCtx 加 WithParentBlockID(tc.ID) 让工具内部 emit 自动挂 tool_call 下；Execute 返回后 emit tool_result block 直挂 tool_call 父（含 output delta + status/error stopBlock）；blockID 改用提前铸的 resultBlockID 让 legacy DB block 与新 eventlog block 共享 ID。**main.go**：用 eventLogBridge 构造 chatEmitter 注入 chatService；harness 传 nil emitter 走 no-op。**未做**（留 Phase 2B）：subagent recursive emit / LLM client 内部 emit / subagent 两表 backfill+drop / subagent.go file split / DB dual-write（emit 即写 message_blocks_v2）。**dogfood 现状**：legacy bridge 仍是真消费源（前端没切），新 bridge 已有完整 producer 喂事件，可经 SSE /api/v1/eventlog 订阅 + 重连验证。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[feat]** 事件日志协议 Phase 2B subagent 递归 emit + DB dual-write（~330 行）。Phase 2A 后接续——subagent 拆 chat.message 借壳 + Emitter 接上 BlockV2Repository 让事件流落 message_blocks_v2 表。**subagent recursive emit**：`Service.Spawn` 在 run row 落库后从 parentCtx 取 emitter + parentToolCallID + parentMsgID，铸 subMsgID + msgBlockID，emit BlockStart(message-block, attrs.messageId/type/runId) 占位 + EmitMessageStart(subMsgID, attrs.kind=subagent_run) 真消息；subCtx 加 WithMessageID(subMsgID) + 清 ParentBlockID 让 streamLLM 把 sub 顶层 block 挂 subMsgID 下；loop.Run 返后 emit StopBlock(message-block) 关占位；subagentHost 加 eventLogMsgID 字段 + WriteFinalize 后 emit StopMessage(subMsgID, mapEventLogStatus(status))。设计文档 §3 完整事件流示例（spawn_subagent 父 tool_call → message-block → sub message_start → sub blocks → message_stop → block_stop）现在真实落地。**Emitter DB dual-write**：New(bridge, repo, log) 接 chatdomain.BlockV2Repository（可 nil 给 legacy 测试用）；publish() 改返 (seq, ok) 让调用方拿到 Bridge 分配的 seq；EmitBlockStart / StartBlock / StartBlockUnder 调 saveBlockV2(BlockV2{conv_id, msg_id, parent_block_id, seq, type, attrs JSON, status=streaming, content=""}) — parent==message 时 parent_block_id 留空表示顶层；DeltaBlock 调 repo.AppendDelta(blockID, delta) SQL "content || ?" 原子拼；StopBlock 调 repo.FinalizeStop(blockID, status, errStr)。Message 生命周期不双写（messages 走 legacy chat repo 直至 Phase 4 cutover）。**main.go**：blockV2Store := chatstore.NewBlockV2Store(gdb) → eventlogpkg.New(eventLogBridge, blockV2Store, log) 接入。**测试**：pkg/eventlog 加 6 个 DB dual-write 测试（StartBlock 顶层 parent 留空 / 嵌套 parent / DeltaBlock append 拼 / StopBlock 终态 / error 字段填 / attrs JSON 序列化）。**未做**（留 Phase 3+）：subagent_runs/subagent_messages 表 backfill+drop（依赖前端切到新 bridge）/ subagent.go file split（pure refactor）/ LLM-inside-tool（forge.search 等）的 emit 接入（Forge 用户重写后再做）。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[doc + feat]** 事件日志协议 Phase 3 — 规范同步 + 装包进度 + 历史回放（~360 行）。**CLAUDE.md** §E1/§E2/§D3 重写为 5 events + 6 block types + parentId 路由；新加 §N7（SSE wire format + Last-Event-ID 重连 + 410 Gone）+ §S21（事件流 invariants：parentId 必先于本事件 / status 单向流转 / per-conv seq UNIQUE / append-only delta / tool_call ID 复用 LLM tc_id）；§S18 推流约定改为优先 eventlogpkg.From(ctx) Emitter；项目特殊性段加事件日志协议提示。**events-design.md 整篇重写**（替换 entity-state 为 recursive event log 描述，含 schema / Producer 责任表 / DB dual-write 表结构 / Phase 路线 / 测试覆盖）。**install_mcp_server progress 推流**：mcp/install.go EnsureEnv 调用前 em.StartBlock(BlockTypeProgress, attrs.stage="installing") + 包装 ProgressFunc 把每行 stderr emit 成 BlockDelta + 完成时 StopBlock，让 LLM tool_call 下挂可见的装包进度（用户痛点之一：MCP install 30s-5min 黑屏）。**历史回放器**：BlockV2Repository.ReplayEventsAfter(convID, fromSeq) 返 []ReplayEnvelope（每行产 block_start + block_delta(完整 content) + block_stop 三 envelope，empty content 跳 delta）+ HTTP GET /api/v1/conversations/{id}/eventlog?from=N 端点（410 Gone 后 client refetch 用，响应 {events, tailSeq, count}）。**cleanup**：mcp/mcp.go 删 errAssistImports 反模式（_ = errors.Is / _ = gorm.ErrRecordNotFound）+ 对应 import 一并删；§B3 V2 placeholder 经审计实为 design note 不是 TODO，无需清理。**未做**（Phase 4+）：subagent_runs/messages 表 backfill+drop（依赖前端切到新 bridge）；subagent.go file split（pure refactor，价值低）；前端 chat.js 切新 bridge（CLAUDE.md §4 "V1.2 后端期不动前端"，等 Wails 迁移）。新加 8 测试覆盖 ReplayEventsAfter（顶层/嵌套 parent / fromSeq 过滤 / empty content 不发 delta）。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[doc + test]** 事件日志协议 Phase 5 文档收尾 + 协议契约测试（~270 行）。**database-design.md** 加 message_blocks_v2 表 schema 详（7 字段对比 legacy / UNIQUE(conv_id, seq) / CHECK 6+4 / parent_block_id 嵌套规则）。**api-design.md** 加 GET /api/v1/eventlog (新 SSE) + GET /api/v1/conversations/{id}/eventlog (history refetch) 端点行 + 410 状态码。**chat.md** 加 §X 章节"事件日志协议接入"（接入点 Map 7 处 / Emitter 与 ctx 流图 / DB dual-write 4 行 / tool_call ID 复用说明 / 前端尚未切换说明）。**subagent.md** 加 §16 章节（旧 borrowed shell vs 新 递归 emit 树形图 / Service.Spawn 接入点代码 / subagentHost 加 eventLogMsgID 字段 / sub LLM 流式自动来自共享 streamLLM / 表的命运）。**协议 contract test**：pkg/eventlog 加 TestProtocolContract_ChatRoundtrip 模拟 12 envelopes 完整一轮（msg_start + text 块 4 envs + tool_call 3 envs + tool_result 3 envs + msg_stop）+ 校验 5 项 §S21 invariants（seq 严格单调 / 实体先于引用 / tool_call ID = LLM 自带 / tool_result.parent = tool_call.id / DB 行内容 + 状态终态正确 + parent_block_id 列空值正确）。**Phase 4 frontend rewrite 不在本轮范围**（CLAUDE.md §4 限制：V1.2 后端期不动前端，等 Wails 迁移）。事件日志协议本轮（Phase 1-3 + Phase 5 doc）后端侧全部落地——dual-write 共存到前端切换。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[refactor + feat]** 事件日志协议 final cleanup — 一键到位 (~3000 行净增 / 删多写少)。本轮把所有 dual-write 路径删干净，整个事件总线只剩两条 SSE：`/api/v1/eventlog` (per-conv recursive event log) + `/api/v1/notifications` (global entity updates)。**新建 notifications 协议**：domain/notifications + infra/notifications + pkg/notifications + handler，单 channel 广播 envelope `{type,id,data,conversationId?}`；现期 type=conversation/todo，未来通知模块加 type 字符串即可扩展。**Block 模型重整**：删 chatdomain.Block legacy 5 字段 + TextData/AttachmentRefData/BlockType* 常量；BlockV2 改名 Block，table 改回 message_blocks。Message 加 ParentBlockID + Attrs 字段。chatRepo 合并 message + block + attachment ops 到单 Repository interface。Block 写入唯一路径：emit。**subagent 大重构**：删 subagent_runs / subagent_messages 两表 + infra/store/subagent 整包 + 4 个 GET 端点 + SubagentRun/SubagentMessage domain 类型 + ensureStreamingRow / publishChatMessage / refreshRunFromBlocks 等一坨 helper。Sub-run 现在 = 统一 messages 行（attrs.kind=subagent_run, parent_block_id=占位 message-block）+ 该 msg 在 message_blocks 的 blocks。subagentHost.WriteFinalize 直接写 messages 行 + emit MessageStop。SubagentRun pointer 从 SpawnResult 删，改 {RunID,Type,Status,...} 扁平字段。**loop.Host 简化**：3 方法变 1 (WriteFinalize)。删 Publish/WriteCheckpoint + streamLLM/runTools 的 host/parentBlocks 参数 + 60fps 节流 + publishNow/publishThrottled。emit 是唯一推送路径。**legacy 删除**：domain/events 整包 + infra/events/memory 整包 + EventsBridge field 全栈 (router/Deps/Service/main/harness) + /api/v1/events SSE handler + chatHost legacy publish + subagent borrowed shell + forge.streamCode Forge entity 推 + mcp.publishMCP + skill.Service.Scan Skill 推 + todo 旧 Todo 推 + conv autoTitle 旧 Conversation 推。**conv autoTitle / Create / Update + todo Service.Save/Delete 切到 notifications.Publish**。**Service 构造函数瘦身**：chat/forge/skill/mcp/todo 都删 bridge 参数。**testend 部分适配**：chat.js 切到 /api/v1/eventlog (block_stop/message_stop 触发 REST refetch；leaves live token streaming as Phase 4 work) + 订 /api/v1/notifications 处理 conversation 改名；删 tab-subagent.js + tab-sse.js (legacy debug viewer)；其他 tab 留待下轮 polish。**测试**：删 todo_test/polling_test/mcp_test/install_server_test (legacy bridge 测试)；rewrite history_test/stream_test 用新 Block shape。**doc**：CLAUDE.md §E1/§E2/§N7 + 项目特殊性段全更新。**未做**：subagent file split (低价值 pure refactor)；testend 6 block renderer 完整重写 (~600 行 chat.js polish)；其他 tab REST refetch on focus 逻辑。go build + go vet + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[refactor]** 事件日志 cleanup 收尾两件 — subagent file split + chat.js 完整重写 (~700 行触动)。**subagent split**：subagent.go 401 行拆 3 文件—subagent.go (Service+New+SetTools+filterTools+composeSystemPrompt) / spawn.go (SpawnOpts+SpawnResult+Spawn 全生命周期+Status* 常量) / queries.go (Cancel)。host.go 已经清干净不动。每文件单一职责；Spawn 长函数仍在但有了独立家。**chat.js 完整重写** (从 644 行变 580 行，结构更清)：(1) 删 _pendingMsgs Map / rAF batch / _suppressFlush / _scheduleFlush / _flushPending / _messageFromSnapshot 旧 entity-snapshot 一坨。(2) 新 _connectEventLog 订 /api/v1/eventlog 5 事件 (message_start/stop + block_start/delta/stop)，per-event 实时 mutate state.messages；新 _connectNotifications 订 /api/v1/notifications 处理 type=conversation autoTitle 改名。(3) _blockIndex Map 让 delta 查找快 (block_id → {msg, ref, kind})。(4) 6 block type renderer：text/reasoning/tool_call/tool_result/progress/message。tool_call 用 LLM tc_id 当 block id；tool_result 经 parent_block_id 配对到 tool item；progress 挂 tool_call 父下时附到 tool item 的 progress 字段；message (subagent placeholder) 折叠 pill 显示。(5) _userMsgFromBlocks / _assistantMsgFromBlocks 改读新 Block shape (Content 裸文本 / Attrs JSON 元数据 / 附件来自 Message.Attrs)。(6) send() 简化 — 删 _suppressFlush dance，乐观 user 行直接打真 messageId 留给 SSE message_start 找到原位更新。**未做**：forge 自带 emit 接入 (你重写 forge 时按 pkg/eventlog Emitter pattern 直接做 — 见我之前给的 run_forge.Execute 范例)。go build + GOOS=windows/linux + 全套单测全绿 |
| 2026-05-08 | **[fix + feat]** 事件日志 / 通知协议接入轮 — 把 emit / notifications 路径接到所有 chat 触发的 LLM rerank 与所有非主对话状态变化上。**eventlog 接入**：`skill.Search` + `mcp.calltool::Search` 内部 LLM rerank 加 progress block（chat 触发的 search_skills / search_mcp_marketplace tool 内 LLM 工作现在可见）；subagent `spawn.go` subCtx 显式 `eventlogpkg.With(em)`（防御性，防未来 ctx 重构断 emitter 链）。**notifications 接入**：conversation Create/Update/Delete 推 type=conversation；mcp Connect/Disconnect/AddServer/RemoveServer 推 type=mcp_server（含 publishStatus(name) helper）；skill Scan 检指纹变化推 type=skill；catalog Refresh 推 type=catalog（fingerprint+version+generatedAt）。Service 构造函数加 notif 参数（conv/mcp/skill/catalog 4 个）；`notifications.New(nil, log)` 改返 noopPublisher 让 nil bridge 安全 fallback。**pipeline harness 修复**：test/harness/{harness,helpers,sse}.go 完整重写以适配事件日志协议——deps 切到 EventLogBridge/NotificationsBridge/EventLogEmitter/NotificationsPub，drop SubagentRun/Message AutoMigrate；helpers.go 改 BlockType\* → eventlogdomain.\*；sse.go 重新订两个新端点 + 重构 Message+Block 状态（5 events × 6 block types）+ 合成 envelope `{ok,result,error}` 让老测试零改动；test/subagent/skill 改用 `messages WHERE attrs LIKE %subagent_run%` 替代 subagent_runs 表查询（schema 统一后）；test/mcp 旧 InstallFromRegistry 签名补齐。**doc**：database-design.md `message_blocks_v2` 段重写为 `message_blocks`（v2 表名移除）+ 删 subagent_runs/subagent_messages 段；subagent.md 顶 banner 标 §3 SubagentRun struct 已作废 + 指向 host.go::WriteFinalize；chat.md /api/v1/events → /api/v1/eventlog + /api/v1/notifications + 历史回放端点；events-design.md `message_blocks_v2` → `message_blocks`；CLAUDE.md §D3 v2 引用 + §S15 sar_/smm_ 前缀清理；dev_routes.go 加 eventlog/notifications 端点 + 删旧 subagent-runs 4 个端点字符串。go build + GOOS=windows/linux + 全套单测全绿 + pipeline harness 编译跑 |
| 2026-05-08 | **[refactor]** 屎山拯救计划 #7：catalog generator 删 3-attempt retry → 单次 attempt。`app/catalog/generator.go::Generate` 原 304 行用 `for attempt := 0; attempt < 3; attempt++` 循环 + coverage 校验 + missingHint 拼回 prompt 重试。**问题**：现代 LLM (DeepSeek/GPT-4o/Claude) 这种 "读列表写总结" 任务首试 ~99% 成功，重试基本不工作；真失败的网络/key 原因重试 3 次也救不了；catalog 自身 1s 轮询本身就是天然重试（用户活动下次变 fp 自动再生成）；mechanical fallback 已兜底。**改动**：(a) Generate 收成单次：调 LLM → 解析 → Summary 非空就返；任何失败返 ErrGenerationFailed 让 Service.Refresh 切 mechanical。(b) 删 `findMissing` / `groupSourceIDs` helpers + `generatorMaxAttempts` 常量 + `missingHint` 参数 + buildPrompt 的 retry-hint 段。(c) Coverage 字段 LLM 透传不校验。**测试**：generator_test.go 从 8 个 helper 测试收成 3 个（buildPrompt 覆盖 + 防 retry-hint 文案重引 guard + nil log）。**收益**：净 -159 行（255 → 106 在 generator.go；测试 116 → 47）；每次 catalog 重生成省 token（最多 1 次 LLM 调用 vs 之前最多 3 次）；响应快（不必等 3 次失败才 fallback）。**风险**：极少数 LLM 漏掉 1-2 个 item，但 1 秒后 polling 自然再试；mechanical fallback 兜底世界不塌。同步 catalog.md（§7 整段重写为单次设计 + 历史代码摘要保留）+ dev log。100 单测全绿 staticcheck clean GOOS=windows/linux 编译过 |
| 2026-05-09 | **[audit + fix]** Phase A1.1 calibration shot — `internal/app/apikey/` §S3/§S9/§S15/§S16/§S17 全套审。**机制**：fork 必读 `_spec-extracts.md`（§S3-S17 抠出独立短文件，避免读 CLAUDE.md 全文超时）+ 目标包 3 文件 + reqctx + errmap.go；读完一个 → Write trace → 再读下一个，避免长时间 thinking 触发 watchdog。产物 4 markdown 落 `documents/audit/A-error-2026-05-09/app-apikey/`。**发现**：46 sites / 1 HIGH (apikey.go:#28 MarkInvalid §S9 — d8a5161 修过 Test 但漏 MarkInvalid 同构 bug，UI 假 "OK") / 1 MED (tester.go:#4 default branch 无 sentinel 触发 unmapped 警报) / 8 LOW (style 一致性)。**修复**：MarkInvalid 加 detached ctx + 三处 §S16 wrap；tester.go default 改 panic（programming bug 该立刻暴露而非掩成 500）。audit markdown 状态字段更新为 `FIXED 410f664`。spot-check 7 项验证机制不糊弄。**closeout**：8 LOW 经审计 5 FIXED (1b96a5e §S16 wrap-format) + 2 WAIVED + 1 EDGE-FLAG (409f8c7)。 |
| 2026-05-09 | **[fix + refactor]** §S3/§S9 + dev 数据隔离三联。**(a) §S3/§S9 apikey test 终态写入用 detached ctx**：`Service.Test` 之前 LLM 探测后 `UpdateTestResult(ctx, ...)` 用 r.Context()——浏览器 hard refresh 让 ctx cancel 时**测试结果丢失**（成功/失败都写不进 DB），row 维持旧 status 给用户陈旧 "ok" 假象。改用 `reqctxpkg.SetUserID(context.Background(), uid)` 派 detached ctx 走两次终态写。**(b) errmap 静默映射 std context error**：`context.Canceled` → 499 CLIENT_CLOSED、`context.DeadlineExceeded` → 504 REQUEST_TIMEOUT；登记 errTable 抑制"unmapped domain error"误报（之前每次客户端断开都污染日志栏）。**(c) dev forgify-home 隔离**：main.go 加 `--forgify-home` flag；dev + dataDir 非空时默认根到 `<data-dir>/.forgify`（mcp.json / skills/ / .catalog.json 三件套），prod / Wails 仍走真 `~/.forgify/`。删 `defaultMCPConfigPath / defaultSkillsDir / defaultCatalogCachePath` 三函数；router.Deps 加 ForgifyHome 字段一路传到 DevHandler；`/dev/info` + `/dev/forgify-home` 用 h.forgifyHome 替代 hardcoded 路径。Makefile `clear` 同步：删 `.venv/` 老 v1 残留行，注释说明现在 rm -rf BACKEND_DATA_DIR 自动连带清 dev forgify-home（真 ~/.forgify/ 永不被动）。go build + vet + 全套单测 + pipeline 编译性全绿 |
| 2026-05-09 | **[fix]** testend 进入后所有按钮静默卡死。**根因**：浏览器 HTTP/1.1 单 origin 6 connection 上限被 6 个 SSE 长连接占满，新 fetch 没空闲 slot 永远排队。tab-sse.js（事件日志 cleanup 漏删的废弃 viewer）后台静默订 `/api/v1/notifications` + chat.js 与 tab-notifications.js 各订一份 + tab-logs.js 与 tab-errors.js 各订 `/dev/logs` = 6/6。**修法**：(a) 删 tab-sse.js（文件 + html div + script tag），TESTEND_TABS 已先一步去除 sse 项；(b) 加 `Alpine.store('notifBus')` + `Alpine.store('logBus')` 单连接共享 EventSource，多 listener fan-out + 订阅返 unsubscribe；(c) chat.js / tab-notifications.js 切到 notifBus，tab-logs.js / tab-errors.js 切到 logBus。结果：6 → 3 SSE，留 3 fetch slot。HTTP/2 + TLS 升级方向（彻底解 connection limit）暂留 — 等真撞瓶颈再做 mkcert 路。 |
| 2026-05-09 | **[fix]** Ctrl+C 后多溢一行 ERROR `shutdown context deadline exceeded`。**根因**：testend 一打开就订了 `/api/v1/eventlog` + `/api/v1/notifications` 两个 SSE 长连接，handler 在 `r.Context().Done()` 上 select；stdlib `http.Server.Shutdown` **不**主动 cancel request ctx，要等 5s 超时。**修法**：main.go 加 `srvBaseCtx, cancelBase := context.WithCancel(...)` + `srv.BaseContext` 注入；shutdown 流程改为「先 cancelBase 解开 SSE → 再 srv.Shutdown 等空闲连接 drain」，关后秒退不再吃超时。 |
| 2026-05-09 | **[refactor]** Marketplace V3 search → list 化。**LLM 侧痛点**：21 条 curated 用 `search_mcp_marketplace` 关键词 AND-match 实测召回过低（playwright/browser/github/slack 4 词只命中 1 条），LLM 因此误判"无可用 server"。改造：(a) `RegistrySource.Search → List`（无 query；返完整列表 tier asc + name asc 稳排）；删 `ErrQueryRequired` + `MCP_QUERY_REQUIRED` errmap 行。(b) tool `search_mcp_marketplace` 整改成 `list_mcp_marketplace`（无参；删 LLM rerank）；MCPTools 工厂签名简化（picker/keys/factory 全删）。(c) `uninstall_mcp_server` 清 V2 残留：`alias` 字段 → `name` / 删 Docker 注释 / 删 duckduckgo-search 例子。(d) REST `GET /api/v1/mcp-registry?search=` → `GET /api/v1/mcp-registry`（无 query）。(e) testend tab-mcp 切到 eager `loadRegistry` + 客户端 substring `filterQuery` 替代服务端搜索。**测试**：删 3 个 Search 测试 / 加 3 个 List 测试（全列 / tier+name 排序 / 防 mutate）；handler 测从 SearchRegistry 改 ListRegistry 返完整 fixture。同步 mcp.md §5.5 list 接口段重写 / api-design.md / error-codes.md。go build + vet + 全套单测全绿 |
| 2026-05-09 | **[fix]** install 进度可见性 + MCP stderr 日志降级。**前端漏渲**：tester.html tool 模板从未渲染 `item.progress`，chat.js 把 progress block delta 拼进了字段但 UI 看不到——加折叠态最新行预览 + 展开态完整 progress 段。**backend 静默装包**：`installprogress.Run` 块开后立刻发合成 `[starting] runtime/server` delta + close 时发 `[done]` 或 `[error]`，确保 mise 在 `MISE_QUIET=1` 下哪怕零 stderr 也不会零 delta。**MCP stderr 噪音**：`infra/mcp/client.go::drainStderr` 默认 INFO，仅当行自报 WARNING/ERROR/EXCEPTION/TRACEBACK 才升 WARN——避免启动横幅 + "Installed N packages" 淹没真信号。go build + 全套单测全绿 |
| 2026-05-08 | **[refactor]** Marketplace V3 — curated 21 + 砍 sandbox 7 EnvManager + schema 简化（~净删数千行）。**curated 21 替换 5000+ 上游**：新建 `infra/mcp/curated_registry.go::CuratedRegistrySource` hardcoded 21 条目（playwright / chrome-devtools / duckduckgo / context7 / memory + 11 一 key tier1 + 2 OAuth tier2 gmail/ms365 + 3 db tier3 dbhub/mongodb/supabase），全 npm/pypi 验证装机即用；删 `OfficialRegistrySource` + `FakeRegistrySource`。**domain 字段砍**：删 `RegistryEntry.DisplayName` / `License` / `Bundled` / `Hidden` / `OnlineOnly` / `UnsupportedPlatforms` / `PostInstallSteps` / `DefaultTimeoutSec`；新加 `Category` / `Tier (0/1/2/3)` / `Notes`。每 entry 单 `Name` 短 slug 同时是 LLM lookup id + mcp.json key（无 alias）。**sentinels 收**：删 `ErrAliasCollision`；errmap 加 `MCP_QUERY_REQUIRED`。**install signature**：`InstallFromRegistry(ctx, name, alias, env, args)` → `InstallFromRegistry(ctx, name, env, args)`；handler + LLM tool 同步。**LLM tool 重写**：`install_mcp_server` 描述强调 curated + Tier；schema 删 `alias` 字段；phase1Envelope 含 Tier + Notes 让 LLM 渲染前可警示用户。`search_marketplace` 描述列 categories + tier system。**testend Tier 2 OAuth UX**：tab-mcp.js install() 检 `tier===2` 自动开 stderr modal + 6×1s 短轮询抓登录 URL；tester.html entry 列加 Tier badge + Category；selectedRegEntry 详情显示 Tier 全名 + Notes 黄色警告条。**sandbox 砍 7 EnvManager + 4 installer**：删 `internal/infra/sandbox/{rust,golang,java,ruby,php,dotnet,playwright,docker,generic,static}.go`（10 文件 ~700 行）；`registerSandboxStack`（main.go + harness.go）从 11 EnvManager 收成 2（python/node）+ 3 installer（python/node/uv）。理由：curated 全 npm/pypi 不需要其他 runtime；§S20 不留死代码。**docs**：mcp.md §5.5 整段重写为 V3 curated（含 Tier 表 + 21 列表 + Search-only + 两阶段 install + Tier 2 OAuth UX）；error-codes.md mcp 段同步；sandbox.md §4 语言矩阵砍到 python+node 一行。**测试**：`infra/mcp/curated_registry_test.go` 7 测试覆盖 count=21 / 全条目合法 / npm+pypi 唯二 runtime / Search AND 语义 / Get 已知未知 / 至少一条带 Notes。test/harness/test_registry.go 新建 inline 测试 source（不依赖 infra/mcp 老 fake）。`go build ./...` + `go build -tags=pipeline ./test/...` 全过；单测套件全绿（除 pre-existing staticcheck 4 行噪音）|
| 2026-05-08 | **[fix]** chat user-message render race + emit/notification 路径接通 + pipeline 修复 (`fa9b8c4`)。乐观 user 行打真 messageId 后 SSE message_start 找到原位更新；嵌入点 emit/notif 接通；pipeline 测同步 Block shape。chat hot path 真 bug。 |
| 2026-05-08 | **[refactor]** marketplace V3 雏形 (`3c50b8c`) — search-only 改 + 修官方 Registry v0.1 schema 真实形状。删 LLM rerank 走纯 substring；为 V3 curated 21 落地铺路（与 line 408 同步）。 |
| 2026-05-08 | **[feat]** AskUserQuestion testend 交互 UI (`b22417d`) — 工具发问时前端弹自由输入框替代静默卡顿；描述强调用户自由输入而非选项。chat 使用 §S18 ask 工具体验补全。 |
| 2026-05-08 | **[test]** mcp curated marketplace pipeline (`a75dde5`) — 21 smoke + 5 T0 live tool calls 真装机 + 真 ToolCall 验证。`make test-pipeline` 大覆盖批次。 |
| 2026-05-08 | **[test]** mcp AllSmoke 真验装机路径 (`8a9b853`) — stub 凭证 + 严守测试作者 bug；§T6 Live_ 前缀 + ENV gate 实践。 |
| 2026-05-08 | **[chore]** mcp curated entry: gmail → google-workspace (`49792c5`) — 替 taylorwilsdon 全套 + 真维护 fork（V3 curated 21 演化）。 |
| 2026-05-09 | **[refactor + feat]** testend M1-M14 全 tab 适配事件日志协议 + 错误反馈 + 死代码清（13 commit；点出 M3.5 chat 渲染 subagent 嵌套 + progress 独立 block (`3bcdff4`) / M4 notifs tab 重做为统一 SSE + toast feed (`6676211`) / M2 SSE tab 重写为 raw 两通道 viewer (`52ca54a`)；M1 删 subagent 死 tab / M3 chat.js 6 件润色 / M5/M6/M9 错误反馈 / M8 skill tab 6 frontmatter 字段 / M10-M14 polish）。 |
| 2026-05-09 | **[fix]** sandbox conv env owner.ID 用 `_` 替 `:` 解 PATH 冲突 (`3cdf18a`) — 真 PATH 拼接 bug，与 ff8fd77 owner.ID 修复同源。 |
| 2026-05-09 | **[fix]** sandbox+bash 进度共享 helper + 错误 surface + 新加 type=`sandbox_env` notification (`888739c`) — 装包黑屏期 LLM/UI 可见；3 处修复同 commit。 |
| 2026-05-09 | **[fix]** sandbox reset-all conv envs 路由注册漏 `:reset-all` 后缀 (`9789b19`) — 单端点 path 漏注册修。 |
| 2026-05-10 | **[fix]** chat §S9 detached ctx + §S3 错误吞 + §S16 wrap (`f272503`) — chat hot path 真 bug 批修；app-chat audit 13 FIXED + 2 LOW review (`054e242`)。 |
| 2026-05-10 | **[fix]** mcp+loop §S3 + §S16 (`26f9c55` + `505d6e3`) — 错误吞 + wrap-format polish；app-mcp + app-loop audit batch 1 closeout (`4f147b9`)；chat host.go #8 收尾 (`e5b65fa`)。 |
| 2026-05-10 | **[fix]** sandbox §S9 ready/failed detach + §S17 sentinel gaps (`e36f890`) + 6 §S16 wrap LOW + empty-Cmd → sentinel (`0d4a48e`) — 装包终态写入加 detached ctx；audit batch closeout。 |
| 2026-05-10 | **[fix]** llm + errmap 引入 HTTP-status sentinel 家族 (`94ab56a`) — provider error 按 4xx/5xx 分类 + §S17 errmap 单一事实源登记。新 sentinel 簇。 |
| 2026-05-10 | **[fix]** forge owner.ID 分隔符 `:` → `_` 对齐 B1 + 新 sentinel `ErrInvalidOwnerID` (`ff8fd77`) — sandbox owner.ID 解析冲突 CRITICAL bug；app-forge audit drop (`da425c9`)。 |
| 2026-05-10 | **[fix]** sandbox 4 MED %w:%v sentinel-chain truncation (`d6b626f`) — install 路径 §S16 wrap 修；errors.Is 现可贯穿。 |
| 2026-05-10 | **[fix]** infra-llm 17 LOW sentinel + prefix sweep (`363b084`) + infra-sandbox EDGE §S16 prefix + scanner.Err (`d2b8af8`) — 大批 §S16 风格统一。 |
| 2026-05-10 | **[fix]** tool/search + tool/shell 1 HIGH + 8 LOW (`f9d0265`) — audit 找到的真 bug + style 修；tool/web 2 MED sentinel-based MarkInvalid + non-silent BYOK fail (`7dba737`)。 |
| 2026-05-10 | **[fix]** tool/forge 2 §S3/§S17 LOW + skill scan.go 2 MED %w:%v sentinel-chain truncation (`64d9535` + `7f3ef2c`) — 真 sentinel-chain 修复 + audit closeout。 |
| 2026-05-10 | **[fix]** subagent §S9 emit-side detach + mapEventLogStatus drift (`a70d73a`) — emit 路径加 detached ctx + status 映射对齐 eventlog domain。真 §S9 修。 |
| 2026-05-10 | **[fix]** catalog 新 sentinel `ErrAllSourcesFailed` + errmap 行 (`2d47cb0`) — §S17 三处联动；refresh 全失败时清晰错误码。 |
| 2026-05-10 | **[doc]** forge_redesign trinity architecture spec — Function/Handler/Workflow 三位一体 (`f98c152`) + plan 01 Function domain (`41d9212`) + plans 02-06 完整路线 (`2a0a1a0`)。Phase 4/5 重定向到 forge 重设计。完整方案见 `documents/version-1.2/adhoc-topic-documents/forge_redesign/`。 |
| 2026-05-10 | **[fix]** handlers-B1-B4 audit 全套 closeout (`905d141` / `8d7f797` / `a183b16` / `3f89c03`) — 4 batch 共 4 MED + 24 LOW；handler 层 §S16 wrap + §S3 错误处理 + 死代码清。 |
| 2026-05-10 | **[fix]** pkg/eventlog 1 MED + 3 LOW (`87b9fe7`) + pkg/llmclient 4 LOW %w:%v (`a13c21d`) + installprogress §S9 ctx-asymmetry (`9cb09b2`) — pkg 层 audit 滚动。 |
| 2026-05-10 | **[doc]** audit 滚动批量 drop — app-{tool-{filesystem,mcp,ask,subagent,todo,skill},todo,model,conversation} clean (8 traces 0 violations) + cmd 2 LOW (`872a265`) + transport/{middleware,response,router} 4 LOW + pkg/{idgen,llmparse,pagination} 4 LOW + errmap subagent 注释 stale 修 (`54ab931`)。LOW 集合不逐条记，整批 closeout。 |
| 2026-05-10 | **[doc-fix]** D contract-doc audit closeout — 7 HIGH + 11 MED gap close (`5186a95`)。§S14 文档同步纪律 (#7) 执行：service-design / service-contract / progress-record 三处对齐代码现状。 |
| 2026-05-11 | **[fix]** Phase G dead-A 真 bug — loop.Result.Status 终态推算（subagent error-classification 不再误报 completed）+ emitFatalError 加 convID stamp（stub message_stop emit 不再静默 skip）+ ErrBuildClient 加 chat/runner switch case + notifications.Publisher.Publish variadic→必填 5 参数。 |
| 2026-05-11 | **[refactor]** Phase G dead-A 死代码清 — 删 pkg/eventlog 4 死 export（StartMessage/StartBlockUnder/MustFrom/WithParent）+ pkg/notifications 整套 ctx-wiring（With/From/MustFrom/publisherKey）+ Bridge.log/bufferedEnvelope.at 死字段 + tools.go elapsedMs 死计算 + assembleBlocks/host.go 死 Status/CreatedAt 赋值。 |
| 2026-05-11 | **[doc-fix]** Phase G dead-A stale godoc — chat.go file-listing / runner msgID 注释 / stream.go file-header + dual-write godoc / loop.go Skill fork / broadcast.go events/memory 引用 / saveBlockRow seq==0 / eventlog New "legacy callers" / installprogress `_ = ctx`。Build + scope 内 unit tests 全绿。 |
| 2026-05-11 | **[refactor]** Phase G fix-C 死代码清 — mcp dead-4: snapshotLocked / ServerStatus.PID / ErrHandshakeFailed / ErrMarketplaceUnavailable / RegistryEntry.Version 5 死全删 + recordCallResult ctx arg 删（不发 SSE）+ stale "search_mcp_marketplace" 文案改 list_mcp_marketplace（5 处）+ var _ MergeResult keep-alive 删；apikey dead-7: ErrTestFailed / ErrInvalid 永不返 sentinel + APIFormatOpenAICompatible 装饰 const 删 + MaskKey/IsValidProvider 大写 → 小写（包内）；ask: ErrAlreadyAnswered ghost 删；todo: ErrConversationMismatch 永不返 删；errors: ErrInternal ghost 删；catalog: PerCollection future-reserved enum 删（同步 generator prompt）；shell: bash detector 6 语言 trim 剩 python+node（用户拍板：rust/go/ruby/php/java/dotnet 落系统 shell）+ envBinDirsForKind 同步 + Bash description 重写。同步 errmap.go 删 7 行；error-codes.md / apikey.md / ask.md / mcp.md / todo.md / sandbox.md / shell.md 7 文档对齐。 |
| 2026-05-11 | **[refactor]** Phase G fix-B 死代码清 — subagent dead-2: queries.go (Cancel + activeRuns map) + DefaultModel/Description/Roles* domain const 全删 + SpawnOpts.Model ghost 删；sandbox dead-3: Extras 全链路（domain/app/store/2 EnvManager InstallExtras）+ IsDefault flag + FindDefaultRuntime + idx_sr_kind_def + RunningStartedAt + ListAvailable interface method 全删 + macCodesign WalkDir 简化为单文件 codesign + ExtractMiseBinary sig 改 sandboxRoot 直传 + SpawnLongLived envID 走 prepareSpawn 不重查 + mcp install Owner.Kind 用 OwnerKindMCP const + Owner.ID 文档 `:` → `_`；agentstate dead-8 M1/M2: SubagentTokenLog write-only API（slice + mutex + 3 export）+ reqctx WithSubagentRunID/GetSubagentRunID 死 ctx key 全删。同步 subagent.md / sandbox.md / database-design.md 三处文档；测试同步 mkRuntime 改 3 参 + spawn_test fakeEnvManager 删 InstallExtras + mise_test sandboxRoot 改名。 |
| 2026-05-11 | **[refactor]** Phase C tool-result audit closeout — 14 tool 文件 + 5 测试 LLM-facing 文本清理：framework `sanitizeToolErr` 剥 §S16 wrap 链（5 单测）+ Bash `formatAutoRouteError` 重写并删 `description` 重复字段 + 13 tool description 瘦身（AP1/AP3 教学口吻、AP4 路径/UI/sentinel 名泄漏、AP5 "Successfully" 冗余、AP6 KillShell `shell_id`→`bash_id`）。同步 shell/filesystem/ask/mcp 4 处设计文档。build + 170 单测全绿。 |
| 2026-05-11 | **[doc-fix]** D-redo contract docs 同步 41 gaps（read-it-all 重审 vs D1 grep）—— 4 真 bug grep 漏：`:duplicate` ghost route 删 doc + manifest / notifications wire `event: notification` 硬码 vs doc `<type>` 占位 / SEQ_TOO_OLD wire code 补 error-codes 索引 / 4 ghost sentinel 删（chatdomain.ErrProviderUnavailable+ErrVisionNotSupported + mcpdomain.ErrRuntimeMissing+ErrUnsupportedRuntime）+ web BYOK 3 sentinel errmap 删（控制流 sentinel）+ §11.2 producer 表方法名 / 触发场景全修（fsnotify→polling 等 6 处错）+ messages.parent_block_id/attrs 字段补 + sandbox_envs 假 FK 措辞改 + forge_executions retention TODO→已实现 + sandbox nil-Publisher 改构造器兜底（其他 5 service pattern 对齐）+ alias 全局 sed `domain` 后缀对齐 §S13 + buffer 数字明示 + History endpoint response shape 加 + CLAUDE.md §D4 改 aspirational 措辞。0 HIGH ship-blocker；build + tests 全绿。 |
| 2026-05-12 | **[feat]** forge_redesign Plan 01 完整交付(13 commits 直推 main)—— function trinity domain 替代 forge:(1) domain+store 24 单测;(2) app 层 apply.go 6-op 引擎 + crud/pending/accept/revert + sandbox_adapter + execution log(D22) 8 单测;(3) 9 LLM tools(search/get/create/edit/revert/delete/run + search_function_executions/get_function_execution);(4) 11 HTTP endpoints + errmap 14 sentinel + router/main/harness wire-up;(5) Phase 7 destructive 删除 forge ~2500 LOC + 4 个包目录(app/forge / app/tool/forge / domain/forge / infra/store/forge)+ handlers/forge.go + test/forge/ + OwnerKindForge;(6) D22 function_executions 表 + 通用 16 字段 + 函数专属 3 字段 + per-entity LLM 诊断工具;(7) 4 pipeline tests(CRUD lifecycle / 分页 / LLM search empty / Run+exec log sandbox-gated)。Phase 9 同步 backend-design + 4 contract docs + 新 function.md + 删 forge.md。设计 spec [`adhoc-topic-documents/forge_redesign/`](./adhoc-topic-documents/forge_redesign/)。 |
| 2026-05-12 | **[feat]** forge_redesign Plan 02 完整交付(11 commits 直推 main)—— handler trinity 第二条腿(stateful Python class + caller-owns instance lifetime):(1) 18 sentinel + 3 表(handlers/handler_versions/handler_calls);(2) infra/handler/client.go stdio JSON-line RPC client(5 methods + 8 wire-format 单测,fresh-write per D5 — MCP 用 JSON-RPC 2.0 协议不同);(3) app 层:apply.go 10-op method-level + RFC 7396 JSON Merge Patch / config.go AES-GCM Load+Update+Clear+MaskedConfig+ConfigState / rpc.go AssembleClass+DriverScript / registry.go Owner+Instance+caller-owns lifetime(chat=per-call,workflow/test/session=persistent;无 idle GC,scope-end 钩子调 DestroyOwner)/ call.go Service.Call+recordCall(D22)+stderr 256KB ring / 33 单测;(4) 10 LLM tools(search/get/create/edit/revert/delete + call_handler + update_handler_config + search_handler_calls/get_handler_call);(5) 16 HTTP endpoints + errmap 19 sentinel + router/main/harness wire-up + graceful shutdown 钩子;(6) D22 handler_calls 表(通用 16 + handler 专属 6 字段:handler_id/version_id/method/instance_id/owner_kind/owner_id);(7) 4 pipeline tests(HTTP CRUD / Config round-trip / LLM search empty / Call+CallLog sandbox-gated)。**用户细化决策**(forge_redesign 04-discussion 2026-05-12):lifetime 完全 caller-driven 无 idle GC;function 同步移除 per-call timeout knob(只走 ctx cancel);Handler→Handler 不强制禁止(结构上自然防);stdio JSON-line client 独立写不抽公共包(协议跟 MCP 不一样)。Phase 9 同步 4 contract docs + 新 handler.md + backend-design Architecture 树。设计 spec [`03-handler.md`](./adhoc-topic-documents/forge_redesign/03-handler.md) + [`plans/02-handler-domain.md`](./adhoc-topic-documents/forge_redesign/plans/02-handler-domain.md)。 |
| 2026-05-12 | **[doc]** Plan 03 redesign 落档(b0af578/89b0d4c/25db693)— 讨论 §A-I + Doc A env 模型 9 files + Doc B SSE 三流 6 files;CLAUDE.md §E1 双→三协议。完整方案 [`discussions/2026-05-12-env-and-sse-rework.md`](./adhoc-topic-documents/forge_redesign/discussions/2026-05-12-env-and-sse-rework.md)。 |
| 2026-05-12 | **[feat]** Plan 03 C1+C1.1 env 模型 backend(9f0cb4b/2971161,32 files)— EnvID 每版本独立生成(`fnenv_`/`hdenv_`,与 versionID 解耦);Create/Edit 同步装 env;Accept 纯指针、Reject 销 env 删行、Edit iterate-same-pending;Service 前置 sandbox ping;删 ErrPendingConflict + Resync 路径 + `:resync` HTTP 端点。 |
| 2026-05-12 | **[feat]** Plan 03 C2 LLM env-fix loop(5cc57f9)— 新 `pkg/envfix` + 11 单测;4 LLM tool(create/edit × function/handler)装失败调主 chat scenario LLM 修 deps 重试最多 3 次,attempt 进 chat progress block。 |
| 2026-05-12 | **[feat]** Plan 03 C3 SSE 三流 user_id 化 + 通知瘦身(5067c16,13 files)— eventlog + notifications Bridge 改 per-user key(从 ctx 抽);eventlog HTTP 去 `?conversationId=`(client demux);删 env_synced/env_failed 通知;notification payload 瘦身(只 `{action, versionId?}`,UI 经 GET 拉)。 |
| 2026-05-12 | **[feat]** Plan 03 C4 forge stream(031954c,17 files)— 新 SSE 第三流 `/api/v1/forge`(4 events × 3 kinds function/handler/workflow);domain+infra+pkg+transport 全套 + 11 单测;4 LLM tool 双写 forge bus + 保留 chat eventlog progress block。复用 `eventlogdomain.Scope` 嵌套(D-redo-23)。 |
| 2026-05-12 | **[feat]** Plan 03 C5 testend 三 bus listener(2911777,4 files)— app.js 加 chatBus + forgeBus 共享 EventSource;chat.js 改订 chatBus + 客户端 demux `conversationId`;新 `tab-forge.js` 显示 4 类 forge 事件 + kind/type 过滤。Plan 03 6-commit 切分完工。 |
| 2026-05-12 | **[feat]** forge_redesign Plan 04 完整交付(9 commits W1-W9 直推 main)—— workflow trinity 第三条腿(DAG authoring,锻造 vs 执行分离 D6):(1) W1 domain 11 sentinel + Workflow/Version/Graph + 13 NodeType + 9 Op + 6 单测;(2) W2 store 13 Repository 方法 + partial UNIQUE + 12 集成测试;(3) W3 apply.go(450L 9-op engine + RFC 7396 JSON Merge Patch + 12 单测)+ validate.go(380L Kahn cycle + CapabilityChecker + container body 递归 ≤3 + 12 单测);(4) W4 expression.go(Go text/template ~140L + 11 单测);(5) W5 Service(CRUD + iterate-same-pending + 自动 accept v1 + slim notifications D-redo-6 + WorkflowReader 接口预留 Plan 05);(6) W6 6 LLM tools(search/get/create/edit/revert/delete + forge 双写;**无 envfix loop** — workflow 无 env);(7) W7 11 HTTP endpoints + errmap 11 sentinel + 13 httptest;(8) W8 ProductionChecker 装 function/handler/skill/mcp + main/harness 装配 + D9 BootSmoke 加 handler+workflow tool 家族 + 3 pipeline test(CRUDLifecycle / VersionsAndPending 真 CapabilityChecker / LLM SearchEmpty);(9) W9 docs sync 4 contract docs + 新 workflow.md + Plan 04 完工 banner。drive-by 修 filesystem pipeline test 错误断言。**故意不含**:ErrPendingConflict(iterate-same-pending D-redo-11)。Plan 05 territory:trigger / flowrun / scheduler / `:trigger` action / execution log 端点。设计 spec [`adhoc-topic-documents/forge_redesign/04-workflow.md`](./adhoc-topic-documents/forge_redesign/04-workflow.md) + [`plans/04-workflow-authoring.md`](./adhoc-topic-documents/forge_redesign/plans/04-workflow-authoring.md)。 |
| 2026-05-13 | **[feat]** forge_redesign Plan 06 完整交付(5 commits F1-F5 直推 main)—— trinity 收尾 + multi-agent forging 教学:(F1) `app/subagent/Service.filterTools` 加 closed deny-list(Subagent + create/edit/delete/revert/trigger_workflow,D21);保留 search/get_workflow read-only + call_handler/run_function 子 agent 自测必需;3 单测;(F2) 主 chat LLM 的 system prompt 永远拼 multi-agent forging 教学段(`chat/multi_agent_prompt.go`,40 行 markdown:6 step 流程教 explore decomposer / parallel forger spawn / configState 闸 / workflow 装配主 agent 独享 D21 / 简单需求别 spawn);放 `chat/runner.buildSystemPrompt` 不放 catalog generator(generator 是 meta-LLM 生成 catalog summary 的 prompt,跟主 agent system prompt 独立);3 子用例测试(nil/empty/populated catalog 都拼);(F3) test/catalog/trinity_catalog_test.go 2 pipeline test 验 function + handler 都在 catalog.Coverage + Summary 含 name + configState 暴露;(F4) test/scheduler/approval_e2e_test.go 3 HTTP E2E(approve happy path 跑 ack 节点 / decision=maybe 返 400 / wrongNodeID 返 404);RehydrateOnBoot 由 E10 单测覆盖,本测专注用户 UX HTTP 面;(F5) forge_redesign/README.md(新,trinity 完工导航 + Plan 01-06 + 关键数字 + 完工能力清单)+ plans/06 完工 banner + 本 dev log + backend-design Phase 4 行同步。**Scope 调整**:原 plan Task 5 邮件 workflow 全栈 E2E (~25 turn fake LLM script) 跳过 — 长期维护成本太高,效益不如直接 dogfood;multi-agent mechanics 已由 subagent 单测 + filterTools test + system prompt test 覆盖。trinity architecture **全交付**:Plan 01-06 全在 main,V1.2 后端期收尾。设计 spec [`adhoc-topic-documents/forge_redesign/`](./adhoc-topic-documents/forge_redesign/) + [`README.md`](./adhoc-topic-documents/forge_redesign/README.md)。 |
| 2026-05-14 | **[fix]** workflow EdgeSpec port routing 从根上重构 — 历史 V1 用 stringly-typed `From: "<node>.<port>"` 字符串编码 port,LLM/手写都易踩坑(approval edge 没标 port → 静默 park 不跑 → run 假成功 nodesCompleted=2/3)。改成显式 `FromPort` + `ToPort` 字段(n8n/NiFi/Step Functions 主流模式),domain/app/scheduler 三层重构 + 加 `BranchOutputPorts` 表 + IsBranchingNode 接口 + validate 阶段强制(分叉节点必带 fromPort + 单输出节点必空 + 拒 legacy dotted) + scheduler.advance 改读 e.FromPort + 6 个 unit test(approval/condition/legacy/single-out 拒绝 + branching routing 正负向)。tool description 大改 create_workflow / edit_workflow 加 op cheatsheet + branching 规则示例。E2E 实测:trigger→approval→function 走 fromPort=approved → completed,nodesCompleted=3/3,flowrun_nodes 2 行(t1+f1,approval 设计上不写)。同步 workflow.md §3+§6 / flowrun.md §2.2 / error-codes.md / burn-in log。**违反 §S14 文档同步纪律 + 早期 stringly-typed 妥协的根本修复**。 |
| 2026-05-15 | **[test]** burn-in B10 sandbox lifecycle + B13 pagination 补测 — 收尾 24 维度。B10 9 子测试全过(list/destroy/gc/retry-bootstrap/conv-reset/runtime-in-use 409 整合性保护);新 finding #15-17 全低(env destroy 后 function lazy-rebuild 缺失 / ownerKind 非白名单悄吞 / runtime 表 3.12 vs >=3.12 duplicate row)。B13 4 子测试过(cursor base64 keyset {c,i} walk-to-end 干净 / limit 错入 400 / cursor garbage 400);finding #18 limit 无 cap 上限。剩 #7/#11/#15-18 全低,dogfood 时再扫。同步 api-burn-in §B10/B13 节。|
| 2026-05-15 | **[fix]** P3 批次 — 5 issue 一气修(#7/#11/#15/#16/#17;#18 已存在 MaxLimit=200 clamp 是误报)。**#15** function/handler env 被外部销毁后 `Run`/`Call` 自动按 stored spec 重建并重试一次(避免死循环再失败即上抛);live E2E 实测:destroy → :run 触发 lazy rebuild 4.4s 完成。**#17** `RuntimeInstaller` 接口加 `NormalizeVersion(version) string`,MiseInstaller 复用 B-01 的 `normalizeVersionForMise`,`EnsureRuntime` 在 lookup/insert 前归一化,`>=3.12`/`3.12` 共用一行;1 新单测覆盖 11 边界。**#16** `validOwnerKinds` 5 值白名单 + 400 INVALID_OWNER_KIND,告别"空 list 当无数据"误读。**#7** `create_handler` / `create_workflow` description 加 MINIMAL COMPLETE EXAMPLE 段(完整 ops 数组示例),省 LLM 多次试错 tokens。**#11** workflow `validate.go::isPseudoTerminalType` 识别 7 个常见伪 terminal 类型(end/output/finish/terminate/stop/return/exit)并返教学型错误"workflows have no terminal node; the DAG ends implicitly";1 新 sub-test 5 用例。同步 sandbox.md/function.md/handler.md/workflow.md/error-codes.md/api-burn-in。backend 全绿,生产 LLM 之后撞这 6 类痛点直接 ✅。|
| 2026-05-15 | **[fix]** burn-in P2 批次 — 6 issue 一口气修(#5/#14/#8/#13/#4/#6)。**#5** mcp/skill boot publish 缺 user ctx → WARN noise:`main.go` 包 detached ctx + `DefaultLocalUserID`(§S9 模式)。**#14** notif data 字段未瘦身:`conversation`/`sandbox_env` 改 slim shape(只 ID + 关键字段),UI 拿通知 → 主动 GET。**#8** handler `__init__` 注入名 `init_args` 与 method `args` 字典歧义:深修——`AssembleClass` 改 exploded named params(`def __init__(self, p1: str, p2: int = None)` 而非 `**init_args`),method 同样 explode + `pythonType`/`pythonDefault` 类型映射 + identifier 校验。LLM 现在 InitBody 用 bare 名字。**#13** catalog coverage 键名 LLM 决定不固定:改后端 `computeCoverage(items)` 按 `item.Source` 名字机械 group,LLM 只输出 summary;coverage 100% 完整永不漏。**#4** Block.Attrs / Message.Attrs 是 JSON-encoded string:domain 改 `map[string]any` + GORM `serializer:json`,store/SSE/handler/eventlog/subagent/loop 级联适配 + 测试同步改用 map literal。**#6** AskUserQuestion 7d timeout(替 5min)+ Skipped 字段(sentinel `(user skipped)` 让 agent 自决 default)+ options 可选;testend Composer 重写 3-state 状态机(IDLE / AGENT_RUNNING / AWAITING_ANSWER),共用一个输入框智能 route 到 `/messages` 或 `/answers`,Skip 按钮 + 黄光呼吸 + Jump-to-question 导航。剩 #7/#11(LLM tool description / NodeType 设计)留下次独立轮。同步 chat.md / catalog.md / handler.md / ask.md / api-burn-in。backend 全绿,testend 0 ts error。|
| 2026-05-13 | **[feat]** forge_redesign Plan 05 完整交付(17 commits E1-E17 直推 main)—— workflow execution plane(trinity 最后一块,trigger→scheduler→flowrun 单向依赖链):(E1-E2) flowrun domain + GORM store(FlowRun + Node + PausedState + 6 sentinel + 11 Repository 方法 + 23 测试,16 通用字段对齐 spec 08 §2 + flowrun-specific 4 字段);(E3-E4) trigger domain + 4 listener infra + Service(robfig/cron v3.0.1 首次引入 + fsnotify v1.10 升 direct + webhook 共享 ServeMux 子路径;cron missedPolicy=runOnce + fsnotify fail-soft §6.11 + webhook secret §6.6 + 时区锁 time.Local §6.10 + panic recover §6.13);(E5-E6) scheduler skeleton + executeRun state machine(7-gate StartRun + topo Kahn + ExecutionContext + driveLoop + Dispatcher Router + 18 测试);(E7-E8) 13 dispatcher(trigger/function/handler/mcp/skill/llm + http/condition/loop/parallel/approval/wait/variable;loop body 跟 parallel branches subgraph 留 Plan 06 显式 sentinel);(E9) withRetry MaxAttempts + 3 backoff + ctx-cancel + fatal sentinel 短路 + per-node Timeout(per-NodeType 默认:function/handler/mcp/http=30s,skill/llm=60s,approval=7d);(E10) approval pause + ResumeApproval + RehydrateOnBoot 跨进程重启 §6.1,driveLoop 抽 executeRun + continueRun 共享主循环;(E11-E12) mcp_calls + skill_executions 两张 D22 表(infra/store/mcpcalls + skillexec 新包;mcpService.SetCallRepo + skillService.SetExecRepo);(E13) 6 D22 LLM tools(search/get × workflow_executions + mcp_calls + skill_executions);(E14) 8 HTTP endpoints + errmap 14 sentinel + WorkflowHandler + FlowRunHandler 共享 `{idAction}` mux dispatcher(workflow-scoped 路由 `:trigger` 与 `/triggers` state 由 WorkflowHandler 持);(E15) main + harness DI 装配(13 dispatcher 注册 Router + 4 张新表 AutoMigrate + D9 BootSmoke 加 6 D22 tools 名;router/deps.Mux 字段让 trigger.webhook 跟主 router 共享 ServeMux);(E16) 7 pipeline tests(happy :trigger / disabled 422 / flowrun GET / cancel 204 或 422 / triggers states / serial concurrency 409 / BootSmoke;§6 其他 hardening 在单测覆盖);(E17) 3 新 design docs(flowrun/trigger/scheduler.md)+ 4 contract docs sync + Plan 05 完工 banner。134 unit + 38 pipeline 全绿。设计 spec [`05-execution-plane.md`](./adhoc-topic-documents/forge_redesign/05-execution-plane.md) + [`plans/05-execution-plane.md`](./adhoc-topic-documents/forge_redesign/plans/05-execution-plane.md)。 |

---

## 3. Phase 4-5 路线（粗粒度）

各 Phase 开工前在此段展开细节。当前状态均为 ⬜。

### Phase 4：工作流能力（~20h，最大一块）

workflow（DAG + 状态机）+ flowrun（执行实例）+ 节点系统（LLM / Tool / Trigger / Approval / Variable 5 类）+ scheduler（cron / fsnotify / HTTP webhook）+ chat 再升级支持"对话创建工作流"。执行引擎自实现（Eino 已全面移除，不再依赖 eino/compose）。

**桌面端预留**（来自优化轮决策）：
- `Notifier` 接口在此阶段定义（domain/notification/），scheduler 依赖
- `Preferences` service 在此阶段定义（含 startOnLogin / missedTaskPolicy 等配置项）
- scheduler 状态全部走 store 持久化；时间源用 monotonic 算间隔、wall clock 调度具体时间；错过任务策略明确决策（skip/runOnce/runAll）

### Phase 5：智能化（~15h）

knowledge + document（本地 sqlite-vec）+ intent（ReAct Agent）+ mcpserver（`mark3labs/mcp-go`）+ skill（V1 浅版：打标签的工具）+ chat 终极版（意图识别 → 工作流推荐 → 自动建草稿）。

**风险点**：sqlite-vec 是 C 扩展，需验证 modernc.org/sqlite 加载兼容性。Phase 5 开工前先做兼容性 spike，跑不通则评估替代方案（备选：换回 mattn 接受 CGO / 用别的本地向量存储）。

---

## 4. 规范/原则演化

按时间倒序，查最近变化用。

| 日期 | 变化 |
|---|---|
| 2026-05-01 | **桌面端架构边界定型**：`internal/infra/desktop/` 仅 `cmd/desktop` import，`cmd/server` 编译产物保持纯净（不含 Wails / 托盘 / 通知代码）。transport 层永远只 httpapi 一份，不走 Wails binding |
| 2026-04-26 | **S14 hook 落地**：在 `.claude/settings.local.json` 配 PostToolUse hook，编辑 `backend/internal/` 下文件时自动注入文档同步提醒 |
| 2026-04-25 | **S3 扩展"严禁藏错误"**：`_ = err` 静默跳过严禁——隐藏的错误会在意想不到的地方爆发（教训：FTS5 虚拟表建失败后触发器仍建成，INSERT 时才炸）|
| 2026-04-25 | **S12 扩展**：主文件用包名规则推广至 app / infra/store 全层；明确"仅 Service 实现接口 / 小工具函数"合并入主文件，不单独建文件 |
| 2026-04-25 | **providers.go 归属原则**：辅助注册表放在消费它的层（非 domain）；domain 层只放 entity + sentinel + 接口 |
| 2026-04-24 | 立 **设计原则 #7 + S14 "文档同步纪律"（最高优先级）**：每次改代码联动三处文档；发现不符立刻修 |
| 2026-04-24 | 立 **设计原则 #6 "反校验剧场"**（单开发者 + 本地 Electron 不搞前端已覆盖的校验）|
| 2026-04-24 | 立 **"spec 优先于邻居文件"** 审计纪律（避免复制 pre-existing 违规）|
| 2026-04-24 | 文档结构三层分工：`backend-design.md`（规范） / `service-contract-documents/`（索引） / `service-design-documents/`（详设计） / `progress-record.md`（进展） |
| 2026-04-24 | 立 **S13 包命名**（三层同名 + `<name><role>` 调用方别名）|
| 2026-04-24 | 立 **S12 包结构**（domain 平铺按概念拆，禁子目录）|
| 2026-04-23 | 立 **设计原则 #5 "端到端推演先行"**（每 domain 开工前走完整数据流）|
| 2026-04-23 | 路线图升级：V1.0 重写 → Agentic Workflow Platform 完整愿景 |
| 2026-04-23 | S11 扩展为 **"双语 + 节制"** 完整规则；全量瘦身 ~420 行冗余注释 |
| 2026-04-22 | 立 **S11 双语注释规范** |
| 2026-04-22 | 定 **12 条契约标准**（N1-N5 / D1-D5 / E1-E2）|
