# polling.go — Phase A audit

**Path**: `backend/internal/app/catalog/polling.go`
**LOC**: 289
**Role**: Runtime heart of the catalog package. Owns `Start`, `Stop`, `pollLoop`, `tryRefresh`, `Refresh`, and `fingerprint`. This file is where **all** catalog terminal-state writes (`s.cache.Store`, `s.lastFP.Store`, `saveToDisk`, `s.notif.Publish`) happen.

## Trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | polling.go:44-67 | `Start` — `cached, err := loadFromDisk(s.cachePath); switch { case err == nil && cached != nil: ... case err != nil: s.log.Warn("catalog cache load failed; starting with empty cache", ...) }` | A.1 | OK | err != nil branch logs Warn with path + error context (per §S10 async/fire-and-forget logging). Empty cache start is correct semantic per catalog.md §6. The intermediate corruption case is already handled inside loadFromDisk (moves to .bak); this caller just logs + continues — that's the documented contract. Not silent fallback. | — | — | — | — |
| 2 | polling.go:78-81 | `go func() { defer close(s.pollDone); s.pollLoop(pollCtx) }()` | A.1 | OK | Standard goroutine launch; pollLoop is total (returns on ctx.Done). No error path to swallow. | — | — | — | — |
| 3 | polling.go:111-124 | `pollLoop(ctx)` — `s.tryRefresh(ctx); ticker := time.NewTicker(...); for { select { case <-ctx.Done(): return; case <-ticker.C: s.tryRefresh(ctx) } }` | A.1 | OK | tryRefresh is void (errors logged inside). Pure event loop; no errors to swallow at this level. | — | — | — | — |
| 4 | polling.go:138-141 | `tryRefresh(ctx)` — `if err := s.Refresh(ctx); err != nil { s.log.Warn("catalog refresh skipped/failed; keeping previous cache", zap.Error(err)) }` | A.1 | OK | Async polling tick. Per §S10 "异步或 fire-and-forget 必须打" — Refresh err is logged Warn (right level: not user-facing fatal; cache still usable). The "keeping previous cache" message accurately describes outcome (Refresh's all-sources-failed path returns the only non-nil err and explicitly avoids overwriting). Loud. Not silent fallback. | — | — | — | — |
| 5 | polling.go:180-182 | `if _, ok := reqctxpkg.GetUserID(ctx); !ok { ctx = reqctxpkg.SetUserID(ctx, reqctxpkg.DefaultLocalUserID) }` | A.2 | OK | This is the §S9 terminal-write context-injection pattern, applied at the **top of Refresh**. Background goroutine ctx has no middleware-stamped user ID; injecting DefaultLocalUserID lets downstream calls (source.ListItems repo queries; LLM resolve → apikey.GetForProvider) succeed. The HTTP `:refresh` path bypasses (already has user ID). Per §S9 spirit + design doc §11 "实施时发现的关键 bug" — explicitly fixes a discovered bug. Note: this **is not** `context.Background()` reset — it preserves the parent ctx (so Stop's ctx-cancel still propagates). That's correct: Refresh terminal writes don't need full detachment because (a) the goroutine is already detached from request ctx; (b) Stop should still cancel mid-Refresh to avoid hanging shutdown. | — | — | — | — |
| 6 | polling.go:185-187 | `if len(sources) == 0 { return nil }` | A.1 | OK | No sources registered → no-op success. Documented behavior; chat.runner sees empty cache. Not silent fallback (no upstream call has failed; it's the boot window before any source registers — though main.go registers all sources before calling Start, so this branch is mostly defensive). | — | — | — | — |
| 7 | polling.go:194-201 | `for _, src := range sources { srcItems, err := src.ListItems(ctx); if err != nil { s.log.Warn("catalog source ListItems failed; substituting empty", zap.String("source", src.Name()), zap.Error(err)); failedCount++; continue } ... }` | A.1 | OK | Per-source failure isolation per design doc §3 "失败隔离". Logs Warn with source name + error, increments failedCount, continues. Documented intent: one bad source must not poison the whole catalog. Empty-substitution makes fingerprint reflect "this source missing right now" naturally. Not silent — Warn is loud. | — | — | — | — |
| 8 | polling.go:212-214 | `if failedCount == len(sources) { return fmt.Errorf("catalog: all %d sources failed; keeping previous cache", len(sources)) }` | A.1/A.4/A.5 | VIOLATION | **Multi-issue site.** **§S16**: prefix is `"catalog:"` not `"catalog.Refresh:"` — missing method qualifier. **§S17**: this returned error escapes via the HTTP `:refresh` handler (handlers/catalog.go:80 calls `h.svc.Refresh(r.Context())` and forwards err to `responsehttpapi.FromDomainError`). It is **not wrapped** to any registered sentinel (no `%w` against any catalogdomain or other registered err). Result: hits errmap.go::FromDomainError, falls through to "unmapped domain error" ERROR log + returns 500 INTERNAL_ERROR. **This is the exact failure mode §S17 is designed to prevent** — it pollutes the smoke alarm with "unmapped" warnings every time a user hits :refresh while all sources are failing simultaneously. Design doc §10 promises catalog sentinels are "internal — 不进 errmap" but the design didn't account for the all-sources-failed return path being reachable from HTTP. | MED | When user hits POST `/api/v1/catalog:refresh` while all sources are failing simultaneously (rare but possible: filesystem unmount mid-tick, all DB queries timing out, etc.), they get a 500 with body `{"error":{"code":"INTERNAL_ERROR","message":"internal error"}}`. Server logs an "unmapped domain error" ERROR which is the smoke-alarm signal that something is genuinely unhandled — false-positive noise. Polling-tick path is fine (err is logged Warn locally; cache preserved). | Two options: (a) Define `catalogdomain.ErrAllSourcesFailed = errors.New("catalog: all sources failed")` + register in errmap.go as `{503, "CATALOG_ALL_SOURCES_FAILED"}` + wrap here `fmt.Errorf("catalogapp.Refresh: %w (%d sources)", catalogdomain.ErrAllSourcesFailed, len(sources))`. (b) Have polling.go::Refresh always return nil (drop the error) and only log Warn — this matches catalog.md §10 "internal 消化" intent literally but loses signal at the HTTP handler. (a) is cleaner; matches the existing pattern for `catalogdomain.ErrCoverageIncomplete` / `ErrGenerationFailed`. Also fix to `catalogapp.Refresh:` qualifier. | FOUND |
| 9 | polling.go:216-221 | `fp := fingerprint(items); if last, _ := s.lastFP.Load().(string); last == fp { return nil }` | A.1 | OK | The `_` here discards the type-assertion's `ok` bool; this is **not** an error discard. atomic.Value.Load returns (any, ); the `_` is the type-assertion ok. Safe because lastFP is only ever stored as string (lines 49, 62, 66, 247) so the assertion always succeeds. Even if it didn't, `last` would be zero-value `""` and the equality check would be false → fall through to regen, which is the safe direction. Not §S3. | — | — | — | — |
| 10 | polling.go:223-239 | `if s.generator != nil { cat, err = s.generator.Generate(...); if err != nil { s.log.Warn("catalog Generator failed; using mechanical fallback", zap.Error(err)); cat = mechanicalFallback(items, gMap) } } else { cat = mechanicalFallback(items, gMap) }` | A.1 | OK | This is the documented "silent fallback" exception — and it's NOT silent. Warn-logged with err. Per design doc §3 "失败即 mechanical + 写 lastFP" — falling back to mechanical is the **canonical behavior**, not silent error suppression. `lastFP` always updates after this (line 247), implementing the user-activity-driven retry per §3. Generator's ErrGenerationFailed sentinel is intentionally absorbed here (caller never sees it). §S17 OK because the sentinel never reaches a handler — the catalogdomain.ErrGenerationFailed sentinel chain stays inside this file. | — | — | — | — |
| 11 | polling.go:246-247 | `s.cache.Store(cat); s.lastFP.Store(fp)` | A.2 | OK | Atomic stores; no error path. These are the **terminal in-memory writes** for the catalog. They happen synchronously in Refresh; if ctx is cancelled mid-Refresh after this point, the cache is already updated — that's fine (no rollback needed). | — | — | — | — |
| 12 | polling.go:248-251 | `if err := saveToDisk(s.cachePath, cat); err != nil { s.log.Warn("catalog write to disk failed; in-memory cache still updated", zap.String("path", s.cachePath), zap.Error(err)) }` | A.1/A.2 | EDGE | **§S3 borderline**: disk write failure is logged Warn but the function continues + returns nil. The Warn message accurately documents the outcome ("in-memory cache still updated"). Per catalog.md §3 design doc the .catalog.json is a **derived cache** — losing it costs only one cold-start regen; not data loss. The in-memory cache is the source of truth for chat.runner. So §S3 carve-out applies (cleanup-style write failure where loss is recoverable). **However**: §S9 concern — saveToDisk call uses no detached ctx, but saveToDisk takes no ctx at all (file syscalls). Cancel mid-write would not actually interrupt os.WriteFile / os.Rename (they're non-cancellable syscalls). So §S9 is N/A here. The bigger concern raised by user prompt — "polling cache 写盘失败" — is actually well-handled: Warn is loud, message tells operator the in-memory cache survived. The next successful Refresh will retry the disk write. **OK with EDGE flag** because the comment explicitly documents the recovery semantic; not silent. | LOW | If disk is full / read-only, .catalog.json never persists. On next process restart, cold-start has no cache, but mechanicalFallback regenerates within 1s of first poll. No user-visible data loss. | None — this is documented design behavior. Optional: increment a counter / metric so persistent disk-write failures bubble up as a health signal. WAIVE-able. | FOUND |
| 13 | polling.go:252-257 | `s.notif.Publish(ctx, "catalog", cat.Fingerprint, map[string]any{...})` | A.1/A.2 | OK | Publisher.Publish is fire-and-forget; per notifications.go signature returns no error to caller. The notifications package itself logs internally on bridge failures. Per §E1 "通知 (global broadcast)" model + design doc §13 "**不**发 SSE" — wait, the doc says catalog doesn't emit SSE. But polling.go DOES Publish. This is a **post-design-doc evolution**: catalog notification was added to keep UI's `:refresh` button responsive without polling. Not a §S3 issue (no err to swallow); §S9 OK because notification loss on cancel is documented best-effort (UI re-fetches via GET on next page load). The ctx here is the (potentially cancellable) Refresh ctx — if cancelled mid-Refresh, the Publish will be a no-op or a partial write to bridge buffer; the next Refresh re-publishes naturally. Acceptable for a fire-and-forget notification of a derived-cache update. | — | — | — | — |
| 14 | polling.go:269-288 | `fingerprint(items) string` — pure SHA-256 over sorted (source+name+description) | A.1 | OK | Deterministic content hash, no error path, no I/O. Not §S15 (this is a fingerprint not a business ID). | — | — | — | — |

## Sub-checks

A.1 §S3 错误吞没:
  - violations: 0 HIGH, 1 LOW EDGE (#12 — saveToDisk failure path; documented recovery; logged Warn)
  - rationale: every error path in this file is logged Warn with context (per §S10 async-rule). The "silent fallback" prompt-flag at site #10 (Generator → mechanicalFallback) is loud-logged + documented design behavior, not silent. No `_ = err` patterns without justification.

A.2 §S9 detached ctx 终态写:
  - terminal-state writes identified:
    - **In-memory** (sites #11): `s.cache.Store(cat)` + `s.lastFP.Store(fp)` — atomic, no ctx
    - **Disk** (site #12): `saveToDisk` — file syscalls, no ctx
    - **Notification** (site #13): `s.notif.Publish(ctx, ...)` — uses Refresh ctx
  - 各自 ctx 来源:
    - In-memory writes: no ctx involved
    - Disk write: no ctx (file syscalls non-cancellable)
    - notif.Publish: parent ctx from Service.Refresh caller (polling tick OR HTTP `:refresh`); polling tick injected DefaultLocalUserID at top of Refresh (line 181)
  - violations: not present
  - rationale: catalog's "terminal write" is the in-memory cache.Store at line 246, which has no ctx and cannot be interrupted. Disk write is a derived-cache snapshot (loss recoverable on next tick). notif.Publish loss is acceptable per fire-and-forget contract. The only ctx concern was "background-goroutine has no user ID" which is explicitly fixed at line 180-182 (the §S9 detached-ctx pattern, applied as an in-place injection rather than full Background reset because Stop must still cancel).

A.3 §S15 ID 生成:
  - ID generation calls: none — `fingerprint()` is a content hash, not a business ID
  - violations: N/A: package generates SHA-256 fingerprints (deterministic; not §S15 random IDs).

A.4 §S16 错误 wrap 格式:
  - violations: 1 LOW (site #8 — `"catalog:"` prefix lacks `<pkg>.<Method>` qualifier; should be `catalogapp.Refresh:`)
  - other format strings in this file (Warn messages) are fine — those are zap log msgs, not error wrap strings.

A.5 §S17 sentinel 登记 errmap:
  - sentinels defined: none in this file
  - sentinels CONSUMED: catalogdomain.ErrGenerationFailed (transitively via Generator.Generate) — wrapped + absorbed at site #10 (never escapes Refresh)
  - 已登记 errmap: N/A — catalog sentinels intentionally not registered per design §10
  - missing: 1 MED (site #8 produces an unwrapped catalog error that DOES escape via HTTP `:refresh` handler → unmapped errmap warning + 500 response). See §A.4 site #8 for fix proposal.

## Spot-check

- Verified `s.notif.Publish` matches Publisher signature (notifications.go:37): `Publish(ctx, eventType, id, data, conversationID...)` — call passes 4 args + omits conversationID (catalog is global). Correct.
- Verified `fingerprint` includes only (source + name + description) per design doc §6: confirmed lines 280-285 hash exactly those three fields per item. ID is excluded — matches design intent ("用户改 forge code / tags 等不触发 regen, 只 name/description 改触发").
- Verified `s.lastFP.Store(fp)` at line 247 happens **after** cache.Store + saveToDisk — order ensures next-tick fingerprint comparison sees a coherent state. If the process crashes mid-Refresh between cache.Store and lastFP.Store, the next process startup would reload from disk (which is durable post-saveToDisk) and lastFP would be set from `cached.Fingerprint` at line 49 — no anomaly.
- Verified `Stop()` (catalog.go:94-103) blocks on `<-s.pollDone` so test cleanup waits for any in-flight Refresh to finish before tempdir is removed (per §T5 idempotency requirement).
- Verified the busy-guard at line 133-136 (`if !s.busy.CompareAndSwap(false, true) { return }`) is a true single-flight, not a counter. Correct atomic semantics — concurrent ticks during a >1s LLM call are skipped, no queueing.
- Cross-checked HTTP handler path: `/api/v1/catalog:refresh` → `handlers/catalog.go:79-85` → `h.svc.Refresh(r.Context())` → on err calls `responsehttpapi.FromDomainError(w, h.log, err)`. Confirmed site #8's err **does** reach FromDomainError. Confirmed errTable has no `catalog:` entry. Confirmed `lookup` falls through to `{500, "INTERNAL_ERROR"}` + logs "unmapped domain error" ERROR.
