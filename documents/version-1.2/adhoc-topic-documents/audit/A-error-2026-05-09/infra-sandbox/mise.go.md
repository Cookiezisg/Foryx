# audit: backend/internal/infra/sandbox/mise.go

LOC: 472
Read: full file (lines 1-472)

## Trace

| site# | file:line | snippet | category | classification | reasoning | severity | user_impact | suggested_fix | status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | mise.go:65-68 | `if len(miseBinary) == 0 { return "", fmt.Errorf("sandbox.ExtractMiseBinary: no mise binary embedded for %s/%s: %w", runtime.GOOS, runtime.GOARCH, sandboxdomain.ErrRuntimeInstallFailed) }` | A.4 | OK | §S16 canonical: pkg.Method prefix + `%w` with sentinel; sentinel ErrRuntimeInstallFailed registered errmap.go:102 → 502 | N-A | — | — | — |
| 2 | mise.go:71-73 | `if err := os.MkdirAll(binDir, 0o755); err != nil { return "", fmt.Errorf("sandbox.ExtractMiseBinary: mkdir bin dir: %w", err) }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 3 | mise.go:86-91 | `if existing, err := os.ReadFile(hashPath); err == nil && string(existing) == wantHash { ... }` | A.1 | OK | `err` checked via `err == nil &&` guard; "file doesn't exist" / "permission denied" both naturally fall through to re-extract path. Idempotency design — ReadFile failure means "no cached hash, re-extract" which is the safe behavior. Per §S3 spec: this is the "non-essential cache miss" carve-out (cleanup/cache, not a user-visible state change). | N-A | — | — | — |
| 4 | mise.go:87 | `if _, statErr := os.Stat(binPath); statErr == nil` | A.1 | OK | `_` discards os.FileInfo (not error); statErr explicitly checked. §S3 doesn't apply to non-error discards. | N-A | — | — | — |
| 5 | mise.go:98-100 | `if err := os.WriteFile(tmp, miseBinary, 0o755); err != nil { return "", fmt.Errorf("sandbox.ExtractMiseBinary: write tmp: %w", err) }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 6 | mise.go:101-104 | `if err := os.Rename(tmp, binPath); err != nil { _ = os.Remove(tmp); return "", fmt.Errorf("sandbox.ExtractMiseBinary: rename: %w", err) }` | A.1/A.4 | EDGE | §S3: `_ = os.Remove(tmp)` discards Remove error without inline justification. **However**: this is panic-path-style cleanup (rename already failed; we're returning the rename error which is the actionable signal; tmp file leftover is fs noise next boot will clean). Per §S3 spec: cleanup-after-cleanup-after-failure is an acceptable carve-out. Inline comment would still be best practice. | LOW | tmp file leak if Remove fails — innocuous, next boot's tmp+rename cycle reclaims it | add inline comment: `_ = os.Remove(tmp) // best-effort cleanup; rename failure is the actionable error` | FOUND |
| 7 | mise.go:113-117 | `if runtime.GOOS == "darwin" { if err := macCodesign(...); err != nil { return "", fmt.Errorf("sandbox.ExtractMiseBinary: codesign: %w", err) } }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 8 | mise.go:124-126 | `if err := os.WriteFile(hashPath, []byte(wantHash), 0o644); err != nil { log.Warn("mise hash file write failed (will re-extract next boot)", zap.Error(err)) }` | A.1 | OK | §S3 documented best-effort soft-fail with WARN log + audit context. File-header comment line 119-123 explains intent (idempotent, next boot reclaims). Compliant. | N-A | — | — | — |
| 9 | mise.go:134-136 | `if err := writeMiseConfig(...); err != nil { log.Warn("mise config write failed (attestation may not be disabled)", zap.Error(err)) }` | A.1 | OK | §S3 documented best-effort with WARN log; main path continues with degraded behavior (attestation may fire = slower under repeated calls but functional). Acceptable. | N-A | — | — | — |
| 10 | mise.go:206-208 | `if err := os.WriteFile(configPath, []byte(body), 0o644); err != nil { return fmt.Errorf("write mise.toml: %w", err) }` | A.4 | EDGE | §S16: has `%w` ✓; prefix is `write mise.toml:` (descriptive) not canonical `sandbox.writeMiseConfig:`. Same family as codesign.go LOWs. | LOW | minor — caller (line 134) wraps with log.Warn; not currently propagated as error | tighten: `fmt.Errorf("sandbox.writeMiseConfig: write mise.toml: %w", err)` | FOUND |
| 11 | mise.go:274-276 | `if err := os.MkdirAll(dataDir, 0o755); err != nil { return "", fmt.Errorf("sandbox.MiseInstaller.Install: mkdir mise data: %w", err) }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 12 | mise.go:292-297 | `if err := RunWithStderrCapture(...); err != nil { return "", err }` | A.4 | OK | bare `return err` — RunWithStderrCapture wraps with `msgPrefix: %w: %v: %s` (sentinel preserved per exec_helper.go #4 — the inner-err truncation MED is logged separately). Caller's bare return propagates that wrapped error correctly. | N-A | — | — | — |
| 13 | mise.go:304-307 | `actual, err := m.where(ctx, dataDir, version); if err != nil { return "", fmt.Errorf("sandbox.MiseInstaller.Install: locate after install: %w", err) }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 14 | mise.go:308-311 | `rel, err := filepath.Rel(sandboxRoot, actual); if err != nil { return "", fmt.Errorf("sandbox.MiseInstaller.Install: rel path %q from %q: %w", actual, sandboxRoot, err) }` | A.4 | OK | §S16 canonical with context | N-A | — | — | — |
| 15 | mise.go:337-340 | `installDir, err := m.where(...); if err != nil { return "", fmt.Errorf("sandbox.MiseInstaller.Locate: %w", err) }` | A.4 | OK | §S16 canonical | N-A | — | — | — |
| 16 | mise.go:367-368 | `return "", fmt.Errorf("sandbox.MiseInstaller.Locate %s@%s: binary not found in %s (dir contains: %s)", m.kind, version, installDir, strings.Join(names, ", "))` | A.4 | EDGE | §S16: pkg.Method prefix ✓ but **NO sentinel + NO %w** — same family as mcp.go:#6 (FIXED 505d6e3 as panic) and sandbox.go:#22 (FIXED e36f890 as panic). However **this one IS reachable in practice** when an upstream tarball layout changes — not a wiring bug. Should have sentinel for errmap mapping. | LOW | hits "unmapped domain error" alarm + 500 INTERNAL_ERROR if triggered (e.g. uv tarball restructured upstream and walkForBinary misses) — surfaces as opaque 500 to user when error message is actually rich + actionable | introduce `sandboxdomain.ErrBinaryNotFound` (or reuse ErrRuntimeInstallFailed) + wrap with `%w`. Or accept as impossible in practice + log at info level | FOUND |
| 17 | mise.go:362 | `entries, _ := os.ReadDir(installDir)` | A.1 | EDGE | §S3: `_` discards ReadDir error without inline justification. **Functional**: this is the error-path "include directory listing in error message" branch — if ReadDir itself fails, we still return the not-found-in-installDir error with empty `names` slice. Caller still gets actionable error. Per §S3 spec: failures inside an error-construction path are tolerable. | LOW | minor — error message just lacks the "(dir contains: ...)" detail if ReadDir also failed | add inline comment: `entries, _ := os.ReadDir(installDir) // best-effort: failure here just omits dir listing from error message below` | FOUND |
| 18 | mise.go:381-401 | `_ = filepath.WalkDir(installDir, func(...) { ... })` | A.1 | EDGE | §S3: `_` discards WalkDir error without inline justification. **Functional**: walk is best-effort search ("if hit found we use it; if walk errored partway we still return whatever we hit, or empty"). Comment at line 394 explains "Prefer exact match: stop walking once we have it." | LOW | minor — if walk fails midway we may miss a candidate binary deeper in tree; but caller (Locate) will still return its full "binary not found" error with directory listing, which is more useful than the walk error itself | add inline comment: `_ = filepath.WalkDir(...) // walk errors are non-actionable; we return whatever 'hit' captured plus caller's "not found" error if empty` | FOUND |
| 19 | mise.go:436-440 | `out, err := cmd.CombinedOutput(); if err != nil { return "", fmt.Errorf("mise where %s@%s: %v: %s", m.kind, version, err, strings.TrimSpace(string(out))) }` | **A.4** | **VIOLATION** | **§S16: `%v` not `%w`** — caller cannot `errors.Is(err, &exec.ExitError{})` to discriminate "tool not installed at this version" from "mise binary missing / segfaulted". Same defect class as exec_helper.go:#4 + mcp install.go:#5. Prefix `mise where %s@%s:` is descriptive but not canonical pkg.Method. | MED | error message readable but downstream errors.Is checks (e.g. caller wanting to retry vs surface to user) cannot work; calling code at line 304-307 just wraps further with `locate after install:` which compounds the issue | switch to multi-`%w` (Go 1.20+): `return "", fmt.Errorf("sandbox.MiseInstaller.where %s@%s: %w: %s", m.kind, version, err, strings.TrimSpace(string(out)))` — preserves underlying ExitError chain | FOUND |
| 20 | mise.go:451-455 | `out, err := cmd.CombinedOutput(); if err != nil { return nil, fmt.Errorf("sandbox.MiseInstaller.ListAvailable %s: %v: %s", m.kind, err, strings.TrimSpace(string(out))) }` | **A.4** | **VIOLATION** | **§S16: `%v` not `%w`** — same defect as #19. ListAvailable callers (UI version picker) can't discriminate transient network failure from mise misconfig. | MED | UI just shows opaque error string for any failure mode; can't suggest "retry" vs "install mise" vs "check network" | same multi-%w fix as #19: `fmt.Errorf("sandbox.MiseInstaller.ListAvailable %s: %w: %s", m.kind, err, strings.TrimSpace(string(out)))` | FOUND |

## Sub-check

A.1 §S3 错误吞没:
  - violations: sites #6 (LOW — `_ = os.Remove(tmp)` cleanup-after-cleanup, no comment), #17 (LOW — `_ = os.ReadDir`, error-message-construction path), #18 (LOW — `_ = filepath.WalkDir`, best-effort search). All are documented or self-evident best-effort patterns; missing inline comments are the only specific gap.

A.2 §S9 detached ctx 终态写:
  - terminal-state writes identified: none (file performs filesystem extraction + spawns mise commands; no DB writes)
  - 各自 ctx 来源: N/A
  - violations: N/A — package is filesystem + exec only; terminal-state DB writes happen in app/sandbox layer (already audited)

A.3 §S15 ID 生成:
  - ID generation calls: none in this file (file uses sha256 hash for binary integrity, not ID generation; hash is content-addressed not random)
  - violations: N/A — no business ID generation

A.4 §S16 错误 wrap 格式:
  - violations: sites #19, #20 (**MED** — `%v` not `%w` in `where` and `ListAvailable`, breaks underlying ExitError chain), #10, #16 (LOW — descriptive prefix or missing sentinel)

A.5 §S17 sentinel 登记 errmap:
  - sentinels defined: none (file uses `sandboxdomain.ErrRuntimeInstallFailed` via compile-time check at exec_helper.go and via direct reference at mise.go:67)
  - 已登记 errmap: N/A — file doesn't define new sentinels
  - missing: N/A — sandboxdomain.ErrRuntimeInstallFailed already registered errmap.go:102
